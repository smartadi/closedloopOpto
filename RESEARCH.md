# Brain Paper — Research Coordination

## Project Context
Mouse widefield closed-loop PI controller vs open-loop analysis.
Controller uses kernel average of a cortical region as output; PI controller drives input.
Two mice: AL_0033 (9 sessions), AL_0039 (4 sessions) = 13 controller sessions, Jan–Apr 2025. (Verified 2026-06-16 from loaded cache m1–m13.)

**Core question:** Does the closed-loop controller reduce neural variability and error compared to open-loop?
**Secondary questions:** Do motion, frequency-band power, and wide-brain activity explain trial-to-trial variability? Do these differ between OL and CL?

**For tasks and priorities → `TASKS.md`**
**For completed findings and paper claims → `FINDINGS.md`**
**For meeting decisions → `MEETINGS.md`**

---

## Change Log

### 2026-08-25 — ols_tf_pipeline.m: fold imp_state_xsess into §18 + diagnose/validate modes
**Changed/Found:** `ols_tf_pipeline.m` §18 now ends the whole story on the pooled cross-session state-dependence: added `RUN_XSESS` (default true) that calls `imp_state_xsess` at the §18 tail (consumes the `.ST` payloads `local_stimblind_session` already builds via `imp_statedep_trials`, line ~2595 — verified present so the "no ST payload" assert can't trip). Added `RUN_MODE`: `'validate'` (default) runs straight through; `'diagnose'` merges the §18 build+panel loops (extracted the per-amp Actual/Global/Local panel into new local fn `local_draw_agl_panel`) so each session is drawn AS BUILT with a GOOD/CHECK verdict (spont R² vs 0.85 floor; %Local / Global-leak; CHECK prints to stderr/red) and PAUSES one-by-one (`input`: Enter=accept, q=abort) before the pooled state-dep. Added `addpath(impulseDir)` in §1 so the folded `imp_state_xsess`/`cp_draw_roi` resolve by name. Code Analyzer: no new issues (all flags pre-existing).
**Why:** User wants ONE script that (a) in diagnosis mode lets them confirm each session's stim-blind model is good one-by-one THEN runs state-dep, and (b) in validation mode skips straight to results. `imp_state_xsess` was previously a manual second command; folding it makes the single run end on the result.
**Next:** Run both modes end-to-end on `[3 1 2]` to confirm the diagnose pause + q-abort behave and `imp_state_xsess` finds `ALLSESS`. ⚠ §17c single-session state-dep still runs BEFORE §18 (Stage 3 < Stage 4), so in diagnose mode the primary session's state figures appear before the per-session confirmation — decide whether to defer §17c too. ⚠ §18 keys off the energy-based `bledA` map, not the TF detector — reconcile before any pooled number reaches the paper.

### 2026-08-25 — Residual-error story consolidated to ONE script; 4 redundant scripts archived
**Changed/Found:** `git mv`'d `contra_prediction.m`, `ols_pixel_predictor.m`, `ols_pixel_predictor_wip.m`, `kernelmap_paint.m` → new `impulse-analysis/archive/` (with `README.md`). `ols_tf_pipeline.m` is now THE single runnable residual-error script. Ran a full-repo dependency sweep (878 `.m` files) first: only these entry-point scripts were moved; **all `utils/cp_*.m` helpers stay in place** (dense web — `cp_residual_core`/`cp_bleed_control`/`cp_state_batch`/`cp_spont_predq`/`cp_find_stim_site`/`cp_roi_masks` are still used by kept scripts). Fixed dangling refs: pipeline's stale "run contra_prediction.m S04" ROI errors (×2) + mask comment now point at `cp_draw_roi.m`; `motion_analysis.m` `cperr` comments/warning + `load_experiments.m` path comment updated. Verified `load_experiments.m:14` uses `addpath(impulseDir)` (NON-recursive) → `archive/` stays OFF the MATLAB path, so archived scripts can't run/shadow. Code Analyzer on the edited pipeline: no new issues.
**Why:** User: "for the residual error story i only want one script that will run all … archive all others so that i can bring it back if we spot a terrible error." SELECT (§17d) has been the sole stim-blind model since 2026-07-18, so the two `ols_pixel_predictor*` versions and the CP-* workbench were redundant entry points. Archiving (not deleting) keeps them restorable via `git mv`.
**Next:** ⚠ `contra_prediction.m`'s reviewer-defense controls (`[CP-BLEEDCTRL]`/`[CP-CLEAN]`/`[CP-STIMAFF]`/`[CP-PREDQ]`) are NOT replicated in the pipeline — restore the script if a referee needs them regenerated. `motion_analysis.m` default `dev_metric='cperr'` now always falls back to `peakdev` (loud guard) until `contra_prediction.m` is restored — decide whether to flip its default to `peakdev`. Optional follow-up: archive the 7 now-dormant `cp_*` helpers only `contra_prediction.m` used.

### 2026-08-25 — ols_tf_pipeline.m: RUN_ALL headless flag (single-command full residual run)
**Changed/Found:** Added a `RUN_ALL` knob (top of §1, after the `OLS_OVERRIDE` hook). `RUN_ALL=true` sets `RUN_ALLSESS=true`, `RUN_SESSION_VIEWER=false`, and `OLS_OVERRIDE.affect_mode='matched'` (skips the §10T3 `uiwait` CONFIRM gate) so the script runs headless SETUP → SELECT (§17d) → state-dep (§17c/§17c2) → §18 cross-session batch. Documented the two-line recipe (`load_experiments` once, then `RUN_ALL=true; ols_tf_pipeline`).
**Why:** `load_experiments.m` does its own `clear all`, so it CANNOT be called from inside the pipeline (would wipe the workspace mid-run) — the loader stays a required first step and `RUN_ALL` drives the rest of the story in one command, satisfying "one script that will run all."
**Next:** Run it end-to-end on `selExp=3` + the `[3 1 2]` batch to confirm the headless path reaches §18 and `imp_state_xsess` finds `ALLSESS`. Reconcile the §18 engine's energy-based `bledA` affected map vs the single-session TF detector before any pooled number reaches the paper.

### 2026-08-25 — Added OS-Pulse per-pulse gain-law figure to grant.tex Aim 1 (macaque)
**Changed/Found:** `draft/grant_2026_10_YazdanSteinmetz/grant.tex` — added the macaque OS-Pulse frequency-sweep figure as **Fig.~\ref{fig:gainlaw}** (`figs2/ospulse_gainlaw.png`, cropped from `fig_a1_tf.png` via PIL to strip the baked-in title + bottom caption so the LaTeX \caption carries the text), inserted as a `wrapfigure` in the "Preliminary data — macaque" paragraph with a sentence: per-pulse input→output gain falls >10× from 10→150 Hz in M1 & S1 (CaMKIIα-C1V1 through µECoG) = frequency-invariance (LTI) violation = the parameter/state-dependence Aim 2 models, explicitly tied to the mouse-grid Fig 8C. Recompiles clean (2 passes, exit 0, 0 undefined refs, 22 pp, no >50 pt overfull). Figure numbering: gainlaw=Fig 4, grid=Fig 8 (renumbered).
**Why:** User: "both then ill start editing on overleaf" (add OS-Pulse + set up Overleaf push). OS-Pulse >10× roll-off is a distinct quantitative preliminary result (parameter-dependence) not covered by the existing `fig:sifcc` (connectivity state-dependence), and it directly mirrors the mouse-grid prediction-error-vs-state panel — strengthens the cross-species state-dependence premise for Aim 2.
**Next:** (1) The wrapfigure floats down onto the next page (lands amid the "shared behavioral paradigm" text) and its number (Fig 4) is visually out of order vs Figs 5–7 — a float-placement artifact; easiest to nudge interactively on Overleaf (convert to a normal figure near fig:nhpint, or `[!t]`). (2) Overleaf git push still pending the project's git URL from the user.

### 2026-08-25 — Ported grid preliminary data into the Overleaf grant.tex (moving to Overleaf for collab)
**Changed/Found:** Edited `draft/grant_2026_10_YazdanSteinmetz/grant.tex` (backup `grant.tex.preport-bak`): added the mouse dual-opsin 52-site grid composite as **Fig.~\ref{fig:grid}** (`figs2/grid_prelim.png`, copied from `fig_a1_grid_composite.png` — A brain+sites+exemplar rings, B LTI fits, C prediction-error-vs-state), a new "Preliminary data — the site-mapping sweep is de-risked" \lead paragraph in Aim 1, a single-site LTI transfer-function equation **Eq.~\ref{eq:lti}** `H(s)=cᵀ(sI−A)⁻¹b=Σ rᵢ/(s−λᵢ)`, and a one-line pointer in the "Preliminary data — mouse" summary. Compiles clean: `pdflatex` exit 0, 0 undefined refs after 2 passes, 22 pages (one cosmetic 90 pt overfull hbox in the prelim-mouse paragraph). NOT ported: my 4 rendered equation PNGs and a separate GDAR/DMD equation block — **grant.tex already has superior, comprehensive math** (`eq:model` state-switching graph LDS `x_{t+1}=A(s_t;𝒢)x_t+B(s_t;𝒢)u_t+ε_t` + nested levels 0–5), and the **macaque state-dependence is already present as `fig:sifcc`**. So only the genuinely-missing pieces were added (single-site LTI eq + the mouse grid prelim figure).
**Why:** User: "get latest from the overleaf, we are moving to overleaf for easier collab" + AskUserQuestion answers: source = the local `grant_2026_10_YazdanSteinmetz` folder (their latest), and "yes port into grant.tex." Integrating my prelim data into the existing (much more developed) grant.tex — rather than duplicating its math or importing my thinner Word draft — is the correct consolidation. The grid figure fills a real gap: Aim 1 plans a "dense site-mapping sweep" but had no preliminary figure showing it is feasible / cell-class-specific / state-dependent.
**Next:** (1) Folder is a zip download, NOT a git clone — to actually land on Overleaf the user must either upload changed `grant.tex` + `figs2/grid_prelim.png` via the Overleaf UI, or give me the Overleaf git URL (git.overleaf.com/<id>) with a cached token so I can push. (2) OS-Pulse per-pulse gain roll-off (`fig_a1_tf.png`, the >10× LTI-violation) is genuinely additive (distinct from fig:sifcc) but NOT yet added — grants are page-limited (already 22 pp), so offered rather than inserted. (3) Fix the 90 pt overfull hbox if it bothers the collab. (4) `grant.tex.preport-bak` can be deleted once the port is accepted.

### 2026-08-24 — grid_stim_fields.py: parametric optogenetic stimulation fields (excit exp vs inhib DoG)
**Changed/Found:** New `bilateral/grid/grid_stim_fields.py` — fits the paper-style spatial "stimulation field" (Barzon/De/Kiani microstim-control preprint, Fig 5f) to the AL_0048 2026-07-10 grid off the fitted signed peak-gain matrix (`grid_tf_fits_2amp.npz`). Per stim site: response vs cortical distance to every readout, self-normalized; pooled by opsin class over reliable driver sites (diagonal CV-R²≥0.2, correct self-sign). **Result: excitatory (left) field = exponential decay, λ=0.71 mm (amp 1.0) / 0.76 mm (amp 2.0), R²=0.65, n=12 drivers — nearly identical to the paper's electrical-microstim λ=0.71 mm and amplitude-invariant.** Inhibitory (right) field = **broad monotonic exponential suppression, λ=1.79 (amp 1.0) / 2.62 mm (amp 2.0), R²=0.91, n=7 — NO surround rebound**, unlike the paper's center-surround difference-of-Gaussians; DoG fit did not beat plain exp (B→0). Inhib field is ~3–4× broader than excit and mildly broadens with laser power.
**Why:** Deep-dive of the microstim closed-loop-control preprint flagged its Fig-5 "stimulation fields" + linear-superposition as the most portable analysis to our optogenetic grid; superposition is blocked (grid is single-site-only), so the field-decay fit is the doable half and converts our spatial-linearity assumption into a measured, cross-modality-comparable result. Excit/inhib split exploits our dual-opsin design (the cell-type specificity the paper concedes electrical microstim lacks).
**Next:** (1) Decide whether the excit intermediate-distance negative dip (binned medians ~−0.25 at 2–3 mm) is a real weak surround or noise — check anisotropy (within-hemi vs across-midline) before claiming isotropic decay. (2) Build the reachable-manifold/controllability SVD on the same gain matrix (paper Fig 4 / GCA analog) as the companion panel. (3) The true linear-superposition test needs a NEW simultaneous multi-site (persistently-exciting) stim experiment — scope it.

### 2026-08-24 — Single-session CV panel: fewer amps + drop SNR-floor amplitudes
**Changed/Found:** `impulse-analysis/imp_tf_cv.m` — single-session supp panel now draws `CV_SINGLE_NAMP` (default **3**) amplitudes instead of 5, and **drops amplitudes with held-out R² ≤ 0** (the SNR floor) before spacing the rest. AL_0033 now shows 1.1 V (R² 0.51) / 3.2 V (0.93) / 4.9 V (0.99); the 0.5 V amp (R² −2.72) is gone.
**Why:** User: "not so many amps, only a few." Doubles as CV-R² improvement lever #3 (exclude sub-threshold drives where the response is pure noise and no LTI fit can score).
**Next:** The bigger CV-R² question (is Mouse 1a's 0.50 model-limited or noise-limited?) is still open — needs the split-half **noise-ceiling** computation + attenuation-corrected R² in `imp_tf_cv_session`. Not yet built.

### 2026-08-24 — Single-session CV TF fit → supplementary; 2I step response → supplementary (pending)
**Changed/Found:** (1) `utils/imp_tf_cv_session.m` gained per-amplitude accumulation (`opts.perAmp`, default true): returns `C.amp` with split-averaged held-out **measured** and **LTI-predicted** (`uA·hHat`) traces + per-amp held-out R². (2) `impulse-analysis/imp_tf_cv.m` gained the **supplementary single-session panel** (`CV_SINGLE`, default true; `CV_SINGLE_MN='AL_0033'`) → `paper/images/supplementary/tf_cv_single_AL_0033.pdf`: representative amplitudes, measured held-out solid vs held-out prediction dashed, at **4p3z0d** (the widened cross-session constraints — user wanted "same constraints"). Per-amp held-out R²: 0.5V **−2.72** (SNR floor), 1.6V 0.81, 2.7V 0.78, 3.7V 0.95, 4.9V 0.99. (3) PAPER.md: 2I (`ol_tf_trial_avg`, OL step LTI fit) reclassified **→ supplementary**, registered the new single-session panel.
**Why:** User settled two forks: 2C stays the all-session CV overlay, the single-session per-amplitude fit goes to supplementary; and the step response (2I only) moves to supplementary reframed to justify the integral controller. Building the single-session fit through the SAME engine (`imp_tf_fit_session`/`imp_tf_cv_session`) gives constraint-consistency for free — AL_0033 now fits at 4p3z0d (vs the old single-session `tf_fit.m` 3p/0d), and CV proves 4p does NOT overfit (held-out R² 0.95).
**Next:** ⚠ 2I move is NOT complete — still need to (a) redirect its export path `figure2/`→`supplementary/` in the generating script, and (b) reframe its caption/text to the steady-state-offset → integral-term motivation. Both are part of the **Fig-2 reframe** (user's next step), after which comes the **residual-analysis state-dependence** block (Fig-2 row 4, currently empty). The 0.5 V amp's negative held-out R² is honest but ugly — consider dropping the weakest amp from the representative set or annotating it SNR-limited.

### 2026-08-24 — Cross-figure MOUSE-label convention + Fig-2 2A/2B swap
**Changed/Found:** (1) New shared helper `utils/imp_mouse_label.m` — per-ANIMAL labels "Mouse 1a/1b/2/3" (unique mouse names numbered in order, a/b suffix when one animal contributes >1 session). Extracted from `dose_response.m`'s inline logic; `dose_response.m` now calls it, and `impulse-analysis/imp_tf_cv.m` (panel 2C legend) switched from `s1..s4` to the mouse labels. User: "legends across the figures must consistently use mouse instead of session." (2) **Fig-2 2A/2B SWAPPED** in PAPER.md: 2A = `imp_response.pdf` (dose-response), 2B = `imp_single_*.pdf` (single-session traces) — user: "imp_response is new fig 2A, imp_single is the new fig 2B."
**Why:** Consistency — 2A (dose-response) already used mouse labels; the TF/CV panels used session numbers or `imp_sess_label` ("0033 01/29"), so a reader could not carry one identity across panels. One helper = one convention.
**Next:** Propagate mouse labels to the remaining session-labelled Fig-2 panels — TF-A (`tf_tau_forest`, ticks s1…sN via `imp_sess_label`), TF-D (`tf_model_swap`), and the 2G scatter legend (`imp_state_trialvar_fig.m`, s1..s4). Not yet done. `imp_sess_label` (date-based) stays for panels where the DATE distinguishes sessions; audit which convention each panel should use.

### 2026-08-24 — CV shape overlay promoted to paper panel 2C; exports to figure2, legend R², minimal
**Changed/Found:** `impulse-analysis/imp_tf_cv.m` — the cross-validated shape overlay is now **paper panel 2C** (user: "this figure will become 2C"). Changes: (a) exports straight to `paper/images/figure2/tf_cv_shape_across_sessions.pdf` — `CV_OUTDIR` default moved from `paper/explore/tf_cv/` to `figure2/` (user: "only put them in the paper figure folders"), and the `paper/explore/tf_cv/` staging folder was deleted; (b) per-session **held-out R² written on the legend** (s1 0.50 / s2 0.86 / s3 0.95 / s4 0.84), legend moved to **northwest** (top-left is empty while the traces dive to the dip) with a small token so it no longer overlaps traces; (c) **footer text removed** (solid/dashed encoding → caption); (d) the in-sample-vs-held-out R² comparison (old panel B) is gated behind `CV_PANELB` (default **false**) — user: "dont need the r2 comparision for paper". PAPER.md: 2C repointed to the CV panel; old single-session `tf_data_vs_model` (2C-old) and in-sample `tf_shape_across_sessions` (2C-i) marked SUPERSEDED.
**Why:** The CV overlay is strictly better than the in-sample 2C-i (it is cross-validated), so it takes the 2C slot. Distinct filename (`tf_cv_...`) from the in-sample `tf_shape_across_sessions.pdf` so a later `imp_tf_run` cannot overwrite the paper panel.
**Next:** ⚠ `imp_tf_run.m` still writes the in-sample `tf_shape_across_sessions.pdf` into figure2 — it is now the diagnostic/explore version, do NOT assemble it. Update the 2C caption to state solid=measured/dashed=held-out fit + s1's fold instability (dashed = mean over 200 splits). Fig-2 layout row 1 col 3 (2026-08-17) now = CV 2C.

### 2026-08-24 — Grid state panel reframed to Fig-2J/2K PREDICTION-ERROR-vs-state; model math added to grant
**Changed/Found:** New `bilateral/grid/grid_state_prederr.py`; `grid_aim1_panel.py` panel C swapped from the trial-averaged gain split to a **prediction-error-vs-state** curve mirroring paper Fig 2J/2K. Method: per site×amp, the drive-only (amplitude-mean = LTI) predictor gives a per-trial residual on the evoked scalar (mean over 0.03–0.45 s at the stimulated site); trials binned into quartiles of a pre-stim state marker (within-cell percentile rank); **prediction error = pooled-within-cell SD of the residual per quartile** (same "pooled within cell so a bin isn't more uncertain just from bigger responses" logic as `imp_state_trialvar.m`'s `sdRaw`); bootstrap 95% CI over trials. Two markers computed: **relative δ (1–4/0.5–30 Hz) — power-INDEPENDENT, admissible** → monotone rise Q1→Q4 **1.00→1.35, trend r=+1.00**; raw variance (POWER CONFOUND) rises steeper 1.00→2.9 and is NOT plotted (only mentioned). Panel normalised to Q1 because `roi_ts` is raw SVD units (~10⁴), NOT ΔF/F — do not label that axis ΔF/F. Also rendered 4 model equations (`draft/render_eqs.py` → `eq_lti/graph/gdar/state.png`, matplotlib mathtext) into a new Aim-2 "Modeling framework" subsection of `draft/grant_writing.docx`: (1) single-site LTI state-space/TF `H(s)=cᵀ(sI−A)⁻¹b=Σ rᵢ/(s−λᵢ)`; (2) graph forward model `x_{t+1}=A(𝒢)x_t+Bu_t`; (3) GDAR graph-VARX `x_t=Σ A_k x_{t−k}+Σ B_c u_{t−q}+e_t`; (4) state-conditioned operators + LQ control objective. Doc is now 5 pages.
**Why:** User: "not a fan of fig a1.2 panel c, i want the prediction error vs state type analysis like we did in figure 2 of the paper, in parallel blend in some math on all the models we are using." Prediction-error framing is stronger than the trial-averaged gain split AND sidesteps the drift/gain-artifact subtlety: it directly says "the fixed LTI model leaves state-structured error → motivates Aim 2's state-conditioned models." Using the power-INDEPENDENT marker (relative δ) follows the impulse-analysis rule that pre-stim variance/abs-δ are power confounds (root `impulse-analysis/CLAUDE.md`); grid has no motion trace so relative δ is the only admissible axis available.
**Next:** (1) The trial-averaged gain split (`grid_state_trialavg.py`, prior entry) is NOT in the grant now — retained as the mechanistic backup if a reviewer wants the raw effect. (2) If promoted past a prelim, express prediction error in real ΔF/F (needs the SVD→ΔF/F scale for `roi_ts`) instead of Q1-normalised. (3) Equation (4)'s control cost is illustrative LQ notation — reconcile with the actual PI/preview controller form in Aim 3 before this goes in the real Research Strategy.

### 2026-08-24 — Grid state-dependence RESOLVED: drift-invariant but FAST-state-DEPENDENT (high pre-stim activity → larger evoked gain); grant Aim-1 composite built
**Changed/Found:** New `bilateral/grid/grid_state_trialavg.py` (trial-averaged split), `grid_tf_lean.py` (2-exemplar LTI figure), `grid_aim1_panel.py` (3-tile composite → `grid_png/grid_aim1_panel.png`). Reconciled yesterday's apparent state-INVARIANCE (r=−0.05) with a trial-averaged median-split that shows a clear ~40% larger evoked peak in high-pre-stim-variance trials. The two disagree because they measure DIFFERENT state axes: (1) **variance INCLUDING slow drift** — drift continues into the post window but is orthogonal to the fast evoked transient, so it does NOT project onto the mean shape → the template-projection gain sees a slight NEGATIVE relation (drift trials look, if anything, smaller in shape-matched terms) = the r=−0.05 null. (2) **1 Hz-high-passed variance** (drift removed) — now measures fast cortical-state fluctuation, and higher pre-stim activity precedes a LARGER evoked transient. The high-passed median split gives within-cell shape-projection gain **0.72 (low) → 1.28 (high), high>low in 74% of site×amp cells** (n=104 cells) — so it is NOT the noisy-trace-bigger-|peak| artifact (that control is passed). Diagnostic tests run inline: raw split showed a pre-stim offset + sustained plateau (drift signature); per-trial pre-window linear detrend made the high group ramp monotonically to 0.8 (drift over-extrapolation); 1 Hz high-pass gave a clean decaying transient in both groups with the gain difference surviving.
**Why:** User wants the grid state-dependence presented "following the trial-averaged comparison we did for motion and pre-trial var" for the grant Aim 1. Honest message is now positive and on-theme: **cortical state gates the gain of the evoked optogenetic response** (identical stimulation, larger response from a more active pre-stim state) — the mouse analogue of the OS-Pulse parameter-dependence, and squarely "state-dependent cortical responses" for Aim 1. Yesterday's "state-invariant" entry was correct ONLY for drift-inclusive variance; it is superseded for the grant by the drift-removed result.
**Next:** (1) The projection control is a plain (non-leave-one-out) template; if this is promoted beyond a grant prelim, redo with LOO + a phase-shuffled noise floor to fully rule out any residual amplitude-selection bias. (2) `grid_state_dependence.py` figure (scatter) is NOT used in the grant — the trial-averaged `grid_aim1_panel.png` panel C is. (3) Composite feeds `draft/grant_writing.docx` Aim 1 mouse-grid section (LTI fits + brain spot + state panel).

### 2026-08-24 — Grid state-dependence: focal evoked GAIN is state-INVARIANT; the naive |peak| effect is a noise artifact
**Changed/Found:** New `bilateral/grid/grid_state_dependence.py` — tests whether pre-stimulus cortical state (pre-stim variance and 1–4 Hz power over a 1 s pre-onset window) predicts the evoked FOCAL photostim response on the AL_0048 52-site grid, using the single-trial cache `data/grid_trials_2amp.npz` (continuous per-site `roi_ts`; 5200 onsets, 52 sites × 2 amps, fs≈70 Hz). Regressors + response z-scored WITHIN site×amp then pooled. **With a naive signed-|peak| response measure the correlation is strong (Spearman r=+0.49 / +0.47, p≈0) — but this is a NOISE ARTIFACT** (a noisier trace has a larger |peak| by chance). **Switching the response to a leave-one-out template-projection gain (amplitude along the site's mean evoked shape, robust to broadband noise) collapses the effect to r=−0.05 (variance) / −0.045 (low-freq)** — negligible despite n=5200. So the focal evoked GAIN is essentially state-invariant to ongoing pre-stim state. Figure: `bilateral/grid/grid_png/grid_state_dependence.png` (quartile bars + scatter).
**Why:** User asked to add grid state-dependent analysis for the grant. The result corroborates the existing brain_paper finding that the LOCAL stimulus response is state-robust ([[project_impulse_status]] / FINDINGS "Local photoinhibition response is state-robust") — state-dependence lives at the GLOBAL/network level and in stimulation PARAMETERS (the OS-Pulse frequency roll-off), not in the local focal gain. Prevents putting an inflated r=0.49 artifact into a grant.
**Next:** If a positive grid state result is wanted, test state-dependence of the DISTRIBUTED/off-site response (network level), not the focal gain — but control the same |peak|/noise confound (use template projection or contra-style global/local split). Motion is not available in the grid caches, so only variance + low-freq power are buildable state axes here.

### 2026-08-24 — Trial-level cross-validated LTI fit across all sessions (new engine + figure)
**Changed/Found:** New `utils/imp_tf_cv_session.m` + driver `impulse-analysis/imp_tf_cv.m`. Repeated **stratified split-half over TRIALS, both directions** (100 splits ×2 = 200 held-out fits/session): within each amplitude the trials are randomly halved (stratified so both halves keep the full dose range and h(t) is built the same amp²-weighted way), fit the fixed session order on one half, predict the OTHER half's measured h(t). Two metrics: `R2_out` (amplitude-normalised — prediction carries its own gain, the honest number) and `R2_shape` (each peak-normalised — pure shape, matches 2C-i). Matched in-sample control = same train model scored on its own half. Panels → `paper/explore/tf_cv/`: `tf_cv_shape_across_sessions.pdf` (CV version of 2C-i: measured held-out solid vs held-out prediction dashed) and `tf_cv_r2_trial.pdf` (in-sample→held-out R² per session, median+IQR). **Result:** pooled held-out R² **0.86** (amp-norm) / 0.87 (shape). Per session held-out: AL_0033 e1 **0.95** [0.92-0.97], AL_0041 e2 0.86, AL_0048 e1 0.84 (10% folds fail to fit the fixed order on a half), **AL_0041 e1 0.50 [0.05-0.76]** — collapses from 0.97 in-sample.
**Why:** User: "normalised LTI fit comparison for all sessions but cross validated, done properly — randomise both datasets first then fit then test." The existing 2C-i dashed curve is IN-SAMPLE and the only CV in the pipeline (LOAO / `talk/make_lti_cv_fig.m`) holds out an AMPLITUDE but still fits/tests on trial AVERAGES — neither holds out trials. This is the honest generalisation test. AL_0041 e1's in-sample 0.97 was optimistic: fewest amps + simplest order (3p1z0d) → it was partly fitting trial-average noise, which only a trial-level split exposes.
**Next:** (1) Order is selected ONCE on full data and held fixed across folds (matches the trial-bootstrap's choice; re-selecting per fold needs the full sweep ×200) — so the CV validates the DYNAMICS, not order-selection; state in caption. (2) Panel A overlays the MEAN held-out prediction (averaged over 200 models ≈ smooth), so AL_0041 e1's instability is only visible in panel B's IQR whisker, not the overlay — do not over-read the overlay for that session. (3) Partially answers Nick's 2026-05-08 "report R² on held-out test set" ask (this is stronger — trial-level, not a single 20% split). (4) Decide promotion: if this replaces/augments 2C-i, move PDFs to `figure2/` + register in PAPER.md; currently PNG-policy staging in `explore/`.

### 2026-08-24 — Fig-2 state panels 2J/2K adopt the NeuroAI raw-units setup (paper panels)
**Changed/Found:** Re-exported `paper/images/figure2/imp_state_var_motion.pdf` (2J) and `imp_state_var_reldelta.pdf` (2K) via `imp_state_trialvar` + `imp_state_trialvar_fig` with `STVF_UNITS='raw'`, `STV_PLOTCTRL=false`, `STVF_PAPER=true`, `STV_MOTEXCL=false` (single pass, all 4 impulse sessions, 1767 trials). Both panels now show **Prediction error (% ΔF/F)** across Q1–Q4 on a **shared y-axis** as a single red series — no grey stim-free control line (raw units have no control counterpart). Matches the talk build (`talk/make_state_panels.m`).
**Why:** User (2026-08-24): "make the imp_state_var delta … converted to the conference setup, update the panel pdfs." Confirmed via question: convert BOTH 2J+2K (keeps the pair comparable), and drop the control line. Ran a SINGLE non-motion-excluded pass rather than the talk's two-pass (motion-excluded delta), because (a) it keeps 2J and 2K on ONE shared y-axis (the two-pass build did not), and (b) it reuses the SAME trial pool as the committed 2026-08-12 panels, so only the units change — δ Q4/Q1 1.19 [1.04-1.37], strat ρ +0.089, per-session slopes s1 −0.009 / s2 +0.218 / s3 +0.077 / s4 +0.098 are all unchanged. The "δ does NOT beat its stim-free control" caveat can no longer be read off the panel (1.19 stim vs 1.35 ctrl) → it must live in the caption/text now.
**Next:** 2G (`imp_state_reldelta_scatter.pdf`) is collaterally re-exported unchanged (non-excluded, units-invariant). Update the 2K caption so the "does not beat control" negative is stated in words, since the grey series is gone. Re-check 6 pt type at 4.2 cm on both raw panels before assembly.

### 2026-08-24 — dose_response.m: paper panel 2B now defaults to real y-axis + mouse labels
**Changed/Found:** `impulse-analysis/dose_response.m` — flipped the two paper-panel defaults: `DR_YAXIS` false→**true** (real y axis with ticks + "Inhibition energy (% ΔF/F, 0-200 ms)" units, replacing the 0.01 corner scale bar) and `DR_LABEL` 'slope'→**'mouse'** (per-animal direct labels Mouse 1a/1b/2/3, AL_0041's two sessions suffixed a/b). Re-exported `paper/images/figure2/imp_response.pdf` (4.20 × 3.60 cm). Slopes printed: s1 −0.34 (R²0.97) / s2 −0.57 (0.51) / s3 −0.59 (0.92) / s4 −0.96 (0.91).
**Why:** User (2026-08-24): "update the main panel like we did for the neuroai conference where we added the y axis and legends." The knobs already existed (added 2026-08-19 for the talk via `talk/redraw_dose_only.m`); the talk kept them off for the paper default. Flipping the defaults makes the committed paper panel — and the `run_all.m` batch — carry the talk treatment durably.
**Next:** The corner-axes look is now inconsistent with sibling Fig-2 panels (2A etc. still use scale bars) — confirm with the user whether the whole row should get real axes, or 2B stays the exception. PAPER.md 2B row updated.

### 2026-08-20 — 3G session-averaged error offered as RMSE; the CL gain is 29%, not 44%
**Changed/Found:** `talk/redraw_fig3_colors.m` — new `F3_METRIC` knob ('rmse' default | 'mae'). RMSE takes `sqrt(mean(e.^2))` across trials at each sample instead of `abs(mean(e))`, y-limit is now taken from the data (the hardcoded 6 was MAE-era and clips RMSE), and the RMSE version exports to **`all_average_sessions_rmse.pdf`** so the MAE panel the paper text refers to is not overwritten. Numbers, stim-window mean over 13 sessions: **RMSE OL 2.99 → CL 2.12 (29% lower)**; MAE OL 1.68 → CL 0.95 (44% lower).
**Why:** Requested for the talk — RMSE has been the project's error metric everywhere else since 2026-07-16, and MAE on this panel was the standing exception. The 15-point gap between the two is not noise: MAE is `abs(mean over trials)`, so errors of opposite sign on different trials CANCEL and the panel measures BIAS only; RMSE cancels nothing, and RMSE² = bias² + across-trial variance. So the RMSE panel now *contains* what panel 3F (variance) shows separately, and the two are no longer independent evidence. Say that if they are shown on the same slide.
**Next:** `controller-analysis/step_response.m` writes the same `all_average_sessions.pdf` and is still MAE-only — repoint or add the knob there if the paper panel is ever switched too.

### 2026-08-20 — Motion/pupil clips exported as GIF; pupil moved to AL_0033 m5; blink artefacts caught
**Changed/Found:** `talk/make_motion_clip.py` rewritten — every clip now writes **mp4 + gif** (gif through the shared `presentation/gif_utils.save_gif`, one global palette, so no frame-to-frame shimmer), and two pupil clips were added: `pupil_lowarousal_t4331` and `pupil_arousal_t2625`. Pupil clips come from **AL_0033 2025-03-04 exp 1**, not AL_0048: AL_0048's eye camera cannot be segmented — its whole eye interior is dark, so `pupil_seg`'s darkest-compact-blob rule locks onto the crescent shadow between the eyelid glint and the fur and returns a "pupil" swinging 15 → 72 px between neighbouring windows (contact sheet checked). AL_0033 m5 tracks on 100% of frames.
**Why:** GIF was requested for dropping straight into slides. The session swap was forced by the contact sheet, not preference — the AL_0048 numbers would have looked like a clean bimodal state signal and were entirely segmentation failure.
**Next:** If any pupil claim moves to AL_0048, `pupil_seg` needs retuning for that camera first.

### 2026-08-20 — No sleep episode in AL_0033 m5; every apparent dilation was a blink
**Changed/Found:** Swept the m5 pupil at 1 Hz over all 89 min: 14.7 px (1st pct) to 19.2 px (99th), median 15.8, trackable 1.000, corr(motion z, pupil d) = **+0.23** (n = 5341 s). There is **no sustained drowsy episode** — the pupil is at a constricted baseline the whole session. Worse, the apparent large dilations are **blinks**: the lid occludes the pupil, the darkest compact object left is a sliver, and the reopening reads as a 15 → 45 px "dilation" in 1.5 s. The top-ranked window (t = 4786 s) was exactly this. `pick_arousal` now rejects on TWO tests — fraction of frames within 25% of the window median ≥ 0.97 AND min/median ≥ 0.60 — because the fraction alone passes a 1.5 s blink in a 10 s window at 0.92. Rejecting whole windows then discarded the only real dilation, so the picker now SLIDES the window start in 1 s steps to dodge the blink while keeping the dilation.
**Why:** A clip labelled "sleepy" that is really a blink would put an artefact on a talk slide as a brain state. Recorded because the same trap is live for any pupil analysis on these sessions, not just the clip.
**Next:** The +0.23 motion/pupil correlation is measured at 1 Hz, which aliases blinks and probably inflates it (movement → blinks → spurious large diameters). Do NOT quote it as validation without recomputing on blink-screened frames. Blink-free range in this session is only 15.0 → 16.7 px (4th → 87th pct), so if the talk needs a real drowsy/alert contrast, another session has to be scanned.

### 2026-08-20 — RESEARCH.md rotated: 540 entries moved to `RESEARCH_archive_2026H2.md`
**Changed/Found:** Live log was 3841 lines / 691 entries against the 1000-line soft cap. Split at the first entry boundary after line 1000: 151 entries (2026-08-12 → today) stay live, 540 entries moved verbatim into the new `RESEARCH_archive_2026H2.md`. Counts reconcile exactly (151 + 540 = 691); nothing edited or deleted. Live file carries `<!-- older entries → RESEARCH_archive_2026H2.md (then RESEARCH_archive_2026H1.md) -->` and the new archive's header repeats the full grep chain.
**Why:** Note for whoever rotates next — CLAUDE.md's "keep the most recent ~50 entries / ~3 months" cannot be satisfied both ways at this project's entry rate. All 691 entries were inside the last seven weeks (~14/day), so 50 entries would have left two days of log while three months would have archived nothing. Split by the 1000-line scannability target instead, which is the cap's actual purpose.
**Next:** Grep all three files (`RESEARCH.md`, `RESEARCH_archive_2026H2.md`, `RESEARCH_archive_2026H1.md`) when searching project history.

### 2026-08-20 — CV R² panel split into `talk/plot_cv_r2.m`; paired drop = 0.05
**Changed/Found:** Panel-2 drawing moved out of `talk/make_lti_cv_fig.m` into a new function `talk/plot_cv_r2.m`, which takes the fold stats (or loads `talk/tf_cv_stats.mat` with no arguments) — so the panel can now be restyled in seconds instead of re-running `imp_tf_run` behind a ~20 min cold load. Also fixed the off-scale annotations, which overprinted each other at a single y (both clipped folds wrote to the same spot); they are now stacked and read `−6.7 → −7.9` and `−2.7 → −2.7`. Dead `local_bad` helper removed. Final numbers: 21 folds, 4 sessions, median R² 0.777 in-sample → 0.667 held out, **median paired drop 0.050**; 4/21 folds held-out < 0, of which **3 also fail in-sample**.
**Why:** The paired drop of 0.05 is the number that answers "does the plant model generalise" — the 0.78→0.67 median gap is mostly composition, not generalisation loss, because the folds dragging the held-out median down are amplitudes the fit never captured in the first place. The `−2.7 → −2.7` flat line is the visual proof and is why the off-scale labels had to be legible rather than merely present.
**Next:** none.

### 2026-08-20 — LTI cross-validation figure: leave-one-amplitude-out prediction, 21 folds / 4 sessions
**Changed/Found:** New `talk/make_lti_cv_fig.m`; `utils/imp_tf_fit_session.m` now stores the LOAO held-out prediction (`T.loao_pred`, `T.loao_meas`) alongside the existing `R2_loao`, which previously kept only the scalar. Two panel types exported to `talk/`: `tf_cv_traces_<session>.pdf` (measured solid vs held-out prediction dashed, one pair per amplitude) and `tf_cv_r2.pdf` (in-sample vs held-out R², every fold). Results — in-sample median 0.777 → held-out median 0.667 over 21 folds; per session, AL_0033 2025-01-29 e1 spans −2.75…+0.97 (9 folds), AL_0048 2026-07-15 e1 +0.67…+0.87 (3), AL_0041 e1 +0.18…+0.88 (3), AL_0041 e2 −7.90…+0.73 (6).
**Why:** The 2C-i shape panel the talk uses is an IN-SAMPLE fit — the model saw every amplitude it is drawn against — so it cannot support "the plant model generalises". The user asked to confirm the normalised LTI panel was cross-validated; it is not, and rather than mislabel it, this builds the panel that actually is. The LOAO folds already existed in the fitter; only the held-out trace was being discarded.
**Next:** Decide whether the talk's shape panel should be redrawn from the LOAO models instead of the in-sample `S{k}.h`.

### 2026-08-20 — CV R² panel rescaled; the negative folds are fit failures, not overfitting
**Changed/Found:** `talk/make_lti_cv_fig.m` panel 2 rewritten — y clipped to [−1.2, 1] with off-scale folds drawn at the floor and labelled with their true in-sample/held-out values, plus the paired drop (median `R²_in − R²_out`) added to the title and a `talk/tf_cv_stats.mat` dump of every fold's stats. **Finding:** on the worst folds the in-sample R² is essentially as negative as the held-out one (AL_0033's weakest amplitude: −2.75 in-sample, −2.75 held out — the connecting line is flat), so those amplitudes are ones the model fits badly whether or not they were held out. In AL_0041 e2 the −7.90 fold's *measured* trace has no inhibitory dip at all — it rises monotonically to +2.4 %ΔF/F — so the model is being scored against a response that is not an impulse response.
**Why:** The first render let a −7.9 and a −6.7 fold set the ylim, squeezing the other 17 folds into a ~5%-tall band and hiding the in-sample-vs-held-out comparison the panel exists to show. Clipping with labelled off-scale folds relocates them rather than hiding them. The paired drop is the statistic that answers "does it generalise"; comparing the two medians conflates generalisation loss with amplitudes the fit never captured.
**Next:** Check AL_0041 e2's positive-going amplitude before quoting the −7.90 anywhere — it looks like a bad amplitude (wrong stim detection or too few trials), not a model failure, and if so it should be excluded from the fold set rather than explained away.

### 2026-08-20 — `pooled_new_mice.m`: PNM_DROPNEW knob; 13-session 3K without the highlighted pair
**Changed/Found:** `controller-analysis/pooled_new_mice.m` — added `PNM_DROPNEW` (drops the two 2026-07-29 sessions AL_0048/AL_0051 outright instead of drawing them highlighted in black), plus `PNM_OUTNAME` / `PNM_OUTDIR`. Default output basename is now `pooled_ol_cl_rmse_%dsess` computed from the surviving session count, so a dropped-session export cannot be confused on disk with the paper's 15-session panel. Exported `talk/_stage/images/figure3/pooled_ol_cl_rmse_13sess_nohl.pdf/.png`: 13 sessions, OL med-of-med 2.50 → CL 2.02, CL lower in 13/13, signrank p = 2.4e-4 (15-session version was p = 6.1e-5 with the same direction).
**Why:** Asked for on 2026-08-19 for the talk. Note a bookkeeping failure worth recording: the knobs were reported as added that day but the heredoc-based patch had silently failed its `assert`, so nothing reached disk — the edit only landed today. Cause: `bash` heredocs in this environment collapse `\\` to `\` inside the script body, which corrupts both Windows paths and MATLAB `fprintf` format strings; patch scripts are now written with the Write tool instead.
**Why (scientific):** The black highlight advertises "these two are special" and pulls a talk audience into a side question about the new mice, when the claim on that slide is the pooled one. The paper panel keeps all 15 — this is a separate staged export, never the same file.
**Next:** Decide whether the talk uses 13 or 15; if 15, delete the staged copy so only one version of the panel exists.

### 2026-08-20 — Face-camera motion clips for the talk (`talk/make_motion_clip.py`)
**Changed/Found:** New `talk/make_motion_clip.py` cuts face.mp4 (AL_0048 2026-07-15 exp 1, server) at windows chosen from the facemap motion trace and renders video + synchronised z-scored trace with the motThresh = 1.5 line. Exports `talk/motion_high_t1886.mp4` (6 s, mean z +6.17, peak +19.13) and `talk/motion_quiet_t9822.mp4` (mean z −0.33). Gotcha recorded in the header: this face camera is **30 fps with one motion sample per frame**, so frame index == motion index — `load_experiments`' `motion_1(1:2:end)` decimation must NOT be applied here (those rigs run the face camera at 2× the blue-frame rate); applying it would desync video from trace by 2×.
**Why:** The talk asserts high motion is a brain state that changes closed-loop performance, but the audience has never been shown what the motion regressor is. Window picked as the strongest *sustained* 6 s bout (mean-z over a 6 s boxcar), not the peak spike, and restricted to t > 8 min because minute 4 (z = 4.38) is the mouse being mounted, not spontaneous movement.
**Next:** If the talk wants the clip on a controller session instead of AL_0048, re-point `VIDEO`/`PROC` — the picker is session-agnostic.

### 2026-08-20 — Combined talk video pass 2: title card straight into OPEN LOOP, more real-time trials
**Changed/Found:** `presentation/make_video_combined.py` — `INTRO` 108 → 34 (title card only: 24 solid frames + a 10-frame fade; the setup walk-through beats and `THE SETUP` act label are gone, `INTRO_BEATS` left in place but no longer drawn), `NHERO` 3 → 5 per condition, new `MONT_MAX = 16` subsampling the fast montage evenly across the remaining trials, and 4th/5th narration sets added to both `OL_HERO_BEATS` and `CL_HERO_BEATS`. Total 3050 frames (101.7 s at 30 fps) vs ~3020 before, so runtime is held.
**Why:** User asked for the title card to hand straight over to the open-loop card and for more real-time trials than fast ones at unchanged total length. Real-time share rises from 6 to 10 hero trials (900 → 1500 frames) and the montage falls from every remaining trial to 16 per condition; the montage is subsampled rather than sped up further because at FAST = 8 a trial is already ~0.27 s and shaving it turns the montage into a flicker.
**Next:** Watch the OL→CL montage chunking — 16 trials split across 5 hero passes is ~3 trials (~0.8 s) between real-time passes; if that reads as too abrupt, drop `NHERO` to 4 and raise `MONT_MAX` rather than lengthening the video.

### 2026-08-19 - State panels re-expressed as PREDICTION ERROR in %dF/F
**Changed/Found:** `imp_state_trialvar.m` now also computes `R(k).sdRaw` - the SD of the RAW
deviation from the amplitude mean, pooled WITHIN each (session x amplitude) cell - and
`imp_state_trialvar_fig.m` gained `STVF_UNITS` ('sd' default = published 2J/2K axis, 'raw' =
%dF/F). `talk/make_state_panels.m` runs 'raw'. The bootstrap CI is carried over by the per-bin
conversion factor. Numbers (Q1->Q4, %dF/F):
  motion, all trials      2.78 -> 3.18 -> 3.08 -> **2.08**
  pre-trial var, mot-excl 1.11 -> 2.06 -> 3.21 -> **4.09**
**Why:** user, 2026-08-19: "sd of dev is not a great explainer ... use prediction error in its
correct units". The published y is the deviation scaled by the within-amplitude SD, so it is
~1 by construction and unitless - it can say wider/narrower but never HOW wide, which is the
whole point of the slide.
**Next:** pooling within (session x amplitude) cells is load-bearing, not cosmetic - a plain SD
over the bin would inherit the between-amplitude spread of response SIZE, so a bin holding more
high-amplitude trials would read as more uncertain purely because its responses are bigger.
That is the same reason the DV was scaled per amplitude originally; this keeps the protection
and the units. The panels now state the claim directly: prediction error falls to **2.1 %dF/F**
in the top motion quartile and climbs to **4.1 %dF/F** in the top pre-stim-variance quartile -
a ~3.7x span across variance quartiles. Caveat unchanged: pre-trial variance is the 2026-07-02
signal-power confound, and here the confound is MORE visible, not less, because larger ongoing
signal mechanically inflates a residual measured in absolute units. Present it as the confound.


### 2026-08-19 - The normalised-LTI panel is NOT cross-validated; footer now states what it is
**Changed/Found:** `utils/imp_tf_paper_fig.m` gained `opts.cvtag` (wired to RUN_TALK in
`imp_tf_run.m`). The dashed curve in 2C-i is `S{k}.h` - the unit response of a model fitted on
ALL amplitudes of that session, i.e. **in-sample**. The cross-validated quantity in the same
struct is `R2_loao` (refit without one amplitude, predict it), which is a different number and
is not what the panel draws. So instead of labelling the curve "cross-validated" as asked, the
footer states both: `fit: in-sample | leave-one-amplitude-out R^2 = 0.67 (median, 21 folds)`.
**Why:** user asked me to "confirm its cross validated and then put a label on the plot that it
is". It is not, and a CV label on an in-sample curve is exactly the claim a referee checks first.
**Next:** the per-session LOAO column in the TFRUN summary is 0.53 / **-1.22** / 0.39 / 0.79 -
AL_0041 12/02 e2 generalises WORSE THAN ITS OWN MEAN across amplitudes. The 0.67 median over 21
per-amplitude folds hides that. If the talk claims the plant transfers across drive levels, that
session is the counter-example and should be named. Redrawing the dashed curve from the LOAO
models (a genuinely held-out shape panel) is the honest upgrade if the claim needs to be strong.

### 2026-08-19 - Inhibition energy: real y axis with units, per-MOUSE labels
**Changed/Found:** `impulse-analysis/dose_response.m` gained `DR_YAXIS` (default false) and
`DR_LABEL='mouse'`; `talk/redraw_plant_panels.m` + new `talk/redraw_dose_only.m` drive them.
Labels number the UNIQUE animals, not the sessions: the impulse set is AL_0041 e1, AL_0041 e2,
AL_0033, AL_0048 - four sessions from **three** mice - so the two AL_0041 sessions come out as
Mouse 1a / Mouse 1b. Y label carries the units: "Inhibition energy (% dF/F, 0-200 ms)".
**Why:** user, 2026-08-19: "put axis on y and put units and put labels mouse 1,2,3".
**Next:** first attempt set the ticks BEFORE `paperAxes` and they vanished - paperAxes is
cleanAxes + shortCornerAxes_plot and both strip the real axes, so the y axis must be rebuilt
AFTER the call. Noted in the code; watch for the same trap in any other corner-axes panel.

### 2026-08-19 - Amplitude-map GIF: blue-red colormap unified with the other GIFs; harder mask erosion
**Changed/Found:** `presentation/make_ampmap_gif.py` default cmap `jet` -> **RdBu_r** with a new
`--sym` (default on) that scales the colour axis symmetrically about zero. Regenerated with
`--mask-open 5 --mask-erode 6` (7.6% of mask px removed, up from ~2%).
**Why:** user asked to unify the GIF colormaps to blue-red. It also fixes a readability
inversion: under `jet` + `Normalize(min, 0)` the NO-EFFECT background rendered dark red and the
deepest inhibition rendered green - the loudest colour marked where nothing happened. With
RdBu_r about zero, white = no effect, blue = inhibition, and the faint CONTRALATERAL INCREASE
becomes visible in red for the first time (the old map had no colour left to show it).
**Next:** the straight top-left/top-right edges are the imaging-window boundary in `brainMask`,
not mask error - erosion cannot remove them without inventing an outline. `make_brain_gif.py`
already used RdBu_r/+-clim, so the two now agree; `PANEL_system_architecture.gif` is a diagram
and has no data colormap.

### 2026-08-19 - Fig-3 recoloured + new 4-window OL/CL variance ratio
**Changed/Found:** `talk/redraw_fig3_colors.m` (new) re-exports 3F/3G/3H on the current palette
(CL green -> blue, stale since 2026-07-23) and adds `variance_ratio_4windows.pdf`: the OL/CL
VARIANCE ratio on the SAME four windows the RMSE ratio panel uses. Loads only `data` from each
cache, one session at a time, so it sidesteps the ~40 GB full load. 13 sessions.
Ratios: Pre 0.95 (p=0.34) | 0-1 s 1.66 (p=0.0012) | 1-3 s 1.96 (p=0.00024) | Post 0.98 (p=0.64).
**Why:** user is putting the RMSE-by-window and variance comparisons side by side on a slide;
3I splits variance pre/stim/post while 3J splits RMSE pre/0-1/1-3/post, so the two panels
invited the reader to match windows that do not match.
**Next:** 3I is deliberately left alone (it is the published panel). The new one is additive -
decide which of the two goes in the paper before the next Overleaf sync.


### 2026-08-19 - Fig-2 state panels without the stim-free control; STV_MOTEXCL added
**Changed/Found:** `impulse-analysis/imp_state_trialvar.m` gained **STV_MOTEXCL** (default false):
drops trials with motion z > motThresh=1.5 from the pool before any marker statistic, so a
surviving brain-state effect cannot be movement under another name. The motion-threshold split
block is skipped when it is on (or whenever nothing is above threshold) - otherwise it divides by
an empty group and prints NaN ratios that read like results. `talk/make_state_panels.m` rewritten
as two passes with `STV_PLOTCTRL=false`: motion on ALL trials, then rel-delta + pre-trial variance
motion-excluded. PNGs -> talk/figure2/S1..S3.
**Why:** user, 2026-08-19 - "i dont need no stim controls, just state vs prediction error plot for
motion, then motion removed: rel delta, pre-trial var".
**Next:** motion exclusion costs 58/1767 trials (3.3%) and **barely moves the brain-state numbers**:
rel-delta Q4/Q1 1.19 [1.04-1.38] vs 1.19 [1.04-1.37] with all trials, strat rho +0.092 vs +0.089,
3/4 sessions; pre-trial variance 3.22 [2.82-3.62] vs 3.21 [2.89-3.63]. Useful as a robustness
statement - these states are not movement in disguise - but it is not a new result. Pre-trial
variance remains the 2026-07-02 signal-power CONFOUND (the script still prints "CONFOUND - not
interpreted" beside it); it is drawn because it was asked for, not because it is admissible.
Note the DV keeps its within-(session x amplitude) scaling from the FULL trial set by design, so
the excluded-trial panels share a y-axis with the full ones.


### 2026-08-19 - Combined video: variability panel out, preview made visible in the schematic, heroes = best trials
**Changed/Found:** `presentation/make_video_combined.py`, five edits:
1. **Across-trial variance panel deleted** (`axVar` and every artist on it). `axAvg` now spans the
   whole right column, gs[6:92, 59:100], and carries the x-label the variance panel used to.
2. **No running statistic on the sine act.** The tracking-MSE counter is gone from the `ff`
   branch; the mode text is just the mode. The chapter-1 counter is untouched.
3. **The preview branch now shows what preview IS.** Box relabelled `preview / r(t + 143 ms)` and
   widened to 0.200 so the label fits, and a glyph at the branch root draws the reference (dotted,
   ref colour) against the same reference advanced 143 ms (solid green) with a `+143 ms` tag. The
   "ref sine" label drops to y=0.545 when the glyph is up, or the two collide. Narration beats for
   mode 0 rewritten from "a feedforward branch sends it straight to the laser" to the shift.
4. **Hero (real-time) trials are now the BEST trials by RMSE**, chapter 1 and chapter 2 both:
   `pick_heroes` returns the n lowest-RMSE trials instead of quantiles 0.25/0.50/0.80, and the
   sine hero is `nanargmin(e)` instead of the median-error trial.
5. **Live trace clipped to a fixed -10..+10 %dF/F** in the sine act (was percentile-scaled), with
   ticks forced to [-10 -5 0 5 10] so the label stops crowding the axis title.
**Why:** all five are user calls of 2026-08-19. The variance panel was "too much to look at"; the
counter asked the audience to read numbers while the speaker was still explaining the modes; the
schematic said "feedforward" without ever showing that the thing fed forward is a time-shifted
reference; the slow passes are what people actually watch, so they should be the clearest example.
**Next:** hero picks are now the best of each condition, so the OL hero is the best OL trial - if
anyone reads the real-time passes as typical that is now wrong, and the montage behind them is
what carries the spread. Say so out loud when presenting.


### 2026-08-19 - BUG: state quartiles were binned GLOBALLY, not within session; motion panel now reproduces the published one exactly
**Changed/Found:** new `talk/verify_motion_quartiles.m` (read-only audit) + fix in
`talk/state_quartile_panels.m`. `motion_analysis.m` computes its quantile edges INSIDE the
session loop - quartiles are WITHIN-SESSION and only the resulting bins are pooled.
state_quartile_panels.m used one global edge set over all pooled trials. With within-session
edges the motion panel now reproduces the published one to the decimal: OL 30.4 / 29.8 / 29.6 /
32.1 and CL 24.5 / 24.9 / 24.6 / 21.3 on the old un-normalised scale (x sqrt(106)).
Two further fixes in the same pass: (a) the per-session slope is now regressed on the
WITHIN-SESSION PERCENTILE RANK of the state, not the raw state - a raw-unit slope is not
comparable between sessions whose state ranges differ 5x, which is why the median slope used to
contradict the panel it annotated; (b) both median and mean slope are printed, because the
pooled quartile curve tracks the MEAN while the signed-rank test is about the MEDIAN.
**Why:** user spotted that the Q1/Q2 profile did not match the previous analysis. It did not -
global edges sorted trials largely by SESSION (motion medians span -0.29..-0.03, maxima
0.99..4.78, so Q1 was fed by only 7 of 9 sessions) and manufactured a Q2 spike out of session
difficulty. dur = 3 for all nine sessions and all three stored window widths cross-check, so the
window and motion definition were never wrong - only the binning.
**Next:** corrected numbers (RMSE, Q1->Q4, slope-per-rank med/mean, interaction p):
  motion  OL +0.16 (2.95->3.12) CL -0.31 (2.38->2.07) | OL +0.905/+0.761 CL -0.030/-0.131 | **p=0.0039**
  prevar  OL +0.84 (2.51->3.35) CL +0.77 (1.99->2.77) | OL +0.730/+1.03  CL +0.173/+0.622 | p=0.203
  1-4 Hz  OL +0.13 (2.96->3.09) CL +0.24 (2.21->2.45) | OL -0.253/-0.265 CL +0.142/+0.253 | p=0.098
  1-2 Hz  OL -0.42 (3.15->2.73) CL +0.10 (2.22->2.32) | OL -0.193/-0.316 CL +0.034/+0.037 | p=0.074
  2-4 Hz  OL +0.23 (2.80->3.03) CL +0.21 (2.22->2.43) | OL -0.151/-0.126 CL +0.118/+0.298 | p=0.129
**The earlier "2-4 Hz carries the whole delta effect" claim from this morning is WITHDRAWN** - it
was an artifact of the global binning. On the corrected panels every delta variant is nearly flat
and no delta interaction reaches significance. What survives: motion (CL flattens a +0.9
dependence to -0.03, p=0.0039) and pre-stim variance (both loops rise together, CL offset below OL).


### 2026-08-19 - Motion panel must NOT be drawn on motion-clean trials (circular); split the trial set per state
**Changed/Found:** `talk/state_quartile_panels.m` - the motion cut is now carried as a MASK
(`Ko`/`Kc`) rather than applied in the gather loop, and each state declares whether it uses it:
motion -> ALL trials (`state_motion_quartile_allTrials.png`), brain states -> motion-clean.
Filenames and x-labels carry the set, and the console prints it per line. Added 2-4 Hz to the
exemplar figures. Motion on the full range: OL 2.47->3.05, CL 1.84->2.05, within-session slope
OL +0.554 vs CL -0.010, interaction **p=0.0078**.
**Why:** user, 2026-08-19 - "motion with motion clean trials makes no sense". Correct: binning
motion inside a set the same 1.5 threshold already truncated makes Q4 "the most movement among
quiet trials", not the high-motion tail. The clean-set motion panel from earlier today was
deleted.
**Next:** MATLAB gotcha worth remembering - a logical mask accumulated as `K=[]; K=[K; logical]`
comes out DOUBLE, and a double 0/1 vector then fails as a subscript ("indices must be positive
integers or logical"). Seed it `false(0,1)`.


### 2026-08-19 - Delta split 1-2 vs 2-4 Hz: the 2-4 sub-band carries the whole delta effect
**Changed/Found:** `talk/state_quartile_panels.m` extended - state list is now motion, pre-stim
variance, relative 1-4, relative 1-2, relative 2-4 Hz (all ratios to P(0.4-10)), plus a Q1-vs-Q4
EXEMPLAR figure (4 real trials per bin, OL row / CL row, deterministic pick spread across the
bin's own state range) for motion and relative delta. Pooled Q1->Q4 trial RMSE, 9 sessions:
  motion   OL 2.46->2.95  CL 1.83->2.01 | within-sess slope OL +0.648 CL +0.023 | **p=0.0039**
  prevar   OL 2.42->3.81  CL 1.93->3.33 | OL +0.089 CL +0.038 | p=0.25
  1-4 Hz   OL 2.83->3.45  CL 2.00->2.78 | OL -0.436 CL +0.328 | p=0.13
  1-2 Hz   OL 3.19->2.75  CL 2.35->2.22 | OL -0.463 CL +0.074 | p=0.055
  2-4 Hz   OL 2.76->3.48  CL 1.95->2.79 | OL -0.498 CL +0.141 | p=0.16
**Why:** user asked for the delta band broken into the same 1-2 / 2-4 split figure 4 already uses
(delta12/delta24), and for real traces behind the bin means.
**Next:** two things fall out. (1) **Motion is the only state with a significant OL-vs-CL
interaction inside the motion-clean set** (p=0.0039, OL slope +0.648 vs CL +0.023) - i.e. the
"CL rejects motion" claim survives even after the high-motion tail is cut, which is a stronger
version of it than the full-range panel. (2) **The 1-4 Hz effect lives entirely in 2-4 Hz**: the
1-2 Hz panel is flat-to-falling in both loops, the 2-4 Hz panel reproduces the 1-4 Hz rise almost
exactly. Consistent with figure 4's framing of 2-4 Hz as the hard-to-control sub-band. Caveat
unchanged and now flagged automatically in the console: for 1-4 and 2-4 the pooled OL trend and
the median within-session OL slope have OPPOSITE signs, so those two panels need a
session-normalised redraw before they can be called a within-animal state effect. 1-2 Hz and
pre-stim variance are self-consistent.


### 2026-08-19 - State-quartile OL/CL panels (pre-stim variance, relative delta) on motion-clean trials
**Changed/Found:** new `talk/state_quartile_panels.m` - the state twin of the motion-quartile
panel. Motion cut first (|mean z-motion, onset-2 s .. trial end| <= 1.5 = motThresh), then trial
RMSE pooled into quartiles of a PRE-onset state measured over [-2, 0) s: variance, and relative
1-4 Hz power (P(1-4)/P(0.4-10), a ratio, so power-independent). Lean - loads only `data` from
each cache, one session at a time. **Only 9 of the 13 sessions have a motion trace** (m1, m3, m7,
m8 have none), so these panels are n=9, not n=13. Motion removes just 24/990 trials (2.4%).
Numbers: prevar Q1->Q4 OL 2.42->3.81, CL 1.93->3.33, per-session slope OL +0.089 vs CL +0.038,
interaction signrank p=0.25. Delta Q1->Q4 OL 2.83->3.45, CL 2.00->2.78, slope OL -0.436 vs
CL +0.328, p=0.13.
**Why:** user wants the motion panel's argument repeated with brain state on the x-axis, with
motion excluded so it cannot be the driver.
**Next:** **the delta panel's pooled curve disagrees with its own within-session slopes** - pooled
OL rises Q1->Q4 while the median per-session OL slope is NEGATIVE, i.e. the rise is between-session
difficulty leaking into a raw-trial pool (exactly the effect cl_factor_olcl_panels.m removed on
2026-08-13 by subtracting each session's own level). Do not put the delta panel on a slide as a
within-animal state effect until it is redrawn session-normalised. Pre-stim variance agrees in
sign both ways and is safe; neither interaction is significant, so the honest claim is "CL sits
below OL at every level of state", not "CL flattens the state dependence".


### 2026-08-19 — Figure-5 panels exported as talk PNGs; make_panel_pngs.py takes a group argument
**Changed/Found:** `talk/make_panel_pngs.py` — added a `FIG5` list (5B-5K plus the two pooled
RMSE-with-clean variants) reading from `paper/images/figure5/`, and replaced the hard-coded
three-group loop with `GROUPS` + `sys.argv` so one figure can be re-exported alone. 12 panels
-> `talk/figure5/` at 600 dpi.
**Why:** the deck needs the sine/preview performance panels as slide images, and re-running the
whole 2/3/4 sweep to get them was wasteful. Session variant fixed to **2026-07-21**, the
representative session the PAPER.md Fig-5 caption names (a 2026-07-14 set also exists on disk).
**Next:** 5I/5J/5K are the pooled three-session claims — use those, not the single-session
5G/5H violin/phase, for any statement about feedback or preview in the talk.

### 2026-08-19 — Fig-3 F/G/H still drawn with the retired GREEN closed-loop colour
**Changed/Found:** `controller-analysis/variance_mse.m` — replaced every hard-coded `[0 0.5 0]`
green and `[1 0 0]`/`'r'`/`'g*'` red with `PS.col_ol`/`PS.col_cl` (panel F line, panel G violin
mean stars, and the G2 windowed-MSE block). `controller-analysis/step_response.m` already used
the PS constants.
**Why:** `PS.col_cl` changed green -> blue on 2026-07-23, but 3F/3G/3H were last exported
2026-07-16, so they are the only Fig-3 panels still showing CL in green (A-E, 2026-08-17, are
already red/blue; I/J are greyscale). The hard-coded greens meant a plain re-run would only
have fixed part of it.
**Next:** re-export 3F/3G/3H — see the `talk/redraw_fig3_colors.m` entry.


### 2026-08-19 — Individual paper panels exported to talk/figure{2,3,4}/
**Changed/Found:** New `talk/make_panel_pngs.py` → 12 Fig-2, 11 Fig-3 and 21 Fig-4 panels as individual PNGs, 600 dpi from PDF where a PDF exists, white-margin cropped, filenames prefixed with the panel ID (`3K_pooled_ol_cl_rmse_15sess.png`). Panel lists come from PAPER.md's registry for Figs 2 and 3; Fig 4 has no registry table, so its panels are grouped by the three-block story (`b1_` trial-average state dependence, `b2_` error contribution + factor slopes, `b3_` disturbance rejection, `map_` the contra→ipsi coupling). Retired panels (2D step response, 2H pre-var heatmap) are deliberately excluded. The earlier flat `talk/FIG3_*.png` dump was removed.
**Why:** User wanted the individual panels, not the assembled figures, and in per-figure subfolders.
**Next:** ⚠ The assembled `Figure3.pdf` still carries pre-rename axis labels ("Trial MSE", "MSE dF/F") — the metric was renamed RMSE on 2026-07-16 and 3H is genuinely MAE. The individually exported panels inherit those labels wherever the panel PDF predates the rename; re-export from source before any of them carry a metric claim on a slide.

### 2026-08-19 — Combined video: closing stats slide off, stale trace fixed, sine act lengthened
**Changed/Found:** `presentation/make_video_combined.py` — three changes. (1) The closing cross-session statistics slide is OFF by default (`OUTRO = 200 if args.outro else 0`, new `--outro` flag); `act_start` now uses `next(..., None)` because with no outro phase the old bare `next()` raised StopIteration. (2) `enter_ff()` now also hides `df_line` / `df_head` — the chapter-1 live trace was never added to `const_art`, so a stale closed-loop-blue curve survived the switch and sat under the sine trace on the live panel. (3) The moving-reference act gets more time: the hero trial plays `FF_REPS = 2` times per condition and the montage grew (`FAST_FF` 4→5, `FF_MONT` 22→30). Schedule is now 3020 frames ≈ 101 s at 30 fps.
**Why:** User: "in the combined video, i dont want that statistics panel at the end. and in the sine wave case there is a blue trace left over on the live trace panel, also give more time to sine wave case on both those modes as i need to explain what they are."
**Next:** The extra sine time comes from REPEATING the real-time pass, not from slowing it — so the "REAL TIME" label stays honest. If more air is still needed, raise `FF_REPS` rather than stretching a single pass.

### 2026-08-19 — Grid star now marks the auto-tune endpoint, not the grid minimum
**Changed/Found:** `controller-tuning/gain_grid.m` — new `GG_MARKPT` / `GG_MARKLBL` knobs. When `GG_MARKPT` is set the panel skips its own minimum star and marks that point instead; label offset in DATA units (0.04 × x-span), since a 13 pt star is wider than any run of leading spaces. Regenerated AL_0033 2025-03-05 with the star at the auto-tune's converged gains **(0.0677, 0.0636)**, which land inside the swept basin (grid min is (0.05, 0.10), J = 16.1).
**Why:** User: "remove the star from grid and place the one generated from path onto the grid location". The two methods are independent, so *where the online search lands relative to the exhaustive surface* is the actual claim — and two stars on one axis would muddle which is which. Drawn ON the point rather than offset because it is not a grid node, so nothing is hidden underneath.
**Next:** none. `PANEL_tuning_AL0033.png` rebuilt (grid | path | cost, 600 dpi).

### 2026-08-19 — load_grid caches to data/grid_tuning_cache.mat
**Changed/Found:** After a `clear`, `gain_grid.m` can be re-run from `load('data/grid_tuning_cache.mat')` instead of paying the full server load again — the cache holds both `G` (grid) and `A` (auto-tune).
**Why:** Re-running `load_grid.m` reads every session off the share; the cache turns a several-minute round trip into a second.
**Next:** none.

### 2026-08-19 — Star marker moved OFF the point it marks (new `utils/starOffset.m`)
**Changed/Found:** New `utils/starOffset.m`; `controller-tuning/gain_grid.m` now calls it for both the 2-D surface minimum and the 1-D sweep minimum. The star is placed a fixed fraction of the axis span away with a hairline leader, and flips direction when the point sits near an edge.
**Why:** User: "the star on that figure is hiding the color of the last point". On a cost surface the marked node IS the result, and an opaque white star on top of it hid the one datum the panel exists to report. Regenerated `AL_0033 2025-03-05` — the minimum at (0.05, 0.10) now shows its own dark-navy fill.
**Next:** `auto_tune.m` still plots its cost star with a plain `'p'` marker at line ~69; swap it for `starOffset` when that figure is next touched.

### 2026-08-19 — The published auto-tune path figure has no source in the repo
**Changed/Found:** `autotune_convergence_*.pdf` (2026-06-29) draw a (Kp,Ki) trajectory and an axis labelled "tune iteration". Nothing in the repo — at HEAD or anywhere in git history (`git log -S "tune iteration" --all` is empty) — produces that figure; the committed `auto_tune.m` draws Kp/Ki against trial on a yyaxis pair plus a cost scatter, a different panel entirely. New `talk/redraw_autotune_AL0033.m` rebuilds it from source: accepted gains `Kdata.npy` + costs `Kval.npy` off the server, 9 accepted iterations, cost 16.6 → 12.3, best (0.0677, 0.0636).
**Why:** The figure could not be changed at all without a generator. Also note the published version's grey "whiskers" were **rejected probes from input_params**, drawn unlabelled — `controller-tuning/CLAUDE.md` says figures must use accepted `Kdata`/`Kval`, so the redraw omits them.
**Next:** Decide whether the talk wants rejected probes shown *as* rejected probes (honest, and it makes the panel read as a search). If so, add them as a labelled faint layer — do not resurrect the unlabelled whiskers.

### 2026-08-19 — Trailing Kdata slots log cost 0, which is not a cost of zero
**Changed/Found:** `Kdata.npy` for AL_0033 03-17 is 10×2 but only 9 rows were evaluated; row 10 carries `Kval = 0`. `redraw_autotune_AL0033.m` filters on `V > 0` before plotting.
**Why:** Kept in, that row is the global minimum of the colour scale and the "best" point — the star would land on an iteration that never ran.
**Next:** Check the same guard is applied wherever else Kval is read.

### 2026-08-19 — Tuning panels narrowed to AL_0033 only
**Changed/Found:** Deleted the AL_0034 tuning PNGs and the multi-mouse sheets from `talk/`; `PANEL_tuning_AL0033.png` is now the single sheet — grid, auto-tune path, cost curve, side by side at 600 dpi.
**Why:** User: "lets keep AL33 grid and controller path". AL_0034 is a tuning-only mouse that appears nowhere else in the talk, so it cost a sentence of explanation for no gain.
**Next:** none — the AL_0034 source PDFs remain in `paper/images/tuning/` if the methods figure still wants them.

### 2026-08-19 — Controller-tuning contact sheets: every gain path beside its cost curve
**Changed/Found:** New `talk/make_tuning_sheet.py` → `talk/PANEL_tuning_AL0033_0317.png`, `PANEL_tuning_AL0034_1025.png` (path | cost per session), `PANEL_tuning_grids.png` (both cost surfaces), `PANEL_tuning_overview.png` (all of it on one sheet), plus the individual halves. The published `autotune_convergence_*.pdf` stack path above cost; these re-lay them left-to-right and caption each panel, since the source PDFs are inconsistent about carrying their own session title.
**Why:** User asked for all controller-tuning images as side-by-side PNGs of the cost curves and their paths. Left-to-right is the readable pairing on a slide — "where the search went" then "what that bought".
**Next:** Only 2 of 4 grid sessions and 2 of 3 auto-tune sessions have publishable figures — see the entry below before anyone asks for "the rest".

### 2026-08-19 — Which tuning sessions can and cannot be shown
**Changed/Found:** Confirmed against `controller-tuning/CLAUDE.md` + PAPER.md while assembling the sheets. Grids: AL_0033 03-05 and AL_0034 10-17 are exported; AL_0033 01-10 / 03-03 and AL_0034 10-18 are `needs rewire`, and the two 7-column AL_0034 sessions have **no onset column and no input_amps.csv**, so onsets are unavailable and they sit at `status='hold'`. Auto-tune: only AL_0033 03-17 and AL_0034 10-25 e1 have a LIVE online cost; **AL_0033 12-19 had a dead online cost (random walk)** and **AL_0034 10-25 e2 stuck at (0,0)**.
**Why:** "Give me all of them" needs an answer about what all is. These four are missing for concrete, documented reasons, not oversight.
**Next:** The 7-col onset adapter (derive onsets from Timeline `lightCommand`/`galvo` @2 kHz mapped onto the 35 Hz states axis) is the one unblock that would add two more grid sessions.

### 2026-08-19 — Splitting stacked figures by fixed fraction leaves a sliver
**Changed/Found:** `talk/make_tuning_sheet.py` cuts stacked panels at the widest all-white row band in the middle third rather than at a hard-coded 0.5/0.52 fraction. The fraction left a strip of the upper axis on the lower crop — visible as a stray glyph floating above the cost curve.
**Why:** Same trap will hit any future split of a multi-panel PDF; the gap-detection version is 15 lines and exact.
**Next:** none.

### 2026-08-19 — Impulse-trace legend moved clear of the traces (talk variant)
**Changed/Found:** `impulse-analysis/trace_overlay.m` — in talk mode (`TO_FITWIN=false`) the amplitude legend is now pinned to the bottom-right corner of the axes in normalised units and shrunk to 0.75× the paper font. At `Location='southeast'` the 5-row key still landed on the rebound.
**Why:** User: "the imp response plot needs the legend shifted farther to bottom right to not overlap the traces, maybe make the text smaller". Paper output unaffected — the tweak is inside the `~TO_FITWIN` branch.
**Next:** none.

### 2026-08-19 — New `talk/make_site_map.m`: the stim site and its effect
**Changed/Found:** New script. Trial-averaged peri-stim map for AL_0033 2025-01-29 e1 at 4.9 V — `imp.resp_map{end} − imp.base_map{end}` (the locked 0–200 ms window) painted inside `brainMask`, drawn in the canonical TRANSPOSED view with the cached data-derived site [row 373, col 353] ringed. Depth at site −3.17 %ΔF/F. → `talk/PANEL_site_map.png` (400 dpi).
**Why:** User asked for "a image of brain with stim site and its effect for this particular imp response dataset" — the talk shows the impulse traces without ever showing where on the cortex they come from.
**Next:** none. Note the map shows focal inhibition at the site spreading across the whole ipsilateral hemisphere with the contralateral side near zero — consistent with the Global/Local framing later in the talk.

### 2026-08-19 — brewermap is not installed; the silent fallback was flipud(jet)
**Changed/Found:** `talk/make_site_map.m` builds its own blue–white–red diverging colormap instead of `try brewermap ... catch flipud(jet)`. The fallback painted the entire ipsilateral hemisphere saturated red and hid the focal spot.
**Why:** A `try/catch` colormap fallback fails silently and produces a scientifically misleading picture rather than an error. Negative is now blue, so inhibition reads as suppression.
**Next:** Any other script using the same `try brewermap` idiom will be hitting the same fallback — worth a grep before trusting a diverging map from this repo.

### 2026-08-19 — System-architecture diagram animated as a signal-flow GIF
**Changed/Found:** New `talk/make_arch_gif.py` → `talk/PANEL_system_architecture.gif` (1200 px, 90 frames, ~6 MB). Composites a moving comet along a hand-traced polyline over the published `mouse_sys.pdf` backdrop — main loop plus the Kr feedforward branch, offset by a third of a cycle. The figure itself is not redrawn. Also exported `PANEL_control_architecture`, `PANEL_system_architecture`, `PANEL_rig_schematic`, `PANEL_widefield_lightpath` as high-res PNGs.
**Why:** User: "can we actually convert the system architecture image into a gif showing signal flow?" The static block diagram cannot show that the loop RUNS; a comet says it in one glance, and a GIF autoplays in PowerPoint with no animation setup.
**Next:** Path coordinates are hard-coded against the 2000-px view of the current `mouse_sys.pdf` — if that figure is re-exported with a different layout, the polyline in `MAIN`/`FF` must be re-traced.

### 2026-08-19 — Talk-only variants of the three plant panels, redrawn into talk/
**Changed/Found:** `impulse-analysis/trace_overlay.m` (`TO_FITWIN`, `TO_OUTDIR`), `impulse-analysis/dose_response.m` (`DR_LABEL='none'`, `DR_OUTDIR`), `utils/imp_tf_paper_fig.m` (`opts.preshow`/`tmin_s`/`lgd_len`), `utils/imp_tf_fit_session.m` (stores `T.tPre` / `T.h_measPre`, new `opts.preShow_s=0.15`), `impulse-analysis/imp_tf_run.m` (`RUN_TALK`), `impulse-analysis/imp_tf_figs.m` (`FIG_TALK`, `FIG_OUTDIR`), new driver `talk/redraw_plant_panels.m`. Redrew all three into `talk/`: 2A without the model-fit window, 2B without the per-session direct labels, 2C-i with a 150 ms pre-onset lead-in, a red stim marker at t=0, and a 20 pt legend token so the dashed sample reads as a dash. The 2C-i legend also moves to the top-right in talk mode — at the longer token it ran straight through the dip in the southeast corner.
**Why:** User, for the NeuroAI deck: no fit window on the impulse panel, no legend on the inhibition-energy panel, and on the normalised-response panel "the legend trace longer so that the dash is visible as a dash", plus a baseline before 0 and a stim marker. Every knob defaults to the paper behaviour, so `paper/images/figure2/` is untouched.
**Next:** The talk PDFs sit in `talk/` alongside the paper copies in `figure2/` — if 2C-i's lead-in is wanted in the paper too, flip `RUN_TALK` there rather than duplicating the file.

### 2026-08-19 — Controller slide: tuning figures into talk/, gain path added
**Changed/Found:** Copied `grid_cost_surface_AL_0033_0305.pdf` and `autotune_convergence_AL_0033_0317.pdf` into `talk/`; the lean deck's controller slide now shows the gain grid beside the **2-D (Kp, Ki) auto-tune trajectory** (coloured by cost) instead of the cost staircase, with the path spelled out: (0,0) → (0.046, 0.018) → (0.068, 0.062), cost 16.6 → 12.3, landing in the grid's basin.
**Why:** User: "for the controller slide i want the figures put into same folder and I want to see the data driven controller algorithm showing the progression of kp.ki gain on 2d plot too". The staircase showed that cost fell; the 2-D path shows *where the algorithm went*, which is the actual claim — and it lands in the same basin the exhaustive grid found.
**Next:** none — the cost staircase stays available as `talk/img/t_tune_cost.png` if a backup slide wants it.

### 2026-08-19 — Video embed was failing silently after the mp4s moved to talk/
**Changed/Found:** `talk/build_deck_lean.py` — `movie()` now looks in `talk/` first and falls back to `presentation/`. The two mp4s had moved to `talk/`, so both embeds were failing and quietly degrading to poster images; the only visible symptom was the deck dropping from 84 MB to 3 MB.
**Why:** A missing video degrades to a still with no error on the slide — a failure you would discover on stage. `build_deck.py` (verbose deck) still points at `presentation/` only and will need the same fix if that deck is used.
**Next:** Apply the same fallback to `build_deck.py`, or retire it once the lean deck is chosen.

### 2026-08-19 — Dose-spread mask: brainMask has 31 components, only 1 is brain
**Changed/Found:** `presentation/make_ampmap_gif.py` — added a subtractive mask cleanup (`--mask-open` 3, largest connected component, `--mask-erode` 2, disable with `--keep-blobs`). `load_experiments.m` builds `brainMask` as a bare intensity cut (`mimg > 0.1*max(mimg)`), which for AL_0033 2025-01-29 e1 yields **31 connected components**: the brain is 231 425 px and the other 30 (1947, 810, 170, ...) are bright non-brain junk outside the window that was rendering as islands in the animation. Cleanup removes 3.5% of the mask pixels. Deliberately SUBTRACTIVE ONLY — `imp.resp_map` is defined solely on the original mask, so an excluded region cannot be filled in without inventing data.
**Why:** User: the brain mask in the laser-level GIF was bad, with blobs from outside the brain region.
**Next:** A large white notch remains at the bottom-centre of the field. It is NOT an outside blob — it is where `mimg < 0.1*max`, i.e. excluded by the original threshold, so no `resp_map` values exist there. Widening it would mean re-deriving the mask AND recomputing the maps (allExperiments stores neither U nor V, so that needs another 281 s `load_experiments.m` run), and ΔF/F in those dim pixels is unreliable anyway since the normalisation divides by a small `mimg`.

### 2026-08-19 — Dose-spread animation rebuilt: single image, cross-dissolve, no chrome
**Changed/Found:** `presentation/make_ampmap_gif.py` rewritten from a building 3x3 tile grid to ONE panel that cross-dissolves between amplitudes. The dissolve blends the DATA (`(1-a)*map_k + a*map_k+1`) rather than alpha-stacking two images — stacking double-darkens through the crossover and looks muddy. Title, subtitle, colorbar and peak marker all removed; the only text is the amplitude, which fades out and back in through each crossover so two labels never overlap. Last map dissolves back into the first for a seamless loop (`--no-loop-back` to stop on the strongest). Also emits an mp4 with `--mp4`.
**Why:** User: no text but the laser amplitude, and images replacing each other one after another rather than a tile.
**Next:** GIF is 10.0 MB at 540 px (cross-dissolve frames are all unique, so nothing collapses); the mp4 of the same animation is 2.0 MB and looks better — prefer it if the deck can hold video.

### 2026-08-19 — LASER ON badge: larger, orange, colour split from the spot
**Changed/Found:** `presentation/make_brain_gif.py` — badge font `px/26` -> `px/15` with a new `--badge-scale`, dot 0.016 -> 0.021 of frame width, and a new `--badge-color` (default `#ff8c00`) so the badge no longer inherits `--light-color`. The laser spot itself stays cyan.
**Why:** User asked for larger, orange "LASER ON" text.
**Next:** Badge orange vs cyan spot is deliberate but not physically motivated — the AL_0033 impulse sessions use `lightCommand` (AL_0041 uses `lightCommand638`, i.e. red light), so `--light-color '#ff8c00'` would arguably be more faithful if the two should match.

### 2026-08-19 — Rendered media moved to talk/; renderers write there by default
**Changed/Found:** All rendered mp4/gif moved out of `presentation/` into `talk/` (which already held the deck: `neuroai_seattle_2026.pptx`, `build_deck.py`, `img/`, `render/`). `make_video.py`, `make_video_talk.py`, `make_video_combined.py`, `make_ff_video.py`, `make_brain_gif.py`, `make_ampmap_gif.py` now resolve outputs against a new `MEDIA = <root>/talk` constant instead of `presentation/`. Added `talk/.gitignore` (`*.mp4`, `*.gif`, `*.png`) mirroring the rule already in `presentation/.gitignore`; the tracked `.pptx`/`.py` in `talk/` are unaffected. Probe PNGs still land in `presentation/` (scratch, already ignored).
**Why:** User asked for all media content in one folder so the deck has a single place to pull from.
**Next:** `presentation/session_previews/` (12 MB of per-session preview mp4s) and the empty `clips/` were left where they are — they are working files for choosing trials, not deck assets.

### 2026-08-19 — GIF jitter was a palette artefact, not the data
**Changed/Found:** New `presentation/gif_utils.py` with `save_gif()` and `tsmooth()`. PIL quantises every GIF frame independently by default, so a smooth widefield gradient lands on a slightly different 256-colour palette each frame and shimmers. `save_gif()` builds ONE palette from a strip of 16 evenly-spaced frames, quantises every frame against it with `dither=NONE` (per-frame dither patterns are themselves a source of shimmer), and uses `disposal=1`. `make_brain_gif.py` also gained `--tsmooth` (default 3): a moving average applied to the SVD temporal components before projection — 200 components instead of 19 600 pixels, and identical to smoothing the frames since the projection is linear. Side effect: the GIF shrank 12.9 MB -> 9.7 MB, because a shared palette compresses far better.
**Why:** User reported the slowed-down GIF looked jittery.
**Next:** `--tsmooth 3` at 35 Hz is an 86 ms window — fine for display, but do not measure onset latency off this GIF.

### 2026-08-19 — Laser dose-spread animation from the supplementary maps
**Changed/Found:** New `presentation/export_ampmaps.m` + `presentation/make_ampmap_gif.py`. The exporter pulls `imp.resp_map` per amplitude out of `allExperiments` (selected by mouse/date/en, not by index, since the position in the experiments list shifts) and saves `assets/ampmaps_AL_0033_2025-01-29_en1.mat` — the same maps `impulse-analysis/spatial_spread.m` plots, so the animation cannot drift from the paper. 9 amplitudes, 0.18–1.80 mW, map range −4.34..+1.32 %ΔF/F. The Python side reveals the panels one at a time in ascending power, ending on the supplementary figure, and holds. Ran `load_experiments.m` to populate the workspace: 281 s, 4 sessions.
**Why:** User wanted the supplementary dose/spread panel animated for the talk.
**Next:** Colour map defaults to `jet` to match the supplementary figure; `--cmap magma_r` was tried and reads oddly (deepest inhibition becomes the BRIGHTEST patch while 0 is black). If the panel is restyled for the talk, pick a map where more inhibition is darker.

### 2026-08-19 — Brain GIF playback halved: `--step` default 2 -> 1
**Changed/Found:** `presentation/make_brain_gif.py` — `--step` now defaults to 1, so all 176 data frames of the -1..+4 s window are kept and the loop runs 8.8 s at 20 fps (was 88 frames / 4.4 s). Chose this over dropping to 10 fps, which halves the speed identically but makes the motion visibly steppy. Cost: the GIF grew 6.4 MB -> 12.9 MB.
**Why:** User asked for the GIF at half speed.
**Next:** If 12.9 MB is too heavy to email inside a deck, `--px 400` gives 10.4 MB and `--step 2 --fps 10` gives the same 8.8 s at 6.4 MB with choppier motion.

### 2026-08-19 — Brain GIF: open-loop stimulation window + command-driven laser graphic
**Changed/Found:** `presentation/make_brain_gif.py` — default trial is now OPEN LOOP (`--cond`, default `OL`) over −1 s to +4 s, i.e. the full stimulation window rather than a short clip. Added a laser graphic at the controlled site: a radial spot plus a ring, and a corner "LASER ON" badge, all gated and modulated by the trial's REAL command trace `U[trial]` — so the light cannot show as on when the command is zero. Two failed attempts first: additive compositing washed out against the bright anatomy (fixed by compositing *towards* the laser colour instead), and normalising the command by the session maximum left this trial's spot at 27% brightness and nearly invisible (fixed by normalising within the trial and lifting "on" to a 0.45 floor). Activity clim relaxed 90th → 94th percentile, since the post-stimulation rebound saturated most of the field.
**Why:** User wanted the GIF to show a long open-loop stimulation with a light-on graphic indicating that stimulation is happening.
**Next:** The "on" floor means spot brightness is not proportional to absolute laser power across trials — it tracks the shape within a trial only. Fine for a schematic; do not read intensity off it.

### 2026-08-19 — Combined talk video (constant + moving reference) + clean brain GIF
**Changed/Found:** New `presentation/make_video_combined.py` — derived from `make_video_talk.py`, adds a fourth act ("A MOVING TARGET", AL_0048 2026-07-21 sine) that reuses the SAME panel geometry so the two chapters read as one video. Only two of the four sine conditions are carried over (`FF_SEQ = [2, 0]` = open loop and closed loop + preview); `LoopDiagram` gained a feedforward branch (green, ref → preview block → summed into the laser command) shown only for CL+preview, plus a `moving` flag that relabels the reference input "sine". `enter_ff()` performs a one-time swap of the shared panels (axis limits, stim window 0–4 s, brain U/mimg/mask/ROI/colourbar, legends built from explicit handles). New `presentation/make_brain_gif.py` — chrome-free looping widefield GIF for slide schematics.
**Why:** User asked for one combined video with the FF states column restricted to CL+preview and OL, keeping the control-architecture schematic; and, separately, a clean brain GIF for a PPT schematic.
**Next:** The combined cut drops the FF video's camera / pupil / face-motion panels (they need a different layout) — `make_ff_video.py` remains the place to see those. Also `make_ff_video.py` and `make_video.py` are still on the lavender deck while the two talk cuts are on bone.

### 2026-08-19 — Brain GIF: anatomy needs median filtering and clim retuning
**Changed/Found:** `presentation/make_brain_gif.py` — first render was unusable: the raw mean image is peppered with dust specks that read as noise at slide size, and the 96th-percentile clim plus a 0.72·clim alpha ramp left the activity nearly invisible. Fixed by median-filtering the anatomy layer only (the ΔF/F normalisation still uses the unfiltered `mimg`), compressing the grayscale into a mid band (0.30 + 0.58·g) to kill blown highlights, eroding the mask 2 px before feathering, and steepening the overlay alpha to 0.45·clim. Same tuning problem appeared in reverse in the combined video's sine act: a 93rd-percentile clim clipped the whole field to a flat blue slab and the 99th washed it out — settled on the 96th.
**Why:** The GIF is going into a PowerPoint schematic, where it is small and sits next to clean vector art.
**Next:** GIF is 5.2 MB at 460 px / 70 frames; if PPT size matters, `--px 360 --step 3` roughly halves it.

### 2026-08-19 — Checkpoint-commit the alpha WIP backlog; set up review worktree
**Changed/Found:** Committed the full uncommitted backlog on `alpha` (~20 modified + ~30 new files) in 5 logical commits grouped by sub-area (bilateral, impulse-analysis, controller-analysis, utils, docs/agent-config); pushed to `origin/alpha`. Then added a git worktree at `../brain_paper_review` on a new branch `alpha-review-fixes` (branched from `alpha`'s tip — git forbids checking out `alpha` itself in two worktrees) so a code review can run against a frozen copy in parallel and any fixes land off `alpha`.
**Why:** User wants a code review isolated from ongoing work. A defined, pushed baseline is a prerequisite for a meaningful review diff; a worktree lets review + active editing proceed without touching each other's working tree.
**Next:** Run `/code-review` (scope: alpha vs main or WIP subset) in the worktree; quarantine any review-driven fixes on a `alpha-review-fixes` branch before merging back to `alpha`.

**Changed/Found:** New `talk/build_deck_lean.py` → `talk/neuroai_seattle_2026_lean.pptx` (15 slides). Same slides, order and figures as `build_deck.py`, but on-slide text cut to claims and number callouts: body type 18–22 pt (was 13–15), numbers at 24–30 pt, ≤ 3 short lines per slide. Every stripped sentence was moved into the speaker notes, which are loaded verbatim from `talk/notes.json` (extracted from the verbose deck) and appended to. The verbose deck is untouched.
**Why:** User: "slides are too verbose only deliver punchline or short stuff, make a fresh version that does that". A fresh script rather than an edit so the verbose version stays available to fall back on.
**Next:** Decide which deck to take to Seattle; if the lean one, retire `build_deck.py` or keep it as the notes source. Animation and theme passes still open.

### 2026-08-18 — Talk deck: controller-design slide added; user's PowerPoint edits ported back
**Changed/Found:** `talk/build_deck.py` — new slide 5 "The controller: feedforward inversion, then PI on top" between the loop-plant slide and the CL video: eq:ff / eq:kr / eq:pi / eq:aug_state from `methods_edit.tex`, plus `tune_grid.png` + `tune_auto_cost.png` (converted from `paper/images/tuning/grid_cost_surface_AL_0033_0305.pdf` and the left column of `autotune_convergence_both.pdf`, both AL_0033). Also ported three edits the user had made directly in PowerPoint and saved at 14:37 — title-slide author placeholder deleted, opening bullets reworded ("a control system", not "control theory proper"), plant-ID panel captions dropped and bullets rewritten. Their saved file is backed up as `talk/neuroai_seattle_2026.USEREDIT-2026-08-18-1437.pptx`.
**Why:** User: "a slide is missing between 4 and 5 showing the actual controller design", then "i did make some edits dont burn them away" — the generator rewrites the whole deck, so hand edits in PowerPoint are destroyed on the next build unless ported back into `build_deck.py` first.
**Next:** ⚠ Any further PowerPoint edits must be ported into `build_deck.py` before the next build, or backed up first — this will keep happening. Budget now ~13:00 vs 12:00; confirm with organisers whether the 15 min includes questions before cutting further.

### 2026-08-18 — Both tuning panels are AL_0033 on purpose
**Changed/Found:** The controller-design slide shows the gain grid (03-05) and auto-tune convergence (03-17) from AL_0033 only, not the AL_0034 cross-mouse panels.
**Why:** PAPER.md records that grid↔autotune comparisons are same-mouse only, and AL_0034 is a tuning-only mouse that appears nowhere else in the talk — introducing it would cost a sentence of explanation for no gain.
**Next:** none.

### 2026-08-18 — Talk deck: fold the prior-work/gap slide into the opening
**Changed/Found:** `talk/build_deck.py` — deleted the standalone "What already exists, and the gap" slide (old #3) and folded its content into the opening: the citation list compressed to one sentence (Newman 2015 named as the direct PI ancestor), the two-threads gap kept as two bullets, and the "controlled independent variable" line promoted to a bold block across the lower left. The opening's roadmap bullets collapsed to one italic line ("identify the plant → close the loop → characterise the residual → model the disturbance"). Opening retimed 1:00 → 1:15; both slides' speaker notes merged. Deck now 14 slides (11 content + 3 backup).
**Why:** Budget was ~12:45 against a 12:00 target. The gap slide was the cheapest 0:45 to recover — its argument is one sentence plus a citation list, and it reads better as the second half of the opening than as its own beat.
**Next:** Budget now ~12:15. Animation build (slide-duplication for the IMP and close slides + video autoplay) and the theme/background pass are still open, as are the title-slide authors/affiliations and trimming `ff_demo_0721_hd.mp4` to ~30 s.

### 2026-08-18 — Talk deck: drop the pivot slide, reorder to paper logic
**Changed/Found:** `talk/build_deck.py` — removed the "pivot" slide (old #8, the Fig-4 block-1 `f4_decouple` decoupling panel), moving its spine sentence ("once you are actively rejecting a disturbance, the disturbance is the rest of the brain") onto the contra→ipsi map slide. Then reordered the whole deck to follow the paper's figure logic: Fig 1 loop → Fig 2 plant ID → Fig 3 CL vs OL → Fig 4 block 2 (error contribution, then factor slopes / waterbed) → Fig 4 block 3 (contra→ipsi map setup, then rejection + IMP) → Fig 5 sine/preview → MPC close. The sine/preview slide had been sitting at position 6, ahead of all the Fig-4 material.
**Why:** User: "slide 8 seems unneeded remove it", then "ordering is bad follow paper logic". The old order also broke the argument — preview (Fig 5) interrupted the controller story, and the residual/rejection block (Fig 4 block 3) preceded the error-contribution block (block 2), inverting the paper.
**Next:** Budget is now ~12:45 vs the 12:00 target — recommend folding the gap slide into the opening (−0:45). Animation build and the theme/background pass are still open.

### 2026-08-18 — IMP first stated on the factor-slope slide, not the rejection slide
**Changed/Found:** `talk/build_deck.py` — the factor-slope slide's lead-in changed from "Read through the internal model principle:" to a full one-line statement of the principle ("to reject a disturbance, the loop must contain a model of its dynamics"); bullets nudged 5.30 → 5.45 in.
**Why:** The reorder moved that slide ahead of the rejection+IMP slide, so it became the first place the principle appears — the old lead-in was a forward reference to a slide that now comes later.
**Next:** none — the rejection slide's fuller IMP box now reads as reinforcement, which is what a talk wants.

### 2026-08-17 — Artboard audit: the standard is 17.0 cm wide, but `width=\linewidth` reprints every figure at a DIFFERENT font size
**Changed/Found:** No file edited — measured the MediaBox of every `.ai` and assembled PDF in `paper/images/`, then cross-checked how `results.tex` places them. **Artboard widths are not uniform: 17.0 cm (`481.89 pt`) on the recent set — `Figure4.ai`, `Figure4-temp.ai`, `Supplementary1.ai` (all 17 × 15), `Figure5.ai` (17 × 19), `Figure3_extra.ai` (17 × 14) — but 17.5 cm (`496.063 pt`) on the older `Figure2.ai`/`Figure3.ai` (17.5 × 10) and `Figure2_extra.ai` (17.5 × 20), and 15.5 × 7 on `FIgure1.ai`.** So `PAPER.md`'s "Figure total width = 17 cm" describes the three newest assemblies only. **The consequence is the real finding:** `\textwidth` is `542.025 pt` = **19.05 cm** (letter, 0.5 in margins), and every figure is placed with `width=\linewidth`, so each artboard is rescaled by a different factor — Fig1 ×1.23, Fig2_extra ×1.09, Fig3_extra ×1.12, Fig5 ×0.92 (placed at `0.82\linewidth`). Against the 6 pt bold project standard the printed panel text is therefore **7.4 / 6.5 / 6.7 / 5.5 pt** across the four figures. Every artboard height is a whole centimetre, so the canvases were sized deliberately; only the LaTeX placement undoes it.
**Why:** user asked what artboard size to use. The number alone would mislead — a correctly-sized 17 cm artboard still prints at the wrong font size under the current `\includegraphics` calls. This compounds the 2026-08-12 panel-size audit, where vector export already crops each panel below its declared `paperFig` size.
**Next:** decide between (a) keep 17.0 cm artboards and place with a fixed `width=17cm` — recommended: no artwork changes, 6 pt stays 6 pt, and Fig 5 at 17 × 19 cm still clears the 25.4 cm textheight with its caption — or (b) move every artboard to 19.05 cm and keep `\linewidth`. Then bring `Figure1`, `Figure2`, `Figure3`, `Figure2_extra` onto the winning width, and correct the "17 cm" line in `PAPER.md` to note the standard is not yet uniformly applied.

### 2026-08-17 — Local-first manuscript workflow: `draft`/`main` gate, VS Code build, figure-sync script
**Changed/Found:** Set up the LaTeX edit loop inside `Closedloop_edit/` so the manuscript is written locally and Overleaf becomes downstream. Added (all local-only, none tracked): `.vscode/settings.json` (LaTeX Workshop + latexmk recipe, `main.tex` pinned as root, build-on-save), `sync_figs.ps1` (copies only `\includegraphics`-cited figures from `brain_paper/paper/` into `images/`, flags drift), and entries in `.git/info/exclude` for local helpers. Created branch `draft` (stayed on `main`). Verified `latexmk -pdf main.tex` builds locally: **31 pages, exit 0**. Updated `paper-writing/CLAUDE.md` with the branch protocol and a corrected file map.
**Why:** the user wants to draft in the repo with a review gate ("you edit, then I edit, then we push") rather than in the Overleaf editor, without shipping local build/helper files to the shared Overleaf project. `.gitignore` is itself synced to Overleaf, so local-only files must go in `.git/info/exclude`, which never travels.
**Next:** decide whether to prune the 59 tracked-but-uncited images (see next entry). Optionally add the Overleaf git remote so pushing skips the manual Overleaf-side pull.

### 2026-08-17 — `images/` audit: 59 of 76 tracked figures are uncited; `Figure4.pdf` is cited but does not exist
**Changed/Found:** `sync_figs.ps1` dry run against `Closedloop_edit/`. Only **17** figures are cited by `\includegraphics`; **59 tracked images are cited by nothing** and are dead weight in the Overleaf project (old `panel_*.png`, `step*.png`, `all_*.png`, `Figure2.pdf`, `Figure3.pdf` superseded by the `_extra` variants, etc.). Three cited figures are missing from `images/`: `Figure4.pdf` (cited by the live `results.tex` — `brain_paper/paper/` only has `Figure_4.png`, no PDF export yet), plus `lowfreq_examples.pdf` and `spont_variance.png`, both cited only by the **dead** `methods.tex` (`main.tex` inputs `methods_edit.tex`). Also confirmed the 2 undefined refs at build (`fig:lowfreq_examples`, `sec:disturbance`) come from a `\todo{}` block in `results.tex` pointing at labels defined only in dead `methods.tex` — benign, not a build break.
**Why:** the user explicitly wants to avoid pushing supplementary files Overleaf does not need; quantifying the drift is the precondition for pruning. `Figure4.pdf` matters because it is on the live build path.
**Next:** user call on `git rm --cached` for the 59 uncited files (recoverable from history either way). Export Fig 4 as PDF into `paper/` per the paper-panel rule, then `.\sync_figs.ps1 -Apply`.

### 2026-08-14 — ⚠ Stage 5a undercuts "forecast the disturbance from contra"; talk slide 9 corrected
**Changed/Found:** Surfaced while reframing the talk opener, from `ctrl_subspace_id.m` / `ctrl_spatial_modes.m` (both already RUN on m4, logged 2026-07-28). At the loop-delay horizon (kPred=3 ≈ 86 ms) adding 8 stim-blind contra POD channels makes held-out ipsi prediction **worse**: OL R² **0.835 contra-informed vs 0.863 ipsi-only** (−0.028); CL **0.670 vs 0.737**. Stage 5b states it directly — contra carries **no temporal information beyond the ipsi readout's own past** (gain ~ +0.009); contra and ipsi are two views of one shared latent state, ipsi in *time*, contra in *space*. `TALK_neuroai_2026.md` slide 9 premise 3 corrected to say **"reconstructable", not "predictable"**, with the forecasting burden moved onto the network-dynamics models (graph-wave / delay-DMD) where it actually belongs.
**Why:** The talk's closing MPC argument was about to rest on "we can predict the disturbance ahead of time from the other hemisphere" — a claim this project's own Stage-5 result contradicts. Contra buys **spatial** interpretation of the regulated state, not temporal lead. This is the deeper form of the already-known "contra→ipsi predictor is instantaneous (pX=0)" caveat, and in a room containing control-literate people it is the most likely place to be cornered. Also recorded that `utils/ctrl_gram_build.m` is a **regression** Gram (`X'X`), NOT a controllability Gramian — easy to conflate now that minimum-energy control is in the opening.
**Next:** This is a live scientific question, not just a talk-framing one — TASKS ⭐PORT objective-3 candidate (c) assumes "if Global is predictable ahead of time from contra, a preview controller should beat the PI", and Stage 5a is evidence against that premise. Re-read objective-3(c) against this result before any work is spent on it. `ctrl_gramians.m` (5b) and `ctrl_mimo_control.m` (5c), named as NEXT in the 5a header, still do not exist.

### 2026-08-18 — Fig-4 now fully represented in the talk: decoupling, error contributions, waterbed
**Changed/Found:** Completed the Fig-4 treatment across three slides after the user pointed at `paper/images/figure4`. **(1) Block 1 / panel 4A** folded into the pivot slide, replacing the Ye-validation panel: `ctrl_state_dependence.m` bottom-right panel — pre-stim contra-derived Global level → trial outcome, **OL b = 0.340 (r = 0.40) vs CL b = 0.084 (r = 0.23)**, m4, OL n=92 / CL n=108. **(2) New slide, "What drives the tracking error — and when"**: `f4p1_error_decomp.png` (unique partial R², early vs late), `claim3_initdev_early_late.pdf` (RMSE by initial-deviation quartile, transient 2.40→4.13 vs settled 1.95→2.44), `claim2_delta_hi24_late.pdf` (OL vs CL by relative 2–4 Hz quartile). **(3) The waterbed slide** keeps the three paired-slope panels. Deck is **16 slides**, validates clean.
**Why:** The error-contribution numbers were being *spoken* rather than shown — the decomposition panel existed on disk and was only a sentence in the notes. Shown together the three slides make one argument: early error is the transient (unique R² 0.381 → 0.029 late, a 13× collapse), motion contributes nothing (0.001 both windows, and re-scoring with the motion gate off across 852 trials / 15 sessions moves nothing, which is what licenses reading it as a real null rather than missing data), and what survives late is **relative 2–4 Hz at 93 % of the explained variance** — the band the loop has no internal model of, and makes worse.
**Next:** ⚠ **Panel 4A is single-session with a bootstrap ΔSlope 95 % CI of [−0.501, +0.001] — the upper bound touches zero**, and the cross-session sweep has never run. The slide carries that caveat in red; do not upgrade the claim without the sweep. ⚠ Panel hygiene held: `claim2_delta_hi24_late` appears alone as the magnitude companion, and the 1–2 Hz null is shown only in paired-slope form (`factor_slope_delta12`) on the following slide. ⚠ **Budget is now ~13:45 against a 12:00 target** — cut decision pending with the user.

### 2026-08-18 — Bilateral 52-site grid/network slide cut from the talk; sine/preview kept
**Changed/Found:** User: *"just remove the bilateral mouse content"*, then clarified *"keep the sine preview, just remove the grid stuff"*. Removed the AL_0048 **52-site dual-opsin grid + network-model** slide (`grid_spatial`, `net_coupling`, `net_freerun`). The AL_0048 **sine/preview** slide stays. Deck is **15 slides** (12 content + 3 backup), validates clean; running order now Opening → Gap → Loop → Plant → CL video → Sine/preview → Pivot → Contra→ipsi map → Rejection+IMP → Waterbed → MPC close.
**Why:** Cutting the grid slide costs the talk its only *forward model*, which was premise 4 of the MPC close. The close's speaker notes were corrected accordingly: the forecast is now stated as an **open problem**, not something the talk demonstrates — do not imply a forward model exists. What survives as support for the close is (a) preview/lookahead demonstrably working on a *known* reference (sine slide), and (b) the waterbed slide showing the loop lacks a model of exactly the band that dominates the residual. That is a weaker but honest chain.
**Next:** ⚠ **Pooled cross-session statistics still include the bilateral mice and were NOT changed** — 3K is 15 sessions incl. AL_0048/AL_0051 (m14/m15), the rejection result is 13 sessions incl. both, and the Fig-4 factor panels are 15 sessions across four mice. Removing them there would require re-running the analyses. Flag if the intent was to exclude them from the pooled numbers too. Budget now ~12:30 against a 12:00 target; the cheapest remaining cut is merging the pivot slide into the contra→ipsi map slide (they overlap), which would bring it to ~11:45.

### 2026-08-18 — Fig-4 Part 1 added to the talk as the IMP argument with data; deck now 16 slides (later 15, see above)
**Changed/Found:** New slide 11, *"What the loop can reject — and what it makes worse"*, from the Fig-4 Part-1 / Block-2 factor analysis (`cl_factor_slope_panels.m`, `cl_rmse_factor_windows.m`, `cl_factor_decomp_panel.m`). Three paired-slope panels (n=15 sessions, four mice; motion n=11): **motion OL +0.686 → CL −0.016, p=0.0020**; **relative 2–4 Hz OL −0.511 → CL +0.813, p=0.0125**; **relative 1–2 Hz null control p=0.76 n.s.** Plus the unique-R² decomposition (613 CL trials / 11 sessions, 2000-boot CI): initial deviation 0.381 → 0.029 late (13× collapse), motion 0.001 → 0.001, relative 2–4 Hz 0.089 → 0.122, so rel-2–4 Hz goes from 22 % of explained variance early to **93 % late**.
**Why:** This is the internal model principle stated in data, and it is the missing bridge between the rejection slide and the MPC close. A PI integrator *is* an internal model of a constant, so motion — slow and quasi-DC — is decoupled completely. The loop has no oscillatory mode at 2–4 Hz, and the slope there does not merely survive: it **flips sign**, which is the discriminating observation. A controller that simply lacked a model of that band would leave the OL slope untouched (S≈1: no rejection, no harm); a flip means feedback is *actively making that band worse* — the Bode sensitivity integral / waterbed effect, not a null. The 1–2 Hz control makes it band-specific rather than generic. Together: the loop models DC and rejects it, has no model of the oscillatory band, and pays for its low-frequency suppression — which is exactly the argument that MPC with a learned network model is what the theory prescribes.
**Next:** Two panel-hygiene items carried into the notes. (a) The 1–2 Hz null **must** be shown as `factor_slope_delta12` (per-session paired slope), never `claim2_delta_lo12_late` — in the pooled-quartile form the 1–2 Hz series rises almost identically to 2–4 Hz through between-session baseline structure, so side by side they argue *against* the dissociation; only the paired-slope form has picture and statistic agreeing. (b) `factor_slope_motion` and `factor_slope_delta24` are still exported with **different y-ranges**, which blunts the contrast between the two strongest panels — fix at source before paper assembly. ⚠ Budget: the talk is now ~13:30 against a 12:00 target; a cut decision is pending with the user.

### 2026-08-18 — Talk gains a contra→ipsi setup slide; carries the filter-vs-pattern correction
**Changed/Found:** New slide inserted before the rejection slide (deck now **15 slides**), at user request: the coloured contra→ipsi kernel plus a statement of what the method actually does — fit on **spontaneous, laser-off frames** using only contra pixels the laser does not affect (stim-blind by construction), then deployed during trials to give **Global**, the counterfactual "as if no laser" ipsi trace, with **Local = Actual − Global**. Panels cropped from `hemi_coupling_maps_sq.png` (per-pixel R² map, contra → dFk, target site marked) and `ctrl_spatial_pattern_AL_0033_0226_e2.png` (FILTER vs Haufe forward PATTERN). Budget held at ~12:00 by trimming slides 2, 5, 6 and 7.
**Why:** Global had been used on two slides (pivot, rejection) without ever showing how it is built, and ER is meaningless until the disturbance is defined. The slide also carries a correction the project already made but that had not reached the talk: **the raw regression filter is not a spatial map.** On m4 `ρ(filter, pattern) = −0.020` with **47 % of pixels flipping sign** — the filter map is salt-and-pepper, the forward pattern is a coherent posterior-lateral contra region. A filter is optimised to predict, not to localise, so it will place large opposite-sign weights on correlated neighbours. Worth the 15 s in this room specifically: Amy Orsborn is an organiser and BCI decoder work is where this distinction is best known (Haufe et al. 2014).
**Next:** The pattern figure is single-session (m4). If anyone asks for generality, that is the honest answer. Also note `hemi_coupling_maps_sq.png`'s right panel plots signed OLS weight² — i.e. a filter map — so it should not be shown as "where the information is" without the same caveat.

### 2026-08-18 — ⚠ MPC HAS run on m4; the talk's "no numbers" guard was wrong. `_global` batch is n=2 and unusable
**Changed/Found:** Correcting the 2026-08-14 talk-plan claim that `ctrl_optimal_xsess.m` is "built and UNRUN" and that slide 9 may carry no numbers. **`ctrl_optimal_control.m` HAS run on m4** and the figure is on disk (`paper/images/predictor_saga/ctrl_optimal_AL_0033_0226_e2.png`, plant nx=2, u_max=2.06, ref=−5, trial-averaged): **steady RMSE (1–3 s) PI 1.202 → causal optimal 0.146 (−87.8 %), non-causal 0.000**; full window 1.510 / 0.994 / 0.927; **mean laser command PI 1.563 → optimal 1.300**; measured disturbance drifts +1.82 % over the trial. Two readings matter more than the headline: **causal ≈ clairvoyant**, so nearly all the gain is reachable causally and the PI gap is not about future knowledge; and the optimal controller achieves it with **less command**, which is the empirical payoff of the minimum-energy framing in the talk's opener. **Separately, the `_global` cross-session batch is NOT usable**: `f4_mpc_rmse_global.png` reads **n=2, p=0.5**, and `f4_mpc_traces_global.png`'s exemplar is **AL_0033_0212_e2 — the session already flagged for exclusion** (frame transposed at load, laser ~5× weak, spot straddling the midline; its transmission goes the wrong way, OL 0.99 → CL 1.27).
**Why:** The user asked to promote disturbance rejection and show MPC doing better, so the MPC evidence had to be pinned down exactly rather than assumed absent. The distinction that governs what may be said is the project's own 2026-08-12 reading rule: `'residual'` (`d = y_PI − H·u_PI`) **absorbs plant mismatch**, so part of the optimal controller's advantage is it repairing the plant model, not controlling better; only a gap surviving under `'global'` (the contra-predicted, real-time-available disturbance) is a control result. The m4 numbers are `'residual'`, hence quotable only as a single-session, trial-averaged, explicitly-flattered demonstration.
**Next:** Run `ctrl_optimal_xsess.m` with `src='global'` across sessions once the Stage-2 rebuild clears — that is the number that would turn this from a ceiling calculation into a control claim. Until then the talk says "one session, disturbance measured post-hoc, and we are running the real-time version now". Also check `frac_u0` when it runs: if large, most of the residual error is one-sided actuation and no design fixes it.

### 2026-08-18 — Talk restructured around the internal model principle; deck now 14 slides
**Changed/Found:** User: *"promote more of the internal model principle and disturbance rejection story alongside showing that MPC can do even better."* Added **slide 8 — disturbance rejection + IMP**, now the empirical centre of the second half and the strongest cross-session result in the talk: **ER median OL 0.291 → CL 0.203, CL better in 13/13 sessions, signrank p = 2.4e-4**; per-session significant in 11/13, the two misses being exactly the two new mice with weak predictors (so their inclusion is conservative); per-trial transmission β m4 OL 0.97 → CL 0.56; rejection **state-INVARIANT** (state-dependence returned null — [IMP-STATE-QUARTILE] all four r_s null, [IMP-XSTATE] 7 sessions no surviving slope, Friedman n.s.). The close (now slide 10) was rewritten from an aspirational MPC slide into **"MPC does better — and needs less light"**, carrying the m4 panels. Budget re-cut to 12:00 by trimming slides 2/4/5/6/7/9. Deck rebuilt: **14 slides**, validates clean.
**Why:** The IMP is the rigorous bridge the talk was missing. A PI controller's integrator is an internal model of a **constant**; ongoing cortical activity is not constant; therefore ER floors at ≈0.20 rather than 0 **because the model is missing, not because the tuning is bad**. That makes MPC a *prediction of the theory* rather than a wish — and it makes the opener's minimum-energy promise land, since the optimal controller tracks better on less light. Also reframes what the residual means: not controller failure, a missing model.
**Next:** Panel prep note — the `f4_reject_*` panels carry a burned-in `UNGATED (no R² floor, n=13)` annotation that **collides with their own titles**; the deck crops the top band and carries the caveat in the slide caption instead. If those panels are ever regenerated, fix the annotation placement at source.

### 2026-08-18 — NeuroAI talk deck BUILT: `talk/neuroai_seattle_2026.pptx`, 13 slides, videos embedded
**Changed/Found:** New `talk/` sub-area — `build_deck.py` (python-pptx generator, re-runnable), `render.py` (PowerPoint-COM export to `talk/render/*.PNG` for visual QA), `img/` (14 panels converted from the paper PDFs at 400 dpi via pdftoppm, then white-margin auto-cropped), and the deck itself: **13 slides, 84 MB, both videos embedded as real media objects** with extracted poster frames. Structure = the 10-slide plan in `TALK_neuroai_2026.md` (title + 9 content) plus the 3 backup slides. **Speaker notes carry the numbers and every honesty guard** (11.7k characters total; the slide-9 note alone is 1.9k and contains all three MPC caveats). No theme or background applied — user deferred that. Toolchain notes for next time: **no node on this machine**, so pptxgenjs is unavailable and python-pptx is the route; **no LibreOffice**, so the skill's `soffice.py` fails (`AF_UNIX` on Windows) and rendering goes through PowerPoint COM (`pywin32`); the skill's `validate.py` needs `PYTHONUTF8=1` or it dies on a charmap decode of the UTF-8 slide XML. Validation passes clean.
**Why:** Talk is 2–3 days out (20–21 Aug), so the deck had to exist before any styling decisions. Three QA-driven layout fixes worth recording because they will recur if the generator is re-run: (1) the timing marker moved from under the title to the **top-right corner** — two-line titles were overprinting it; (2) all panel PNGs **auto-cropped for white margins** — `wfpath` was 1272×1800 with most of it blank, so it rendered tiny with its caption stranded far below; (3) `net_wave.png` is a **4-panel exploratory composite and is unreadable projected** — cropped into two legible panels (`net_coupling`, `net_freerun`) carrying the coupling map and the actual-vs-graph-wave-vs-delay-DMD comparison.
**Next:** ⚠ **Re-assess the "talk-scale panel re-export" task — it is much less urgent than the plan assumed.** Rasterising the vector PDFs at 400 dpi and displaying them ~4 in wide scales the 6 pt paper fonts to ~12 pt equivalent, and the renders are legible; the remaining concern is line weight, not type size. Still open: title-slide authors/affiliations, the receding-horizon schematic for slide 9 (currently text-only), trimming `ff_demo_0721_hd.mp4` to ~30 s, and the theme/background pass.

### 2026-08-14 — Aerospace analogy adopted for the talk opener (rocket) and close (gust rejection)
**Changed/Found:** `TALK_neuroai_2026.md` slide 1 gains an opening-analogy block, at the user's suggestion and drawing on their own background (PhD, aero/astro + control). Split deliberately across the talk so neither analogy overstays: **powered descent / rocket landing in slide 1** for the *actuation* structure — one-sided thrust ↔ a laser that can only inhibit (`0 ≤ u ≤ u_max`), minimum-fuel ↔ minimum-energy, bounded non-convex thrust solved as a convex program ↔ the one-sided **QP, not LQR** — and **aircraft gust-load alleviation in slide 9** for the *forecasting* structure, since forward-looking gust sensing feeding a feedforward controller is exactly preview control generalized to a predicted disturbance. Three disanalogies written in as the scientifically interesting content: (1) no first-principles plant — it must be identified per animal, which is why slide 4 exists; (2) **the disturbance IS the system** — unlike wind, ongoing cortical activity is the object of study, so you are rejecting a signal you also want to measure; (3) **the reference is unknown** — a rocket's desired trajectory comes from the mission, but what trajectory *should* neural activity follow is an open scientific question.
**Why:** The user asked how to frame "a control problem like rocket landing or aircraft flight, but with so much variability it's too hard — so how do we figure it out?". The answer written onto the slide is that the talk's own structure IS the answer: identify the plant from data → close the loop and measure how much variability feedback removes → characterize the residual and its source → model the disturbance and go model-predictive. That is the standard control-engineering program applied to a plant with no first-principles equations, and stating it on slide 1 hands the audience the whole talk in one sentence.
**Next:** Two guards recorded on the slide. **Dosage** — use each analogy once and drop it; half the room is neuroscientists and an extended aerospace metaphor reads as engineering-imperialism, with the three disanalogies as the insurance. **Citation** — spoken as analogy nothing needs a reference, but if lossless convexification / powered-descent guidance (Açıkmeşe & Ploen) appears on a slide as a citation it must be verified first; not in `refs.bib`.

### 2026-08-14 — Talk opener reframed from problem-statement to capability/control-toolkit
**Changed/Found:** `TALK_neuroai_2026.md` slide 1 rewritten on user instruction: *"the opening should not be about the problem but more that now that we have precise measurements and active control we can use brain states and feedback for better control — optimization, minimum-energy control, model-driven control."* The deficiency framing ("open-loop is state-blind") drops from a whole slide to one clause, immediately turned positive. New opener names four unlocks: brain state **as the feedback signal**; **optimization** (command solved for, not tuned — cost surfaces T-A/T-B already show this); **minimum-energy control**, argued scientifically as *least-perturbative causal manipulation* (less light, less heating, less off-target drive — "what is the smallest push that produces the effect?"); and **model-driven control**. Slide 2's gap restated as a two-thread intersection: closed-loop optogenetics has the loop but not the scale, network-control theory has the theory but no actuator. Budget held at 12:00 by trimming slide 3 (1:00→0:45), which slide 1 now covers.
**Why:** Opportunity framing is stronger for this audience than complaint framing, and it makes the talk's two ends rhyme — slide 1 announces the control-theoretic program, slide 9 (MPC + learned forward models) is its completion. Also surfaced that the framing is better supported than the plan showed: `ctrl_subspace_id.m` (Stage 5a, subspace ID) and `ctrl_spatial_modes.m` (Stage 5b, Gramians/PBH/modal maps) are real and already run.
**Next:** ⚠ **Citation gap** — the connectome-controllability literature (Gu et al. 2015 *Controllability of structural brain networks*; the Pasqualetti/Bassett line) is **not in `refs.bib`** (grep-verified). The slide-1 positioning line leans on it, so the exact reference must be verified before it goes on a slide — do not cite from memory. Likely also wanted in `introduction.tex`, which currently has no network-control paragraph.

### 2026-08-14 — Talk now closes on MPC + learned forward models; slide 9 expanded 0:30 → 1:15
**Changed/Found:** `TALK_neuroai_2026.md` slide 9 rewritten from a throwaway "where this goes" into the talk's thesis, at the user's direction: *learned forecasting models can steer neural trajectories via MPC.* Built as a four-premise argument in which **every premise is something shown earlier in the same talk** — (1) the plant model transfers (slide 4 / TF-D), (2) lookahead demonstrably works, since preview at a 143 ms horizon cancels the plant lag on real data (slide 6), (3) the disturbance is reconstructable from the opposite hemisphere at R²≈0.93 (slide 7), (4) the graph-wave / delay-DMD models roll network dynamics forward (slide 8). Compose → forecast the disturbance, optimize a command sequence over a receding horizon, and the set-point becomes a trajectory. Paid for by dropping TF-D to backup (−0:15) and compressing slide 8 (−0:15); budget still exactly 12:00.
**Why:** It is the strongest available framing for this specific audience, and it inverts the usual NeuroAI direction — not "can a model predict neural data" but "is a learned forward model good enough to **steer** the system", with the argument that closed-loop control is the sharpest benchmark for a dynamics model because a wrong model surfaces immediately as tracking error. Also changes how slides 4/6/7/8 should be delivered: each is now simultaneously a result and a premise, so they need signposting forward rather than standing alone.
**Next:** ⚠ Three honesty guards are written onto the slide and must survive into the deck. (a) **No numbers** — `ctrl_optimal_xsess.m` is built and UNRUN; say "what we're building next", never "we find". (b) **The contra→ipsi predictor is instantaneous (pX=0), not a forecast** — it reconstructs from contemporaneous cortex, so making it genuinely predictive is the open problem and the honest gap between slides 7 and 9; name it before Q&A does. (c) **One-sided actuation** — constrained QP, not LQR, and where the disturbance already sits below reference the error is irreducible. This slide is TASKS ⭐PORT objective-2 plus objective-3 candidate (c), stated as a direction.

### 2026-08-14 — Talk plan restructured: prior-work positioning added as its own slide
**Changed/Found:** `TALK_neuroai_2026.md` — user flagged that the plan asserted a contribution without ever situating it. New **slide 2 "What already exists, and the gap"**, built from `Closedloop_edit/introduction.tex` ¶4–7 rather than invented: names Newman 2015 (the optoclamp — a PI controller on single-neuron firing rate, our direct methodological ancestor), Bolus 2018/2021 (state-space LQR, awake mice), Kanta 2019 (amygdala gamma → memory consolidation), Nicholson 2018 / Zaaimi 2023 (oscillations, slice + NHP), Clancy 2014 (volitional calcium neuroprosthetic), Grosenick 2015 (the framework review). The gap stated as: nearly all prior closed-loop optogenetics is **single-neuron or LFP with ephys feedback**; mesoscale population control with widefield calcium as the readout is largely unexplored, and Matveev 2024 is what makes it feasible. All citations already in `refs.bib`. To pay for it, old slides 3+4 (plant ID, plant transfer) merged into one 1:45 slide; budget re-cut to exactly 12:00 with an explicit cut order (TF-D → ff-video trim → delay-DMD aside).
**Why:** Two reasons beyond completeness. (1) The "why it matters" payoff only lands against a baseline — the point is that feedback turns the perturbation into a **controlled independent variable** ("we held this region at −5 %ΔF/F", not "we delivered 2 mW"), which is unreadable without knowing what open-loop does. (2) **Svoboda is an organizer and in the room**, and the foundational open-loop inactivation work is his — the slide is explicitly marked *do not frame open-loop as flawed; right tool, different question.* Noted a second time that the same idea is the talk's spine: the disturbance you reject IS ongoing multi-area activity, so slide 2's bridge line, slide 7's pivot and slide 9's close are deliberately the same sentence three times.
**Next:** Confirm with the organizers whether the 15 min includes questions — the plan assumes 12+3, and if it is 15+Q the restore order is TF-D, the 1:05 m13 talk cut, and +30 s each on slides 2 and 7.

### 2026-08-14 — NeuroAI Seattle talk planned; NO new analysis will be run for it
**Changed/Found:** New `TALK_neuroai_2026.md` (10-slide plan, verified asset paths, backup slides, off-limits list, 6-day schedule) + a 🔴 block in `TASKS.md`. 15 min slot at the 6th NeuroAI meeting, Allen Institute, **20–21 Aug 2026**, theme *multi-area interactions*; organizers Fairhall / Svoboda / Pereira-Obilinovic / Orsborn / **Tim Kim**. Framing decided by the user: locked closed-loop control story (slides 1–6) pivoting to contra→ipsi Global/Local + the 52-site network model (slides 7–9).
**Why:** Six days, and the interesting unfinished work is all blocked — the Global/Local controller port waits on the Stage-2 rebuild, `ctrl_optimal_xsess.m` is built but UNRUN, the 2G rebuild never ran (server down 2026-08-12), and the whole Fig-4 state-dependence panel set was audited UNUSABLE on 2026-07-21. Attempting a new result under deadline risks putting an unverified number in front of Svoboda and Fairhall. Explicitly off the slides: pre-stim variance / absolute-δ (retracted 2026-07-01 signal-power confound) and relative-δ predictability (sign-flipped on Ye `AB_0004`) — that is the one slide that could be correctly taken apart in this room.
**Next:** Talk-scale re-export is the real build job — every paper panel is 6 pt bold at 4–6 cm and is unreadable projected; needs a talk mode in `utils/paperStyle.m` (~14–16 pt, ~20 cm) for 2A/2B/2C-i/3K/5I/5J. Then backup slides, speaker notes, two timing runs.

### 2026-08-14 — The controller demo video already exists (`presentation/`); it anchors two talk slides
**Changed/Found:** The 🟢 TASKS item "Make a video of the real-time controller setup (Rainier rig)" is **already done** and was sitting unticked — `presentation/` implements exactly the offline-replay approach that item proposed. Five talk-ready 1920×1080 H.264 files: `controller_demo_m13_full_hd.mp4` (0:39, AL_0039 2025-04-20, step, 100 trials, ~30% CL-vs-OL gap), `controller_talk_m13_hd.mp4` (1:05), `controller_demo_m5_full_hd.mp4` (0:24, AL_0033 2025-03-04, pulse, ~28% gap), `ff_demo_0721_hd.mp4` (1:03, feedforward/preview), plus 7 `session_previews/`. Each replays the true widefield ΔF/F movie reconstructed from the session SVD over a masked anatomy underlay, through a white-theme replica of the live Rainier GUI, with trial-average and across-trial variance accumulating as trials arrive. TASKS item ticked with the file inventory.
**Why:** Changes the talk plan materially. The m13 video shows single trial, trial-average and evolving variance simultaneously and in motion, so it **replaces static panels 3A/3B/3C** on slide 5 rather than supplementing them — they drop to AV-failure fallback, which also shrinks the re-export job. Slide budget moved 12.5 → 13.5 min, so folding TF-A/TF-D into slide 3 stopped being optional.
**Next:** Trim `ff_demo_0721_hd.mp4` to ~30 s (ffmpeg via the `imageio-ffmpeg` package in `.venv`). Say the honesty line aloud once — replay/reconstruction, not screen capture. Separately: these are a **supplementary-video candidate for the paper**, which no PAPER.md entry currently records.

### 2026-08-13 — RAN: rejection gallery + summary now exist for all 13 sessions; the two new mice are the weak links
**Changed/Found:** Share came back, so `imp_reject_gallery_all.m` ran clean — **26 figures** (`internal_model_principle_reject_{gallery,summary}_<sess>.png`) for all 13 sessions with caches, into `paper/images/predictor_saga/`, joining the 0226 pair that was previously the only one, plus `data/imp_reject_gallery_all.mat`. Also re-ran `ctrl_reject_trial_gallery.m` with `RG_SRC='svd'`, so the three single-trial strips are now in ABSOLUTE %ΔF/F with the reference line and true ER labels, replacing this morning's response-domain fallback (which read misleadingly — CL diving to −8 looked worse when it was in fact tracking ref at −5). One bug fixed on the way: the SVD branch never set `B.absolute`, a field the cache branch had introduced — `Unrecognized field name "absolute"` on the first svd-mode run.
**Found — the batch reproduces the headline exactly, as it must (same kernel), and localizes the weakness:** **ER median OL 0.291 → CL 0.203, CL better 13/13, signrank p = 0.000244** — identical to `[F4-REJ]`, which is the whole point of routing both through `imp_reject_core`. Legacy transmission rho median 1.097 → 0.880. **Per-session rank-sum is significant in 11/13**, and the two that are not are exactly the two new mice with weak predictors — **AL_0048_0729_e2 (p=0.299, R² 0.441)** and **AL_0051_0729_e2 (p=0.755, R² −2.058)** — so the cross-session result does not rest on them and their inclusion is conservative. Strongest: AL_0033_0304_e1 (0.477→0.207, p=1.6e-07) and AL_0039_0430_e3 (0.311→0.182, p=6.1e-08).
**Found — a nuance on AL_0033_0212_e2:** its ER contrast IS significant (0.568→0.492, p=0.0015), yet transmission stays above 1 in both loops (rho 1.41→1.25) and its per-trial β went the WRONG way this morning (0.99→1.27). So the flagged session is not uniformly broken — it improves on the energy metric while never actually rejecting. Consistent with a laser spot straddling the midline: some control authority, but not over the site the predictor is scored on. Exclusion recommendation unchanged.
**Why:** user asked for the predictor-saga gallery and stats across all sessions, not just the hardwired one.
**Next:** (1) The per-session summaries show the OL-vs-CL contrast one session at a time, which is what a referee asking "is this driven by one animal?" will want — consider a supplementary grid of the 13 panel-(b)s. (2) AL_0048/AL_0051 predictor quality (R² 0.44 and −2.06) is now the weakest link in every Part-2 claim; worth a targeted look at their ROI/geometry before submission. (3) `AL_0033_0120_e3` and `AL_0039_0420_e2` still have no Stage-2 cache in any mode — the latter is the 75-frame dFk/SVD mismatch, still undiagnosed.

### 2026-08-13 — Rainier rig: `input_params.csv` `kk` is the trial END, not the onset — new `kk_onset` column (the real bug behind the "kkTemp divergence")
**Changed/Found:** `StLab_Rainier` — `BaseController._log_trial` now appends the true stimulus onset (`self.inp`) as a **`kk_onset`** column, and `_save_data` builds the CSV header from the **actual row width** so it can never disagree with the data. **The defect:** the long-standing `kk` column carries `kkTemp2`, which `_mp_reader` rewrites *every control sample* (`mp_controller.py:235`) as a free-running index — it is not a trial anchor at all. Because `_log_trial` runs after the stimulus window, the logged value is the index at the **end** of the stimulus, so `onset ≈ kk − dur·35`. This is exactly the compensation already recorded in the project memory for the 2026-07 builds ("col2 = trial END"), now traced to its cause in the rig code. Worse, the same-looking column means different things across scripts: `controller_analysis.py` logs a genuine onset under the name `kk_start`, while `Main_experiment`/base/`bilateral_main` log the end under `kk`. `kk` is left **untouched** (downstream analysis compensates for it, and redefining it would silently break those loaders); the unambiguous value is added alongside. Four scripts now route through `_log_trial` and gain `kk_onset`; `characterization.py` and `impulse_bilateral.py` deliberately do not — neither is an OL/CL protocol, both already capture their own true onset before firing, and neither has a controller mode the demo panels could bin by. Documented in `EXPERIMENTS.md` (Saved Outputs) and `README.md`.
**Why:** looking for the `kkTemp` divergence flagged earlier today turned up something more consequential — a per-trial index whose name says onset and whose value is the end, differing in meaning between scripts.
**Next:** ⚠ **Rig check, one line:** after a session, `kk_onset` should equal `kk − dur·35` to within a sample or two. If it does not, the `kk`-as-end reading is wrong and every analysis that compensates for it needs revisiting. Also worth deciding whether `controller_analysis`'s `kk_start` should be renamed to `kk_onset` for consistency (not done — it would break anything reading that column by name).

### 2026-08-13 — CORRECTION: the `kkTemp` "divergence" is benign; base and Main unified on `_trial_begin()`
**Changed/Found:** Reverses this morning's claim (entry below) that unifying base's and Main's trial-start code "would change which sample index a trial is anchored to" and that base's trials "may be anchored to a stale index". They are not. Tracing every read and write of `kkTemp`/`kkTemp2`/`inp` shows the **acquisition thread is authoritative**: on the `start_exp` rising edge `_mp_reader` sets `kkTemp = inp = kk` (`mp_controller.py:185-186`), overwriting whatever the control thread put there within one control period. So both variants converge on the same onset and the difference is confined to a ~28.6 ms window before the reader fires. Within that window Main's extra `kkTemp = last_logged_k` is **strictly better** — it is the consumer of `kkTemp` for custom-avg waveform indexing (`_sync_params_to_shm`, `step = kk - kkTemp`), so leaving it stale would index the previous trial's waveform. Base omits it and gets away with it only because base has no custom-avg path. New `BaseController._trial_begin(a)` encodes Main's version (flags, gain, mode capture, index seeding) with the authority/placeholder relationship written down, and is now used by base, `Main_experiment`, `bilateral_main` and `controller_analysis`.
**Why:** the earlier caution was right to stop the refactor, but the conclusion drawn from it was wrong, and it was left as an open question about possibly-corrupt trial windows — which would have sent someone auditing recorded sessions for a problem that does not exist.
**Next:** none for the divergence itself — resolved. The genuine index problem it uncovered is the entry above.

### 2026-08-13 — Rainier rig: session identity moved to `--mouse`, with an overwrite guard and provenance (`experiment_core/session.py`)
**Changed/Found:** New `StLab_Rainier/experiment_core/session.py` + all 8 entry scripts converted. (a) **Identity out of source:** `mouse`/`save_dir`/`data_dir` were hardcoded in five scripts, every one carrying a stale date, so each session began by editing Python — the pattern that produced AUDIT.md B4 (real sessions silently written to `test/`). Now `--mouse AL_0048/2026-08-13/1`; the module constant is only a fallback for the launchers that import `save_dir`/`data_dir` from `Main_experiment`. Path resolution is pure — **nothing is created at import time** any more. (b) **Overwrite guard:** `os.makedirs(exist_ok=True)` silently reused a directory, so re-running with an unbumped index wrote over the previous session's frames and CSVs. `prepare_session()` now refuses to start if `data_dir` holds `input_params.csv`/`frames.csv`/`states.csv`, printing the next free index; `--next` takes it automatically, `--force` overwrites deliberately. (c) **Provenance:** `params.mat` gains a `provenance` struct and the session writes `provenance.txt` — git commit, branch, **dirty flag**, script, python, start/end wall clock, frame and trial counts. Verified in isolation (this box has no numpy, so `session.py` was exercised via `importlib` directly): resolve, marker detection, next-free search, the abort guard, `--next`, and the git fields all behave. Two robustness fixes came out of that test — an unmounted frame drive now aborts with an explanation instead of a raw `FileNotFoundError`, and the module's printed strings are ASCII-only (a cp1252 Windows console raises `UnicodeEncodeError` on `—`, which would kill startup at the banner).
**Why:** every session started with a source edit, and the only thing standing between a mistyped index and a destroyed session was the operator noticing. Nothing in a saved session recorded which of the repo's eight branches produced it.
**Next:** `announce_data_paths()` in `utils.py` is now unused (superseded by the `prepare_session` banner) — left in place as an exported name rather than removed blind. Confirm on the rig that `E:/` and the data root both resolve under the new code path before a real session.

### 2026-08-13 — Rainier rig: trial primitives extracted; `control()` unification stopped short on purpose
**Changed/Found:** `StLab_Rainier/experiment_core/base_controller.py` gains `_wait_interruptible()`, `_trial_gap()` and `_log_trial()`, adopted in `base_controller.control()`, `Main_experiment.control()` and `controller_analysis.control()`. `_log_trial` appends the `input_params` row **and** publishes the demo event in one call — the pairing I had to add by hand in two places earlier today, which is exactly the divergence the copies invite. **The planned `_run_one_trial(a, dur)` was NOT built, and this is the finding:** base and Main are not actually equivalent at trial start. Base sets only `kkTemp2 = last_logged_k` and takes `inp = kkTemp` from whatever the reader thread's `start_exp` rising edge left there; Main forces **both** `kkTemp2` and `kkTemp` to `last_logged_k` before reading `inp`. Unifying them would change which sample index a trial is anchored to in one of the two paths — a control-semantics change, not a refactor. Left alone.
**Why:** the shared-primitive win is real for the gap wait and the log/publish pairing, and those are provably identical across scripts. The flag sequencing is not, and discovering that mid-refactor is a reason to stop rather than to pick whichever version looked right.
**Next:** ~~Decide which `kkTemp` convention is intended~~ — **RESOLVED same day, see the CORRECTION entry above:** the acquisition thread is authoritative and overwrites both variants within one control period, so the difference is benign and base/Main are now unified on `_trial_begin()`. The concern that base's trials might be anchored to a stale index was wrong. The investigation did surface a real index bug (`kk` is the trial END) — also logged above.

### 2026-08-13 — Rainier rig: GUI quality-of-life — progress/ETA, table-driven fields, demo PNG export, clamped gap
**Changed/Found:** `Main_experiment.py` + `base_controller.py` + `demo_panels.py`. (a) **Progress label** — `progress_text()` ("trial 12/99 · ~18 min left", ETA from a rolling mean of the last 8 trial+gap cycles), refreshed once a second on the tkinter thread; sequence length wired up for Analyze, FF Analysis and controller_analysis. Progress previously existed only as console prints, which sit behind the figure. (b) **Table-driven GUI fields** — the 15 parameter entries are now one `FIELDS` table instead of four parallel blocks (labels / constructors / `grid()` / `insert()`); **the `eN` attribute names are kept as aliases on purpose**, because `apply_gains` reads them by number and a mis-mapped field there would silently apply the wrong gain. Verified all 15 aliases the read path uses still exist. (c) **`Save demo PNG`** button — 300 dpi export of the demo column into the session directory. (d) **Clamped inter-trial gap** — `Main_experiment` draws `extra_stop = uniform(-2, 2)` against base's `uniform(0, 1)`, so with a small `Tstop` the wait timeout could go negative; `Event.wait` returns instantly on a negative timeout, silently dropping that trial's gap **and the integral-reset window that depends on it**. Now clamped in `_wait_interruptible`. Also added `README.md` (how to run a session — the repo had HANDOFF/AUDIT/EXPERIMENTS but no entry point) and `requirements.txt` (deliberately unpinned, with instructions to produce a real lock file from `pip freeze` on the rig, since inventing versions from a box that cannot import numpy would be worse than leaving them open).
**Why:** all four are things that cost time or risk every session; (d) is the only one that could corrupt data, by shortening a gap the PID's integral reset relies on.
**Next:** the gap clamp is the one item here worth watching on the first rig run — if trials look differently spaced than expected, this changed the timing (in the direction of restoring the intended gap).

### 2026-08-13 — `imp_reject_gallery_all.m`: the IMP rejection gallery + summary for every session — WRITTEN, BLOCKED on the dead SVD share
**Changed/Found:** New `controller-analysis/imp_reject_gallery_all.m` `[RGALL]` — runs the two per-session figures that `internal_model_principle.m` only ever made for its hardwired `selField = 4`: `internal_model_principle_reject_gallery_<sess>.png` (4 OL over 4 CL trials at transmission percentiles; D=Global grey, raw A light dashed, E=A−ref coloured, 1–3 s window shaded) and `internal_model_principle_reject_summary_<sess>.png` (transmission distributions 0–1 s and 1–3 s, plus transmission vs disturbance SIZE). Same file names, same folder (`paper/images/predictor_saga/`), so the existing 0226 pair is simply joined by the rest. Adds a cross-session table + `data/imp_reject_gallery_all.mat`. **Nothing reimplemented:** trials from `imp_build_session`, metrics from `imp_reject_core` — the same front end and kernel the cross-session batch uses, so every number matches the paired plots. Extracted the panel drawer from `internal_model_principle.m` into **`utils/imp_reject_panel_plot.m`** and pointed both callers at it rather than letting the batch keep a private copy — the same drift trap that file's own header already warns about for the rho lambdas. Code Analyzer clean on both files.
**Found — BLOCKED, and the failure mode changed mid-session:** `\\sahale.biostr.washington.edu\data` went unreachable earlier today. At first it failed FAST (`File not found`, per session); by the time this script ran it HANGS instead — a `run` of the batch sat 30 min without completing a single session, and `Test-Path` on the share root hangs past 20 s. Zero figures produced; the accumulator `T` came back empty. There is no cache-only fallback for these two figures (unlike `ctrl_reject_trial_gallery.m`): the gallery needs ABSOLUTE %ΔF/F for the reference line and for the pre-stim "A ≈ G ≈ 0" argument to mean anything, and the Stage-2 / CL-deploy caches store response-domain traces only.
**Why:** user asked for the predictor-saga disturbance-type gallery and its stats across all sessions rather than the single hardwired session.
**Next:** (1) Re-run `imp_reject_gallery_all.m` unchanged once the share is back — it needs no edits, only the network. (2) ⚠ A hanging SMB share leaves MATLAB unresponsive for tens of minutes; check `Test-Path \\sahale...\data` before launching anything that routes through `imp_build_session`. (3) The run will overwrite the two existing 0226 files with regenerated copies — same code path, expected.

### 2026-08-13 — Rainier rig: live demo panels (trial-locked tracking + per-mode average + variance + score) added to the experiment GUI
**Changed/Found:** New `StLab_Rainier/experiment_core/demo_panels.py` (`DemoPanels`) plus wiring in `live_plotter.py`, `base_controller.py`, `mp_controller.py`, `Main_experiment.py`, and an offline check `demo_panels_test.py`. Brings the presentation-video panels (`presentation/make_video_talk.py`) into the live rig figure as a **second column** (`python Main_experiment.py --demo`; figure 19×11 in demo mode, 13×15 otherwise): (A) trial-locked ΔF/F vs setpoint, x = time from onset; (B) running mean per controller mode with a 15-deep ghost pool; (C) across-trial variance per mode + a live score. Modes come from one taxonomy shared by both Main_experiment entry points — FF Analysis cond 0/1/2/3 → CL+p / OL+p / OL / CL, Analyze `gain_feedback` 0/1/0.5 → OL / CL / replay — so OL and CL keep the paper colours and the same baseline whichever button is pressed. Handoff is a `deque` of per-trial dicts appended by the control thread; **all** averaging happens on the plot thread. RMSE is over the stim window, sample-normalised, matching the project's locked RMSE definition. Score is signed as a *change* vs OL (`ratio − 1`), so an improvement prints `error −62%`, not `+62%`. Averages auto-reset when `dur` / readout site / setpoint changes (pooling across those would be meaningless), plus a manual `Reset demo avg` button.
**Why:** results shown alongside the GUI during live demos were requested, and the existing figure had nothing trial-locked or averaged — `ax3` is a free-running scroll of the global sample index. The design constraint is that the PID lives in Process B but the plot thread shares a GIL with `_mp_reader`, the time-critical thread that copies each frame into shared memory and releases the semaphore Process B waits on; that is the only path where plotting can cost control latency. Hence: per-trial work only (~1 per 15 s), Welford accumulation (O(window), never O(trials)), a **constant** artist count so blit cost cannot creep over a 99-trial session, every artist animated so nothing forces a full canvas redraw, and no `data_lock`. Panels default OFF so ordinary recording sessions run unchanged.
**Next:** ⚠ **Nothing has been executed** — this dev box has no numpy/matplotlib, so only `py_compile` passed. Run `python demo_panels_test.py` on the rig box first (it fakes an OL/CL and an FF session and asserts the accumulators, taxonomy, artist-count invariant and failure containment), then take the panels-off/panels-on jitter A/B described below before trusting the latency claim.

### 2026-08-13 — Rainier rig: retired the dead `ax5` "markers" axis and three permanently-empty artists; single generic blit pass
**Changed/Found:** `StLab_Rainier/experiment_core/live_plotter.py` — `ax5` and its two traces (`line5v`, `line5f`), plus `line4ei` and `line4erlp` on the error axis, were set to empty data on **every** frame (a leftover from AUDIT.md B7, which retired the underlying `var`/`fpow`/`eri`/`erlp` arrays but kept the artists "so the blit machinery is undisturbed"). They drew nothing. Removed; the freed rows go to the panels that do carry signal, so the diagnostic column is now image 0–3 / dF/F 4–7 / input 8 / error 9–11 of a 12-row GridSpec — every remaining panel is larger than before. The repetitive per-axes `restore_region` / `draw_artist` / `blit` blocks were replaced by one pass over a `[(axes, [artists])]` list, which is also how the demo column plugs in.
**Why:** Blitting five artists that are guaranteed empty costs work every frame in the process that shares a GIL with the acquisition thread, and the "undisturbed blit machinery" rationale disappears once the blit pass is table-driven. The list also removes the failure mode the old code invited — updating an artist but forgetting to add it to the draw block.
**Next:** Confirm on the rig that nothing was silently depending on `ax5` existing (nothing in the tree references it outside `backup/`, which is untouched legacy). The layout change is visible to operators — worth a heads-up before the next session.

### 2026-08-13 — Rainier rig: `timer_dt` now measures the interval it claims; jitter report + `timing_stats.csv` on save
**Changed/Found:** `StLab_Rainier/experiment_core/mp_controller.py` `_mp_reader` — `T` is now re-stamped each control sample, so `timer_dt` holds the true inter-sample interval instead of an absolute epoch stamp (see the finding logged further down). Added `BaseController.jitter_stats()` / `print_jitter_report()` (n, p50, p95, p99, max, count and fraction >1.5× the 28.57 ms nominal, first sample dropped since it spans the wait for the trigger) and `_save_data` now writes `timer_dt.csv` + `timing_stats.csv` alongside the rest of the session.
**Why:** "Without losing latency" needs to be a measurement, not an assertion, and this is the only per-sample timing record the rig keeps. Saving the stats means a panels-off vs panels-on comparison can be made after the fact rather than only from console scrollback.
**Next:** Take the **panels-off baseline first** (`python Main_experiment.py`, ~5 min of trials), then the same with `--demo`, and compare p99/max. Accept the panels only if p99 is unchanged within noise; if not, the fallback is to move the demo view to its own process on the same shared-memory block.

### 2026-08-13 — Rainier rig: FF-analysis condition stayed latched after an early stop
**Changed/Found:** `StLab_Rainier/Main_experiment.py` `ff_analysis_start()` — pressing the button a second time to stop a running FF sequence cleared `self.ff_analysis` but left `self._ff_analysis_cond` at its last value; it is only reset to −1 when the sequence runs to completion. Any subsequent Analyze trial therefore kept writing that stale condition into the `ff_analysis_cond` column of `input_params.csv`, and (with the new demo panels) would have been binned and coloured under an FF condition it was never run in. Now reset to −1 on the stop path.
**Why:** Found while wiring the demo-panel mode taxonomy, which reads `_ff_analysis_cond` to decide a trial's mode — the latch turned a display bug into a data-labelling bug that predates the panels.
**Next:** Check whether any already-recorded session was stopped mid-FF-sequence and then run in Analyze mode; those trials' `ff_analysis_cond` column would be wrong (the `mode`/`gain_feedback` column is unaffected, so the OL/CL split in existing analyses is safe).

### 2026-08-13 — Single-trial galleries (`ctrl_reject_trial_gallery.m`); SVD share went down mid-session, so it reads caches instead
**Changed/Found:** New `controller-analysis/ctrl_reject_trial_gallery.m` `[RGAL]` — per session, a 2×5 strip of INDIVIDUAL trials (OL top, CL bottom) showing Global (grey) against Actual, with trials drawn at rejection percentiles 10/30/50/70/90 so the strip spans the range rather than flattering it. Supplementary PNG → `figure4/supp/f4supp_trials_<sess>.png`. **The `\\sahale.biostr.washington.edu\data` share became unreachable mid-session** (`Test-Path` on the share root returns False; the same three sessions had loaded fine ~10 min earlier during `[SENS]`), so the script gained a `RG_SOURCE` switch — `'svd'` (absolute %ΔF/F, ref line, true ER, needs the share) / `'cache'` (per-trial `A_tr`/`G_tr` straight out of the Stage-2 and CL-deploy caches: no network) / `'auto'`. Cache mode covers only the 3 sessions that have a CL-deploy cache (0212, 0224, 0226).
**Found — single-trial transmission separates OL from CL cleanly, and independently re-flags 0212:** cache traces are RESPONSE-domain so ER is not computable; trials are ranked by per-trial **transmission β**, the regression slope of Actual on Global over the stim window (1 = disturbance passes through untouched, 0 = rejected). **AL_0033_0226_e2 β median OL 0.97 → CL 0.56; AL_0033_0224_e2 OL 0.88 → CL 0.57.** Visually the tiles do the job the energy ratio cannot: before onset Actual and Global lie on top of each other on single trials with no averaging to hide behind, and after onset they separate. **AL_0033_0212_e2 goes the wrong way — OL 0.99 → CL 1.27** — an independent corroboration of the 2026-08-13 geometry audit that flagged it (frame transposed at load, laser effect ~5× weak, spot straddling the midline). Exclusion recommendation stands.
**Why:** user asked to see example OL and CL trials of the prediction and the rejection before the Part-2 panels go any further.
**Next:** (1) ⚠ **The response-domain gallery is misleading on its own** — CL Actual dives to −6…−8 while OL only reaches −2…−4, which reads as "CL is worse" when in fact CL is driving to the reference at −5. The absolute frame with the ref line is what makes that legible, so regenerate with `RG_SRC='svd'` once the share is back before showing this to anyone else. (2) Only 3 sessions have CL-deploy caches; `ctrl_ols_cl_deploy.m` would need running on the rest for cache-mode coverage. (3) The share outage also blocks any further `[SENS]`/`imp_build_session` work — nothing to fix locally.

### 2026-08-13 — Fig 4 Part 2: rejection resolved in FREQUENCY — the waterbed crossover is real (`ctrl_sensitivity_freq.m`)
**Changed/Found:** New `controller-analysis/ctrl_sensitivity_freq.m` `[SENS]` — per trial, over the response window, with the contra-derived Global as the measured disturbance: T(f) = ⟨|E(f)|²⟩/⟨|D(f)|²⟩ per condition and **S(f) = T_CL(f)/T_OL(f)** = what feedback changed. 13 sessions (the two without Stage-2 caches skip). Exports to `paper/images/figure4/supp/` — **supplementary, PNG only, pending approval** (user). Band restricted to 1–10 Hz per user (below 1 Hz is out of scope). Two bugs caught while building: `cell2mat(arrayfun(@(a) a.X(kb).', ...))` transposed the CONTENTS instead of the cell array, silently producing one long column (out-of-bounds on the first use, a 10⁵-scale nonsense ratio on the second); and a band-shading patch with hardcoded y-corners hijacked every axis — the same failure mode as `ctrl_fit_figs`'s laser patch earlier today, now fixed with a helper that shades to limits the data already set.
**Found — the IMP prediction holds, and the crossover sits where Part 1 said it would:** S(f) = **0.936 at 1–2 Hz** (suppression, n.s. p=0.11, 4/13 above 1) → **1.089 at 2–4 Hz (p=0.021, 10/13 above 1)** → **1.131 at 4–10 Hz (p=0.0024, 11/13)**. Feedback suppresses below ~2 Hz and **amplifies above it** — the Bode waterbed, and the mechanism behind Part 1's sign flip (CL error rising with 2–4 Hz power while OL error falls). ∫log S df over 1–10 Hz = **+0.665**, i.e. net amplification inside the measured band; the compensating suppression sits below 1 Hz, excluded by design. Steady-state offset from ref over 2–3 s: **OL +1.38 → CL +0.38 %ΔF/F, signrank p=0.008, smaller in 12/13 sessions** — the integrator's internal model of a constant, working.
**Found — all three controls pass, and one of them retires a planned worry:** (a) [SENS-NULL] the same estimator on the matched PRE-STIM window (laser off in both conditions) is **flat at 1** — 1.016 / 1.026 / 0.995 across the three bands — so the ratio-of-ratios is not an artifact of the estimator; (b) [SENS-DIST] the OL and CL disturbance spectra ⟨|D|²⟩ overlie each other, so both conditions genuinely see the same disturbance and it cancels in S; (c) [SENS-LEAK] **the stim-locked (leak) component is only 1–3% of disturbance power above 1 Hz.** That measures the leak-bias concern directly instead of bounding it — the leak is slow, so restricting the claim to ≥1 Hz removes it, and the planned deflate-Global bound is not needed for this claim.
**Why:** ER is a scalar over 0–3 s and cannot separate "rejects everything by 30%" from "rejects DC and amplifies 3 Hz" — which is the whole IMP result. User approved building all the proposed additions, supplementary until reviewed.
**Next:** (1) ⚠ Above ~8 Hz the disturbance power has fallen ~3 decades and both T curves shoot up — the 4–10 Hz band partly rides on that noise-dominated top end. **2–4 Hz is the safe claim**; either cap the band at 8 Hz or state the caveat. (2) The predicted-vs-measured S(f) panel (1/(1+CP) from the Fig-2 plant TF and the real Kp/Ki) is NOT built — per-session gains are not in the session structs (`d.params` holds only pixel/laser/dur/horizon/kernel); they live in the controller-tuning rig config and must be recovered first. (3) Panel (c) shows a slight CL excess of disturbance power in 2–4 Hz — check it is not the controller *injecting* at that band, which would be a different story from amplifying an existing disturbance.

### 2026-08-13 — Rainier rig: `timer_dt` stores absolute epoch time, not per-frame dt (found while planning live-GUI demo panels)
**Changed/Found:** No code change yet. In `StLab_Rainier/experiment_core/mp_controller.py` `_mp_reader`, `T = 0` is set once before the acquisition loop (line 161) and **never reassigned**, so `self.timer_dt[0, self.kk] = time.time() - T` (line 170) writes an absolute epoch timestamp every control sample rather than the inter-frame interval the name implies. Consequence: any past reading of `timer_dt` as a latency trace was wrong; but `np.diff(timer_dt)` recovers the true inter-sample interval, so the array is still a usable jitter instrument. Relevant because the planned live-GUI demo panels (trial-locked tracking trace + per-mode running average, video-style) add work to the plot thread inside **Process A**, which shares the GIL with the time-critical `_mp_reader` that copies each frame into shared memory and releases `_frame_sem` for the PID process — the one place a plotting change can actually cost control latency.
**Why:** The user's constraint is "without losing latency". `timer_dt` is the only existing per-sample timing record, so fixing/renaming it and dumping p50/p95/p99/max of the inter-sample interval turns that constraint into an A/B measurement (panels off vs on, 28.57 ms nominal at 35 Hz) instead of an assertion.
**Next:** Fix to a true per-sample dt (or rename to `t_stamp`) and take a panels-off baseline BEFORE adding any panel, so the comparison exists. Log the fix in the Rainier repo's `AUDIT.md`/`EXPERIMENTS.md` as well, since that is where its bug list lives.

### 2026-08-13 — Reframe: Part 1's delta result is a WATERBED signature, not just "a factor feedback fails on"
**Changed/Found:** No code change — a reinterpretation of an existing result, recorded before it gets written up the weaker way. In `factor_slope_delta24` the two loops have **opposite signs**: OL slope **−0.474** (open-loop error *decreases* with relative 2-4 Hz power) vs CL **+0.847** (closed-loop error *increases*). A controller that merely lacked an internal model of that band would leave the OL slope untouched (S≈1, no rejection, no harm); a slope that flips sign means feedback is actively making 2-4 Hz **worse** — which is the Bode-integral/waterbed prediction, not a null. Meanwhile motion, a slow quasi-DC nuisance, is fully decoupled (OL +0.652 → CL −0.016, p=0.0020), which is the integrator's internal model of a constant doing its job. So Part 1, read through the Internal Model Principle, already says: the loop contains a model of DC and rejects it; it contains no oscillatory mode, and pays for the low-frequency suppression at 2-4 Hz.
**Why:** Fig 4's argument is being built as an IMP argument, and this is the strongest existing evidence for it — currently presented as a state-dependence result, which undersells it.
**Next:** The claim is not established until rejection is resolved in FREQUENCY — the current Part-2 metric (ER, a scalar over 0-3 s) cannot distinguish "rejects everything by 30%" from "rejects DC perfectly and amplifies 3 Hz", and that distinction *is* the IMP result. Proposed: empirical sensitivity S(f) = T_CL(f)/T_OL(f) with T = ⟨|E(f)|²⟩/⟨|D(f)|²⟩, E = A−ref, D = G−ref, from `imp_build_session`'s Aol/Gol/Acl/Gcl. ⚠ Two things to check first: (a) the ridge Global leaks ~23% and the leak is SLOW, which inflates D at low f and therefore biases S *downward* there — i.e. anti-conservative for the DC claim, so it must be bounded (the retired deflate Global is the natural bound); (b) 3 s windows give ~0.33 Hz bins, so below ~1 Hz there are only 1-3 cycles and the estimate is weak.

### 2026-08-13 — Diagnosed and FIXED the quartile-panel artifact: within-session normalization + session-level error bars
**Changed/Found:** Diagnosed the 1-2 Hz control panel problem properly and fixed it rather than working around it. **The mechanism is not what my first note said.** Session-mean δ predicts session-mean CL RMSE about equally for BOTH bands (rel 2-4 Hz r_s=+0.66 p=0.009; rel 1-2 Hz r_s=+0.61 p=0.019, n=15 sessions), so pooling raw trial RMSE lets each session's overall difficulty leak into the quartile means — **and it leaks by a different amount per band, in opposite directions**. Replicating the panel's own binning exactly and splitting each trend: **2-4 Hz** pooled Q4−Q1 = +0.32 but session-centred **+0.43** (between-session structure was *hiding* 25% of the real effect); **1-2 Hz** pooled +0.34 but session-centred **+0.16** (between-session structure *manufactured* 52% of it). Two errors pointing opposite ways, compressing effect and control to look identical (+0.32 vs +0.34) when the within-session truth is +0.43 vs +0.16 — a 2.7× difference. Per-session Q4−Q1 confirms: 2-4 Hz median +0.27, signrank p=0.018, 11/15 sessions positive; 1-2 Hz median +0.02, p=0.76, 8/15 (a coin flip). **Second, independent defect: the error bars were the wrong unit** — trial-SEM 0.042 vs session-SEM 0.120–0.131, i.e. **~3× too small**, because trials within a session are not independent; this affected every panel in the family, including the good ones.
**Changed:** `cl_factor_claim_panels.m` and `cl_factor_olcl_panels.m` now accumulate per-session bin means and each session's level, and plot session-centred means with the grand mean added back (axis stays %ΔF/F) and **SEM across sessions**. The session level is the mean over its OL *and* CL trials together, so subtracting it removes difficulty without touching the OL−CL gap or the quartile trend. `CLM_WITHIN` reverts. **Result: the 2-4 Hz CL series rises 1.88→2.21 while the 1-2 Hz CL series is flat (2.04, 2.09, 2.08, 2.07)** — the dissociation is now visible, and the two panels can sit side by side, which is the clearest statement of it in the figure. Two z-score leftovers also caught and fixed in the same pass: the claim-3 slopes were still z-scored per window (printed +0.297/+0.089; now raw **+0.275/+0.042**, matching `cl_factor_slope_panels` exactly), and the mixed-effects outcome was z-scored (now raw: **+0.158 %ΔF/F per SD of rel24 [0.065, 0.251], t=3.33, p=0.00092** — t and p unchanged, only units).
**Why:** user asked for a fuller explanation of the flagged problem and a suggestion. The honest answer needed the decomposition, not an assertion — and once measured, the fix was obvious and better than the workaround (which was "never show the control panel next to the effect panel").
**Next:** (1) The `_slope` panels are unaffected (they were always per-session) — the two families now agree by construction rather than by luck. (2) PAPER.md red-flag line replaced with the resolution. (3) Shared y-axis for `factor_slope_motion` / `factor_slope_delta24` still outstanding.

### 2026-08-13 — Quartile panels rebuilt point-and-line with a cropped axis; ⚠ the magnification exposes that the 1-2 Hz control panel is unusable
**Changed/Found:** New `utils/cl_quartile_line.m` — shared neuroscience-style quartile panel (mean ± SEM as points joined by lines, y-axis fitted to the data rather than anchored at 0, box off / ticks out / 6 pt bold, legend northoutside). `cl_factor_claim_panels.m` (`mk_ol_cl`, `mk_earlylate`) and `cl_factor_olcl_panels.m` now both call it; ~60 lines of duplicated bar-drawing deleted. **The mark type had to change before the axis could be cropped:** a bar encodes value as length from zero, so a cropped bar chart misstates every ratio on it; a point encodes position and carries no claim about zero, which is what makes the crop legitimate. Rationale is in the helper's header so it does not get "simplified" back to bars. All six panels regenerated, sizes unchanged at 6×4 cm.
**Found — the panels are much stronger, and one of them is now dangerous:** the decoupling and transient claims read instantly — `factor_olcl_motion` shows OL jumping at Q4 (2.85→3.31) while CL stays flat (2.56→2.37); `claim3_initdev_early_late` shows Early climbing 2.38→4.19 against Late 2.00→2.52. **But `claim2_delta_lo12_late`, the 1-2 Hz specificity NULL control, now looks like a positive result: its CL series rises 2.00→2.34 (Δ+0.34), essentially identical to the 2-4 Hz panel's 1.93→2.26 (Δ+0.33).** This is the between-session baseline structure flagged 2026-07-22, and cropping the axis magnifies it along with the real effect. The pooled-quartile form simply cannot carry the 2-4 vs 1-2 Hz dissociation; only the per-session paired-slope form can (p=0.0067 \*\* vs p=0.76 n.s.).
**Why:** user asked to magnify the difference and produce a cleaner neuroscience-style version of the bar panels.
**Next:** (1) 🔴 **Do not place `claim2_delta_lo12_late` next to `claim2_delta_hi24_late` anywhere** — side by side they argue against the dissociation the paper claims. The 1-2 Hz null must be shown as `factor_slope_delta12`, where the picture and the statistic agree. Updated in PAPER.md. (2) `claim2_delta_hi24_late` remains the right magnitude companion for the 2-4 Hz result on its own. (3) Shared y-axis for `factor_slope_motion` / `factor_slope_delta24` still outstanding.

### 2026-08-13 — All Fig-4 Part-1 images regenerated z-score-free; orphaned July panel archived; layout recommended
**Changed/Found:** Re-ran `cl_rmse_factor_windows.m` (the 6-tile grid) so every Part-1 image in `paper/images/figure4/` now dates 2026-08-13 and reproduces from current code — verified by timestamp audit. Its partial-R² table is unchanged (0.381/0.029, 0.001/0.001, 0.089/0.122) as predicted by scale-invariance, and its title now reports the true **11 sessions**. Archived `claim2_delta_rel_late.{png,pdf}` to `_archive_zscored/`: it is the July **1–4 Hz** delta panel, superseded by the hi24/lo12 sub-band pair, and its generating code path no longer exists — it was the last stale Part-1 file. Added a recommended layout to PAPER.md.
**Found — the raw-units quartile panel reads better than the z version it replaced:** `claim2_delta_hi24_late` now shows CL RMSE climbing **1.93 → 2.25 %ΔF/F** across 2–4 Hz quartiles while OL stays ~2.7–3.0, so the OL−CL advantage visibly narrows (0.74 → 0.56) — a magnitude statement in real units that the slope panels cannot make. Good supplementary companion to the delta24 slope panel.
**Why:** user asked for the images without z-scoring plus a recommendation for how they enter the Fig-4 story; the audit was to guarantee no z-era file survives in the panel pool.
**Next:** ⚠ `factor_slope_motion` (y −0.5…2) and `factor_slope_delta24` (y −2…2.5) are the same plot with opposite outcomes and are the strongest argument in the set, but their different y-ranges blunt the contrast. Give them a shared y-axis before assembly — not yet done.

### 2026-08-13 — PROJECT RULE: RMSE is never z-scored. Removed from every Fig-4 Part-1 script; two effects strengthen
**Changed/Found:** Per user ("RMSE needs to be positive, and individual-session variability should not be normalized"), stripped outcome z-scoring from the whole Part-1 family: `cl_factor_slope_panels.m` (pooled OL+CL `zpool` → identity; the within-CL Early/Late z-scoring in the init-dev branch → raw; y-label `z-RMSE` → `RMSE`), `cl_factor_olcl_panels.m` (per-session pooled z removed from the slope stat), `cl_factor_claim_panels.m` (`zwin` → identity), `cl_factor_decomp_panel.m`, `cl_rmse_factor_windows.m`, `cl_mse_factors.m` (all three: `y = zscore(...)` → raw). Also dropped the `_z` panel variants from `cl_factor_olcl_panels.m` and `cl_factor_claim_panels.m` — they plotted the same panels in the forbidden units — and moved the 14 stale `_z` files to `paper/images/figure4/_archive_zscored/` (moved, not deleted; they are no longer reproducible since the code paths are gone).
**Found — removing the normalization made two results STRONGER and changed none of the decomposition:** **rel 2–4 Hz OL-vs-CL went from p=0.0125 \* to p=0.0067 \*\*** (OL −0.474 / CL +0.847) and the **init-dev Early→Late slope collapse went from ~3× to ~7×** (+0.275 → +0.042, late still ≠0 at p=0.002), which now sits much closer to its 13× unique-R² collapse. Motion unchanged (p=0.0020 \*\*), and the 1–2 Hz specificity null stayed null (p=0.76). The **unique-R² decomposition is bit-identical** (init-dev 0.381/0.029, motion 0.001/0.001, rel 2–4 Hz 0.089/0.122, R²_full 0.407/0.132) because R² is invariant to linear rescaling of the outcome — the change is that the code no longer *looks* like it normalizes away session variability, and the slope panels (which are not scale-invariant) are now consistent with it. Mixed-effects on the current pool also improved: **rel24 +0.127 [0.052, 0.202], t=3.33, p=0.00092, 852 trials / 15 sessions / 4 animals** (was p=0.004 on 752 / 13 / 2).
**Why:** the pooled z divided each session by its own SD, which left the within-session OL/CL ratio intact but rescaled every session onto a common yardstick — discarding the across-session spread in tracking quality that the figure is partly about. It also put a positive quantity on a signed axis.
**Next:** grep the Fig-3/Fig-5 scripts for the same pattern before drafting — this rule was applied to Part 1 only, and `variance_mse.m` / the sine panels have not been checked.

### 2026-08-13 — Fig-4 Part-1 supplementary set fixed: burst gallery + error-type exemplars (user)
**Changed/Found:** Per user, `delta_burst_gallery.png` (`cl_delta_burst_explore.m`) and `cl_mse_exemplars.png` (`cl_mse_exemplars.m`) are **Part-1 supplementary panels**, and the whole quartile-bar `factor_olcl_*` / `claim*` family **stays in the figure** rather than being demoted. Reverses my 2026-08-13 note (entry below) that the gallery was "not a Part-1 panel" and that the quartile family should go to supplementary/archive. Both regenerated on the current pool: gallery now **252/852 CL trials (30%)** carry ≥1 short 0.4–1.5 s burst (was 226/752); exemplars on 613 valid CL trials, pool RMSE mean 2.45 / median 2.06 / sd 1.18. PAPER.md Part-1 block rewritten accordingly.
**Why:** user: "I love all the factor_ images in figure 4 folder and the gallery as a supplementary figure, they tell a cool story, also the exemplar images showing the types of errors for the supplementary." The panels do work the slope panels cannot — the gallery shows what the activity *looks like*, the exemplars show what each error type looks like on one trial.
**Next:** ⚠ the gallery must be captioned so it cannot be read as a positive result — burst *count* does not predict RMSE (1.74 vs 1.59, ranksum p=0.56, every timing variant n.s.). It is the qualitative picture; the quantitative claim is the continuous 2–4 Hz measure in P1-d.

### 2026-08-13 — Fig 4 Part 1 wrapped: the error-DECOMPOSITION panel finally exists (`cl_factor_decomp_panel.m`)
**Changed/Found:** New `controller-analysis/cl_factor_decomp_panel.m` `[F4P1-DECOMP]` → `paper/images/figure4/f4p1_error_decomp.png` (6×4 cm, paperFig/paperStyle, PNG only — not yet declared a paper panel). Part 1 had four paired-slope panels that each test ONE factor, and no panel showing the decomposition itself; the unique-R² numbers existed only in a printout and inside one tile of the 19×12 cm exploratory grid. Measures copied byte-identically from `cl_rmse_factor_windows.m`, so they reproduce exactly. **Unique (partial) R², 613 CL trials / 11 sessions, 2000-boot 95% CI: initial deviation 0.381 [0.299 0.458] early → 0.029 [0.004 0.077] late (13× collapse); motion 0.001 → 0.001; relative 2–4 Hz 0.089 [0.054 0.130] → 0.122 [0.077 0.176]. Full model R² 0.407 early / 0.132 late. Relative 2–4 Hz goes from 22% of the explained variance early to 93% late.** PAPER.md gained a locked Fig-4 Part-1 panel table (P1-a…P1-e) that also reconciles the naming — "Part 1" in RESEARCH/conversation = "Block 2 / panel 4B" in PAPER.md's 2026-07-29 block numbering.
**Found — the motion gate was costing 4 sessions and buying nothing:** `cl_rmse_factor_windows.m` gates the WHOLE pool on `has_motion`, but m1/m3/m7/m8 (AL_0033) have no motion recording, so a third of the data was dropped for a factor whose unique R² is 0.001. Re-scoring the two power-independent factors with the gate off: 852 trials / 15 sessions, full R² 0.389 early / 0.123 late vs 0.407 / 0.132 gated — init-dev 0.370/0.043 vs 0.381/0.029, delta 0.074/0.105 vs 0.089/0.122. Nothing moves. The panel therefore keeps all three factors on one pool (internally consistent) and the script prints the all-session check, which is what licenses reading the motion bar as *motion does not matter* rather than *motion was not measured here*.
**Why:** user asked to wrap up Fig 4 Part 1 ("the one that talks about error decomposition"). The decomposition was the one thing Part 1 claimed and never drew.
**Next:** (1) ⚠ Two collapse numbers for init-dev, both real, must not be conflated in the text — unique R² collapses **13×** (0.381→0.029) while the *slope* collapses ~3× (+0.297→+0.089, late still ≠0 at p=0.003); variance ownership falls faster than the slope because late RMSE is much less variable. Quote R² for "what the error is made of", slope for "how strongly it acts". (2) Flip `FD_EXPORT_PDF` when the panel is confirmed as a paper panel. (3) Drafting.

### 2026-08-13 — Fig-4 Part-1 slope panels re-run on the current 15-session pool; every stat strengthens
**Changed/Found:** Re-ran `cl_factor_slope_panels.m` — the four panels on disk dated 2026-07-23 and were built on the 13-session registry, which has since gained AL_0048 (m14) and AL_0051 (m15). New values (old in brackets): **init-dev Early +0.297 / Late +0.089, signrank p=6.1e-5 \*\*\* [was 2.4e-4]; motion OL +0.686 / CL −0.016, p=0.0020 \*\* [0.0078]; rel 2–4 Hz OL −0.511 / CL +0.813, p=0.0125 \* [0.0479]; rel 1–2 Hz null control OL −0.319 / CL +0.068, p=0.76 n.s. [0.376]**. n=15 for all but motion (n=11, coverage). Every effect got stronger and the specificity null got *more* null — the 2–4 vs 1–2 Hz dissociation is now p=0.0125 vs p=0.76 on four mice.
**Why:** panels shipped as "locked" in July were stale against the data; locking a panel set means the exported files match the current registry, not that they are frozen.
**Next:** init-dev's late slope is now +0.089 at p=0.003 (July: +0.027, p=0.013) — still ~3× smaller than early, but "negligible" is too strong for the caption. Say "collapses ~3× and owns 13× less of the error variance", not "negligible".

### 2026-08-13 — Bug: `cl_rmse_factor_windows.m` titled every figure with a hardcoded, stale session count
**Changed/Found:** The 2×3 grid's title read `(n=%d trials, 9 sessions)` — trials computed, session count a literal. It was true when written and silently went stale; the run that exposed it reported 613 trials from **11** sessions under a title claiming 9. Replaced with `numel(unique(SESS))` plus a comment saying why. No numbers change — the figure was always right, only its title lied.
**Why:** found while auditing the Part-1 pool; a wrong n in a figure title is the kind of thing that survives into a caption.
**Next:** none. (Worth a grep for other hardcoded n in titles if a similar surprise shows up.)

### 2026-08-13 — LOCKED: ridge is the project predictor; rank and deflate retired
**Changed/Found:** `utils/ctrl_pred_tag.m` — default mode flipped `'rank'` → `'ridge'`, so any script run without `CTRL_PRED` set now reads the ridge caches (verified: `ctrl_pred_tag()` returns `ridge`/`_ridge`, 13 caches present). Header rewritten: ridge documented as THE project model, `'rank'` and `'deflate'` marked RETIRED but still reachable as revert switches (all three keep their own cache suffix, so nothing was overwritten — 13 rank + 13 ridge + 13 deflate caches all intact on disk). `run_fig4.m` `RUN_PRED` comment updated to say locked-not-provisional. `ctrl_ols_ol_stimblind.m:223-224` deflate branch tagged retired. `ctrl_deflate_check.m` and `ctrl_reject_models.m` given RETIRED headers — kept as the *record of why* deflate was rejected and as the robustness exhibit, not as pipeline steps. Confirmed `data/imp_reject_across_sessions.mat` is byte-equal to the `_ridge` aggregate, then re-ran `f4_reject_panels.m` under ridge so the exported figure-4 panels match the locked model: OL 0.291 [IQR 0.172] → CL 0.203 [IQR 0.116], CL better 13/13, signrank p=0.000244, mean gain +0.100 [95% CI +0.069, +0.136].
**Why:** user decision ("keep ridge only") after the three-way comparison. Ridge is the only one of the three that is *fully stim-blind* — neither the fit nor the λ selection ever sees a laser-on trial — whereas deflate's constraint direction `d` is measured on stim trials, which makes its flat Global and 100% Local the constraint restated rather than a result. Every residual bias ridge carries points away from the conclusion: its Global still leaks (median 23%), which understates the disturbance and therefore *under*-reports rejection. A one-model pipeline also removes the mixing hazard — a ridge Global scored against a rank decomposition compares nothing.
**Next:** none for the switch itself. The deflate work is not wasted and should be cited, not re-derived: leak is driven by the single scalar `b'·dip` (Spearman −0.91 with leak%, 0.89 with the Global trough, vs 0.51 for ‖b‖), and the CL/OL contrast moves only 0.682 → 0.697 across models (signrank p=0.305, CL better 13/13 under both) — that pair is the answer to "did the model choice choose the answer".

### 2026-08-12 — Disturbance rejection under both models: the OL-vs-CL CONTRAST is model-independent, the absolute level is not
**Changed/Found:** ran `imp_reject_across_sessions` under both disturbance models (ridge and deflate), saved both aggregates, and added `controller-analysis/ctrl_reject_models.m` `[REJMOD]` to compare them paired over the 13 common sessions. **Absolute ER moves a lot** — OL 0.291 → **0.205**, CL 0.203 → **0.139** (medians) — because the leak sits in the ER denominator: a dipping Global is closer to ref, understating the disturbance. Under the leak-free model the Global dip drops from −1.033 ± 0.815 to **+0.098 ± 0.115 %ΔF/F**, and the fraction of trials doing work rises to OL 100% / CL 100% (was OL 98%). **The CL/OL ratio barely moves: 0.682 → 0.697 (median), signrank p = 0.305, CL better in 13/13 under BOTH models.** Per session the leak was hiding a median **19%** of the OL rejection (range 5–68%, worst on 0304 at 68%, 0226 53%, AL_0039_0419 50%).
**Why:** this is the algebra made explicit and it is the most useful robustness result so far. The leak lives in the denominator that OL and CL SHARE, so it scales both ER values together and cancels in the contrast. That means the paper's actual claim — closed loop rejects more than open loop — does not depend on which contra predictor is used, while any absolute "the controller removes X% of the disturbance" statement does. Quote the contrast; treat the absolute level as bracketed by the two models (OL 0.205–0.291, CL 0.139–0.203).
**Next:** decide which model supplies the Fig-4 numbers. Recommendation: ridge for the headline (conservative — it under-reports rejection) with deflate as the stated upper bound, since deflate's flat Global is imposed rather than measured.

### 2026-08-17 — Fig 2 laid out as 4 rows × 4 panels; panel size moved into `paperStyle`
**Changed/Found:** `utils/paperStyle.m` gains `PS.pageW = 18.0`, `PS.f2gap = 0.4`, `PS.f2w = 4.20`, `PS.f2h = 3.6`. Eight Fig-2 scripts now size their paper panels with `paperFig(PS.f2w, PS.f2h)` instead of a local literal: `trace_overlay.m`, `dose_response.m`, `utils/imp_tf_paper_fig.m`, `utils/imp_tf_robust_fig.m` (A, D), `imp_state_trialvar_fig.m` (2J/2K/2G only), `impulse-analysis/motion_analysis.m`, `controller-analysis/variance_mse.m`, `controller-analysis/tf_fit.m`.
**Why:** user, 2026-08-17 — rows 1–2 for the fits split 75% impulse / 25% step, row 3 state dependence, row 4 residual; "figures need to be resized to fit about 4 panels in a full row". Worth recording that the 75/25 split **collapses into a plain 4-column grid**: 75% over 3 panels equals 25% over 1 to within a rounding error, so all columns are one width and the step column aligns with column 4 of rows 3–4. `PS.pageW = 18` is inferred from Fig 3, whose paired panels are 8.9 cm — if the journal's text width differs, that one constant is the only edit.
**Next:** 6 pt type is ABSOLUTE, so at 4.2 cm it eats ~43% more panel width than at 6 cm — re-export alone is not enough. 2I re-run and checked (holds; legend still fits below the axes). **Unchecked at the new size: 2J, 2K, 2G, TF-D, 2E, 2A, 2B, 2C-i, TF-A.** Also: row 4 has zero panels and row 2 col 3 is open — see PAPER.md.

### 2026-08-17 — Two files named `tf_fit.m` on the path — edits appeared not to take effect
**Changed/Found:** `which('tf_fit')` resolves to **`impulse-analysis/tf_fit.m`**, not the controller one. Three consecutive runs of `controller-analysis/tf_fit.m` re-exported `ol_tf_trial_avg.pdf` from a **cached copy of the script**, so edits made between runs silently did nothing while the PDF timestamp and byte count kept changing — it looked like the edits were wrong rather than unloaded. `clear tf_fit` did not help; it targets the `which` hit, i.e. the impulse file.
**Why:** worth recording because the failure is invisible — the run succeeds, the figure exports, the numbers are right, only the newest edit is missing.
**Recipe that works:** `close all; clear functions; rehash path; rehash;` then `run` with the **absolute** path. Also note MCP-launched figures are destroyed when the evaluate call returns, so any `exportgraphics` used to eyeball the result must be in the SAME call as the run.
**Next:** rename one of them (`controller-analysis/tf_fit.m` → `ctrl_tf_fit.m` is the smaller blast radius; `imp_tf_run.m` already fronts the impulse one). Not done yet — it touches `run_all.m` call sites.

### 2026-08-17 — 2I legend moved outside; on-panel R² line removed
**Changed/Found:** `controller-analysis/tf_fit.m` — (a) the corner "best 3 of 15 · …" text is gone from the panel and is now printed as `[OL-TF] CAPTION MUST STATE: …`; (b) legend moved from inside `southwest` to a horizontal strip **below** the axes, which are shrunk from the bottom by the legend height to make room; (c) `ItemTokenSize` overridden to `[16 6]` for this legend only.
**Why:** user, 2026-08-17 — panel too verbose, legend to bottom-left, "the dash doesnt appear like a dash". The dash failure was real and mechanical: MATLAB scales dash period with LineWidth, so the 1.0 pt fit line inside the project-default 6 pt token drew a single dash and read as a short solid line — the legend failed at the one distinction it exists to make. Inside-`southwest` was not usable either: the deepest session bottoms out near the axis floor, so the lower-left quadrant is data, not whitespace.
**Next:** the caption now has to carry the outcome-selection disclosure that used to be on the panel — see the entry below. Legend is nudged right by `0.11*axes width` so its solid swatch does not run into the "1 s" scale bar on the same baseline.

### 2026-08-17 — 2I panel restricted to the 3 best-fitting sessions (OUTCOME-SELECTED — read this before writing)
**Changed/Found:** `controller-analysis/tf_fit.m` gains `OL_TF_SHOWN` (default **3**): every controller session is still fitted, but the paper panel draws only the top-N by R². Selected: **s5 (0.964), s6 (0.944), s15 (0.985)**. All-15 distribution unchanged and still printed: median 0.779, ≥0.5 in 11, below 0.5 in s1/s4/s8/s14 (s8 −4.98, s14 −3.59).
**Why:** user, 2026-08-17 — the 15-session overlay "looks absurd", "just keep three best fit sessions". I had flagged on the same day that selecting on R² is the same circular exclusion retracted on 2026-08-12; the user reaffirmed, so this is their call and it is implemented.
**How the circularity is contained (do not remove):** the panel is an **example**, not a population estimate, and it says so on its own face — the corner line reads "**best 3 of 15 · shown R² 0.94–0.99 · all 15 median 0.78**", so the selection travels with the figure rather than living in a caption that can be dropped. `OL_TF_SHOWN = Inf` restores all sessions. The console still prints the four failing sessions in red on every run.
**Next:** the Results sentence must be the **all-15** number ("a low-order LTI model fits the OL trial average in 11 of 15 sessions, median R² 0.78"), with the panel described as showing three representative sessions. Writing "the model fits the OL step response" off this panel alone would be unsupported by the very run that produced it.

### 2026-08-17 — 2I rebuilt on all 15 controller sessions: the LTI fit does NOT hold in 4 of them
**Changed/Found:** `controller-analysis/tf_fit.m` — session set opened from the hardcoded `[4 9 11]` to **all controller sessions** (`OL_TF_SESS` overrides), model order dropped from the panel (user: "dont show the TF design (2p1z) its not required"), per-session R² text block replaced by one corner line, and the legend reduced to the encoding (solid = OL mean, dashed = LTI fit) instead of a row per session. **BUG found on the way:** `ol_colors` was a hardcoded 3×3 from the `[4 9 11]` days, so `col_s = ol_colors(si,:)` threw on session 4 and **the loop died there silently** — the paper panel had already drawn sessions 1–3, so it looked like it had worked. Now sized per session.
**Result — and it does not support the sentence we wanted:** 15/15 fit, **R² median 0.779, ≥0.5 in 11, ≥0.7 in 10**, but **4 sessions are below 0.5 and two of those are catastrophic — s8 R² = −4.98, s14 = −3.59**. A negative R² means the LTI fit is worse than a flat line; those are not "slightly worse" sessions. Orders np [2 2 1 2 2 2 2 2 2 1 1 2 2 2 2], nz mostly 1. s8's poles are a complex pair (−1.65 ± 0.67i) and s14's too (−2.10 ± 0.40i) — both ring, which is why a step fit fails on them.
**Why:** user asked to replace the raw step panel (2D) with the OL trial-average TF fit across all controller sessions, on the reasoning that impulse and step are different measurements so only the "a low-order LTI model fits" claim is shared.
**Next:** the honest caption is "**a low-order LTI model fits the OL trial average in 11 of 15 sessions (median R² 0.78)**", not "in all". Decide before writing: report 11/15 with the failures named, or restrict the panel to a pre-stated subset — but the subset must be defined by something other than the R² itself, or it is the same circular exclusion retracted on 2026-08-12. The panel prints the failing sessions in red on every run so this cannot be forgotten.

### 2026-08-17 — 2B legend/text merged into direct labels; 2F trend line removed
**Changed/Found:** `impulse-analysis/dose_response.m` — 2B carried **both** a legend (colour → "Session k") and a stacked per-session text block (colour → slope/R²), so the same information arrived twice and the reader had to join them through the colour; the text block was also spaced at 0.12 normalized units per session and overlapped the traces. Both replaced by **one direct label per fit line** ("s1 −0.34") in the session colour, placed in a common right-hand column with a minimum-gap nudge so they cannot collide. R² dropped from the panel (goodness-of-fit, not the result) and still printed. x scale bar halved, **0.5 → 0.25 mW** (`XLength` 2 → 1 in data units — bar and label must move together or the bar lies). `DR_LABEL='id'` gives bare "s1…s4". `impulse-analysis/motion_analysis.m` — pooled linear fit removed from 2F: a line through a **signed** deviation tests the MEAN, and the mean is flat by construction (dev is taken about the amplitude mean), r = 0.020 p = 0.40. It invited the reader to look for a slope that cannot exist; the panel's content is the funnel narrowing, and the scale claim lives in 2J.
**Why:** user review, 2026-08-17.
**Next:** the supplementary median/IQR twin (`imp_response_median_IQR.pdf`) still uses the old legend+text style and now looks worse than 2B — apply the same treatment or retire it.

### 2026-08-17 — Ten Fig-2 panels promoted into paper/images/figure2
**Changed/Found:** Illustrator closed, all 21 targets verified writable, `fig2_promote` copied all ten staged panels in one move. Verified on disk by MediaBox + timestamp: `imp_response` 4.55×3.35, `imp_single_*` 4.34×3.60, `tf_shape_across_sessions` 5.64×3.88, `tf_tau_forest` 4.48×3.81, `tf_model_swap` 4.27×3.10, `tf_pole_spectrum` 4.73×3.77, `imp_motion_devscatter_all_sessions_peakdev` 4.09×3.81, `imp_state_var_motion` 5.54×3.81, `imp_state_var_reldelta` 5.54×3.77, `imp_state_reldelta_scatter` 5.57×3.88 cm. PAPER.md updated: TF-A row rewritten (slow+fast, 5×4 declared, censoring caveat), 2G's "not yet run" cleared, and `tf_pole_spectrum` registered as **TF-P — unregistered/overlapping, pick one of TF-A/TF-P**.
**Why:** the panels existed only in staging; the figure folder still held June/July versions.
**Next:** every promoted PDF is still tight-cropped, so none matches its declared size (the `'Padding','figure'` decision from 2026-08-12 is still unmade and is now repo-wide). **Place at 100% — do not scale to nominal size**, or the 6 pt type changes. 2I and 2E remain unbuilt.

### 2026-08-17 — All ten impulse-side Fig-2 panels rebuilt on 4 sessions; staged, not promoted
**Changed/Found:** `impulse-analysis/dose_response.m` now uses `PS.sess` (session-indexed colour) instead of `PS.sessGrad(numel(allExperiments))`, so **2B is on four sessions and matches 2C-i and the TF panels**. New `impulse-analysis/fig2_promote.m` copies the staged set into `paper/images/figure2` **all-or-nothing**, with a pre-flight writability check on every target. New knob `STVF_PAPERROOT` in `imp_state_trialvar_fig.m`. Built (staged in `paper/explore/figure2_build/images/figure2/`): `imp_response` (2B, 4 sessions — slopes −0.34/−0.57/−0.59/−0.96 %ΔF/V, R² 0.97/0.51/0.92/0.91), `imp_single_*` (2A), `tf_shape_across_sessions` (2C-i), `tf_tau_forest` (TF-A, slow+fast), `tf_model_swap` (TF-D, s1…s4), `tf_pole_spectrum`, `imp_motion_devscatter_all_sessions_peakdev` (2F, interleaved), `imp_state_var_motion` (2J), `imp_state_var_reldelta` (2K), `imp_state_reldelta_scatter` (2G, per-session slopes s1 −0.009 / s2 +0.218 / s3 +0.077 / s4 +0.098).
**Why:** user asked for all figures and for 2B on four sessions. Staged rather than written to the paper folder because **21 PDFs there are locked** by Illustrator (`Figure2_extra.ai`, unsaved) and Acrobat — exportgraphics dies mid-loop on a locked target, and the first attempt did exactly that, leaving 2K and 2G undrawn.
**Next:** run `fig2_promote` once Adobe is closed. Still unbuilt: **2I** (TF fit to the OL trial-average across all 15 controller sessions) and **2E** re-run on that same session set — both need the controller loader, not `allExperiments`. Also unresolved: the τ CI censoring (rejection 42/50/2/50% at the 0.5 s window; `scratchpad/tf_window_scope.m` is staged to settle it cheaply via `forceOrder`).
**Found — `_cperr` is still live and still silently wrong:** `motion_analysis.m` printed "dev_metric='cperr' but imp.cp_err is absent ... FALLING BACK to peakdev". The guard now corrects the tag and axis label, so the export is honestly named `_peakdev` — but the default is still `cperr`, i.e. every run asks for a metric that does not exist. Flip the default or delete the option.

### 2026-08-14 — Fit-window scoping test: two runs killed, then designed properly (`opts.forceOrder`)
**Changed/Found:** `utils/imp_tf_fit_session.m` gains `opts.forceOrder = [np nz nd]`, collapsing the (nd,np,nz) sweep to a single fit. Reason: the question "is the 0.5 s fit window too short to identify τ_slow?" was attacked twice by re-running `imp_tf_run` at `tFit_s` 1.0/1.5 s, and **both runs exceeded 30 min and had to be killed** (one at nBoot=300, one at nBoot=50). The cost driver is not the bootstrap — it is the **~100 `tfest` calls per session** in the order sweep, each of which slows with window length. The sweep is irrelevant to a question about the window, and holding the order fixed also removes a confound: with a free sweep, a τ change between windows could be the selected ORDER moving rather than the window. Scoping script staged at `scratchpad/tf_window_scope.m`: orders pinned at the ones AIC/R²h chose at 0.5 s ([3 1 0; 4 3 0; 4 3 0; 4 2 0]), windows 0.5/1.0/1.5 s, nBoot 40 — 12 cells at ~1 fit each instead of ~100.
**Why:** the rejection fractions at 0.5 s are 42/50/2/50%, so three of four sessions have a τ CI whose upper bound is the censoring bound rather than a measurement. Whether that is fixable by a longer window is the difference between reporting τ with a caveat and reporting it properly.
**Next:** run the staged script (not yet run — MATLAB was still busy with the second killed job). If rejection collapses at 1.0–1.5 s, move `RUN_TFIT` and re-fit; if it does not, τ_slow is simply weakly identified in this preparation and the panel must say so. Do NOT re-run the full sweep to answer this.

### 2026-08-13 — τ bootstrap was blowing up, and it was manufacturing the "one shared time constant" conclusion
**Changed/Found:** `utils/imp_tf_fit_session.m` — with 4 sessions the trial bootstrap returned non-physical time constants: **25% of resamples above 1000 s in AL_0041 e1, 17% in AL_0048, 3.4% in AL_0041 e2** (AL_0033 clean at 0%), max 9.5e15 s. Cause: a resample whose slowest pole lands marginally stable (real part → 0⁻) gives τ → ∞. Consequences, both bad: (a) `tauCI` upper bounds of 1e9–1e12 s, so TF-A's x-axis ran to 1e10 s and the panel was unreadable; (b) `tauSD` = **1.4e14 s**, and since the reported between/within RATIO divides by that SD, the summary printed **"ratio 0.00 → consistent with ONE shared time constant"** regardless of the data. That conclusion was an artefact of the blow-up. Fix: resamples with τ longer than the **fitted window** are dropped as non-identifiable (`opts.tauMax`, default = window length — a time constant longer than the observation window is not measurable from it), with the rejected fraction counted and printed. **After the fix the conclusion reverses: ratio 1.55 → "mild inter-session variability", within-session SD 0.097 s, between 0.151 s.**
**Why:** found while adding τ_fast to TF-A — reading `tauCI`'s definition to get row 2 exposed both this and the column-major slip logged below.
**Next:** **the rejection fractions are 42 / 50 / 2 / 50%** at the current `tFit_s = 0.5 s`, so for three sessions the CI's upper bound is essentially the censoring bound (471/502/478/498 ms against a 510 ms cut), not a measurement. That is because τ_slow is 0.30–0.54 s, i.e. the fit window is barely ONE time constant long. Added `RUN_TFIT` to `imp_tf_run.m` to test a longer window; **the 1.5 s test run has not completed** — it is far more expensive than the 0.5 s fit (more samples × 5 poles × 4 zeros × 4 delays × 300 bootstraps × 4 sessions) and exceeded 30 min. Re-test with `RUN_NBOOT = 60` for scoping before committing to a window change. Until then, quote τ point estimates with the rejection fraction, not the upper CI.

### 2026-08-13 — With 4 sessions the TF model swap now says models do NOT transfer
**Changed/Found:** `imp_tf_run.m` TF-D on the 4-session set: **self R² 0.991 vs cross-session 0.714 (drop 0.277)**, and the script's own verdict is "models do NOT transfer -- per-session identification is REQUIRED". PAPER.md's TF-D row still describes the panel as "Diagonal ≈ off-diagonal ⇒ designs transfer", which was written when the set was 3 sessions. The swap matrix also shows the asymmetry plainly: s2's model applied to s1 gives R² 0.09 while s1's model on s2 gives 0.45. Order is not shared either — np = [3 4 4 4], modes per session [2 2 2 3].
**Why:** adding AL_0048 as the 4th session changed the answer, not just the panel.
**Next:** the Methods claim has to change with it — "a low-order LTI model fits each session" is supported; "the same model transfers across sessions" is not. Decide whether TF-D stays as a positive panel about per-session identification being necessary, or is cut. PAPER.md's TF-D description needs rewriting either way.

### 2026-08-12 — BUG: TF-A's τ error bar was never a confidence interval (column-major index slip)
**Changed/Found:** `utils/imp_tf_robust_fig.m` — `imp_tf_fit_session` returns `tauCI` as **[maxPoles × 2]** (row = pole, col = bound). The forest read it with LINEAR indices, `lo = tauCI(1); hi = tauCI(2)`. MATLAB is column-major, so `tauCI(2)` is **row 2 of column 1 — the FAST pole's 2.5th percentile**, not the slow pole's 97.5th. Whenever the AIC-selected order had ≥2 poles the TF-A bar therefore ran from slow-τ-lower to fast-τ-lower: a backwards interval that is not a CI for anything. Correct only in the accidental `maxPoles == 1` case. Now indexed explicitly as `tauCI(1,1)`/`tauCI(1,2)`, with `tauCI(2,:)` feeding the new fast-pole row.
**Why:** found while adding the second pole to the forest — reading the field definition to get row 2 is what exposed what row 1 had been doing.
**Next:** the panel has not been re-run (server down), so the size and direction of the change to the published-intent bars is unknown until it is. Any τ CI quoted from a TF-A export before today is void.

### 2026-08-12 — Fig-2 cut acted on: colour unified, TF-A rebuilt, TF-B/TF-C/2D/2H/old-2G retired, 2G rebuilt
**Changed/Found:** Acting on the user's 2026-08-12 panel review. **(1) `utils/paperStyle.m`** — session colour is now indexed by session NUMBER: `PS.NSESS = 4`, `PS.sessFix`, `PS.sessColor(k)`, and `PS.sess` re-pointed at the 4-row ramp. The old `PS.sessGrad(n)` **resampled the ramp to however many sessions a panel drew**, so session 1 in a 3-session panel was a different shade from session 1 in a 4-session panel — the root cause of "2c-i does not follow colors set for different session in other plots", which appeared the moment AL_0048 became the 4th impulse session. **(2) `utils/imp_tf_paper_fig.m`** (2C-i) — uses `PS.sessColor`; the four per-session legend rows are gone, leaving one encoding legend (solid = measured, dashed = fit). **(3) `utils/imp_tf_robust_fig.m`** — TF-A now plots **τ_fast beside τ_slow**, same session colour, distinguished by marker (filled circle / open square) so pole type does not steal the hue that carries session identity; log x-axis because the two constants differ by ~an order of magnitude; widened 4→5 cm. Tick labels are now **s1…sN** everywhere including the TF-D swap matrix, un-rotated. TF-B/TF-C stay undrawn (already the default `panels = {'A','D'}`). **(4) `impulse-analysis/motion_analysis.m`** (2F) — the rows were already shuffled, but the draw loop **re-grouped them by session**, painting session 1 first and session 4 last, so the newest session covered the other three and the shuffle bought nothing; replaced with a single scatter over the shuffled order with per-point colour. **(5) `impulse-analysis/imp_state_trialvar_fig.m`** — in-panel stats text removed from 2J/2K (printed to console for the caption instead), and a **new 2G**: |dev| vs rel-δ percentile, all 4 sessions, one colour each, per-session fit lines, shuffled draw order.
**Why:** user review, verbatim asks. The 2G form, the τ_fast pairing, and the 15-session OL scope were confirmed by question before building.
**Next:** **nothing has been re-run — the data server is down**, so every PDF on disk is still the old version and none of this is verified. Outstanding builds: (a) the **TF fit to the OL trial-average across all 15 controller sessions** replacing 2D and expanding 2I — deliberately NOT written blind, it needs the controller loader and iteration against real data; (b) **2E re-run on those same 15 sessions**; (c) 4th session into 2A/2B. Order and rationale are in PAPER.md's "Fig 2 — agreed cut".

### 2026-08-12 — Every Fig-2 panel PDF is smaller than its declared size (exportgraphics crops to content)
**Changed/Found:** `PAPER.md` — panel size audit added to the Fig-2 reconciliation. Read the `/MediaBox` from all 16 registered Fig-2 PDFs: **not one matches its declared size**, and the shortfall is not constant. The seven nominally 6 × 4 cm panels come out **5.15 → 5.86 cm** wide; declared-4 cm heights range **3.25 → 4.09**. Cause verified experimentally, not assumed: a fresh `paperFig(6,4)` exported with `exportgraphics(...,'ContentType','vector')` gives **5.54 × 3.88**, and the same figure with `'Padding','figure'` gives **6.03 × 3.99**. The default crops to the content bounding box, so the amount removed depends on tick-label and axis-label length and therefore differs per panel.
**Why:** user asked whether the PDF sizes are correct. They are not, and the consequence is not cosmetic: panels that must line up in a column differ by up to 0.71 cm, and **rescaling a panel to its declared size rescales its text** — 2I taken from 5.15 to 6 cm turns 6 pt into 6.99 pt, against the project's 6 pt-bold-throughout standard. Placing at 100% keeps fonts right and is the only safe handling of the files as they stand.
**Next:** the fix is `'Padding','figure'` in `paperExport`'s vector branch (R2025b supports it) — **not applied**, because it re-exports every locked panel in the paper and shifts content within the page, so Illustrator placements need re-checking rather than a blind relink. User's call. Note this is a repo-wide issue, not a Fig-2 one: the same export path built Figs 3/4/5.

### 2026-08-12 — Figure 2 reconciliation: registry vs disk vs current science
**Changed/Found:** `PAPER.md` — new "🔍 Figure 2 reconciliation" section. Findings: **(a)** the Fig-2 story's three blocks have wildly uneven coverage — block 1 (response + LTI) has **11** panels including a 4-panel τ suite no layout can hold and a `2C-ii` that is referenced by 2C's note but **never defined**; block 3 (state dependence of the RESIDUAL) has **zero** panels, because every residual panel in the registry belongs to Fig 4, and `utils/f2_model.m` is still unapproved. **(b)** Block 2 carries **three generations of panels for one claim**: 2F (2026-06, signed-dev scatter + fit line), 2G/2H (2026-05/06, pre-stim variance, retracted 2026-07-01 as a power confound), and 2J/2K (today). Recommended cut is **2J + 2K only**. 2F is superseded specifically because its fit line tests a LOCATION effect while the signed deviation has no directional component — its r measures the thing that is not happening. **(c)** Asset audit of `paper/images/figure2/`: 27 PDFs, 16 registered, **11 orphans**, plus ~55 PNGs that belong in `paper/explore/` — **a folder that does not exist**. **(d)** Confirmed the `_cperr` mislabel by byte size: `prevar_vs_dev_allamps_motexcl_*.pdf` and its `_cperr` twin are both **exactly 23807 bytes**, i.e. identical content under two names, because `cp_err` is never produced and the scripts fall back to `Peak_imp_dev` while labelling the axis "Prediction error".
**Why:** user asked for a Figure 2 reconciliation. Three generations of panels for one claim is how a retracted result walks back into a figure — 2G/2H are marked CONTESTED but are still sitting in the panel table with live PDFs on disk.
**Next:** decisions the user has to make, none taken unilaterally: (1) move 2F/2G/2H to supplementary or `_archive`; (2) pick 1–2 of TF-A…TF-D; (3) define or delete `2C-ii`; (4) build a Fig-2 residual panel or drop the third clause of the Fig-2 story; (5) create `paper/explore/figure2/` and move the ~55 PNGs + 11 orphan PDFs. No files were moved or deleted.

### 2026-08-12 — Panels 2J/2K locked: SD-by-quartile for motion and rel δ
**Changed/Found:** `impulse-analysis/imp_state_trialvar_fig.m` — paper build. `STVF_PANELS` (default `'quartile'`) cuts the output to the two SD-per-quartile panels; `'all'` restores the funnels and the per-session panel. `STVF_PAPER=true` additionally writes vector PDFs to `paper/images/figure2/` as `imp_state_var_motion.pdf` / `imp_state_var_reldelta.pdf`, registered in PAPER.md as **2J** and **2K**. Panel changes for the paper build: **shared y-axis** across the two (computed from both markers' CIs, rounded to 0.05) — two panels of the same quantity on different y-scales invite a slope comparison the differing scales make invalid; x ticks relabelled **Q1–Q4** rather than 0/0.25/0.5/0.75/1, because on a percentile axis the bin medians land at quartile centres by construction and numeric ticks invite reading x as a magnitude it does not carry; corner annotation carries **Q4/Q1 + CI** and **stratified ρ + n**.
**Why:** user, 2026-08-12: "i like these plots lets get them paper ready, we will keep the two quartile plots".
**Next:** both are exported with `STV_PLOTCTRL=false`, i.e. no stim-free control on the axes. 2J's actual claim is that the response tightens MORE than the ongoing signal does (residual variance ratio 0.25 vs sham 0.41), which is unreadable from the red trace alone — re-export with the control before assembly. 2K is registered as a **negative**: it fails its own control (1.18 vs 1.32) and inverts on Zhiwen's data.

### 2026-08-12 — STV_STATESCALE: state axes normalized (percentile), z-scoring retired from the default
**Changed/Found:** `impulse-analysis/imp_state_trialvar.m` + `imp_state_trialvar_fig.m` — replaced the `STV_ZSTATE` boolean with `STV_STATESCALE` ∈ {`rank` (default), `norm`, `raw`, `z`}. `rank` = within-session percentile (tiedrank, so ties are not spread into a fake gradient); `norm` = per-session 0–1 across p1–p99, clipped (motion_analysis.m's `motN_s` idea, percentile- rather than min/max-anchored). Marker unit strings now follow the mode — an axis labelled "motion z-score" while carrying a normalized value is worse than one with no units. **Why rank and not min-max:** motion is rest punctuated by bouts, not a continuous variable, so every magnitude axis crushes it — measured under `norm`, the four quantile bin medians were 0.010 / 0.016 / 0.024 / 0.11, all inside the left 12% of the panel. Under `rank` they are 0.13 / 0.38 / 0.63 / 0.88. Panels also rebuilt: ±SD drawn as a filled ribbon rather than two lines, per-bin bootstrap CI as a shaded band rather than error bars, stats moved from a two-line title to a corner annotation, per-session panel changed to offset markers with no connecting line (sessions are not an ordered series). Pooled numbers under `rank`: Motion ρ −0.122, SD ratio 0.71 [0.60–0.84]; Rel δ ρ +0.087, 1.19 [1.04–1.37].
**Why:** user, 2026-08-12: "these images are horrible, plus i said not to use zscore, normalise the state axes instead". The raw-unit default from earlier today still *read* as z-scores because `motTrace` is z-scored by the loader, so the axis said "motion z-score" whatever the knob said.
**Next:** the rank axis buys legibility by spending magnitude — bins 1–3 occupy half the panel while differing by ~0.01 in motion z, which is close to nothing physiologically. The magnitude-preserving statement is the threshold split (z>1.5, ratio 0.43), and that is where the effect actually lives. Do not present the rank panel alone; the knob's comment in the file says so. **Every within-session number (stratified ρ, the per-session table) is identical across all four modes** — only pooled statistics and bin edges move — so nothing headline depends on this choice.

### 2026-08-12 — imp_state_trialvar_fig.m: paper-style panels for the state-variability analysis
**Changed/Found:** `impulse-analysis/imp_state_trialvar_fig.m` (NEW) — paper-format panels in the `motion_analysis.m` idiom (`paperFig`/`paperStyle`/`paperLegend`/`paperExport`, 6×4 cm, 6 pt bold, Box off, TickDir out). Consumes the `STV` struct; recomputes nothing except **per-bin bootstrap CIs** (2000 resamples WITHIN each bin — the exploratory figure carries a CI only on the top/bottom ratio, which cannot show whether an individual bin is resolved). Panels: `stv_<tag>_funnel` (signed deviation vs state + ±SD envelope) and `stv_<tag>_sd` (SD per bin + CI, the actual result) for each admissible marker, `stv_persession`, plus `stv_motsplit` and `stv_vardecomp` which are drawn ONLY when `STV_PLOTCTRL` is true. Also added `R(k).gbin` + `R(k).sess` to `imp_state_trialvar.m` so the bin assignment does not have to be re-derived. Export **PNG 300 dpi** (`STVF_EXT` knob) — none of these are registered in PAPER.md, so the PDF rule does not apply yet. With the CIs on, the motion trend is resolved only at the top bin (bins 1–3 overlap heavily, 1.11 / 0.97 / 1.11, then 0.74); rel δ's four bins all overlap.
**Why:** the exploratory 3×2 tiled figure is unreadable at panel size and carries console-style titles. Also: `STV_PLOTCTRL=false` suppresses the two control panels rather than drawing them without their comparison, because a lone "observed" bar with nothing to be measured against is worse than no panel.
**Next:** with per-bin CIs visible the 4-bin quantile split looks under-powered for motion — the raw-motion bins 1–3 span only ~0.11 in z, so three of four bins are measuring nearly the same state. Consider a 2-bin (threshold) presentation for the panel, or bins on rank rather than raw value; the current panel makes the effect look noisier than the threshold split (0.43) says it is.

### 2026-08-12 — imp_state_trialvar.m: STV_PLOTCTRL knob — draw the stim-free control or not
**Changed/Found:** `impulse-analysis/imp_state_trialvar.m` — added `STV_PLOTCTRL` (default **true**). When false, the grey control trace is dropped from the row-2 line plot, the control bar from the row-3 effect-size plot, and every `(ctrl ...)` term from the titles and the sgtitle. PLOTTING ONLY: the control is still computed and still printed in the console table and the motion threshold split, so no verdict changes and nothing leaves the record. Gotcha hit while testing: setting the knob and re-running gave the OLD figure — MATLAB had the script cached; `clear imp_state_trialvar; rehash` first (same trap as the GUI-function cache note in memory).
**Why:** user, 2026-08-12: "i dont need control on this analysis just yet, just give me the plots without it for now" — the control panels are dense and the immediate need is to read the stim trend alone.
**Next:** turn it back ON before any of this becomes a paper panel. The motion claim is specifically that the response tightens MORE than the ongoing signal does (residual ratio 0.25 vs sham 0.41) and that is unreadable from the stim trace alone; a reviewer seeing only the red trace can read the whole effect as a quieter baseline. The knob's comment says this in the file.

### 2026-08-12 — imp_state_trialvar.m: sham was INSIDE the state window — made disjoint; motion survives, rel δ still fails
**Changed/Found:** `impulse-analysis/imp_state_trialvar.m` — the user asked what the analysis windows were and the answer exposed an overlap: sham = −1.000→−0.829 s (7 samples) sat **inside** the state window −1.000→−0.029 s (34 samples), so 7 of the 34 samples used to compute PreVar / abs δ / rel δ **were** the sham. The control for the power markers was therefore partly circular — the sham peak's deviation is built from the very samples whose variance predicts it. Fixed by deriving the state window's lower edge from the sham: `stWin(1) = tAxis(iSham(end)) + 1/fs` → state is now −0.800→−0.029 s (27 samples), with an assert that the index sets do not intersect. Effect on the controls: PreVar ctrl ρ +0.569→**+0.505**, abs δ ctrl +0.459→+0.424, rel δ ctrl +0.241→**+0.185**. Effect on the motion decomposition (supersedes yesterday's `02dbfa1` numbers, which were computed with the overlapping window): var(obs) hi/lo 0.14→**0.17**, var(sham) 0.25→**0.41**, var(residual) 0.18→**0.25**; per session 0.33 / 0.28 / 0.16. Motion verdict unchanged (PASS, strat ρ −0.120, 4/4 sessions, residual still falls further than the ongoing signal). Rel δ still does NOT beat its control (SD ratio 1.18 stim vs 1.32 ctrl) and per-session agreement fell 4/4 → 3/4 (AL_0041 e1 now −0.009).
**Why:** a stim-free control that shares samples with the predictor is not a control. The overlap was invisible because both windows were written as literal times rather than derived from each other; the fix makes disjointness structural, not something to remember.
**Next:** the circularity was real but NOT the explanation for rel δ failing — the gap narrowed (1.21/1.51 → 1.18/1.32) and did not close. Rel δ should now be treated as a genuine negative in this dataset, consistent with the 2026-07-02 non-replication on Zhiwen's `AB_0004` (ρ = −0.25, opposite sign). Decide whether it stays in the figure as an honest negative.

### 2026-08-12 — imp_state_trialvar.m: matched sham-peak control — motion tightens the RESPONSE, not just the measurement
**Changed/Found:** `impulse-analysis/imp_state_trialvar.m` — the stim-free control was a ~34-sample mean of the whole pre window, SD-normalised, compared against a DV (`Peak_imp`, `peak_mode=3`) that is a **7-sample window mean**. Different statistic, different length, both forced to SD≈1 — so the magnitude comparison was meaningless. Replaced with a **matched sham peak**: same statistic, same 7 samples, window at −1.0 s (i.e. OUTSIDE the −0.5→0 s baseline `dfImp` has already had removed; a sham inside the baseline is partially constrained toward zero — measured var 8.0 inside vs 17.3 outside). Added a variance decomposition in RAW ΔF/F at the motion threshold split, by **regression not subtraction** (obs and sham correlate r≈+0.39, so `var(obs) − var(sham)` assumes an independence that does not hold): fit `obs ~ b·sham` on low-motion trials, apply that b to both groups, compare residual variance. Result — var(obs) hi/lo **0.14**, var(sham) **0.25**, var(**residual**) **0.18**; per session 0.25 / 0.36 / 0.14. Correlation-row control also moves: Motion ρ −0.128 (stim) vs −0.083 (control), stim now the larger; threshold split SD ratio 0.38 stim vs 0.58 control.
**Why:** the user's objection (2026-08-12) was right about framing — the claim is that motion makes the *stim response* more predictable, and a quieter baseline "is of no consequence". My original control was posed as a specificity test, which is the wrong test. But a control is still needed for a *different* reason: `dev` is measured on the raw trace, so var(dev) = response variability + ongoing activity sitting inside the window. If motion only shrank the second term, the measurement would be cleaner without the response being more reproducible — and the two look identical in var(dev) alone. The residual falls MORE than the ongoing signal does (0.18 < 0.25), so the response itself is genuinely more reproducible.
**Next:** the Rel-δ column still fails its own control (stim SD ratio 1.21 vs control 1.51) — decide whether it stays in the figure as an honest negative or comes out; and re-check whether the −1.0 s sham is far enough from the previous trial's rebound at the shortest ITI.

### 2026-08-12 — ctrl_deflate_check.m: the four non-circular numbers behind the deflated model; bigger dips ARE more removable
**Changed/Found:** new `controller-analysis/ctrl_deflate_check.m` `[DEFCHK]` — read-only, needs both ridge and deflate caches. Four panels, chosen because the deflated model's headline (Global flat, Local 100%) is the constraint and therefore says nothing: **(a)** held-out leak removal per session, **(b)** the R² price of blindness, ridge vs deflate, **(c)** the **bracket** — ridge Local as a lower bound against deflate Local as the ceiling, **(d)** removal vs the leak it started from. Also drew the deflate fit figures for all 13 sessions. **New result in (d): sessions with a bigger ridge leak lose more of it out of sample, Spearman r_s = 0.58, p = 0.040.** So the constraint is not removing a constant amount of noise — it bites hardest exactly where there is a real dip to remove, which is the pattern a genuine stim direction should produce and trial-noise fitting should not.
**Numbers:** held-out removal median 69% (range −16…93, 6/13 below 50%); R² cost median 0.017, worst 0.199; **Local bracket median 77% → 100%, median width 23 points.** The seven sessions above 69% removal are 0212, AL_0039_0420, 0320, 0224, 0304, 0226, AL_0039_0419 — and they are also the ones with the widest brackets, i.e. where the ridge leak was doing the most damage.
**Why:** panel (d) is what turns "the constraint works on average" into an argument. Without it, a median 69% removal is equally consistent with the constraint soaking up trial noise on every session. The correlation with the pre-existing leak is the discriminating evidence. Fixed a legend bug on the way (13 grey bracket bars were entering the legend as data1…data13).
**Next:** decide whether to report the bracket as the headline for the Local-share claim. If so, quote median 77–100% with the per-session held-out removal as the quality flag, and never the deflate figure alone.

### 2026-08-12 — Leak traced to ONE scalar (b'·dip); new 'deflate' model zeroes it — but the resulting "Local 100%" is DEFINITIONAL, not a result
**Changed/Found:** diagnosed what actually drives the Global dip across the 13 ridge sessions. It is not `‖b‖`: it is **`b'·dip`, the alignment of the weights with the per-pixel contra co-suppression pattern** — Spearman **−0.91** with leak% and **0.89** with the Global trough, against 0.51 for `‖b‖` and 0.34 for R². Shrinkage was only ever an indirect handle. Added `utils/ctrl_deflate.m` and a third `CTRL_PRED` mode **'deflate'** = ridge plus one linear equality constraint `d'b = 0`, closed-form (`b_c = b − A\d (d'A\d)^{-1} d'b`), where `d` is the contra stim direction built in the same z-scored coordinates and with the same per-trial baselining as a real trial — so `d'b` *is* Global's stim-window deflection. Built for all 13; caches are `ctrl_ols_ol_stimblind_deflate_*.mat`, so ridge and rank are untouched.
**Result, and the caveat that governs it:** sustained leak is **0% on 13/13 and Local reads 100%** — **by construction, because that is the constraint.** Those numbers must never be quoted as a finding. The only honest number is the held-out test (`d` estimated on ODD trials, leak scored on EVEN trials, both cached): **median 69% of the leak removed, range −16% to +93%, with 6/13 sessions below 50%** and AL_0048 actually worse out-of-sample. R² cost: median 0.017, but 0.199 on AL_0033_0305_e2 and 0.120 on AL_0039_0419_e1.
**Why:** `d` can only be measured with the laser on, so this predictor is no longer stim-blind in the strict sense the rank and ridge models were — it is fit on laser-off frames but forbidden a direction measured with the laser on. Forcing Global flat mechanically inflates Local, which is precisely the circularity a referee would attack, hence the odd/even guard.
**Next:** do NOT promote 'deflate' to primary. Best use is as a **bound**: ridge (leak present, Local under-reported) and deflate (leak removed, Local at its ceiling) bracket the true Local share. Report the pair. Also: the constraint targets the 0–3 s window, so the transient sub-window still leaks 0–16% — that residual is real and worth reporting rather than tightening away.

### 2026-08-12 — RAW state units in `imp_state_trialvar.m` — and the motion threshold split REVISES H1: the effect is not specific to the stimulus
**Changed/Found:** user: "lets not z score the states as they seem uninterpretable". `imp_state_trialvar.m` now uses RAW state values by default (`STV_ZSTATE=false`), so axes read in physical units — motion z-score, pre-trial variance (ΔF/F)², abs δ (ΔF/F)², rel δ as a fraction of 0.5–30 Hz. Row-2 x-positions moved to each bin's MEDIAN RAW VALUE, row-3 now plots the stim bar BESIDE its stim-free control bar. Raw pooling can in principle be manufactured by between-session offsets, so added a **session-stratified ρ** (weighted mean of within-session correlations, which never compares sessions) plus a sign-agreement count. All four markers: pooled and stratified agree to ≤0.01 and 4/4 sessions agree in sign, so between-session structure is not driving anything.
**Raw pooled results:** Motion ρ=−0.128 (strat −0.115), SD ratio 0.64 [0.53–0.76]; Pre-var +0.445 (ctrl **+0.526**); Abs δ +0.417 (ctrl **+0.442**); Rel δ +0.078, BF p now **0.057, n.s.** (ctrl +0.222). Per session, motion: −0.195\*, −0.210\*, **−0.057 n.s. (AL_0033)**, −0.147\* — the primary session no longer reaches significance on its own under raw scaling.
**⭐ NEW SECTION (2b), and it changes the H1 verdict.** RAW motion is threshold-shaped: median −0.196, q75 −0.158, only 56 of 1767 trials (3%) exceed z=1.5. Equal-count quartiles therefore put bins 1–3 inside a **0.10-wide sliver** of motion, so the quartile "trend" across them is noise and the quartile SD ratio is not a clean contrast. Added a two-group split at the LOCKED project constant `motThresh = 1.5`:
| | low motion (n=1711) | high motion (n=56) | ratio |
|---|---|---|---|
| SD, impulse response | 1.007 | 0.385 | **0.38** (BF p=2.7e−5) |
| SD, **stim-free control** | 1.007 | 0.402 | **0.40** (BF p=1.3e−5) |
**⚠ REVISION TO H1.** Motion genuinely lowers trial-to-trial variability — 2.6× compression, robust, replicating in the 3 sessions with enough high-motion trials (0.37 / 0.44 / 0.31; AL_0048 has only 2 such trials). **But the stim-free baseline compresses by the SAME factor.** So it is NOT a statement about how the stimulus is processed: motion reduces the variability of the ongoing signal and the impulse response inherits it. The earlier "PASS, beats its control" verdict came from the quartile contrast, whose bins are degenerate; the threshold split is the cleaner test and it shows no specificity. The script now prints this as an explicit warning.
**Convergence:** three independent routes now agree that the motion effect is not local/stimulus-specific — (1) signed Peak dev has zero directional component (r=0.003), (2) matched-DV `f2_state` gives ACTUAL −0.154 ≈ GLOBAL −0.157 > LOCAL −0.099, (3) this threshold split, 0.38 stim vs 0.40 control.
**Why:** interpretable axes were the ask; the degenerate binning it exposed was not visible while the states were z-scored, which is itself an argument for the change.
**Next:** the defensible H1 is "motion reduces variability of the widefield signal generally, including the impulse response", NOT "motion reduces variability of the impulse response specifically". Decide whether that is worth a panel. `STV_ZSTATE=true` restores the old scaling for comparison. Code Analyzer clean.

### 2026-08-12 — ⭐ NEW `imp_state_trialvar.m`: H1 (motion → lower variability) CONFIRMED in 4/4 sessions; H2/H3 fail their stim-free control
**Changed/Found:** NEW `impulse-analysis/imp_state_trialvar.m` — merges the state analyses of `motion_analysis.m` and `prestim_variance.m` into one script that tests the user's claim directly. DV = SIGNED `Peak_imp − mean(Peak_imp)` scaled by the within-amp SD (signed for display, so the funnel is visible); statistics are SCALE tests (Spearman of |dev| vs state, a Levene score about the within-amp centre) because 2026-08-12 established these are spread effects with no directional component. All four markers computed from the SAME strictly pre-onset window [−1, −0.03] s, so no marker overlaps the response the DV is measured in (RESEARCH A4: strictly-pre reproduces 'peri'). State markers z-scored within session × amplitude. 1767 trials, 4 sessions.
**⭐ THE STIM-FREE CONTROL is what makes the claim falsifiable.** Every test is repeated on a pre-onset, stim-free quantity built identically (deviation of the pre-window mean from its amp-mean). If a state predicts the spread of the stim-free control as strongly as the response, the effect is ongoing signal amplitude, not stimulus processing.
| marker | ρ(&#124;dev&#124;, state) | p | SD ratio top/bottom [95% CI] | stim-free ctrl ρ | verdict |
|---|---|---|---|---|---|
| **Motion** | **−0.135** | 1.3e−8 | **0.68 [0.56–0.79]** | −0.099 | **PASS** |
| Pre-trial variance | +0.453 | ~0 | 3.11 [2.79–3.55] | **+0.544** | POWER CONFOUND |
| Abs δ power | +0.416 | ~0 | 2.39 [1.99–2.93] | **+0.440** | POWER CONFOUND |
| Rel δ | +0.082 | 5e−4 | 1.25 [1.09–1.42] | **+0.213** | pass, but BELOW its control |
**H1 CONFIRMED.** Motion lowers trial-to-trial variability: SD ratio 0.68 with a bootstrap CI excluding 1, Brown-Forsythe p=1.9e−11, monotone trend −0.80, and it **replicates in all four sessions** (−0.173\*, −0.184\*, −0.119\*, −0.137\*, all p<0.05). It also exceeds its stim-free control (0.135 vs 0.099) — though only modestly, and the binned curves overlap in bins 1–3, so part of the motion effect is a general signal-amplitude effect and the claim should be stated as "larger for the response than for baseline", not "specific to the response".
**H2/H3 are NOT supported in the sense intended.** Pre-var and absolute δ ARE signal power and the DV is a spread, so "same or higher" is close to true by construction — and decisively, **their stim-free control EXCEEDS the stim effect** (0.544 > 0.453; 0.440 > 0.416). Same for relative δ, the power-independent marker: **+0.082 stim vs +0.213 control, a 2.6× larger effect on baseline than on the response.**
**The interesting reframing this exposes:** for every high-power state marker the impulse response is LESS state-modulated than ongoing activity is. Rather than "delta/variance leave variability the same or higher", the defensible statement is that **the impulse response is relatively STABILISED against high-power states compared with the baseline signal** — a stronger and more interesting claim than H2/H3 as posed, and one the stim-free control actually supports.
**Why:** user asked to finalise the two scripts into one that checks the claim and makes figures. Figures → `impulse-analysis/figs/state_trialvar/` (`stv_claim.png` 3×4 panels: funnel, spread-vs-state with control, effect size vs claim; `stv_persession.png` replication). Power confounds are greyed in the figure and never interpreted. Code Analyzer clean.
**Next:** decide whether the paper states H1 as "motion reduces response variability" (supported, 4/4) and replaces H2/H3 with the stabilisation framing. `imp_state_trialvar` supersedes the state analyses in `motion_analysis.m`/`prestim_variance.m`; those still own their other panels, so do not delete them.

### 2026-08-12 — ⭐ SIGNED Peak dev in motion_analysis + prestim_variance: both "state effects" are pure SPREAD, with ZERO directional component
**Changed/Found:** user asked for `Peak_imp − mean(Peak_imp)` **without the absolute value**, to expose directionality. Applied to `impulse-analysis/motion_analysis.m` and `impulse-analysis/prestim_variance.m` at every deviation site (5 in motion, 4 in prestim), plus: symmetric y-limits about 0 (the old `ylim` clamp `max(yl(1),−2)` assumed a floor at 0 and would have CLIPPED real negative trials), zero reference lines, `|Peak dev|` → `Peak dev` labels, and a metric-aware `dev_ylabel` in prestim_variance (its labels hard-coded "Prediction error" even after the cperr→peakdev fallback flips the metric). Sign convention documented in-file: **>0 = SHALLOWER dip than the amp average (less inhibition), <0 = DEEPER**.
**⚠ Computed from `Peak_imp` directly, NOT from `imp.Peak_imp_dev`** — that field is `abs(...)` in `load_experiments.m:355` but **signed** in `load_bilateral_impulse.m:224`, so it does not mean the same thing across sessions. Latent inconsistency, still unfixed at the loader level; also consumed by `spectral_heatmap.m` and the two detail callbacks.
**RESULT — MOTION: the directional effect is exactly zero.** Pooled all sessions/all amps **r = 0.003, p = 0.894 (n=1767)**, vs a strong effect under `|Peak dev|` (the 2026-06 headline: median |Peak dev| No-mot 1.23 vs Mot 0.59, p=9e−11). AL_0033 per-amp r spans −0.09…+0.04, all n.s. The scatters show why: high-motion trials cluster TIGHTLY on zero while low-motion trials spread ±10 in BOTH directions. **Motion compresses the deviation distribution symmetrically — `abs()` converted a variance effect into an apparent mean effect.**
**RESULT — PRE-STIM VARIANCE: same story, and worse.** AL_0033 (the only session judged valid) is n.s. at all 10 amplitudes (r −0.15…+0.10, p 0.17–0.97). AL_0041 e1 has significant effects with BOTH SIGNS across amplitudes (0.80 V r=−0.39 p=0.008; 1.60 V r=**+0.32** p=0.027; 2.40 V r=−0.35 p=0.007), which reads as noise rather than an effect. AL_0048 1.00 V r=−0.32 p=0.001. Quintile curves are mostly inverted-U with Q5 dropping — the signature of symmetric spreading, not a directional shift.
**Why this matters:** it corroborates today's independent f2 result (matched-DV `f2_state`: motion ACTUAL −0.154 ≈ GLOBAL −0.157 > LOCAL −0.099) from a completely different code path and DV. Both routes now say the motion effect is about SPREAD and lives in the network. **The "motion → more predictable" finding is a VARIANCE claim and must be stated as one** — `|Peak dev|` is a spread statistic presented as a mean.
**Verification:** both scripts run clean (Code Analyzer: only a pre-existing preallocation info at motion_analysis:132). `paperRoot` was redirected to the scratchpad for the test run, so **no paper asset was overwritten**. Two pre-existing standalone-run blockers surfaced in prestim_variance: `fs` (line ~354) and `nBands` (line ~376) come from the monolith's parameter cell and are not set by `load_experiments`.
**Next:** re-export 2F/2G/2H from the signed version once the metric question is settled. Fix the loader-level `Peak_imp_dev` abs-vs-signed inconsistency, or delete the field so nothing can consume it ambiguously. Decide whether the paper states motion as a variance effect (supported) or drops the directional framing entirely.

### 2026-08-12 — Full ungated ridge pipeline runs end to end: CL beats OL in 13/13, and the T1 control comes back NULL across the whole R² range
**Changed/Found:** ran the complete chain in ridge mode with the gate off — `ctrl_residual_build` (13/14 sessions) → `ctrl_fit_figs` → `imp_reject_across_sessions` → `f4_reject_panels`. Everything completes. **Energy ratio: OL 0.291 [IQR 0.172] → CL 0.203 [IQR 0.116], CL better in 13/13 sessions, signrank p=0.000244, mean gain +0.100 95% CI [+0.069, +0.136]; both differ from the no-work line at p=0.000244.** Legacy transmission agrees (ρ: OL 1.265±0.504 → CL 0.926±0.264, CL better in 12/13, p=0.000488). **The T1 control is the important one: corr(rejection gain, deployed R²) r_s = −0.03, p = 0.92.** Ungating widened the predictor-quality range enormously (R² now −2.06 to 0.977 instead of a narrow band above 0.85), and across that whole range rejection does not track predictor weakness at all.
**Why:** this is what makes the ungated analysis defensible rather than merely permitted. The floor existed because a weak Global was suspected of manufacturing apparent rejection; with every session included, the correlation that would show that effect is flat and non-significant over a 3-unit R² span. The gate was guarding against something the data now says is not happening. The leak caveat still holds and is unchanged in direction — a dipping Global understates the disturbance, so rejection is UNDER-reported.
**Next:** decide whether to retire the 0.85 floor outright given the null T1 control, or keep it as a reported sensitivity check. Either way the ungated n=13 result should be the primary and the gated n=3 the robustness line, not the reverse.

### 2026-08-12 — 🔴 `dev_metric='cperr'` SILENTLY FALLS BACK to peakdev: paper panel 2F is labelled "Prediction error" but plots |Peak dev|
**Changed/Found:** `motion_analysis.m` and `prestim_variance.m` both default to `dev_metric='cperr'`, guarded per-amp by `strcmp(dev_metric,'cperr') && isfield(imp_e,'cp_err') && ~isempty(...)`. **`imp.cp_err` is never produced anywhere in the repo** — a full-repo grep finds only consumption sites in these two scripts (plus an unrelated `utils/cp_error_heatmap.m`), and `allExperiments(k).imp` carries `Peak_imp / Peak_imp_dev / Peak_imp_mean` but no `cp_err`. It is created only by `contra_prediction.m`, which a plain `load_experiments` run never invokes. So the guard fails and every plot falls through to `Peak_imp_dev` — while `dev_tag` stays `'cperr'` and `dev_ylabel` stays `'Prediction error'` (that label is only corrected when `dev_metric=='peakdev'`).
**Evidence:** `prevar_vs_dev_allamps_motexcl_{,cperr_,peakdev_}AL_0033_2025-01-29_en1.pdf` are all exactly **23807 bytes** (different MD5 only because PDFs embed a creation timestamp) — three filenames, one plot. `imp_motion_devscatter_all_sessions_cperr.pdf` (panel **2F**, an ACTIVE Fig-2 paper panel per PAPER.md line 93) was regenerated today 15:10 under the mislabel. Supplementary `imp_motion_devscatter_cperr_*.png` likewise.
**Fixed:** added a loud fallback guard to both scripts — if no session carries `imp.cp_err`, warn and set `dev_metric`/`dev_tag` to `'peakdev'` so the axis label and filename match what is actually plotted. **Output content is unchanged**; only the tag/label are corrected. Panels exported before today under a `_cperr` name must be re-exported or re-labelled.
**Why (the question that surfaced it):** user asked whether the earlier trial-vs-trial-avg analyses used `L1DEVz`. **They do not.** `motion_analysis.m` / `prestim_variance.m` use `|Peak_imp − mean(Peak_imp)|` — a single PEAK scalar's deviation from the amp-mean peak, on the **ACTUAL** trace, in raw units. `L1DEVz` is `mean|r(t) − μ(t)|` over the whole dip window, on the **RESIDUAL**, z-scored within amplitude. Three differences at once (peak vs window-mean, Actual vs residual, raw vs z-within-amp), so numbers from the two streams are NOT comparable. Note `cp_agl.m` DOES use matched L1-dev across Actual/Global/Local, but normalises as a RATIO to the amp-mean (`x./mean(x)`) rather than z-within-amp — rank-identical within an amplitude, different once pooled ACROSS amplitudes.
**Next:** decide whether `cperr` should be implemented (run `contra_prediction.m` in the chain so `imp.cp_err` exists) or retired as a dead option. Re-export 2F. Corroborating note for the existing PAPER.md ⚠ on 2G/2H: with the matched L1 DV, pre-stim variance gives ACTUAL ρ=+0.721 and GLOBAL ρ=+0.700 vs LOCAL +0.287 — the power confound at its most extreme, exactly as those panels were flagged.

### 2026-08-12 — ROOT CAUSE of "only 9 ridge sessions": the 60000-frame cap DECIMATES the spontaneous list, killing every catch window
**Changed/Found:** rebuilt the ridge model for all sessions and the four missing ones failed identically — `ctrl_ridge_path: only 0 admissible catch windows`. Cause: `ctrl_ols_spont.m:319` caps the spontaneous set with `frames = frames(round(linspace(1,numel(frames),maxFrm)))`, an **evenly spaced decimation**. Above the 60000 cap that leaves no two consecutive samples anywhere, and catch windows require a contiguous peri-stim-shaped run entirely inside the laser-off set — so zero are admissible. It failed on exactly the four sessions sitting at the cap (0212, 0224, 0226, AL_0039_0419, all reporting 60000 frames) and succeeded on all nine below it (14598–59189). **Fix:** `ctrl_ridge_path` now takes `opts.spontMask`, and `ctrl_ols_ol_stimblind` passes the UN-decimated laser-off mask rebuilt from the stim onsets via a new `local_spont_mask` helper replicating `ctrl_ols_spont`'s own rule. The ridge fit still uses the decimated `frames` through the Gram — only window admissibility needed contiguity, and contiguity is a property of the recording, not of the subsample. All four now build; the ridge set is **13 of 14** sessions.
**Why:** this had been an open question for two sessions of work ("why only 9?") and was unanswerable because the build log lived in a cleared workspace. It is a silent-truncation bug of the worst kind: the cap is a performance measure with no obvious connection to window contiguity, and the failure mode is total rather than degraded.
**Next:** AL_0039_0420_e2 (m13) is the one remaining failure and is unrelated — `data.dFk` is 91800 frames against 91875 in the SVD, a 75-frame gap that the ≤10-frame rounding tolerance rejects. Diagnose that separately; it has never had a Stage-2 cache in any mode.

### 2026-08-12 — Gate turned OFF project-wide via new ctrl_gate_on(); UNGATED panels stamped
**Changed/Found:** new `utils/ctrl_gate_on.m` — one switch for the R² admission gate, read by `imp_reject_across_sessions`, `imp_state_across_sessions`, `ctrl_ols_xsess` and `ctrl_optimal_xsess` in place of four separate `USE_GATE = true` constants. **Default OFF** (user: "make it for all dont reject any"); `CTRL_GATE = true` in the base workspace re-arms it. `f4_reject_panels` used to *hard-error* on an ungated struct; that refusal also blocked the legitimate all-sessions view, so it now draws the panels and burns an `UNGATED (no R^2 floor, n=…)` annotation into each one, so the caveat travels with the figure instead of living in a script that would not run. Stage 2 itself never rejected — it warns and always writes the cache — so the gate was only ever in the cross-session batches.
**Why:** the user wants every session reported. Keeping the floor as a switch rather than deleting it preserves the reason it existed (a weak Global hands its own error to Local, inflating apparent rejection).
**Next:** none.

### 2026-08-12 — CTRL_* added to the load_sessions clearvars exception (fourth instance of this trap)
**Changed/Found:** `load_sessions.m:21` is now `clearvars -except r_lean RUN_* XD_* RB_* CTRL_*`. Setting `CTRL_PRED = 'ridge'` before `load_sessions` was silently wiped, so a build launched in ridge mode ran the whole **rank** path instead and overwrote a rank cache — no error, just the wrong model. Previous instances: `clear all` ate `r_lean` (2026-08-05), `clear RB` ate pre-set RB config (2026-08-12), `clearvars` ate the run_fig4 launcher config (2026-08-12).
**Why:** `run()` shares the caller's workspace, so any prefix not in the exception list is destroyed and the failure surfaces later as wrong results rather than an error. Note the edit also needs `clear load_sessions; rehash` to take effect in a live session — MATLAB caches the script.
**Next:** none.

### 2026-08-12 — ⚠⚠ BUG in the GLOBAL negative control: it used a MISMATCHED DV. Fixed — and it flips the MOTION verdict
**Changed/Found:** `utils/f2_state.m`. `local_globaldip` always returned the SIGNED dip mean, while the primary Local DV is `L1DEVz`, an UNSIGNED deviation from the amplitude mean. Those are different quantities, so "Local ρ vs Global ρ" was never a comparison — a signed level has no reason to track state the way an unsigned deviation does, which made **every** Global control look reassuringly small. Replaced with `local_compDV(tr, dcc, dv)`, computing the control with the SAME definition as `opt.dv` (L1DEVz / GAINz / DVz). Also added the **ACTUAL** (un-decomposed, trial vs trial-average) row using the same DV, because the ORDERING across Actual/Global/Local is the real result and Local alone cannot distinguish the two cases.
**AL_0033 e1, matched L1 DV, partial Spearman on `dev_pre` (n=748):**
| state | ACTUAL | LOCAL | GLOBAL | reading |
|---|---|---|---|---|
| Motion | −0.154 | −0.099 (p=0.0067) | **−0.157** | Actual≈Global > Local ⇒ **GLOBAL effect, decomposition DILUTES it** |
| Rel-δ | +0.095 | **+0.114 (p=0.0018)** | +0.056 (ns) | Local > Actual > Global ⇒ **ENRICHED by decomposition** |
| Pre-var | +0.721 | +0.287 | +0.700 | confound, plainly global |
| Abs-δ | +0.674 | +0.241 | +0.639 | confound, plainly global |
**⚠ RETRACTION.** Earlier today I reported "Motion ρ=−0.099 vs GLOBAL −0.037, ~3× the control, which is the point in its favour." **That margin was an artifact of the mismatched DV.** With the control matched, GLOBAL (−0.157) is LARGER than LOCAL (−0.099) and `f2_state`'s own leakage warning now fires. The motion effect is NOT local. This independently agrees with `f2_state_var`, which flagged motion as leakage by a completely different route (BF p Local 0.0027 vs Global 0.0066; trend −0.80 vs −1.00). **Any Global-control number quoted before this fix is void**, including the GLOBAL columns of the 4-session pooled table logged earlier today.
**Why it matters for Fig 2:** the Actual/Global/Local ordering is now the decisive display. Rel-δ is the ONLY marker where the effect is larger in the residual than in the un-decomposed trial — the only one where the decomposition buys anything. That is the first genuinely independent support rel-δ has had, and it lands at the same moment motion (the fallback ending proposed earlier today) is eliminated.
**Next:** re-run every state number produced before this fix. Confirm whether `f2_state_var`'s GLOBAL control has the same mismatch (it uses the signed dip on BOTH sides there, so it is internally matched — verify).

### 2026-08-12 — 0212 shown in the corrected frame: its laser spot is MEDIAL, unlike every other session's lateral spot
**Changed/Found:** added a display-only orientation repair to both `ctrl_roi_audit.m` contact sheets — a session failing the mirror-symmetry test is drawn transposed and titled `[shown transposed]`. Nothing on disk changes and no analysis is repaired; it exists so the anatomy is comparable by eye. Drawn this way, 0212's brain matches the others exactly (notch bottom-centre, vertical midline) — confirming the transpose beyond the two numeric tests. **It also exposes a further problem: 0212's laser blob sits MEDIALLY, straddling the midline near the top of the frame, whereas every other session's blob is clearly lateral within one hemisphere.** That is why its midline lands 2 px from the laser centroid — not because the midline was misplaced, but because the spot itself is at the midline. Also fixed: the geometry sheet was drawn before `[ROIAUD-LASER]` ran, so it titled 0212 `CHECK` while the report table said `REDRAW`; the sheet no longer prints a verdict and the table is the sole authority.
**Why:** the user asked to see the repaired plots. The repair is what made the medial spot legible — in the stored frame it read as "some bright patches", and in the corrected frame it is plainly a midline-straddling blob. Combined with its depth of −0.30 (5× weaker than its neighbours), 0212 looks like a session where the laser was aimed differently and worked poorly, not one with a fixable annotation.
**Next:** decide on excluding 0212 outright. A redraw cannot move a spot that is genuinely at the midline, so the hemisphere split will stay ill-defined there whatever is clicked.

### 2026-08-12 — 🔴 AL_0033_0212_e2's IMAGING FRAME IS TRANSPOSED AT LOAD — root cause found, plus the weakest stim in the set
**Changed/Found:** the user flagged that 0212 still looked transposed and that its stim site did not look like the others. Both confirmed, independently. **(a) Transposed frame.** Its brain mask matches other AL_0033 days at Jaccard **0.92 / 0.88 / 0.85 when transposed** vs 0.64 / 0.60 / 0.58 as-is; the 0224 control shows the reverse (0.85 as-is, 0.59 transposed). Left/right mirror symmetry says the same: **0212 scores 0.59 native vs 0.89 transposed**, while 0224/0226/0304/0419 score 0.82–0.90 native vs 0.55–0.65 transposed. **Root cause: 0212 and 0224 are the only two Stage-1 caches with no `Torient` field** — they predate `cp_orient`, so their orientation was never resolved and the legacy path assumed native. 0224 happens to be native; 0212 is not. With a square 560×560 frame a swapped nY/nX raises no error anywhere. **(b) Weak stim.** Its laser effect is **−0.30 %ΔF/F** deep against −0.99…−4.03 for every other session (next-weakest is AL_0051 at −0.71) — roughly 5× weaker than its AL_0033 neighbours. Relative blob focality is normal (area at ≥50% of peak = 10.8% of brain, others 10.0–15.8%), so the effect is not unusually diffuse, just much smaller.
**Why:** this is a third and deeper defect than the ROI drawing — the ROI is downstream of the frame. It also explains the horizontal midline: 0212's midline was clicked correctly on a transposed image, which is why the outline follows the brain while the midline comes out along columns in native coordinates. Added both tests to `ctrl_roi_audit.m` (`local_sym` mirror-overlap → REDRAW; absolute laser depth > −0.5 → CHECK) so neither can pass silently again.
**Next:** rebuild 0212's Stage 1 through `cp_orient` so `Torient` resolves (expect tr=1), redraw the ROI in the corrected view, re-run Stage 2. Then decide whether 0212 belongs in the analysis at all given the 5× weaker stim — it is currently one of only three sessions clearing the 0.85 R² floor, on an R² that is partly self-prediction.

### 2026-08-12 — Fig-5 spectral/tracking work TABLED; parked as `bilateral/sine_ff_error_decomp.m` so it is not re-derived
**Changed/Found:** User tabled the spectral panel line of work. Consolidated the four scratch probes into one committed, runnable script — `bilateral/sine_ff_error_decomp.m` — with three sections: `[DECOMP]` error-power split (offset / at-f0 / broadband, exact by Parseval because the window holds exactly 4 drive cycles), `[TRK]` per-trial `|1−T(jω₀)|`, and `[TRADE]` the trade-off plane with an automatic dominance check. Verified it reproduces every published number (OL/OL+p/CL/CL+p totals 24.67 / 24.07 / 18.61 / 16.80; |1−T| medians 0.974 / 0.408 / 0.673 / 0.563; dominance = OL beaten by both CL modes, CL beaten by CL+p, only OL+p and CL+p non-dominated). The header carries the **negative result and its reason** so the shape-similarity family is not re-run: eight framings tested, all name OL+preview, because "shape" divides out the offset term where CL+preview's advantage lives. `TASKS.md` 🟢 Deferred gained one line pointing at the script with the pick-up instructions.
**Why:** "revisit later" is only cheap if the code survives — the probes were in a session-scoped scratchpad that will be discarded. One committed script costs ~130 lines and removes a full re-derivation from the future session.
**Next:** none — parked by user decision. If resumed, promote `[TRADE]` (5.4 × 5.0) into 5H's freed 4.2 × 5.4 slot and upgrade the Results/Discussion "only mode to engage both mechanisms" sentence to the dominance statement.

### 2026-08-12 — "CL+preview makes trials more reference-SHAPED" tested eight ways and is false; the trade-off plane is the honest version, and it is strong
**Changed/Found:** Scratch probes. **Every shape-normalised metric names OL+preview, in 3/3 sessions** — per-trial Pearson r with the reference (pooled medians OL 0.253 / OL+p **0.438** / CL 0.366 / CL+p 0.351; OL+p-vs-CL+p p=0.001, CL-vs-CL+p **p=0.56 n.s.**), phase-tolerant max-xcorr (OL+p 3/3), |1−T| at 1 Hz (OL+p 3/3), error power at f0 (OL+p 3/3), ensemble coherence (OL+p or no separation), leave-one-out template correlation (CL+p is **worst** pooled, 0.360, p=0.005 vs OL+p), and across-trial dispersion of the z-scored trace (CL+p worst on s2/s3). The one absolute-units measure that favours CL+p, "time on target" (fraction of window within ±2 %ΔF/F), ranks it first pooled (0.393 vs CL 0.375, OL+p 0.343, OL 0.329) but **CL-vs-CL+p p=0.58, OL+p-vs-CL+p p=0.15** — only OL-vs-CL+p reaches p=0.044. **Root cause, and it is structural: "shape" means dividing out offset and amplitude, and the offset term is exactly where CL+preview's advantage lives** (lowest DC error in 3/3 sessions, and 40–65% of total error power sits there). A shape metric cannot show a feedback benefit — by construction. **The defensible panel is the trade-off plane**: x = |1−T| at 1 Hz (waveform fidelity, preview moves a mode left), y = across-trial variance (reproducibility, feedback moves it down). Mode means OL (0.990, 22.34) · OL+p (0.406, 23.51) · CL (0.678, 17.52) · CL+p (0.556, 16.25). **CL+preview DOMINATES plain CL on both axes, and OL is dominated by both closed-loop modes; only OL+p and CL+p are non-dominated — in all 3 sessions.** Draft panel built (5.4 × 5.0).
**Why:** user asked for a marker showing CL+preview squeezes trials into the reference's shape. It does not, and I stopped generating new metrics rather than continue until one favoured the preferred mode — eight framings with a consistent answer is a result, not a failure to find the right statistic.
**Next:** the trade-off plane gives the intended claim rigorously — *CL+preview is the only mode that is simultaneously close in waveform and reproducible, and it beats plain closed-loop on both counts* — without asserting it is best on any single axis (it is not: OL+preview is the more faithful tracker). If adopted, it replaces or joins 5H (4.2 × 5.4 slot is free) and the Results/Discussion text needs the "only mode to engage both mechanisms" sentence upgraded to the dominance statement.

### 2026-08-12 — CORRECTION to the 0212 diagnosis: the frame is rotated, and the real defect is that the midline bisects the LASER SPOT
**Changed/Found:** the earlier entry today claimed AL_0033_0212_e2's midline "splits A/P, not L/R". **That rationale was wrong and is retracted.** `cp_roi_masks` bisects by the signed cross product of the midline direction, so the split is orientation-agnostic — a rotated midline is legitimate when the session's imaging frame is itself rotated, and 0212's *laser-effect map* is visibly rotated ~90° relative to every other session (the cerebellar notch sits on the right edge instead of bottom-centre). So the 90° angle is a property of the data, not an error. Two things are nevertheless established: (1) swapping `bx`↔`by` takes 0212's midline 90°→0° and its outline aspect 1.07→0.93 (others 0.89–0.95), i.e. its stored geometry is a coordinate-order **transpose** relative to the other files — it and 0224 are the only two legacy ROIs with no `frame`/`view_used` field, predating the 2026-08-10 `native_rc` convention; (2) the decisive defect is that **its midline passes 2 px from the laser centroid** — drawn straight through the stimulated territory, so the laser centroid lands outside `ipsi_mask`. Every healthy session puts the laser 74–134 px from its midline.
**Why:** the angle test I first used is frame-dependent — it would fail any legitimately rotated session while passing a midline drawn through the spot. Distance from the laser centroid to the midline is frame-independent and separates 0212 (2 px) from all 13 others (≥74 px) with no overlap. The user spotted the transpose from the contact sheet.
**Next:** redraw 0212 (`XD_SESS=2; XD_REDRAW='all'`) with the midline clear of the spot; its Stage-2 R² = 0.855 is invalid until then.

### 2026-08-12 — Laser-vs-dfk check added to ctrl_roi_audit.m; AL_0051 cleared, 0226 and 0051 flagged CHECK
**Changed/Found:** new `[ROIAUD-LASER]` section — loads the cached `cp_stim_site_ctrl_*.mat` effect maps for all 14 sessions, compares the dfk pixel against the **data-derived** laser location, and draws a second contact sheet of the maps. ⚠ The cache's `rowcol` field is a **copy of (px_prim,py_prim)**, so comparing against it gives distance 0.0 for every session and verifies nothing — the real quantities are `centroid` and the map trough. Results: dfk-to-centroid 5–13 px on ten sessions, 17 px on AL_0033_0226_e2, 20 px on AL_0051_0729_e2, 26 px on 0212. **AL_0051 is fine** — its dfk pixel sits on the blob, its trough is 33 px away, and its laser is 129 px from the midline in the correct hemisphere; the 20 px offset reflects a weak diffuse blob (depth −0.71, second-weakest in the set), not a misassignment. The raw argmin trough is unreliable (AL_0033_0305_e2's lands 290 px away on a frame edge), so the centroid is the metric used.
**Why:** the user asked to verify the laser spot against the dfk spot for AL_0051. Every geometry check takes px_prim/py_prim as ground truth for which side is ipsi, so that assumption needed independent evidence — and the obvious field to check against turned out to be circular.
**Next:** decide whether 0226 (17 px) and 0051 (20 px) warrant a re-pick of the dfk pixel or are within tolerance for their blob widths.

### 2026-08-12 — Coherence at 1 Hz REJECTED as a Fig-5 panel; |1−T| at the drive frequency proposed instead
**Changed/Found:** Scratch probes (not committed). **(a) Coherence fails three ways.** Ensemble magnitude-squared coherence across trials at 1 Hz, γ² = |⟨YR*⟩|²/(⟨|Y|²⟩⟨|R|²⟩), with 2000-resample bootstrap CIs: s1 OL 0.54 / OL+p 0.71 / CL 0.72 / CL+p 0.64; s2 0.74 / 0.75 / 0.71 / 0.62; s3 0.69 / 0.75 / 0.57 / 0.59. CIs are ±0.10 and overlap almost completely, so nothing is separated; where an ordering exists it runs **against** the claim (**CL+p is lowest in 2/3 sessions**); and γ² is an SNR-at-f0 measure, so **CL is penalised for deliberately running a smaller-amplitude response** (|T| 0.70 vs OL's 1.08 on s2) rather than for tracking worse. Per-trial coherence at a single bin is not even defined (one trial, one frequency ⇒ γ² ≡ 1); the closest well-defined per-trial quantity, the R² of a 1 Hz sinusoid fit, gives the same non-result (medians 0.30/0.28/0.29/0.22 on s2, CL+p lowest). **(b) `|1 − T(jω₀)|` works.** T = Y(f₀)/R(f₀) per trial, so |1−T| = 0 is perfect tracking in both amplitude and phase; it is referenced to the REFERENCE rather than to the response's own power, which is exactly the confound that sinks coherence. Pooled over 3 sessions: **OL 0.974 (n=136) → OL+p 0.408 (110), p=6.8e-24; CL 0.673 (100) → CL+p 0.563 (132), p=0.0038; OL vs CL p=2.5e-11; OL+p vs CL+p p=4.1e-5.** Every pairwise contrast significant, ordering identical in 3/3 sessions. Two draft panels built: per-trial distribution (4.2 × 5.4) and the complex plane with distance-from-(1,0) as the error (4.6 × 4.6).
**Why:** user asked for a coherence-with-the-drive panel to support "CL+preview is best". Coherence cannot carry it, and would visibly contradict it; |1−T| is the standard control-theory statement of the same question and separates cleanly.
**Next:** ⚠ **|1−T| makes the same correction the phase data already made, but with far more power: OL+preview is the most accurate tracker at the drive frequency, not CL+preview** (0.408 vs 0.563, p=4.1e-5, 3/3 sessions). Combined with CL+p's phase overshoot (−9°/+4.6°/−24°, i.e. LEAD, where OL+p sits near 0°), the supportable claim is a **trade-off plus a design result: the fixed 5-sample lookahead is matched to the open-loop plant, so it overcorrects once feedback has already removed part of the lag.** A horizon scheduled on loop configuration is the fix. Do not write "CL+preview is best on all axes" — no mode is: at-f0 → OL+p (3/3), broadband/variance → CL (2/3), offset → CL+p (3/3), total error power → CL+p (2/3), per-trial RMSE median → CL (2/3).
**Also answered:** the dots in the draft error-decomposition panel are the **per-session totals** (summed bar height for s1/s2/s3), not per-segment values — ambiguous as drawn; either drop them or split them per segment if that panel is adopted.

### 2026-08-12 — Added ctrl_roi_audit.m: all 15 sessions checked, exactly ONE bad ROI (0212 / m2)
**Changed/Found:** new `controller-analysis/ctrl_roi_audit.m` `[ROIAUD]` — read-only geometry audit over the `cp_roi2_ctrl_*` + `ctrl_ols_spont_*` caches (no session load, no SVD). Five independent checks per session: midline angle off the native ROW axis, site inside `ipsi_mask`, fraction of the contra grid inside `contra_mask`, ipsi/contra pixel balance, and byte-identical (donor-seeded) geometry. Emits a per-session verdict plus an all-sessions contact sheet so the anatomy is inspected, not just thresholded. **Result: 14 sessions have geometry, 13 are clean (midline 0–3° off axis, site in ipsi, 100% of the grid in contra, balance 0.87–1.23); only AL_0033_0212_e2 (= `m2`) fails, at 90°.** The 15th slot, `m1` (AL_0033 2025-01-20), has no ROI and no Stage-1 at all — never drawn.
**Why:** the user asked to fix orientation case by case across all 15 sessions; auditing first turned a 15-session redraw into a 1-session redraw, and the contact sheet is the artifact that makes "case by case" a look rather than a click-through. AL_0051's mirrored geometry (star left, grid right) is visibly correct on the sheet, closing that concern for good.
**Next:** redraw `m2` only (`XD.sess = 2; XD.redraw = 'all'` in `ctrl_roi_draw_all.m`), re-run its Stage 1, then re-check whether the 0.85 floor still has 3 passers.

### 2026-08-12 — 🔴 AL_0033_0212_e2's MIDLINE IS DRAWN HORIZONTALLY — its "contra" mask is the anterior half of the whole brain
**Changed/Found:** added a brain underlay to the kernel panel of `ctrl_fit_figs.m` (Stage-1 `ipsi_mask`/`contra_mask` as a truecolor background, ROI outline + midline over it, hemispheres labelled), and it exposed the bug on sight. Dumped `mx`/`my` from all 14 `cp_roi2_ctrl_*.mat`: **AL_0033_0212_e2 is (row 282, col 158) → (row 282, col 352)** — constant ROW, so the midline runs along columns and bisects the brain **anterior/posterior**. Every other session has a near-constant column (270–316) with the row sweeping 100→370, i.e. a proper left/right split. Consequence: 0212's "contra" grid is the anterior half of the *whole* brain and includes ipsi-hemisphere pixels, so its held-out R² = 0.855 is partly a same-hemisphere self-prediction, not a contralateral one — and 0212 is one of only **three** sessions clearing the 0.85 floor.
**Why:** the midline is what defines contra vs ipsi (`cp_roi_masks` bisects by the signed cross product of the midline direction), so a 90°-rotated midline silently redefines the entire model input while every downstream number keeps looking healthy. Nothing in the numeric pipeline could catch this — it took drawing the brain.
**Next:** re-draw the ROI for AL_0033_0212_e2 (`ctrl_roi_draw_all.m`, redefine that session) and re-run Stages 1–2 for it; until then treat its R², leak and Local% as invalid, and re-check whether the 0.85 floor still has 3 passers or 2.

### 2026-08-12 — Brain underlay added to the kernel panel of ctrl_fit_figs.m
**Changed/Found:** `controller-analysis/ctrl_fit_figs.m` — the kernel scatter now sits on the session's own brain: Stage-1 `ipsi_mask`/`contra_mask` painted as a **truecolor RGB** image (indexed would hijack the axes' diverging weight colormap), the drawn outline and midline over it (`bx`/`mx` = native ROW, `by`/`my` = native COLUMN, so they are plotted swapped for this (x=col, y=row) axes), hemisphere labels drawn *after* the scatter so weights do not bury them, and the axes cropped to the brain bounding box + 15 px. New local functions `local_hemi`/`local_paint`/`local_centroid`/`local_bbox`; a missing Stage-1 or ROI cache costs the underlay, not the figure.
**Why:** the user could not tell whether the orientation was right from a bare scatter — and they were right not to trust it: the underlay found a rotated midline in 0212 within one run.
**Next:** none.

### 2026-08-12 — ctrl_fit_figs.m rewritten to show the OL trial average; two plotting bugs found and fixed
**Changed/Found:** `controller-analysis/ctrl_fit_figs.m` — the left panel was a spontaneous held-out prediction snippet; replaced with the **OL trial-averaged decomposition** (Actual black / Global grey / Local orange, ±SEM ribbons, laser-ON patch), drawn from `Aa/Gg/Lo/A_tr/G_tr/rel/capt_sus/leak_sus` — all fields Stage 2 has always cached, so all 13 rank + 9 ridge sessions redraw with no rebuild. Two bugs surfaced on first run: (1) the laser patch was drawn at y = ±1e4 and then `ylim('auto')` scaled to the patch, collapsing every trace to a flat line — the patch is a data object like any other, so limits are now computed from the traces and the patch drawn to them; (2) the site marker was plotted at `(x=px_prim, y=py_prim)` i.e. **(x=row, y=col), swapped** — `px_prim` is the ROW and `py_prim` the COLUMN (`ctrl_ols_ol_stimblind.m:369` passes them in that order to `cp_orient_fwd(Tor,row,col)`), so it is now `plot(py_prim, px_prim)`.
**Why:** the user asked for the trial-averaged OL panels, not spontaneous ones — and the point of the panel is to see whether Global actually stays flat through the laser window, which a spontaneous snippet cannot show. The site marker had to be right because this panel is the check on whether the contra grid and the site sit in opposite hemispheres.
**Next:** none — figures are in `paper/images/predictor_saga/fits/`.

### 2026-08-12 — AL_0051 hemisphere assignment is CORRECT; the grid/site geometry is just mirrored
**Changed/Found:** dumped `px_prim`/`py_prim` and `grR`/`grC` ranges for all 9 ridge caches. `px_prim` = row (295–352 for every session, mid-frame), `py_prim` = column. For AL_0033/AL_0039/AL_0048 the contra grid spans columns 34–300 while `py_prim` = 332–412 — opposite column halves. For **AL_0051** the grid spans columns **310–518** and `py_prim` = **172** — also opposite halves, just mirrored left/right relative to the other mice. So the site is in one hemisphere and the "contra" grid in the other, exactly as intended.
**Why:** the user flagged AL_0051 as possibly having the wrong hemisphere/pixel/contra definition. It does not — the concern was reasonable (it is the one mirrored session and the most mask-imbalanced at 1.23) but the assignment is self-consistent. Its R² = −2.16 therefore needs a different explanation than geometry.
**Next:** chase AL_0051's R² = −2.16 elsewhere — train/test non-stationarity (train and test blocks are ~20 min apart) is the leading candidate, per the 2026-08-12 pipeline audit.

### 2026-08-12 — Ridge caches on disk are STALE: they were built with the broken lambda rule
**Changed/Found:** ran `ctrl_fit_figs.m` in ridge mode. 5 of 9 sessions still report λ = 4.64–10 with ‖b‖ = 0.1–0.3 and R²_te = 0.43–0.69, and their panels show Global pinned flat at zero with "Local 95%". That is the signature of the *pre-correction* λ rule (which was minimised by the null operator), not of the corrected rule now in `utils/ctrl_ridge_path.m` — the caches predate the fix.
**Why:** a null operator trivially scores ~100% Local and ~0% leak, so those numbers are meaningless as written; reading them as a model result would be reading the artifact.
**Next:** re-run the ridge build (`run_fig4.m` with `RUN_PRED='ridge'`, `RUN_REBUILD=true`, `RUN_FRESH=true`) so the corrected λ rule is applied, then re-check `at_edge` and `catch_falls` per session.

### 2026-08-12 — Fig 5 written into the manuscript: results subsection, full caption, and a new Discussion section on feedforward
**Changed/Found:** `Closedloop_edit/` on branch **`draft`** (never `main`, per the review gate). `results.tex` — the feedforward subsection was two sentences and claimed "two example sessions"; replaced with four paragraphs, results-first per the Li & Lu style: (1) the 2×2 design and n; (2) feedback reduces error, with the s2 numbers, the 3/3 direction and the pooled cross-session test; (3) preview cancels lag but does not reduce error, with the lookahead arithmetic (5 samples = 143 ms = 51° at 1 Hz, measured 51.4°); (4) the error decomposition explaining why the two do not overlap. Every number carries n and a test. Full 11-panel caption written to match the finalised artwork. `discussion.tex` — new subsection **"Feedback and preview address separable components of tracking error"** placed after disturbance rejection and before the prior-work section: why the two mechanisms are structurally independent (plant delay vs disturbance), why RMSE is the wrong summary for a moving reference (the offset term dominates), why the fixed lookahead overcorrects under closed loop, and the frequency/n limitations. Figure copied to `images/Figure5.pdf` (688 KB, 17 × 19 cm). **Build: 32 pages, exit 0**, only the two known undefined refs (`fig:lowfreq_examples`, `sec:disturbance`, both pointing into the dead `methods.tex`).
**Why:** the figure was finalised, so the prose had to catch up — and the old text's "two example sessions" framing predated the single-session + 3-session-consistency composition entirely.
**Next:** the `\includegraphics` width had to be set to **0.82\linewidth** — at full width the 17 × 19 cm figure plus caption overflowed the page by 102.8 pt. If the figure is re-cut shorter, restore full width. User reviews `git diff main..draft` and merges; Overleaf pull is the last step. Nothing pushed from `draft`.

### 2026-08-12 — Fig 5 registry re-lettered to follow the finalised artwork; letters and filenames now deliberately disagree
**Changed/Found:** User rebuilt `Figure5.pdf` (17 × 19 cm, 11 panels). `PAPER.md` letter column updated to match what the figure actually shows: **E = across-trial variance, F = RMSE-over-time** (the files are `sine_5E_rmse_time` and `sine_5F_variance` — the opposite), and **J = across-session phase, K = across-session variance** (files `sine_combined_variance` / `sine_combined_phase` — also the opposite). **5H is restored** alongside 5G. Filenames were deliberately NOT renamed: they are referenced by two scripts and by every RESEARCH entry back to July, and the mismatch is now documented in one place instead of propagating. Registry note rewritten to say **read the path, not the letter**. Also recorded that this figure is **Figure 6** in the manuscript, not 5.
**Why:** the artwork is the deliverable and its letters are what the manuscript cites, so the registry has to follow it; but renaming eleven files to chase a letter swap would break more than it fixes.
**Next:** G/H (single-session) vs I/J/K (across-session) is now the figure's organising split — example vs consistency. If a referee reads H and J as redundant, H is the one to drop, not J.

### 2026-08-12 — RETRACTED a circular exclusion reason; the one-session restriction re-justified on model quality alone
**Changed/Found:** `impulse-analysis/imp_fig2_state_0033.m` header rewritten. The earlier version justified dropping AL_0041 e2 partly because "rel-δ runs the WRONG WAY (ρ=−0.03)". **That is circular** — excluding a session because its outcome disagrees with the hypothesis guarantees the survivors agree, and is indefensible to a referee. Removed, with a standing note not to reinstate it. Also replaced the AL_0041 e1 justification: it had quoted the λ_R=0.3 UNGATED diagnostic (−0.076 dF/F, 11–22% of dip), which is not the model the pipeline runs.
**Correct per-session figures, from the pipeline itself under `F2_SELECT='r2max'`:** AL_0033 e1 — 748 trials, 6/9 amps, capture 91%, leak 9%, catch −2% → KEPT. AL_0041 e1 — 208 trials, 1/4 amps, capture 32%, leak 68%, **catch −21% ⇒ FAILS the 15% threshold, pipeline marks its capture/leak VOID** → hard exclusion. AL_0041 e2 — 260 trials, 3/6 amps, capture 37%, leak 63%, catch −14% (only just inside threshold) → weak model. AL_0048 e1 [R] — 300 trials, 3/3 amps, capture 29%, leak 71%, catch −8%, plus the 2.6 mm off-spot readout caveat.
**Why:** Every surviving exclusion is now decided by a criterion computed BEFORE any state test and blind to state markers, which is the only version that can be defended. The real discriminator turns out to be capture: 91% vs 29–37%. Leak 63–71% on the other three means the contra prediction absorbs most of the stim effect there, so their residual is largely not the local effect.
**Caveat that must be reported with any n=1 result:** AL_0033 is also by far the highest-powered session (748 trials vs 208/260/300; 9 amps vs 4/6/3). "The only session where the effect is measurable" cannot be distinguished from "the only session where the effect is real" with n=1, and it is one session from one mouse.
**Next:** If the Fig-2 ending rests on this, the honest framing is a single-session result with the power asymmetry stated, not a replicated finding. Check whether any additional AL_0033 impulse session exists that could serve as a within-mouse replicate.

### 2026-08-12 — Spectral probe: error power splits cleanly into offset / at-f0 / broadband, and it rewrites the CL+preview claim
**Changed/Found:** Scratch probe (not committed) — exact-4-cycle FFT of each trial's response and error over the 4 s window (dur×f0 = 4 cycles, so 1 Hz lands exactly on bin 5 at 0.25 Hz resolution; no windowing needed). By Parseval the per-trial error power **is** RMSE², so it can be split with nothing lost: **E@DC (offset) + E@f0 (gain/phase mismatch) + E_rest (broadband)**. Mean over the 3 sessions, (%ΔF/F)²: OL 10.97/3.14/10.55 (tot 24.67) · OL+p 13.13/**0.89**/10.04 (24.07) · CL 10.22/1.78/**6.62** (18.61) · CL+p **7.86**/1.22/7.72 (**16.80**). Best-per-session counts: **at f0 → OL+p 3/3**; **broadband → CL 2/3, CL+p 1/3**; **offset → CL+p 3/3**; **total → CL+p 2/3**. Also tested and REJECTED as markers: trial-to-trial phase-locking value (0.80–0.92, CL+p is *lowest* in 2 sessions) and f0's share of response AC power (12–30%, no consistent ordering). The plain response PSD is likewise a weak panel — the spectra are broadband-dominated and the four modes overlap almost everywhere except a small 1 Hz bump.
**Why:** user asked whether a power-spectrum analysis could show "how tight the controllers are at forcing the reference frequency", to support a claim that CL+preview wins on variance + RMSE + phase together. The decomposition answers it far better than a PSD, and it makes the mechanism explicit: **preview cuts the at-frequency error ~3× in 6/6 mode pairings; feedback cuts the broadband error in 3/3.** It also explains why plain RMSE hid both — **40–65% of total error power is the DC offset term**, which neither mechanism targets.
**Next:** ⚠ **the claim as stated does not hold and must be rewritten.** Phase: **OL+preview**, not CL+preview, has the smallest |lag| in all 3 sessions (−3.0/+6.9/−11.2° vs −4.8/−8.5/−25.0°) — CL+preview overshoots into lead. RMSE: CL is better on the *typical* trial (s2 median 2.80 vs 3.30) while CL+preview is better on the *mean* (Etot 14.12 vs 15.41) purely because its tail is smaller. The defensible version is: **CL+preview is the only mode that engages both mechanisms, giving the lowest total error power (2/3 sessions), the lowest offset error (3/3) and the tightest trial-to-trial spread — while OL+preview alone is the most phase-accurate and CL alone is the quietest.** Decide whether the decomposition becomes a panel in 5H's freed 4.2 × 5.4 slot.

### 2026-08-12 — Fig 5 layout block re-derived from the assembled PDF; panel letters E and F are swapped in the artwork
**Changed/Found:** `PAPER.md` Fig 5 layout block rewritten against the actual `Figure5.pdf` instead of the never-built plan it carried. Real arrangement is **three rows**: row1 A(6×4)|E(5.2×4)|F(5.2×4); row2 col1(12.5) = B/C/D stacked at 12.5×3.5, col2(4.2) = G/H at 4.2×5.4; row3 = I|J|K at 5.5×4.5. Fit: row1 17.0 ✓, row2 12.5+4.2+0.3 = 17.0 ✓ with both columns 11.1 cm tall (flush by construction), **row3 3×5.5+2×0.3 = 17.1, 0.1 over** → drop the row-3 gap to 0.25 (no re-export) or re-cut to 5.47 wide. Total height 20.2 cm on a 23 cm page. The old block's "col1 11.4 / col2 5.3" was never exported — the fit comment at the head of `sine_ff_plots_combined.m` (12.5 + 4.2) has been correct the whole time. **Found separately: in the artwork the panel lettered E is the VARIANCE trace and F is the RMSE trace, the opposite of the registry and the filenames** (`sine_5E_rmse_time`, `sine_5F_variance`).
**Why:** the layout block had drifted two compositions behind and would have misdirected the next artwork pass; and a swapped letter pair is the kind of error that survives into the manuscript because both panels are plausible under either letter.
**Next:** relabel the artwork rather than the files (filenames and every log entry use the registry convention) — and do it before any letter is cited in `results.tex`. Deleting 5H frees a 4.2 × 5.4 slot in col2 for the error-decomposition panel.

### 2026-08-12 — Fig 5 panels re-exported (s2) once Acrobat released the file locks
**Changed/Found:** Re-ran `bilateral/sine_ff_plots_combined.m` (default s2) and `bilateral/sine_ff_across_sessions.m` with `EXPORT=true`. All 8 single-session panels + 3 combined panels written to `paper/images/figure5/` as PDF + 300-dpi PNG mirrors, carrying the 2026-08-12 axis treatment (5E unit-labelled bar, 5F no y arm, 5G log axis with 0 trials clipped, 5I/5J/5K corner axes). `sine_rmse_pooled_all_with_clean.png` also written — the first time it has been produced, since the class-demotion bug had been killing that section.
**Why:** the earlier attempt failed on per-file "Permission denied" — Acrobat and Illustrator held 16 of 17 `figure5/sine_*.pdf`. Acrobat has since closed; Illustrator released the placed links.
**Next:** re-link/refresh the placements in the Illustrator master, delete panel H, and fix the E/F letter swap.

### 2026-08-12 — Fig 5 primary session REVERTED s3 → s2; 5G restored as a panel, 5H stays dropped
**Changed/Found:** User decision, applied. `bilateral/sine_ff_plots_combined.m` default `SESSION_TAG` s3 → **s2**. `PAPER.md`: all 5B–5G paths swapped to the `AL_0048_2026-07-14_1` set; the "primary session" line, the layout block and the session-switch note rewritten; **5G un-struck and given a full panel row** (4.2 × 5.4, with its s2 stats); 5H left dropped (duplicates 5K in every session). Claim (a) rewritten — the single session now carries the feedback result (s2 OL-vs-CL **p=7.7e-4**) *as well as* the combined panels, with an explicit warning not to write "significant in every session" since s3 is n.s. New **claim (d)** added to stop a framing that keeps resurfacing: CL+preview is better than OL (p=0.020) but **not** better than CL (p=0.093 in CL's favour). Claim (b) now carries both sessions' phase numbers. The retired "only mode whose error falls" claim re-checked against s2 and confirmed **still false there** (OL also falls, −0.08) — it came from quoting CL+p's 4.01→3.67 without checking OL. `EXPORT_ECDF=false` added: the ECDF alternative is built and previewable but is not a paper panel. s2 caveats (Ki=0, Kref 0.05, amp 2, ~2× optical power vs s1) copied into the primary-session line so absolute RMSE is never compared across sessions.
**Why:** the assembled artwork had been s2 since before the 2026-07-29 switch and was never re-linked. Resolving it in s2's favour rather than re-linking is the stronger option: s2 is the only one of the three sessions where the within-session OL-vs-CL is significant, so it is the only one where a single-session RMSE panel says anything at all.
**Next:** PDFs still cannot be written — Illustrator (`Figure4.ai`, unsaved) and Acrobat hold per-file locks on 16 of the 17 `figure5/sine_*.pdf`. Re-export both scripts once released. Then in the artwork: delete panel H, and re-solve the Fig 5 fit — the `PAPER.md` layout block describes a two-column plan with no slot for 5G, while the real figure is three rows with I/J/K across the full measure. `PAPER.md` now flags that block as stale rather than guessing which arrangement wins.

### 2026-08-12 — Assembled `Figure5.pdf` is a session behind the registry: it is built from s2 panels and still carries the two DROPPED panels
**Changed/Found:** No file edited — audit of the user's `paper/images/figure5/Figure5.pdf` against `PAPER.md` + the change log. The assembled artwork is **entirely s2 (`AL_0048 2026-07-14/1`)**, not the registered s3: panel C reads n = 51/46/46/57 (s2; s3 is 62/45/34/57) and panel H reads OL 60° / OL+p 5° / CL 36° / CL+p −9° with a 51° preview line (s2; s3 is 60/−9/37/−24 with a 62° line). It also still places **panels G and H**, which `PAPER.md` struck through on 2026-07-29 as replaced by the combined 5I and 5K — so the figure states the RMSE claim twice (G and I) and the phase claim twice (H and K). Illustrator has `Figure4.ai` open with unsaved changes and Acrobat holds `sine_combined_phase.pdf`, which is why every `paperExport` into `figure5/` currently fails with "Permission denied" (folder is writable; the locks are per-file).
**Why:** the 2026-07-29 switch to s3 was a deliberate user decision (s3 = the dark-screen session that carries the pupil/presentation story), with the feedback claim moved onto combined 5I/5J because s3's own OL-vs-CL is n.s. (p=0.36). The artwork was never re-linked to the s3 exports, so the figure and the registry have been describing two different sessions since then.
**Next:** user decision — either (a) re-link the placed panels to the `*_AL_0048_2026-07-21_1.pdf` set and delete G/H, or (b) formally revert the primary session to s2 in `PAPER.md` (defensible: s2's within-session OL-vs-CL is p=7.7e-4, which is what would let 5G stay). Panels for BOTH sessions have been regenerated with the new axis treatment, so either choice is one re-link away. PDFs cannot be overwritten until Acrobat/Illustrator release them.

### 2026-08-12 — 5G rebuilt: log y axis instead of a p95 clip, so no trial is thrown off the page
**Changed/Found:** `bilateral/sine_ff_plots_combined.m` §5G — replaced the clipped linear half-violin with a **log-y** panel: full-data half-violin (density estimated on log10 RMSE, where the distribution is ~symmetric), every trial drawn as a jittered dot (`rng(0)` so the jitter is stable across re-exports), median + IQR bar per mode, and significance brackets drawn only where ranksum p<0.05. `RMSE_CLIP_P` is now only a reporting number. The panel keeps real y ticks (1/2/5/10/20) rather than a corner scale bar, because a fixed-length bar is meaningless on a log axis. Added a companion **`sine_5G_rmse_ecdf_<sfx>.pdf`** (cumulative fraction vs log RMSE, 4 modes) as a candidate replacement. s2 now spans the full 1.02–16.23 %ΔF/F with 0 trials off-axis (was 10/200 above p95); s3 spans 1.48–13.89 with 0 off-axis (was 10/198).
**Why:** user: "the trial RMSE distribution plot is getting tails cut off". Per-trial RMSE is strongly right-skewed (skew 2.3–3.0, max/median ≈ 4×), so clipping at p95 hid exactly the motion/drowsy trials a referee will ask about. Log scaling compresses the tail instead of deleting it and still resolves the bulk.
**Next:** user picks violin vs ECDF. Note the panel only *shows* an effect on s2 (OL vs CL p=7.7e-4 ***, OL vs CL+p p=0.020 *); on s3 nothing is significant and the panel renders bracket-free.

### 2026-08-12 — s2 per-trial RMSE: CL+preview beats OL but NOT CL — preview's win is spread and phase, not error
**Changed/Found:** Probe (scratch, not committed) of per-trial RMSE by mode on s2 and s3. **s2 medians** OL 3.82 / OL+p 3.55 / CL 2.80 / CL+p 3.30; ranksum OL-vs-CL **p=7.7e-4**, OL-vs-CL+p **p=0.020**, CL-vs-CL+p p=0.093 (CL better), OL-vs-OL+p p=0.32. **s3 medians** 4.57 / 4.84 / 4.16 / 4.67, every pairwise p ≥ 0.30. CL+p does have one distinct property on s2: the **tightest distribution** (SD 1.57 vs OL 1.85 / OL+p 2.75 / CL 2.19, and the lowest p90 at 4.81).
**Why:** the user asked for a 5G presentation that "shows CL+preview has a clear benefit". The data does not support CL+preview < CL on error in either session — that framing would be a claim the numbers refuse. What IS supportable, and is what the rebuilt panel shows: CL+preview sits significantly below OL on error *and* cancels the plant lag (−9° vs OL 60°), i.e. it is the only mode that wins on both axes at once.
**Next:** if the caption wants a single "CL+preview is best" sentence, it must be phrased as "error significantly below open-loop with the lag removed", not as an improvement over CL. Keep consistent with `PAPER.md` claim (c) ("preview does NOT reduce tracking error").

### 2026-08-12 — 5E/5F y-axis: unit on the RMSE bar, no y bar at all on variance
**Changed/Found:** `bilateral/sine_ff_plots_combined.m` — 5E's y scale bar shortened 2 → 1 and relabelled `2` → `1% ΔF/F`, with the rotated text cut from `RMSE (% dF/F)` to just `RMSE`. 5F's y arm **removed entirely** (`YLength 0`); only the rotated `Variance across trials` name and the `1 s` bar remain.
**Why:** user: the corner axes "only give a number and not the name of the activity". On RMSE the fix is to put the unit on the bar and the quantity name in the rotated text (they were redundant before). On variance the units are (%ΔF/F)² and the panel is read for shape and between-mode ordering, not off an absolute value, so a bare `10` was worse than nothing.
**Next:** if the caption ends up needing an absolute variance number, quote it in text rather than restoring the bar.

<!-- older entries → RESEARCH_archive_2026H2.md (then RESEARCH_archive_2026H1.md) -->
