# Brain Paper — Research Coordination

## Project Context
Mouse widefield closed-loop PI controller vs open-loop analysis.
Controller uses kernel average of a cortical region as output; PI controller drives input.
Two mice: AL_0033 (9 sessions), AL_0039 (4 sessions) = 13 controller sessions, Jan–Apr 2025. (Verified 2026-06-16 from loaded cache m1–m13.)

**Core question:** Does the closed-loop controller reduce neural variability and error compared to open-loop?
**Secondary questions:** Do motion, frequency-band power, and wide-brain activity explain trial-to-trial variability? Do these differ between OL and CL?

**For tasks and priorities → `TASKS.md`**
**For completed findings and paper claims → `FINDINGS.md`**
**For meeting decisions → `MEETINGS.md`**

---

## Change Log

### 2026-06-27 — Resolved controller-tuning data model from the share; rewired ct_load_session
**Changed/Found:** `controller-tuning/ct_process_set.m` + `controller-tuning/load_grid.m` — empirically resolved the loader's data/column assumptions (the 2026-06-26 first-run failures) and rewired the reader. Facts established from the share: (1) **`states.csv`** = regulated kernel-mean signal in **% ΔF/F**, logged at **35 Hz** (laser-on run = 105 samples = `dur`=3 s), 300000-sample pre-allocated buffer, median ≈0, with a few hundred logging-glitch samples (|y| up to 1e5) → NaN'd via `cfg.YCLIP=80`; needs a robust `textscan` reader (single space-delimited row in sci-notation; `readmatrix` returns 1×1 NaN). (2) input_params layout is **per rig-version, not per mouse** — detect by #columns: **8-col** → onset=col2 (index into the SAME 35 Hz states axis; matches `input_amps.csv` rising edge off-by-1), Kp=col5, Ki=col6, |ref|=col7; **7-col** → gains=col2:3, NO onset column. (3) **ref = −|col7| = −5** for all 8-col sessions (settled t=2–3 s median −4.3…−5.2), overriding the stale registry `ref=−2`. Rewrote `ct_load_session` to read states.csv as y with ncol-based layout + `find_session_file`/`read_csv_row` helpers; simplified `load_grid.m` registry (`mk(mn,td,en,ip,status,note)`, ref derived from data); replaced fixed `KP/KI/ONSET_COL` knobs with `cfg.YCLIP`. Both lint-clean; cost made glitch-robust (RMSE×√n with omitnan).
**Why:** The 2026-06-26 handoff blocked on 5 rig questions; the user's answers on AL_0034 onset-source and AL_0033 onset-space were contradicted by the actual data (AL_0034 7-col has no onset column; AL_0033 col2 onset matches the 300000-sample axis, not imaging frames). Verified everything against the files before rewiring to avoid burning a run.
**Next:** Source onsets for the two **7-col AL_0034 grid sessions** (2024-10-17 e30, 2024-10-18 e1) — no onset col and no input_amps; needs a Timeline (lightCommand/galvo @2 kHz → 35 Hz) adapter. They are `status='hold'` for now.

### 2026-06-27 — Ran gain_grid.m: AL_0033 03-03 cost surface (min at grid boundary)
**Changed/Found:** `controller-tuning/gain_grid.m` — updated `SEL=2` (was 4; G now holds only the 4 AL_0033 grid sessions after the rewire, prototype 03-03 is index 2, the richest 13-node 4 Kp×5 Ki grid). Exported `gain_cost_surface_AL_0033_0303e2.png` + `gain_node_traces_*.png` to `paper/images/tuning/`. J(Kp,Ki) minimum = 17.4 at (Kp=0.2, Ki=0.2) — the **corner / boundary** of the swept grid, with a flat low basin (J≈17–19) across high-gain nodes; (0,0) no-control node is worst (J≈24). Node traces confirm higher gains pull dF/F to the −5 setpoint faster.
**Why:** First valid run of the cost-surface figure on the rewired loader.
**Next:** Optimum sits at the swept boundary → consider whether a wider Kp/Ki grid session exists, or report the boundary optimum honestly. See FINDINGS.md entry.

### 2026-06-27 — Ran auto_tune.m: AL_0034 10-25 e1 convergence; fixed legend call
**Changed/Found:** `controller-tuning/auto_tune.m` — fixed `legend(... 'ItemTokenSize', ...)` which errors on this MATLAB version (set `lg.ItemTokenSize` on the handle instead). Exported `autotune_convergence_AL_0034_1025e1.png`. Best trial cost 15.6 at trial 55, gains (Kp=0.042, Ki=0.137) — a **different region than the grid corner-min (0.2,0.2)**; gains keep wandering (zero-order exploration), per-trial cost cloud shows no clear downward trend.
**Why:** Complete the autotune-convergence paper figure on the rewired loader.
**Next:** Decide how to frame autotune vs grid: it does not visibly converge to the grid optimum — verify the running-best/cost metric and whether a smoother convergence emerges on the other two autotune sessions (A(2)=AL_0034 10-25 e2, A(3)=AL_0033 12-19). See FINDINGS.md entry.

### 2026-06-26 — Scoped the utils/ "DO NOT MODIFY" rule to vendored subfolders only
**Changed/Found:** `CLAUDE.md` (project layout) — the blanket `utils/ ← DO NOT MODIFY` label was wrong; the rule only ever applied to the four vendored deps (`npy-matlab/`, `Rigbox/`, `widefield/`, `Pipelines/`). Reworded so those four are marked DO NOT MODIFY and the rest of `utils/` (e.g. `cp_*.m`, `paperFig.m`) is explicitly project-owned/editable.
**Why:** Audit of uncommitted work flagged `utils/cp_*.m` edits as rule violations; user clarified the prohibition is scoped to the four subfolders only (which are also already `.gitignore`d).
**Next:** none.

### 2026-06-26 — Restored deleted contra_residual.m; excluded concurrent cp_residual_core edit from commit
**Changed/Found:** `impulse-analysis/contra_residual.m` had been **deleted from disk** mid-session (session-start `git status` already showed it staged `D`; a concurrent session's git clean/checkout removed the untracked workbench). Recreated the full sectioned workbench from this session's authored content ([CP-SETUP]/[CP-RESi]/[CP-LOCAL]/[CP-MOTION]/[CP-MOTION-AMP]/[CP-VAR]/[CP-DELTA]). Also found `utils/cp_residual_core.m` carries an **uncommitted concurrent edit dated today** ("per user 2026-06-26"): the MOTION / VARIANCE / DELTA state-covariate windows were moved from pure pre-stim to **peri-stim spanning the onset** (var/delta `[-1,+0.5] s`, motion total |z| `[-2,+0.5] s`), with an in-code note acknowledging "mild circularity vs the DV; partialcorr(dev_stim|dev_pre) is the safeguard." The committed state-dependence numbers (motion partial +0.003, var 0.085, delta 0.073) were computed on the OLD pre-stim windows.
**Why:** Committed only my own deliverable (contra_residual.m) + handoff docs (CLAUDE.md/FINDINGS/RESEARCH/TASKS). Deliberately left `cp_residual_core.m` **uncommitted** — it is a concurrent session's in-flight work; committing it would capture their WIP and silently change the methodology underneath the documented findings. The helpers (cp_agl/cp_cont_state/cp_motion_amp/cp_res_inspector) were already committed by the concurrent session, so the workbench's deps are tracked.
**Next:** User to reconcile the peri-stim vs pre-stim state-window choice in `cp_residual_core.m` and re-run [CP-VAR]/[CP-DELTA]/[CP-MOTION] — the partial-correlation numbers in FINDINGS/CLAUDE.md must be refreshed if the peri-stim windows are adopted (and the circularity vs dev_pre re-checked).

### 2026-06-26 — Contra→ipsi RESIDUAL stream handoff docs (carry-over for a fresh session)
**Changed/Found:** Consolidated the contra→ipsi residual / local-effect state-dependence stream into committed repo files (a new account has no local ~/.claude memory). Wrote: (1) `impulse-analysis/CLAUDE.md` — new "Residual / state-dependence workbench — PRIMARY ACTIVE STREAM" section (file/section map, run order, design decisions, headline finding); (2) FINDINGS.md — "PARTIAL: Local photoinhibition response is state-robust to motion" entry + a ⚠ TENSION note on the existing panel-2F "Motion predicts impulse response deviation" finding (reproduced fig 6 shows the OPPOSITE sign: motion→LESS deviation); (3) TASKS.md — STATE banner + 4 open tasks under the Contra→ipsi section (replicate AL_0041, decide L1-vs-dev_stim, reconcile fig-6 sign, commit). Quick map for a fresh session:
  - WORKBENCH: `impulse-analysis/contra_residual.m` — run `load_experiments.m`, then `%%` sections: `[CP-SETUP]` (first; builds R,S,AGL) → `[CP-RESi]` inspector / `[CP-LOCAL]` overview / `[CP-MOTION]` / `[CP-MOTION-AMP]` (amp_sig switch) / `[CP-VAR]` / `[CP-DELTA]`.
  - CORE: `utils/cp_residual_core.m`; helpers `utils/{cp_agl,cp_cont_state,cp_motion_amp,cp_res_inspector}.m`; batch `cp_state_batch.m`; abs dose-response `cp_doseresponse.m`.
  - DECISIONS: decontam=false (state), use_motion=false (so motion is testable), dip window 0–200 ms; dev_stim DV (L1 proposed, not adopted).
  - FINDING (AL_0033, single session): local stim effect motion-NULL; motion→predictability is GLOBAL; weak local pre-stim VARIANCE (partial 0.085 p=0.022) + borderline delta.
  - FULL HISTORY: RESEARCH.md grep 2026-06-17 → 2026-06-22.
**Why:** User asked to "properly log so a new account can carry this over." Durable carry-over must live in committed repo files (FINDINGS/TASKS/sub-area CLAUDE.md surfaced via normal area-detection), not local memory.
**Next:** Commit all of it to `alpha` (handoff docs + the contra_residual restructure + new utils helpers). Then the PRIMARY analysis task is AL_0041 replication.

### 2026-06-26 — Grid analysis handoff docs (carry-over for a fresh session)
**Changed/Found:** Wrote `scratch_grid/README.md` — a self-contained handoff for `grid_analysis.py` (what it is, run command + venv deps, the RAW-session data layout, onset/position derivation, calibration constants + provenance, bregma/pixel geometry, the power finding, Block↔Timeline alignment, outputs, knobs, findings, open items, file roles). Added two FINDINGS.md entries (dual-opsin opposite-polarity confirmed; 0.25 power did not lase), a TASKS.md 🟡 subsection (AL_0048 galvo site-grid: port done + open causal-map/low-power items), and a discoverability pointer in `bilateral-analysis/CLAUDE.md`.
**Why:** User asked to "properly log so a new account can carry this over" — durable carry-over must live in committed repo files (not local ~/.claude memory, which doesn't travel to a new account).
**Next:** none — handoff complete. A fresh session can start from `scratch_grid/README.md`.

### 2026-06-26 — cl_ol_single_session.m: per-condition (CL) onset lead to realign CL stim to t=0
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — CL trials were windowed with the OL-measured `ONSET_LEAD_S = 2.918 s`, landing the CL marker ~1 s late so the CL stim appeared at t = −1. Added knob `ONSET_LEAD_S_CL` (default `ONSET_LEAD_S + 1.0`); the REDETECT block now builds a per-trial `lead` vector (OL trials → `ONSET_LEAD_S`, CL trials [`OLCL_COL == CL_CODE`] → `ONSET_LEAD_S_CL`) and computes `d.stimStarts = timeBlue(fr) − lead`. Console line reports both leads + CL trial count. `[]` reverts to a single shared lead.
**Why:** CL input is continuously active (no clean rising edge), so the OL-derived trial-end lead does not transfer to CL; reusing it offsets CL onsets by ~1 s. A condition-specific lead realigns CL to t = 0 to match OL.
**Next:** Run AL_0048 2026-06-24 e5 and confirm the +1.0 s value lands CL onset exactly at t = 0 (check the DEBUG example-trial input overlays); tune `ONSET_LEAD_S_CL` if the residual offset isn't exactly 1 s.

### 2026-06-26 — cl_ol_single_session.m: add input-trace and tracking-error figures
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — added Fig 4 (input-command traces, OL | CL two-panel, all trials overlaid + across-trial mean) and Fig 5 (mean instantaneous absolute tracking error |dF/F−ref| across trials, OL vs CL overlaid on one axes with ±SEM ribbon). New helpers: `sliceInputTrials` (interp1 each trial's combined input onto a common onset-relative axis at the input's own sample rate, NaN-padded), `drawInputPanel` (no ref line), `plotMeanErr`. Exports added: `clol_input_*` (guarded on input availability) and `clol_trackerr_*` PNGs. check_matlab_code clean for new code (only pre-existing `clear all` L25 and unreachable-`continue` L325 warnings remain).
**Why:** User requested an OL-vs-CL input-command trace overlay and an averaged instantaneous tracking-error comparison for the bilateral single-session controller analysis.
**Next:** Run on AL_0048 2026-06-24 e5 to confirm input windowing aligns (inpTime shares onset clock) and that CL tracking error sits below OL post-onset; verify input panels are non-empty for this session.
**Changed/Found:** Appended `contra_residual.m`'s seven driver sections (`[CP-SETUP]/[CP-RESi]/[CP-LOCAL]/[CP-MOTION]/[CP-MOTION-AMP]/[CP-VAR]/[CP-DELTA]`) verbatim to the end of `impulse-analysis/contra_prediction.m` (after the explorer launchers, under a "RESIDUAL + STATE-DEPENDENCE WORKBENCH" banner), then `git rm impulse-analysis/contra_residual.m`. No variable-name collisions with the host script (grepped R/S/AGL/dA/dG/dL/states/mot — none assigned in contra_prediction). `[CP-SETUP]` header adapted (mfilename fallback → contra_prediction.m; error msg renamed). check_matlab_code: clean except one PRE-EXISTING keyword-alignment warning at line 248 (the `try/catch` in `[CP-HEMI]`, untouched by this merge).
**Why:** User: "move everything to contra_prediction." Single entry point for the whole contra→ipsi pipeline (prediction + kernel + bleed + residual + state-dependence).
**Why NOT the deeper rewire:** The deferred plan proposed wiring the residual sections to contra_prediction's own `[CP-HEMI]` H/yhat to avoid "duplicate prediction." Rejected as unnecessary: `cp_residual_core` already builds its prediction via the SAME `cp_hemi_predictor` field→kernel map that `[CP-HEMI]` uses, so there is no fidelity gap — only the same map computed in two independently-run sections. Extracting the residual machinery out of cp_residual_core to inline it would duplicate logic and add untested risk for zero scientific gain. `cp_residual_core.m` stays (shared by `cp_doseresponse.m` + `cp_state_batch.m`).
**Next:** Run end-to-end in MATLAB to confirm held-out R²≈0.885 + partials (VAR/DELTA significant, MOTION n.s.) reproduce post-merge; then commit (contra_prediction.m + deletion + this log together). The circularity caveat on the peri-stim VAR/DELTA windows still stands.

### 2026-06-26 — PLAN (deferred): merge residual + state-dependence into contra_prediction.m, retire contra_residual.m
**Changed/Found:** Decision (user): consolidate everything into the main `contra_prediction.m` using its OWN faithful `[CP-HEMI]` prediction; physically merge the `[CP-RESi]/[CP-LOCAL]/[CP-MOTION]/[CP-MOTION-AMP]/[CP-VAR]/[CP-DELTA]` sections in (wired to the H/yhat contra_prediction already computes — no duplicate prediction), then delete `contra_residual.m`. NOT executed this turn (past budget; refactoring the main script untested is too risky). Nothing changed this turn.
**Why:** Single faithful pipeline, one entry point. cp_residual_core duplicates the contra→ipsi prediction that contra_prediction already builds; the merge removes that duplication.
**Next (execution plan for next session):** (1) In contra_prediction, after `[CP-HEMI]` build `yhat = V_c_full(1:H.K,:)'*H.bk+H.b0; resid = y_full - yhat`. (2) Port cp_residual_core's residual machinery that DOESN'T duplicate prediction: per-amp `res_imp`/`act_imp`/`prr_imp` trial arrays, dev_stim/dev_pre (0.2s pre), and the state metrics WITH the new peri-stim windows already added (motion total|z| [-2,+0.5]s; var/delta [-1,+0.5]s, win_r sized to nWvd=54). (3) Add the section drivers reusing helpers `cp_agl`, `cp_cont_state`, `cp_res_inspector`, `cp_motion_amp` (keep these in utils). (4) Run end-to-end, confirm held-out R²≈0.885 + partials (VAR +0.192, DELTA +0.183, MOTION n.s.). (5) Delete contra_residual.m only after the merged version verifies. Watch: `cp_state_batch.m` also calls cp_residual_core — keep cp_residual_core or update the batch.

### 2026-06-26 — cp_residual_core: state windows -> peri-stim (motion [-2,+0.5]s total, var/delta [-1,+0.5]s); deleted prototype dupes
**Changed/Found:** Deleted this session's redundant prototype files (`utils/cp_statedep_explorer.m`, `data/cp_statedep_explorer_*.mat`, `data/cp_residual_*.png`) — superseded by the existing workbench's `[CP-RESi]`/state sections. Then, per user, changed the state-metric windows in `utils/cp_residual_core.m` (2 edits, lines ~494 + ~524): VARIANCE & DELTA(1-4Hz) now over **[-1,+0.5]s** (peri-stim; spectral win_r/nfft_r resized to nWvd=54), MOTION now **total |z-motion| over [-2,+0.5]s** from `motz_full` (replaces `imp_data.mot` per-trial scalar). Verified: runs clean, held-out R²=0.885 unchanged. Partial(dev_stim,state|dev_pre): MOTION +0.029 p=0.42 (n.s.); **VAR +0.192 p=2.2e-7; DELTA +0.183 p=7.9e-7** — both stronger than the pre-stim version (+0.147/+0.099).
**Why:** User asked for these specific peri-stim windows ("last three" = the three window specs).
**Next:** CIRCULARITY CAVEAT (noted in-code): the [-1,+0.5]s / [-2,+0.5]s windows span +0.5 s INTO the stim response, overlapping the DV; dev_pre (0.2 s pre) doesn't control that overlap, so these are UPPER-BOUND effect sizes — the pre-stim numbers (var +0.147, delta +0.099) are the conservative estimate. Affects all state sections + cp_state_batch. Commit when ready.

### 2026-06-26 — Existing contra_residual.m workbench verified faithful; supersedes this session's prototype
**Changed/Found:** Discovered `impulse-analysis/contra_residual.m` already exists as a complete sectioned workbench (another session): `[CP-SETUP]` → `cp_residual_core` with `predictor='hemi'` (faithful CP-HEMI field→kernel, contra-only, motion-free), plus `[CP-RESi]` clickable inspector (`cp_res_inspector`), `[CP-LOCAL]/[CP-MOTION]/[CP-VAR]/[CP-DELTA]` state sections. Did NOT overwrite it (user: "verify, change nothing"). Ran `[CP-SETUP]`: clean, held-out R²=0.885 (HEMI K=50 rank43). **State-dependence (Spearman + PARTIAL controlling dev_pre, fig-6 |dip−amp-mean| metric, z-within-amp): MOTION partial ρ=+0.013 p=0.72 (n.s.); PreVAR ρ=+0.147 p=7.6e-5; PreDELTA ρ=+0.099 p=0.0079** — variance & delta ARE significant. **This SUPERSEDES this session's "delta vanished under faithful prediction" entry** — that prototype used Pearson, no partialling, the |min-peak| metric and K=200; the workbench's partial-Spearman is the rigorous version to trust.
**Why:** User asked to consolidate Route A into one runnable `contra_residual` script; it already exists and is more complete + more faithful than my single-pixel/OLS prototype.
**Next:** My session prototype files (`utils/cp_statedep_explorer.m`, `data/cp_statedep_explorer_*.mat`, `data/cp_residual_*.png`) are redundant duplicates of `[CP-RESi]` — delete only if user confirms. Use `contra_residual.m` going forward.

### 2026-06-26 — State-dependence collapses under faithful prediction (delta effect was a prototype artifact)
**Changed/Found:** Regenerated `impulse-analysis/data/cp_statedep_explorer_AL_0033_0129_e1.mat` using `yhat_zw` (Zhiwen CP-HEMI K=200 rank98) instead of the OLS-K=50 prototype, so the interactive `cp_statedep_explorer` now reflects the faithful model. The |peak-deviation| state-dependence correlations (n=748) CHANGED: MOTION r=−0.002→+0.047 (p=0.20); VARIANCE r=+0.068→−0.027 (p=0.47); **DELTA r=+0.093 p=0.011 → r=−0.014 p=0.69**. The previously "significant" delta effect VANISHED. Interpretation: delta = slow-wave shared brain state; the weak OLS-K=50 model left delta-correlated prediction error in the residual, which masqueraded as state-dependence. The rank-98 field-RRR absorbs that shared variance → spurious correlation gone. **Under the proper pipeline, no significant |peak-dev| state-dependence on motion/variance/delta for this metric.**
**Why:** User asked whether the interactive plotter was using the faithful prediction (it wasn't — it read the pre-swap dump). Regenerating exposed that the state-dependence conclusion was prediction-quality-dependent.
**Next:** Relaunch explorer (`clear; close; cp_statedep_explorer(mat)`) to view faithful version. Re-test state-dependence with the SIGNED impulse-strength metric (not just |dev|) under yhat_zw before concluding no state-dependence. Re-run over-subtraction check under rank-98. Crystallize into a script; commit.

### 2026-06-26 — Switch Route A to Zhiwen-faithful CP-HEMI (contra-only field RRR @K=200), drop OLS/single-pixel
**Changed/Found:** Replaced the session's prototyping prediction (single-pixel OLS on K=50 contra modes, R²=0.885) with the proper `cp_hemi_predictor` (whole-ipsi-field reduced-rank regression via CanonCor2 + kernel-footprint readout), K=200, win_mode='interstim', **contra modes only — no motion, no lags, no other regressors** (per user). Result: primary-pixel held-out R²=**0.899**, RRR auto-selected **rank 98**, whole-field pooled R²@10=**0.969** (nTr=40000/nTe=20000). Deployable map `H.bk,H.b0` → `yhat_zw = V_c_full(1:200,:).'*H.bk + H.b0` now in base workspace. Held-out R² ≈ OLS-K=200 (0.898) but properly rank-regularized → protects the residual from over-subtraction (the rank constraint stops the fit chasing ipsi-local structure).
**Why:** User: "now to proper zhiwen faithful pipeline, dont regress to other predictors." OLS + single-pixel were prototyping shortcuts; the production/Route-A model must be the published contra-only field-RRR.
**Next:** Regenerate Route-A residual (`resid = y_full - yhat_zw`), the trial figures, and the state-dependence dump/explorer (`cp_statedep_explorer_*.mat`) using `yhat_zw`; re-run the over-subtraction permutation check under the rank-98 model (expect leak ≤ the earlier 18% n.s.). Then commit today's work.

### 2026-06-26 — cp_statedep_explorer: interactive scatter→trial state-dependence (new |peak-dev| metric)
**Changed/Found:** New GUI `utils/cp_statedep_explorer.m` + dump `impulse-analysis/data/cp_statedep_explorer_AL_0033_0129_e1.mat`. Y-axis redefined per user: **|min(trial residual over [0,0.3]s) − min(amp-avg residual)|** (abs deviation of the trial impulse peak from the amplitude average). New peri-stim state windows: MOTION = total |motion| over [−2,+0.5]s (z within amp); VARIANCE & DELTA(1–4Hz) over [−1,+0.5]s on y_full (z within amp). With this metric (n=748): **MOTION r=−0.002 p=0.95; VARIANCE r=+0.068 p=0.065; DELTA r=+0.093 p=0.011** — higher pre-stim delta → larger trial-to-trial deviation from the average impulse. GUI: left scatter (popup: motion/variance/delta), click a point → right column top = true/contra-pred/amp-avg-true overlaid, bottom = trial residual + amp-avg residual with both peaks marked. check_matlab_code clean; launch verified (clear cp_statedep_explorer; rehash first per cache gotcha).
**Why:** User wanted the impulse measured as per-trial deviation from the amplitude-average peak, peri-stim state windows, and a clickable scatter→trial explorer like the kernel/bleed tools.
**Next:** VARIANCE & DELTA windows now span the onset ([−1,+0.5]s) → partly peri-stim, mild circularity with the impulse; consider a pure pre-stim variant. Variance/delta collinear. Replicate across sessions.

### 2026-06-25 — Route A residual: trial figures + state-dependence (variance & delta significant, motion n.s.)
**Changed/Found:** Prototyped Route A (contra prediction → residual) on AL_0033 0129 (K=50, spont R²=0.885); saved 3 PNGs to `impulse-analysis/data/`. (1) `cp_residual_trials_bc.png` — per-trial baseline-corrected true/pred/residual, 3 trials/amp. (2) `cp_residual_trialavg.png` — trial-averaged per amp: amp-0 catch residual FLAT (control passes), residual onset dip grows cleanly with amplitude (−1.7 at amp2.7 → −3.5 at amp4.9) while prediction reconverges after onset — residual isolates the LOCAL impulse. (3) `cp_residual_statedep.png` — per-trial residual impulse strength (−[mean resid 0–200ms − pre-baseline]) vs pre-stim state (2 s pre window), z-scored WITHIN amplitude, n=748 nonzero trials: **MOTION r=+0.045 p=0.22 (n.s.); VARIANCE r=−0.122 p=7.9e-4; DELTA 1–4Hz r=−0.099 p=6.8e-3.** Higher pre-stim local variance / delta power → weaker inhibitory impulse. Small but significant state-dependence; motion irrelevant (consistent with motThresh philosophy).
**Why:** User chose Route A (residual for state-dependence over clean-mode selection); wanted trial overlays, trial-averaged dose-response, and state-dependence for motion + variance/delta.
**Next:** Variance & delta are collinear (both index slow-wave/synchrony) — consider partialling. Replicate across sessions for power; crystallize prototype into a script (`cp_route_a_state.m`) — currently base-workspace only.

### 2026-06-25 — Grid per-site trial×time raster (notebook cell 19)
**Changed/Found:** Added the per-site raster to `scratch_grid/grid_analysis.py` (`RASTER` knob) — for each grid site an `imshow` of the per-trial dF/F matrix (trials × time, ±8% bwr, stim period marked), 8×8 layout. Reuses the per-trial `dff` already computed in the timecourse loop (stashed in `dff_by_site`), so no extra data load. Output `grid_png/grid_raster.png` shows the post-stim response bands + single-trial variability, consistent with the snapshot/timecourse polarity. Completes the port of all notebook analysis plots (cells 10/14/17/18/19); only the dropped probe cells remain unported.
**Why:** User asked to also create the raster plot.
**Next:** none — notebook grid analysis fully ported. (Open idea raised but not built: an aggregate stim→response causal/connectivity map across sites, beyond the per-site spatial snapshot.)

### 2026-06-25 — Grid spatial dF/F snapshot map: clean dual-opsin polarity
**Changed/Found:** Added the spatial dF/F snapshot grid (notebook cell 10 port) to `scratch_grid/grid_analysis.py` — per-site full-frame response image (baseline window vs 40–120 ms post-stim, SVD-projected, ±2% bwr), 8×8 layout, stim site marked. Loads `U[:,:,:50]` once. Result (`grid_png/grid_spatial.png`) shows the expected **dual-opsin signature**: left-hemisphere sites (galvo x<0, excitatory opsin) give focal POSITIVE/red dF/F; right-hemisphere sites (x>0, inhibitory opsin) give NEGATIVE/blue dF/F, each localized near the stim site. End-to-end validation of the onset/position/calibration pipeline.
**Why:** User asked to run the remaining notebook grid analysis and generate plots.
**Next:** Optional remaining notebook plot = the per-site trial×time raster (cell 19). `BASE_WIN`/`STIM_WIN` are knobs if a different response window is wanted.

### 2026-06-25 — Grid power split: only the 0.5 level fired; 0.25 sub-threshold (Block-aligned)
**Changed/Found:** Added Block-based power labeling to `scratch_grid/grid_analysis.py` (loads session 3 `Block.mat`, greedy in-order alignment of detected onsets onto the randomized trial log by galvo-position fingerprint; new `AMP_SEL` knob). Finding: of the 2600 detected 638 onsets, **2541 are the 0.5-power condition and only 59 the 0.25** — and probing both light commands at the Block-predicted times shows 0.25-power trials produce **no 638 (or 594) pulse at all** (median peak −0.01 V, 0% over threshold) while 0.5 does. So the `laserAmp=[0.25,0.5]` design effectively delivered only the high power; **0.25 was sub-threshold and did not lase**. Block↔Timeline clock offset ≈ 98.9 s (std 0.2 s) — Block times carry ±0.2 s jitter, so hardware `lightCommand638` edges remain the accurate onsets. Script now defaults `AMP_SEL=0.5` → 2541 onsets, 52 sites, ~48 trials/site; figures relabeled "0.5 power".
**Why:** User asked to proceed to the power split after confirming bregma. Turns out there is effectively one usable power, which is itself a data-quality finding worth recording.
**Next:** A 0.25-power map is not recoverable from this session (no stim delivered). If low-power data is wanted, it needs a session where the 638 command clears the lasing threshold at 0.25. Confirm with the experimenter whether 0.25 was expected to fire (laser threshold / AOM calibration issue).

### 2026-06-25 — Python port of the galvo site-grid, run on AL_0048 2026-06-24/2
**Changed/Found:** Created `scratch_grid/grid_analysis.py` (numpy/scipy/matplotlib/colorcet in repo `.venv`; installed colorcet) and ran it on the `opto_brainGrid638` session. That session is RAW — no preprocessed `laserOnTimes`/`galvoPositions_mm` — so the script derives them from session 2's raw 2 kHz Timeline: onsets by thresholding `lightCommand638` (a fixed 1.38 V gate; time = sampleIdx/2000, same clock as the corr SVD timestamps), positions by galvo-volts→mm via the rig calibration in session 3's `hardwareInfo.json` (`mmPerV_X=1.111`, `mmPerV_Y=-1.075`, `bregmaOffset_X/Y`). Recovers exactly **2600 onsets = 52 sites × 50 reps**, snaps to the 52 mm grid nodes, builds per-site ROI dF/F timecourses (8×8), exp-rise τ fit, τ map. Clear evoked responses with L/R structure. Dropped all notebook probe code (slider, single-site probe, rastermap, mp4 anim, unused `deconvolve`, multi-session walk).
**Why:** User asked whether the notebook is runnable here in a venv on a session they give (AL_0048 2026-06-24/2), Python instead of MATLAB. It is — the data is native `.npy`, and every missing dep is in the dropped probe layer.
**Next:** (1) Verify/nudge `BREGMA_PX`/`PX_PER_MM` in Fig 0 — `bregma_coords.mat` ([530,217]) is in the 512×640 calibration image, not the 560×560 SVD frame, so default [280,250]@57.8 px/mm is a dial-in (looks close). (2) Session 2 Timeline holds only 2600 of the planned 5200 trials and the gate doesn't encode the two powers (0.25/0.5), so power is currently POOLED; splitting needs Block→Timeline alignment from session 3's `Block.mat`.

### 2026-06-25 — Clean-mode contra predictor: mode selection can't de-bias the impulse; over-subtraction is n.s.
**Changed/Found:** Prototyped in the base workspace (AL_0033 0129, K=50 contra modes; not yet a script). Goal: select stim-clean contra SVD modes to predict the primary pixel without over-subtracting the impulse. Findings: (1) full-mode contra→primary OLS readout test R²=0.885 (≈ CP-HEMI 0.898), so a direct mode→y readout is a faithful stand-in. (2) Per-mode stim-effect (max-amp [0,200]ms dev vs catch, perm z): max |z|=2.92, only 4/50 modes |z|>2, none >3 — the localized contra bleed dilutes across global modes, and the most stim-affected modes are the WEAKEST predictors. (3) Trial-avg over-subtraction (max-amp n=84): actual impulse −1.75 dF, full-model yhat leak −0.37 raw / **−0.32 catch-corrected (18.5%)**, but **z=−0.87, p=0.39, bootstrap 95% CI [−0.86,+0.21] straddles 0 — NOT significant.** (4) Mode selection cannot remove it: drop |z|>2 → R²=0.884, leak −0.28 (≈unchanged); drop the dominant global mode 1 → R² collapses 0.885→0.247 but leak stays −0.32; drop top-contrib modes → R²→0.13, leak WORSE (−0.45). OLS reconstructs the stim-correlated component from the remaining correlated modes.
**Why:** User asked to build a mechanism that picks the most-informative, least-stim-affected contra kernels to avoid over-subtraction. The prototype shows the premise doesn't hold: the contra prediction is not significantly biased w.r.t. the impulse, and mode selection structurally can't de-bias it (the leak is low-dim, correlated with the predictive structure, and reconstructable from any rich mode subset; removing it costs all the R²).
**Next:** Do NOT build the "mode cleanliness knob" explorer — it would imply a fixable bias that is neither significant nor selection-removable. Optional: package the diagnostic as a one-off robustness figure/function (over-subtraction estimate + CI + R²-vs-drop tradeoff) for the paper's methods/robustness. Revisit only if pooling sessions/all-amps raises power enough to make the 18% bias significant.
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — added `USE_LAST_N = 100` knob; per-side trial masks now AND in a `useMask` keeping only the last USE_LAST_N trials (`firstUse = nTrials-USE_LAST_N+1`), dropping session-start warm-up, and print the used range. On AL_0048 06-24 e5: drops trials 1–4 (all CL) → OL stays 51, CL 53→49; OL/CL MSE ratio 0.91 (all) → **1.01** (last 100), CL mean MSE 40.27→36.31. check_matlab_code clean.
**Why:** User: "only last 100 trials are actually supposed to be used for OL vs CL analysis" — the opening CL block (trials 1–7) is warm-up, including the onset-contaminated early trials.
**Next:** Last-100 keeps trial 5 (the −1.0 s early CL trial, now the first included CL trial); set `USE_LAST_N=99` if it should also be dropped. Confirm the "last 100" protocol holds for other AL_0048 sessions before reusing this value.

### 2026-06-25 — cl_ol_single_session: 2 early-session CL trials (4,5) carry stim/input before t=0
**Changed/Found:** Diagnostic — per-trial input-onset timing (first |input|>0.3 crossing vs marked onset) on AL_0048 06-24 e5: OL 51/51 aligned (median 0.000 s, std 0.009 s); CL 51/53 aligned (median 0.000 s) but trials **4 and 5** cross at −0.315 s and −1.0 s. Both sit in the opening CL block (trials 1–7 are CL; first OL is tr 8). Their frame-index spacing is regular (~402 frames), but their pre-onset dF/F is high (+5..+13) and neither shows the inhibition dip in [0,3]; tr 5's 3 s input block is shifted ~1 s early (runs ≈ −1→+2 s). Not ITI bleed (preceding inter-onset gap ~11.5 s). The 2.918 s frame-index lead is otherwise valid for CL.
**Why:** User observed "some CL trials where stim appears to start before 0 s." Localised it to 2 onset-contaminated early-block trials (controller already actuating on a large pre-existing error) rather than a systematic CL lead error.
**Next:** Decide whether to add an onset-sanity guard (drop trials whose |input| crosses before t=0−tol from the CL/OL sets) or exclude trials 4,5 for this session — pending user choice.

### 2026-06-25 — cl_ol_single_session: verify frame-index onset alignment via windowed input traces
**Changed/Found:** Diagnostic only (no script edit) — windowed the combined |input| command (inpVals638/594 vs inpTime) around the frame-index onsets `timeBlue(input_params(:,2)) − 2.918 s` for OL and CL, AL_0048 2026-06-24 exp 5, and plotted all trials + mean. OL |input| is a razor-sharp rectangle across all 51 trials: 0.017 baseline → 1.288 stepping exactly at t=0, flat through [0,3], dropping at t=3, **no edge smearing**. CL |input| ≈0.033 pre-onset, spikes at t=0 then settles to ~1.0 (controller reacting). Both rising edges land on t=0.
**Why:** User reported "stims misaligned again" on the new session and asked to plot input traces for both cases + average — needed an across-trials confirmation (not just the 51-trial lead measurement) that the frame-index method aligns before trusting CL/OL MSE.
**Next:** Alignment confirmed for this session; still verify ONSET_LEAD_S=2.918 s holds for other AL_0048 sessions/rig configs before reuse (per the frame-index entry below).

### 2026-06-25 — cl_ol_single_session: derive stim onsets from input_params(:,2) frame index − 2.918 s lead (fix 37 s misalignment)
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — replaced the rising-edge re-detection (`detectStimsFromInput`/`correctOnsets`/`min_idx`, all removed) with `d.stimStarts = d.timeBlue(round(input_params(:,ONSET_FRAME_COL))) - ONSET_LEAD_S` (`ONSET_FRAME_COL = 2`, `ONSET_LEAD_S = 2.918`). On AL_0048 2026-06-24 exp 5 the old path left onsets a uniform **−36.98 s** early: `correctOnsets`' "median nearest-neighbour residual" offset estimator **aliases** when the true offset (~37 s) exceeds half the ~11.4 s trial spacing (collapsed to +3.0 s instead of +37 s), and the input is continuously active for CL trials so rising-edge detection is meaningless there. col 2 is an imaging-frame index logged at TRIAL END, not onset: measured against the 51 OL input-pulse rising edges (the true laser-on for discrete OL pulses), col2 sits **2.918 s after** onset, std 0.009 s / IQR [2.917, 2.918] — extremely tight (≈ dur=3). Subtracting the 2.918 s lead aligns OL pulses to t=0; mean OL trace now drops to −5 ref exactly at 0, holds 0–3 s, recovers at 3. OL/CL MSE ratio 0.72 (misaligned) → 0.91. check_matlab_code clean; re-ran end-to-end. User chose the measured 2.918 s over a flat 2 s.
**Why:** User reported stims/inputs misaligned and identified input_params(:,2) as the stim frame index, then flagged it is saved at trial end. The frame index is exact and one-per-trial; the measured lead removes the trial-end offset without heuristic edge matching.
**Next:** Re-run any earlier AL_0048 sessions analysed with the old re-detection path (their cached MSE/ratios are suspect); verify ONSET_FRAME_COL=2 and the 2.918 s lead hold for other bilateral sessions/rig configs before reusing.

### 2026-06-25 — Bleed explorer: sensitivity knob (live z-threshold slider, instant re-tint)
**Changed/Found:** `utils/cp_bleed_explorer.m` — added a "bleed |z| ≥ X (left = more sensitive)" slider (range 1.0–4.0, default 1.96) under the Show-bleed button. `local_onSig` now caches the per-pixel z (`st.zlast`); the slider's `local_onZthr` re-thresholds that cached z and re-tints the green WITHOUT re-running the 1000× permutation (`local_update` clears `st.zlast` on any window/metric change, so the slider only re-tints a z that matches the current map). Verified: at post[0,500] base1000 max-amp, z≥1.96 → 513 green px (ipsi core only); dragging to z≥1.30 → 17912 px in 0.08 s, lighting up BOTH the ipsi inhibition core and the contralateral homotopic blob (the bilateral bleed). check_matlab_code clean.
**Why:** User asked for a knob to tune bleed-detection sensitivity. A z-threshold slider that reuses the cached null is instant and lets the user trade off strictness vs coverage live.
**Next:** none — feature complete. (Still uncommitted along with the rest of today's explorer work.)

### 2026-06-25 — Bleed explorer: significance = transparent-green per-pixel perm-z tint (clusters dropped)
**Changed/Found:** `utils/cp_bleed_explorer.m` — per user, replaced the cluster-based significance with a simple per-pixel permutation z: build the label-shuffle null (1000×) → per-pixel null mean/std → z=(map−nullmean)/nullstd → "bleed" pixels = |z|>1.96 (p<0.05) tinted transparent GREEN on a third overlay axes (α=0.40); primary marker recolored magenta. Removed `local_clusters`/`local_cc`. Verified the GUI's z now matches an independent computation exactly at a fixed window (e.g. post[0,500] base1000 → 513 green px at the inhibition core; post[0,2500] base2000 → max|z|=1.78, 0 px). The green is **window-dependent and correct**: short windows light up the significant inhibition core; long windows (diffuse rebound + larger null variance) fall sub-threshold. check_matlab_code clean.
**Why:** User: "dont do clusters just color all the bleed pixels with transparent green." Per-pixel perm-z (not FDR) sidesteps the p-floor wall and gives an honest, simple "is this pixel beyond spontaneous fluctuations" tint.
**Next:** Root cause of the long debug: MATLAB ran a STALE CACHED cp_bleed_explorer (edits, incl. zthr, weren't reloaded) — fixed with `clear cp_bleed_explorer; rehash` before relaunch. Always clear a running GUI function after editing. Threshold 1.96 is adjustable if user wants more/fewer green px.

### 2026-06-25 — Bleed explorer: max-amp-deviation mode; per-pixel FDR found structurally under-powered
**Changed/Found:** `utils/cp_bleed_explorer.m` — added a metric popup: 'dose-response slope' (existing) + 'max-amp deviation' Δ = mean(top-amp [post−baseline]) − mean(amp-0 catch [post−baseline]) (catch = per-pixel "no input" reference). Added per-window trial selection and extended the baseline slider to 2000 ms (default 1000). Max-amp Δ is much cleaner: short window [0,500] ms → BILATERAL inhibition (ipsi stim-site blob + a contralateral homotopic blob = the bleed); long window [0,~1200] ms → positive rebound. **BUT Significance = 0/164818 px, and that is a statistical artifact:** with nPerm=1000 the smallest achievable p is 1/1001≈1e-3, while BH-FDR across ~165k pixels needs the top pixels at p≲3e-7 — so per-pixel permutation+FDR cannot reject here regardless of effect size (same wall hit by the earlier [CP-BLEED] 0/80987). The maps are clearly real (coherent blobs, sensible physiology). check_matlab_code clean.
**Why:** User proposed max-amp-vs-catch deviation as a more sensitive/intuitive bleed measure; building + testing it exposed the per-pixel-FDR limitation.
**Next:** Replace per-pixel FDR with CLUSTER-BASED permutation (cluster mass vs a max-cluster null — few comparisons, 1000 perms suffice; standard imaging fix) and/or a permutation-calibrated parametric z. Pending user's call.

### 2026-06-25 — Interactive whole-brain bleed explorer (live time window, FLIPPED primary)
**Changed/Found:** New `utils/cp_bleed_explorer.m` + a param save-step appended after `[CP-BLEED]` in `contra_prediction.m` (dumps `cp_bleed_explorer_<sess>.mat`: onsets/amps incl amp 0, whole-brain idx, FLIPPED primary row↔col, Fs). The explorer reloads the SVD and shows the per-pixel dose-response slope β over the WHOLE brain (ipsi + contra); 3 sliders (post-start, post-end, baseline ms) update the β map LIVE via a cumsum-of-V window mean + one matvec (β=U_brain·g, g=(dv_c·ac)/den·100); a "Significance" button runs the amp-shuffle perm (1000×) + BH-FDR for the current window and outlines significant pixels in green. Verified the explorer's β path == validated static [CP-BLEED] exactly (corr=1.0, maxdiff 7.5e-16). At post [0,500] ms it shows a strong ipsi inhibitory (blue, β<0) blob at the stim site + a contralateral positive (red) blob that the 200 ms window missed. check_matlab_code clean; launched OK.
**Why:** User asked to (a) plot ipsi too (whole brain), (b) increase the window, (c) make the window interactive; and to use the flipped primary going forward without changing prior work.
**Next:** Static [CP-BLEED] map/decay still reference the TRUE primary (left unchanged per "don't change previous"); offer to switch those to flipped if wanted. Significance is window-specific — recompute after moving the sliders.

### 2026-06-25 — [CP-BLEED] dose-response bleed map: primary control passes, no contra pixel FDR-significant
**Changed/Found:** New `[CP-BLEED]` section in `impulse-analysis/contra_prediction.m` — per contra pixel, amplitude dose-response slope β of the peri-stim response (mean ΔF/F over [0,200] ms minus a [-500,0] ms baseline) over the 10 amplitudes (0–4.9), amplitude-LABEL permutation null (1000×) + Benjamini-Hochberg FDR. Result: primary-pixel positive control β=−0.412, **p=0.001** (method IS sensitive to a real effect); but **0/80987 contra pixels survive FDR q<0.05**. Mean |β| decays smoothly with distance from the primary (0.125 near-midline → 0.02 far) with a faint inhibitory (negative-β) gradient — a weak, spatially-coherent, sub-threshold contra bleed. Exported `cp_bleed_slope_map.png` + `cp_bleed_decay.png`. check_matlab_code clean.
**Why:** Redo of the old CP-BLEED with a sensitive amp-graded + permutation method to characterize how far the input bleeds into contra; user expected decay with distance (confirmed by the decay curve).
**Next:** To claim per-region contra significance, upgrade to a one-tailed (inhibitory β<0) + cluster-based permutation test (pools the spatially-coherent weak effect) or a region-level test on the decay bins — pending user's call.

### 2026-06-25 — Interactive kernel explorer (click ipsi pixel → contra weight map)
**Changed/Found:** New `utils/cp_kernel_explorer.m` + a save-step in `contra_prediction.m` [CP-KERNEL] that dumps `cp_kernel_explorer_<sess>.mat` (~70 MB rank-K fit products: `U_svd_raw_K`, `U_ipsi_raw_K`, `M1=h_scoresTr\V_ipsi_full(1:K,tr)'`, `h_b_K`, `sd_tr`, masks, mimg). Back-projects ANY clicked ipsi pixel's contra weight map via one matvec (`T=U_svd_raw·diag(1/sd)·h_b(:,1:n)·M1`; `pixw=T·meanU'`); rank slider + squared toggle; transposed display matches `cp_roi_masks`. Verified it reproduces the [CP-KERNEL] primary-pixel map EXACTLY (corr=1.0, relative diff 2.9e-15). Launched OK; check_matlab_code clean.
**Why:** User wanted a clickable tool to debug the x,y-flip by sweeping pixels and watching the homotopic hot-spot track; RAM concern solved by keeping only the rank-50 truncated products (no raw SVD, no pixel×time movie) → ~70 MB, one matvec/click.
**Next:** User clicks around to decide which orientation is the true primary, then sets the pending `flip_primary` toggle default (toggle not yet built).

### 2026-06-25 — [CP-KERNEL] add secondary squared "energy" map (w²)
**Changed/Found:** `impulse-analysis/contra_prediction.m` [CP-KERNEL] — added a secondary squared-weight map `cp_hemi_kernel_map_sq.png` (w²=`h_pixw.^2`, white→red sequential cmap, α∝w², clim [0, 99pct]) alongside the signed primary map. Sign-agnostic localization view: the diffuse low-weight background drops out and the focal homotopic patch isolates cleanly. check_matlab_code clean; rendered for AL_0033 0129 (energy 99pct ≈ 1.2e-7).
**Why:** User asked whether to square for plotting; decided keep signed primary (sign = in-phase homotopic vs anti-phase, Ye Fig 3D-G), add squared as secondary energy view.
**Next:** Both kernel figures + the [CP-KERNEL] section remain uncommitted. Marker still drawn at the transposed position (revert pending the user's call from the flip-diagnostic).

### 2026-06-25 — MATLAB port of the AL_0041 galvo site-grid notebook
**Changed/Found:** Created `scratch_grid/grid_analysis.m` — a faithful MATLAB port of `scratch_grid/2025-10-29_AL41_grid.ipynb` (galvo photostim site-grid). Keeps the analysis path: grid-window trial filtering, U(blue)/V(corr) SVD load via `readUfromNPY`/`readVfromNPY` (same source as `package_grid_for_collab.m`), per-site ROI dF/F timecourses on an 8x8 grid (trial-windowed, pre-stim baseline, getpixels_dFoF projection idiom), exponential-rise tau fit (`lsqcurvefit`), and a tau spatial map (optional `data/ctxOutlines.mat` overlay). Dropped all "probe" cells: ipywidgets slider, single-site probe, rastermap, mp4 animation, the unused `widefield_deconv.deconvolve`, and the multi-session os.walk/.npy round-trip. Passed MATLAB Code Analyzer with zero issues.
**Why:** User asked for a MATLAB port that reuses our existing functions and omits the probe/exploration code.
**Next:** Not yet run on data — `serverRoot`/`bregma` need setting (notebook recorded no bregma for 2026-02-17; placeholder [325 245] from 2025-11-21). Fig 0 is the bregma sanity-check; dial it in before trusting the timecourse/tau figures.

### 2026-06-25 — Kernel diagnostic: original primary pixel is correct, NOT x,y flipped
**Changed/Found:** Ran [CP-KERNEL] for the original pixel `mimg(px_prim,py_prim)` vs the x,y-flipped pixel `mimg(py_prim,px_prim)` (ad-hoc in the live session, reusing the pixel-agnostic field RRR + ipsi SVD; flipped footprint = 441/441 px inside the ipsi mask). Original → ONE FOCAL contra hot-spot (the homotopic signature). Flipped → DIFFUSE salt-and-pepper, no focal homolog. Exported `cp_hemi_kernel_map_flipped.png`.
**Why:** User suspected the primary pixel was x,y flipped (the marker looked misaligned with the hot-spot) and asked to confirm via the flipped-pixel kernel map.
**Next:** Only the original pixel yields a clean homotopic kernel, so `y_full`/kernel/fit are sampling the CORRECT pixel — no data change. The misalignment is purely the 2026-06-24 cosmetic marker flip in `cp_roi_masks` (the `+` is drawn at the transpose). Recommend REVERTING that flip (draw `+` at `px_prim,py_prim`) so it sits opposite the focal patch — pending user confirm.

### 2026-06-24 — [CP-KERNEL] contra predictive-weight map (Ye Fig 3D-G analog) on HEMI predictor
**Changed/Found:** `impulse-analysis/contra_prediction.m` — added a [CP-KERNEL] section that back-projects the deployed HEMI readout's contra-mode weights into a signed per-pixel map: `w_z = h_b(:,1:n)*h_aKern(1:n)`, de-z-scored by the train per-mode std, then `pixel_w = U_svd_raw(:,1:K)*w_raw`, drawn over `brain_overlay_fig` (diverging cmap, α∝|w|, + at primary pixel) → `cp_hemi_kernel_map.png`. Ran AL_0033 0129 (selExp=3) headless via MCP (caches loaded, no redoSVD/ginput): pooled ipsi-hemi R²=0.932@rank10 (0.937 peak@49); primary-pixel R²=0.861@10 / 0.865@49; per-segment baseline-removed 0.925. Map (rank h_kbest=49) shows a focal POSITIVE predictive hot-spot in the contra hemisphere ~homotopic to the recording site + a smaller negative spot; most pixels weak.
**Why:** User: "plot the kernel weights most predictive of ipsi primary kernel" — rebuild of the old [CP-RRR] kernel map on the Zhiwen-faithful HEMI predictor (the prior CP-RRR/CP-BLEED were dropped in 043b3ab).
**Next:** Map uses h_kbest=49 (near-full rank → noisier); rank 10 already reaches ~99.5% of peak primary R², so a rank-10 (or r*≥99%) map would be cleaner/more interpretable — offered to user. The [CP-KERNEL] section is uncommitted.

### 2026-06-24 — cp_roi_masks: flip verification marker to transposed screen pos (display only)
**Changed/Found:** `utils/cp_roi_masks.m` — the green primary-pixel `+` in the verification overlay now plots at the transposed screen position (`plot(prim_yA, prim_xA)` instead of `plot(prim_xA, prim_yA)`), per user display preference. Confirmed via all `getpixel_dFoF` modes (lines 47/59/64/94) that `y_full` is sampled at `mimg(pixel(2)=px_prim [row], pixel(1)=py_prim [col])`, so the un-flipped position is the actual sampled pixel; the user chose a **cosmetic, marker-only** flip and explicitly left `y_full`, the kernel, the ipsi/contra split, and all masks UNCHANGED. check_matlab_code clean.
**Why:** User: "x and y need to flip, for it to be where I need it." Chose "marker display only" when asked the flip scope — masks looked right, only the marker's screen position needed to match their visual reference. The side test is robust (the recording site is well inside one hemisphere, so ipsi is assigned correctly regardless of the row/col order of the point).
**Next:** No redraw/cache rebuild needed (masks/geometry untouched) — re-run the section to see the moved marker. Open item flagged but deferred by the user: if their true site really is the transpose, `getpixel`/`y_full` would be sampling the transposed pixel (would affect the impulse pipeline too) — not pursued now.

### 2026-06-24 — cp_roi_masks: graceful ginput guard for "figure deletion"
**Changed/Found:** `utils/cp_roi_masks.m` — wrapped both draw `ginput` calls in try/catch that turns MATLAB's cryptic "Interrupted by figure deletion" (thrown when the draw window is closed mid-click) into a clear "draw window closed -- re-run to redraw" message. The user hit the raw error once, but it was from a STALE cached old-version run (stack pointed at line 46 = `ginput` in the old file, now `clim`); the subsequent run on the new code succeeded and saved a balanced split (brain 164818 = contra 80987 + ipsi 83831). check_matlab_code clean.
**Why:** "Interrupted by figure deletion" is opaque; a redraw that gets its window closed should fail with an actionable message, not a stack trace into ginput.m.
**Next:** Only affects the redraw path (ginput); the saved `cp_roi2_*` cache loads without it. Confirm the verification overlay (primary + in red/ipsi, split anatomical). If MATLAB shows odd stack/line mismatches, `clear cp_roi_masks` to flush the stale copy.

### 2026-06-24 — cp_roi_masks: display transposed (match codebase), masks transposed back
**Changed/Found:** `utils/cp_roi_masks.m` — first version displayed `imagesc(mimg)` (non-transposed) and the user reported the image looked transposed vs the rest of the code (which uses `imagesc(mimg')`). Reworked: display/draw/overlay all in the TRANSPOSED frame `A = mimg.'` so clicks and plots are natural; build brain/contra/ipsi in that frame; transpose the masks back to mimg's native `[nY x nX]` before returning (so `find(mask(:))` still indexes `U`/`V` column-major). Primary pixel mapped to transposed coords (x=prim_row, y=prim_col); split + side test verified to round-trip (primary lands in ipsi). check_matlab_code clean.
**Why:** Orientation must match the codebase convention so the drawn outline/midline and the verification overlay are readable; masks must stay in mimg orientation for the SVD indexing.
**Next:** If a `cp_roi2_*` geometry cache was already saved by the previous (non-transposed) version, its stored click coords are in the old frame — set `redefine_roi=true` (or delete the `cp_roi2_*` file) and redraw once. Then confirm the verification overlay.

### 2026-06-24 — Replace contra ROI with full-brain mask bisected by midline (cp_roi_masks)
**Changed/Found:** New `utils/cp_roi_masks.m` (single source of truth) + rewired `impulse-analysis/contra_prediction.m`, `utils/cp_residual_core.m`, `utils/cp_hemi_predictor.m`. OLD setup: predictor = a hand-drawn CONTRA polygon, target = `brain_mask & ~polygon` ("everything else" — sloppy, included midline/sinus/missed pixels). NEW: draw the FULL-BRAIN outline + the midline; `brain = inpolygon(outline) & mimg>prctile(20)`; bisect by the midline via an orientation-robust signed cross product; the side holding the primary (recording) pixel = IPSI (target), the other = CONTRA (predictor). cp_roi_masks draws a verification overlay (contra=blue, ipsi=red, primary=green +). Removed the duplicated midline+polygon ROI blocks (and the `elsec` typo) from both scripts; `cp_hemi_predictor` now takes `ipsi_mask_cp` directly instead of `brain & ~contra`. New ROI cache `cp_roi2_*` (forces redraw, old `cp_roi_*` untouched) and new SVD caches `*_svdraw_ml.mat` / `*_svdraw_ml_ipsi.mat` (forces fresh contra+ipsi redoSVD under the new masks; old caches kept for rollback). All four files check_matlab_code clean (only pre-existing trivia).
**Why:** User: "the model is using the [left-hemisphere] mask to predict everything else in the image… segment the brain and split it using midline." The polygon+complement target was geometrically wrong; a clean midline bisection of the full brain gives proper predictor/target half-brains.
**Next:** RELOAD contra_prediction.m in the editor; run [CP-HEMI] — first run pops the redraw (brain outline → 2 midline pts) → CONFIRM the verification overlay (primary in red/ipsi, split looks anatomical) → it rebuilds both SVDs (~7 min ×2) → prediction + figures. Note cp_roi_masks draws/masks in native [row,col] (non-transposed) orientation, so the view may look rotated vs the rest of the code — correctness is checkable from the colored overlay. If the split looks wrong, set `redefine_roi=true` and redraw.

### 2026-06-24 — [CP-HEMI] level-shift diagnosis: report offset-removed R², center trace plots
**Changed/Found:** `impulse-analysis/contra_prediction.m` [CP-HEMI] figures — user observed the WORST held-out segments are mostly LEVEL-SHIFTED (shape tracks, DC offset wrong), because the field→kernel readout predicts zero-mean fluctuations (no intercept) and can't follow slow per-segment baseline drift in `y_full`, which `sseExplainedCal` (no offset fit) then punishes. Added a per-segment baseline-removed ("shape") R² alongside the raw R², a pooled baseline-removed primary-pixel R² printout next to the raw peak, and changed the example/best-worst panels to center each trace on its OWN mean (shape comparison) with titles showing `R²=raw (shape=offset-removed)`.
**Why:** To show the prediction is better than the raw ~0.85 implies (worst trials are offset, not shape errors) and quantify it. The residual pipeline already subtracts each trial's pre-stim baseline, so a per-trial offset cancels — the Local-residual/state-dependence science is unaffected; this is a [CP-HEMI] benchmark/viz artifact only.
**Next:** After the rerun, compare raw vs baseline-removed primary-pixel R² in the console; if the gap is large, the contra→ipsi map is mainly losing R² to slow drift, not shape. Decide whether to also add a readout intercept (option 3, deferred) for the absolute number — not baked into the residual.

### 2026-06-24 — [CP-HEMI] fix blank per-pixel R² map + add prediction-trace figures
**Changed/Found:** `impulse-analysis/contra_prediction.m` [CP-HEMI] — (1) the per-pixel R² map rendered ~blank (only the colorbar) because `h_perpix` is scored on the DOWNSAMPLED ipsi set (~`h_maxPix` px) and only those sparse pixels were painted (`AlphaData=~isnan`) → ~0.6% coverage of a 560×560 grid; also `imagesc(C,'AlphaData',A)` isn't valid syntax. Replaced with a nearest-neighbour fill (`scatteredInterpolant`) to the full ipsi mask + `set(h_im,'AlphaData',…)`. (2) Added `cp_hemi_examples.png`: predicted vs actual primary-pixel trace on 6 example held-out test segments (3-s "trials", per-segment R²). (3) Added `cp_hemi_bestworst.png`: best-3 vs worst-3 segments by R². Prediction = field→kernel readout at the best held-out rank (`h_kpk`). check_matlab_code clean (only the 2 pre-existing trivia).
**Why:** User reported the [CP-HEMI] map showed only the colorbar and asked for prediction-on-trials + best/worst figures. The blank was a downsample-rendering artifact, not bad data (per-pixel median R² was 0.924).
**Next:** RELOAD contra_prediction.m in the editor before re-running (on-disk edit, file is open live). Re-run [CP-HEMI]; check the dense R² map + the two new trace figures. If "trials" should be the actual inter-stim windows (not fixed 3-s chunks of the concatenated test block), say so and I'll segment by window boundary.

### 2026-06-24 — Fix cache path resolution: reject MATLAB editor-temp staging dir
**Changed/Found:** `impulse-analysis/contra_prediction.m` + `utils/cp_residual_core.m` — hardened the `mfilename('fullpath')` folder resolution to reject paths under `tempdir`. When a file is run from the editor with UNSAVED changes, MATLAB stages a copy to `...\AppData\Local\Temp\Editor_*\`, so `mfilename` pointed there and `dataDir` became `Temp\Editor_xxxxx\data\` — the contra/ipsi SVD + ROI caches were written to temp (wiped next run; NOT found by cp_residual_core/cp_hemi_predictor, which look in `impulse-analysis/data`). Now: if `mfilename` is empty OR under `tempdir`, fall back to `which()` then cwd candidates. check_matlab_code: no new issues.
**Why:** User hit `Saved contra SVD raw cache: C:\...\Temp\Editor_tsxbn\data\...` — caches scattered to temp, defeating the ipsi-SVD cache (forces the ~7-min redoSVD every run) and desyncing the residual pipeline's cache lookups.
**Next:** 10 other .m files share the `mfilename('fullpath')` pattern (contra_residual, lds_ipsi, cp_doseresponse, cp_state_batch, run_all×2, package_for_collab×2, compute_face_motion, load_experiments) — mostly addpath-only (harmless if utils already on path) or cwd-relative caches; sweep them with the same guard if wanted. Deeper fix = central `bp_root()` helper (bootstrap caveat for path-setup scripts). NOTE: user must RELOAD contra_prediction.m in the editor to pick up this on-disk fix (saving an unsaved buffer would clobber it); `elsec` typo at ~L144 still open.

### 2026-06-24 — Repointed cp_residual_core "Global" to the [CP-HEMI] field→kernel predictor
**Changed/Found:** New `utils/cp_hemi_predictor.m` — factors the [CP-HEMI] whole-hemi RRR + field→kernel readout into a reusable AFFINE map on the contra modes: `yhat_dF(t) = V_c_full(1:K,t)'·bk + b0` (%dF/F, y_full units). Builds the ipsi SVD (cached), CanonCor2 on inter-stim spontaneous windows, kernel-mean readout `aKern = scores\kernel-mean`, picks the RRR rank by held-out primary-pixel R², and bakes TRAIN-stats z-scoring into (bk,b0) so it deploys pointwise to any frames. `utils/cp_residual_core.m`: added `opts.predictor` ('hemi' default | 'ols' legacy); when 'hemi' it builds the map and routes ALL 5 prediction sites through one `predFun(g)`, forces decontam OFF, sets `cv_mean = Hp.cv`. The OLS `beta_cp` path is fully preserved under 'ols'. `contra_residual.m` [CP-SETUP]: surfaced a `predictor` knob (default 'hemi'), passed via opts. check_matlab_code: cp_hemi_predictor + contra_residual CLEAN; cp_residual_core shows only pre-existing setup-var warnings (PS/mlag/t_trial/w_r/idx_r_cp/U_svd_cp), none from these edits.
**Why:** [CP-HEMI] is the single canonical contra predictor, so the residual's "Global" component must come from it, not the 200-mode OLS. KEY correctness points: residual = A − Pr with BOTH baseline-subtracted per trial → b0 cancels and the `/mI_kern·100` scaling puts Pr in absolute %dF/F = y_full units, so A−Pr is a genuine local residual; the shared 50-mode latents structurally can't absorb onset stim-bleed, so decontam is off (bleed rebuilt later). HEMI vs OLS single-pixel R² ~0.85 vs 0.898, but the shared-latent Global is the cleaner subtractor (less local leakage) — the scientific reason HEMI was chosen.
**Next:** RIG RUN to verify (could NOT execute — empty MATLAB workspace): run load_experiments → contra_residual [CP-SETUP] on AL_0033; confirm the printed HEMI held-out R² (~0.85) and re-read the state partials (Motion/PreVar/PreDelta) — the locked OLS result was PreVar partial +0.085 (p=0.02), Motion null. Then A/B vs `predictor='ols'` to quantify how the cleaner Global shifts the Local state-dependence. If numbers move materially, update FINDINGS.

### 2026-06-26 — First real run: input_params layout differs per mouse; states.csv is the uniform output
**Changed/Found:** Ran load_grid (post-flush). Findings from inspecting the actual server CSVs (all space-delimited; `readmatrix` parses them):
(1) **Column layout is mouse-specific.** AL_0033 input_params = 8 cols: `col2`=onset frame index (2866,3571,…), `col5/col6`=Kp/Ki (prototype map correct for AL_0033 only). AL_0034 = 7 cols: gains in `col2/col3`, `col4`=|ref|(=5), `col6`=0-based trial counter, **no onset-frame column** (my loader read col2 as onset → 0 → every trial skipped, Jmin=NaN; and read col5/col6 → got const 4 and the 0→204 trial counter = the "Ki 0–204" bug).
(2) **Output units differ per source.** Bare `pixel101730.mat` = raw F (~1322), not dF/F → `pick_trace` is unreliable. `mean_states.csv` = 1×300000, ≈1.0 → F/F0 ratio. **`states.csv` = 1×300000 exists for ALL sessions** (root, or `data/` for subdir sessions), starts at 0 → the regulated dF/F signal; `input_amps.csv` (1×300000) = control input. states/input_amps are the clean uniform source.
(3) Crash: empty `y` from mean_states (single-row, wrong column pick) hit `y(NaN)` in the align-check.
**Why:** The cache-first + fixed-column assumptions don't survive the heterogeneity. states.csv is logged by the controller for every session and avoids SVD/video entirely.
**Next:** Confirm with user: states.csv = regulated kernel-mean dF/F? its units (%/fraction) + sample rate (300000 samples = ? Hz)? AL_0034 onset source (no col)? Then rewire loader: y=states.csv, onset=input_params col2 (AL_0033) / detected from input_amps (AL_0034), per-mouse column map. HOLD further runs until confirmed.

### 2026-06-26 — Split load_grid.m helpers into ct_process_set.m (fix stale-cache gotcha)
**Changed/Found:** A user run executed a TWO-revisions-old `load_grid.m` (showed `e1`+`initialize_data`+9-arg `process_set`, none of which are in the current file) → MATLAB had cached the script + its local functions on first run and never reloaded after edits (the documented local-functions-in-a-script caching trap). Moved `process_set`/`ct_load_session`/`pick_trace` into new function file `controller-tuning/ct_process_set.m`; `load_grid.m` is now a plain script (no local functions) → re-parsed every run, so registry/knob edits always take effect. Added `addpath('controller-tuning')` so the helper resolves. Both files `check_matlab_code` clean.
**Why:** Prevent recurrence of running stale code after editing the registry (the common edit), which had silently re-triggered the old initialize_data face-decode path.
**Next:** User must flush the cached copy ONCE — `clear all; rehash;` (or restart MATLAB) — then re-run `load_grid.m`. After that, registry edits no longer need a manual clear (only edits to ct_process_set.m would).

### 2026-06-26 — Rewrite load_grid.m motion-free + cache-first with per-session source tags
**Changed/Found:** Replaced `controller-tuning/load_grid.m`'s `initialize_data → getpixel_dFoF` path with a self-contained `ct_load_session()` that reads only what each session has — no `initialize_data` (kills the face.mp4 decode), no `loadData` (avoids its hard root-only `input_params/states/input_amps` reads), no SVD. Each registry row now tags `ip` (root|`data/`), `gain` (input_params 5:6 | `Kr.npy`), `ysrc` (`pixel*.mat` cache | `mean_states.csv`), `status`. `pick_trace()` tolerates unknown cache variable names; gains read from Kr.npy for 10-18; AL_0034 10-25 e2 marked `hold` (no output source found). Per-session diagnostic + `align-check` print on every run; `r_grid` defaults 0. Output schema (G/A) unchanged so gain_grid/auto_tune are unaffected. `check_matlab_code` clean.
**Why:** User stopped the original run (too slow) and noted data is saved haphazardly. Confirmed via server scan: motion decode + root-only loadData reads were the slowness/load-error causes; mean_states.csv = kernel-mean dF/F, Kr.npy = per-trial (Kp,Ki) (both per user). Cache-first reuse chosen as the dF/F source.
**Next:** User runs `load_grid.m`; check the `align-check` line (onset units → set `ONSET_OFF` if off) and that `pick_trace` picks the kernel dF/F from each bare cache. Resolve 10-25 e2 source or drop. Then `gain_grid`/`auto_tune`. Not committed.

### 2026-06-26 — Diagnose controller-tuning load slowness + heterogeneous session layouts
**Changed/Found:** `controller-tuning/load_grid.m` was slow because it calls `initialize_data`, which decodes the **entire `face.mp4`** via VideoReader whenever `motEngF.npy` is absent (utils/initialize_data.m:36–46) — and a server file-capability scan shows NONE of the 9 sessions have `motEngF.npy`, while 5 have `face.mp4` (03-03, 03-05, 03-17, 12-19, 01-10) → every one triggers the multi-minute decode (same stall logged 2026-06-18). Second cost: `getpixel_dFoF` mode=1 reconstructs from `blue/svdSpatialComponents.npy` (nSV=2000) over the share when no pixel cache. Layout is inconsistent: `input_params.csv` sits in a `data/` subfolder for 10-17/30 + 01-10/2 (loadData reads root → load error; loadData.m:4 has the commented `/data/` alt), `blue/` SVD exists at root ONLY for the three 2025-03 sessions, `mean_states.csv` only for 10-18 + 10-25/1, `Kr.npy` for 10-18 + 10-25/1 + 12-19. AL_0034 10-18 has `input_params.csv` but "no input_amps" (gains likely in `Kr.npy`).
**Why:** Motion is not used by the cost analysis at all, so the video decode is pure waste; and there is no single uniform controller-output source across sessions — confirming the user's note that one procedure won't load all.
**Next:** Redesign loader to be motion-free + cache-first (reuse existing `pixel<MMDD><en>.mat`), read `input_params.csv` from root-or-`data/` per session, and tag each registry entry with its output-trace source. Pending from user: exact load-error text, and what `mean_states.csv` / `Kr.npy` contain. Do not modify utils/.

### 2026-06-26 — Build controller-tuning pipeline: load_grid + gain_grid + auto_tune
**Changed/Found:** `controller-tuning/load_grid.m` upgraded from stub to full loader (initialize_data → getpixel_dFoF per session, per-trial cost = `‖dF/F − ref‖₂` over 0–3 s, grouped by unique `input_params(:,5:6)`=(Kp,Ki), baseline gate `|dF/F(0)|≤2`, compact summary cached to `data/grid_tuning_cache.mat`). New `gain_grid.m` (Fig 1: J(Kp,Ki) filled-contour surface w/ marked minimum, 1D fallback if one gain fixed; Fig 2 supp: per-controller mean±std traces) and `auto_tune.m` (Kp/Ki trajectory + per-trial cost + running-best vs iteration). All three pass `check_matlab_code` clean. Exports PNG to `paper/images/tuning/`.
**Why:** User confirmed scope = two paper figures (grid cost surface + auto-tuning convergence), cost reference per-session, auto-tune is zero-order/model-free with gains logged in input_params cols 5:6. Modeled the loader→figure split on `load_sessions.m` + the controller-area figure scripts. Reference set explicitly per session in the registry (default −2 from prototype) because `initialize_data` does not populate `d.ref`.
**Next:** User to run `load_grid.m` then `gain_grid.m`/`auto_tune.m` from repo root. Verify on first run: (a) input_params cols 5:6 really are Kp,Ki for AL_0034 sessions, (b) per-session `ref` values (edit registry if not −2), (c) which SEL index gives the cleanest cost surface / convergence. Not yet committed.

### 2026-06-26 — Create controller-tuning subarea (gain grid + auto-tuning, secondary analysis)
**Changed/Found:** New `controller-tuning/CLAUDE.md` + `controller-tuning/load_grid.m`; added an area-detection row + disambiguation note in root `CLAUDE.md`. Registry holds 6 gain-grid sessions (AL_0034 2024-10-17/10-18 e1; AL_0033 2025-01-10 e2, 2025-03-03 e2, 2025-03-05 e1, 2025-03-17 e3) and 3 auto-tune sessions (AL_0034 2024-10-25 e1/e2; AL_0033 2024-12-19 e1). Prototype origin documented as `Grid_mouseDataAnalysis.m` + `scratch_grid/`.
**Why:** User wants the controller-gain grid + auto-tuning work formalized into paper-ready (but secondary, "how-to-tune") analysis, isolated from CL-vs-OL (controller-analysis), AL_0048 bilateral tuning, and AL_0046 photostim site-grid. AL_0034 is a new mouse not in the root locked-in list — scoped to tuning only.
**Next:** Resolved with user (cost ref = per-session, scope = 2 figures, auto-tune = zero-order/model-free, gains in input_params 5:6) — see the build entry above.

### 2026-06-24 — Consolidated contra_prediction.m to [CP-HEMI] only; chucked all other predictors
**Changed/Found:** Executed the consolidation (committed current HEMI state as `ea0582d` first for backup). `impulse-analysis/contra_prediction.m` 2042→**483 lines**: deleted [CP-FIT] OLS fit/CV, [CP-2] kernel viz, [CP-RRR] rank sweep, [CP-BLEED], [CP-BLEED-SD], [CP-IMP], [CP-4]. Kept Setup + Contra ROI + contra `redoSVD` (→ `V_c_full`) + a trimmed `[CP-STIM]` block (`all_starts_cp` + `pre_frames` only) + `[CP-HEMI]`. Removed dead knobs (`nSV_use`, `bleed_spont_ref`/`offset`) and the OLS predictor matrix (`X_cp`/`X_cp_z`/`X_cp_m`/`nPred_cp`/`U_svd_cp`), keeping only `nF_m`. `git rm impulse-analysis/contra_lds.m`. check_matlab_code: clean (only 2 pre-existing trivia — line-116 unreachable ROI branch, line-329 one-liner-END style). grep confirms no dangling refs (beta_cp/build_onset_artifact/best_sys/etc. appear only in one comment).
**Why:** User locked [CP-HEMI] as the single canonical contra→ipsi predictor and said chuck everything else; the bleed analysis will be rebuilt later on top of the faithful HEMI. `U_svd_raw` (contra spatial map) deliberately kept in the SVD cache for that future bleed work.
**Next:** One consolidation task remains — repoint `cp_residual_core` / `contra_residual.m` "Global" component to the [CP-HEMI] field→kernel readout (re-derives the state-dependence numbers). Then run [CP-HEMI] on AL_0033 to get inter-stim vs pre-stim pooled/primary R². Push.

### 2026-06-24 — DECISION: [CP-HEMI] is the canonical contra→ipsi predictor; consolidation plan
**Changed/Found:** No code yet. Inventoried every contra-prediction pipeline and chose ONE canonical predictor: **[CP-HEMI]** (whole-hemi RRR, 50 shared modes, field→kernel primary-pixel readout). Full list — predictors: [CP-FIT] instantaneous OLS single-pixel (`beta_cp`, ~200 modes, CV R²≈0.898; the residual engine is the same model via `utils/cp_residual_core.m`), [CP-HEMI] (pooled 0.925), [CP-RRR] single-pixel rank sweep, [CP-4] contra+TF composite, `contra_lds.m` dynamic LDS/ERA; diagnostics/processing: [CP-2] kernel viz, [CP-BLEED]/[CP-BLEED-SD] bleed map, [CP-IMP] bleed-negation residual; consumer: `contra_residual.m` (state-dependence) via `cp_residual_core`.
**Why:** User wants a single main predictor. Chose HEMI for Zhiwen-fidelity and because its 50 SHARED latents are the principled "global" subtractor — a 200-mode single-pixel OLS can absorb stim-bleed/local structure and deflate the local residual. Accepts single-pixel R²~0.85 vs 0.898.
**Next (consolidation, pending go-ahead — deletes are irreversible):** (1) chuck redundant predictors — remove [CP-RRR] + [CP-4] sections, delete `contra_lds.m`; (2) decide fate of bleed/decontam diagnostics ([CP-2]/[CP-BLEED]/[CP-BLEED-SD]/[CP-IMP]) — HEMI's shared map sidesteps bleed-absorption, so the decontam may be unneeded; (3) preserve `all_starts_cp`/`pre_frames` (HEMI needs them) if [CP-FIT] is trimmed; (4) REPOINT `cp_residual_core`'s Global component to the HEMI field→kernel readout → re-derives all state-dependence numbers. Commit current HEMI state first.

### 2026-06-23 — [CP-HEMI] lever-1: spontaneous windows = post-settle inter-stim gaps
**Changed/Found:** `impulse-analysis/contra_prediction.m` [CP-HEMI] — replaced the short pre-stim spontaneous windows with the FULL gap between consecutive stim starts: drop `h_settle_s=1.0 s` after each stim for settling, use the rest (`[onset_j + 1s , onset_{j+1})`; last stim → tail to `nF_m`). New knobs `h_win_mode` ('interstim' default | 'prestim' legacy) + `h_settle_s`. Train/test still contiguous early/late on the time-sorted frame set; `h_maxFrm` cap unchanged. Diagnostic now prints gap count, total spont frames + seconds, train/test counts. check_matlab_code: 0 new issues.
**Why:** Lever-1 toward Zhiwen's 0.99 — he regresses one long contiguous spontaneous recording; we had only sub-second pre-stim snippets, which chop the slow global fluctuations that carry bilateral coherence. Inter-stim gaps give far longer windows while still excluding each stim's response. Per user instruction ("increase trial length, take time between stim starts, remove 1 s for settling, use the rest"). Amp-0 gap-fill events inside a gap are no-laser → kept as spontaneous.
**Next:** Run [CP-HEMI] on AL_0033 (selExp=3) with `h_win_mode='interstim'` vs `'prestim'` and compare pooled R²@rank10 — longer window lifts toward 0.99 ⇒ gap was timebase fragmentation; stays ~0.925 ⇒ intrinsic prep coherence ceiling (consistent with the uniform per-pixel R²). Raise `h_settle_s` if 1 s is too short for the response.

### 2026-06-23 — Decided AGAINST region restriction as the 0.925→0.99 lever (variance-weighted R² reasoning)
**Changed/Found:** No code change. Reconsidered (user push-back) whether restricting the ipsi target to sensory cortex (Zhiwen's `select_area`) would close pooled 0.925→0.99. Conclusion: NO — region restriction is a non-lever for THIS data, and recommending it contradicted our own 2026-06-22 result. Reasoning: pooled `sseExplainedCal` = variance-weighted mean of per-pixel R² (`R²_pool = Σ_p var(y_p)·R²_p / Σ_p var(y_p)`). RRR gives unpredictable pixels ~0 contra-covariance → they get ŷ≈0 and do NOT distort the canonical latents or good-pixel predictions (user's point, correct). A meaningless-weight pixel dilutes pooled R² ONLY in proportion to the variance it carries (its residual = its full variance), so only high-variance-AND-unpredictable pixels hurt. Our 06-22 test shows those are absent: dark/vessel exclusion barely moved pooled (0.925→0.927) and per-pixel R² is spatially UNIFORM (median 0.924, p10 0.852, p90 0.952). So 0.925 is a genuine uniform per-pixel predictability ceiling = intrinsic bilateral coherence/SNR of this prep, not a region-inclusion artifact.
**Why:** User: bad-region weights are forced meaningless by the regression, so region inclusion shouldn't hurt R². Correct for the FIT; the only exception (high-variance unpredictable pixels) is empirically absent here. My earlier "sensory restriction is the main 0.925→0.99 lever" (entry below) was wrong and self-contradictory with the 06-22 vessel-exclusion null — recorded as a corrected dead-end.
**Next:** Real levers to approach 0.99, re-ranked: (1) FRAME SET — Zhiwen regresses one contiguous spontaneous recording; we concatenate short pre-stim windows, which can discard the slow global fluctuations carrying bilateral coherence → test pooled on a contiguous spontaneous block if the session has one. (2) Hemodynamic-correction parity. (3) redoSVD SNR/frame-count. (4) DEFINITIVE: run our CanonCor2/sseExplainedCal port on Zhiwen's OWN figshare session (positive control) — reproduces ~0.99 ⇒ gap is prep, not code. Region restriction dropped.

### 2026-06-23 — [CP-HEMI] primary-pixel = field→kernel-average; audited vs Zhiwen source
**Changed/Found:** `impulse-analysis/contra_prediction.m` [CP-HEMI] — replaced report number (3) (the old RAW primary-pixel R^2: a direct latent→y_full OLS with fitted intercept) with a **field-then-kernel-average** readout, and verified the rest of CP-HEMI against Zhiwen's actual source `YE-et-al-2023-spirals/spirals_mirror/preprocessing/getReducedRankRegressionHEMI.m`. Final (Zhiwen-faithful) implementation: reconstruct the primary pixel's full-resolution recording-kernel mean straight from the ipsi SVD (`h_meanU = mean(U_ipsi_raw(kernel rows))`, `h_meanU*V_ipsi`), then read it out through the SAME canonical latents (`a_kern = scores\kernel-mean`; CanonCor2 scores are orthonormal on train so `a_kern(1:n)` == the field's `a(:,1:n)` truncation → this is genuinely "predict field, average kernel pixels", not a free OLS). Crucially this does NOT enter the RRR fit, so pooled (1) + per-pixel map (2) are byte-identical to before (≈0.925). Kernel footprint taken from `getpixel_dFoF`/`load_experiments` (square `rows=px_prim±k, cols=py_prim±k`, `k=d.params.kernel`); the `[CP-BLEED]` line-~783 iy/ix comment has that convention flipped — followed the getpixel ground truth. `/mean(mimg_kernel)*100` kept for interpretable %dF/F but it cancels in R^2. (Superseded an intermediate union/force-include design from earlier this turn — never run.) Added `k_prim` capture in setup + standalone `loadData` fallback. check_matlab_code: 0 new issues (17 flags all pre-existing).
**Why:** User: "be as close to Zhiwen's implementation as possible." AUDIT RESULT — our core already matches his: predictor = `zscore` of the **re-SVD'd contra-hemisphere** modes (our `V_c_full` = `redoSVD(U_contra,V_cp)` == his `zscore(Vleft)` from `redoSVD(UselectedLeft,V)`); target = re-SVD'd ipsi pixel field (== his `Uright*Vright`); `CanonCor2(signal',regressor')`; contiguous early-train/late-test; pooled `sseExplainedCal`; train/test z-scored independently. Zhiwen computes ONLY the pooled area number — the single primary pixel is OUR addition, so the most faithful thing is to extract it from his exact field, which is what (3) now does. MATH NOTE (in code): readout+kernel-avg both linear ⇒ predict-field-then-average == predict-the-kernel-mean ⇒ lands near the single-pixel ceiling (~0.85), NOT pooled 0.925; the 0.925 is a variance-weighted-pooling effect.
**Next:** Two real divergences from Zhiwen remain, both judgment calls for the user: (a) **sensory-area Allen restriction** — he `select_area(sensoryArea)` after atlas `imwarp`; we use the whole hand-drawn hemisphere + complement (no tform for AL_0033). This is the likely main driver of 0.925<0.99 (we pool over non-sensory/edge/vascular pixels he excludes); approximate with a tighter hand-drawn cortical-core ROI if pursued. (b) **frame set** — he uses a contiguous spontaneous recording (1:40000/40001:60000); we concatenate pre-stim windows (stim paradigm). Then run on AL_0033 (selExp=3; ipsi cache `impulse-analysis/data/AL_0033cp01291_svdraw_ipsi.mat` present) and report new (3) R^2@rank10.

### 2026-06-23 — [CP-MOTION-AMP] Local view confirms the motion dissociation per amplitude
**Changed/Found:** Ran the new contra_residual `[CP-MOTION-AMP]` section with amp_sig='Local' (R.res_imp) on AL_0033 → `cp_motion_amp_local.png`. Direct contrast with Actual (`cp_motion_amp_actual.png`): Actual per-amp Pearson r all slightly NEGATIVE (high motion→low deviation, red dots on floor, y 0–12); LOCAL r MIXED/slightly POSITIVE (−0.04,−0.13,+0.17,−0.09,−0.05,+0.02,+0.22,−0.12,+0.33; y collapses to 0–1.5 since residual ≈21% of dip). At strong amps (3.70 V r=+0.22, 4.90 V r=+0.33) high-motion trials are among the LARGEST local deviations (biggest 4.90 V local dev ≈1.5 is a high-motion trial, z≈8.5). Removing contra-predicted global activity ABOLISHES the motion→predictability pattern — per-amplitude visual confirmation of CP-LOCAL/fig-6: motion buys predictability in the GLOBAL signal, none in the LOCAL stim response (if anything inverts at high amp).
**Why:** Continued the [CP-MOTION-AMP] thread (amp_sig switch) to view the isolated-local per-amplitude breakdown.
**Next:** none (confirmatory). actual-vs-local panels = single-glance demo of the global-vs-local motion dissociation.

### 2026-06-23 — cl_ol_single_session.m: robust per-trial onset correction (snap-or-fallback)
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — replaced the re-detection block's two branches (blind `numel(det)==nTr` 1:1 chronological assignment, and `det(1)-stimStarts(1)` constant-shift fallback) with a new `correctOnsets(ss0, det, SNAP_TOL)` helper. It estimates the findStims global offset robustly (median of each detected edge's nearest-neighbour residual to findStims onsets), shifts findStims by it, then snaps each trial to its nearest detected edge within `SNAP_TOL=0.5 s`, else keeps the offset-corrected findStims onset. Always returns one onset per `input_params` trial; prints snapped-vs-fallback counts.
**Why:** The old `numel(det)==nTr` path mis-assigns trials *silently* if detection misses one real edge and adds one spurious edge (count still matches but all later trials shift onto the wrong onset) — a real risk with oscillating CL controller output crossing `IN_THRESH` past the 2 s refractory. The `det(1)`-based offset was also poisoned by a single spurious first edge. The new method is robust to a minority of bad edges and degrades gracefully per trial. Verified faithful to canonical windowing/MSE (`load_sessions.m`: n_pre=105, n_win=210, n_err=105, `norm(seg-ref)`).
**Next:** Re-run on AL_0048 2026-06-20 exp 2; check the console "%d snapped / %d kept fallback" line — if many fall back, detection threshold/refractory or single-channel selection needs revisiting.

### 2026-06-23 — cl_ol_single_session.m: fix ksdensity NaN crash in MSE half-violin
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — Fig 3 now filters `er_nc`/`er_wc` to finite values (`e_nc = er_nc(isfinite(er_nc))`) before `ksdensity` and the mean-marker plot. `sliceTrials` leaves `NaN` for trials whose pre(3 s)/post(6 s) window runs off the recording edge; `ksdensity` does not drop NaN, so any off-edge trial errored out the violin.
**Why:** Off-edge trials are likely near recording start/end; the raw-vector `ksdensity(er_nc)` call would throw on NaN, killing Fig 3 silently after the traces/variance figures rendered.
**Next:** none — mean/median console stats already used `'omitnan'`, so only the violin needed the guard.

### 2026-06-23 — cl_ol_single_session.m: apply onset re-detection on every run (fix stale-cache)
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — moved the `REDETECT_STIMS` onset override OUT of the recompute branch to run after the load/assemble block on every execution. Bug: with `r_load=1` the cached `d` (saved with the offset findStims onsets) was loaded and the override never ran, so the debug full-trace figure still showed the old/early stimStarts. Re-detection works on the cached `d` too (inpVals638/594 + inpTime are cached).
**Why:** User: "figure2 debug did not make the stim start change I asked for" — the corrected onsets weren't applied on cached loads.
**Next:** Re-run (cache is fine now); the full-trace red lines should sit on the input onsets. Cache still stores the raw findStims onsets; the override re-applies each run, so no need to clear it.

### 2026-06-23 — cl_ol_single_session.m: bake in debug figures (brain overlay, full trace, example trials)
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — added a `DEBUG` block (knobs `DEBUG`, `N_EX`, `RUN_ANALYSIS`): (1) brain mean-image (`d.svd.mimg`) with each controlled pixel marked + kernel box, to check pixel/kernel; (2) full-experiment dF + combined input (`combinedInput`) + corrected `stimStarts` verticals per site (`drawStimVerticals`); (3) `plotExampleTrials` — N_EX evenly-spaced OL and CL example trials, dF on the left axis with the onset-windowed input on the right axis (yyaxis), ref + stim markers. `RUN_ANALYSIS=false` stops after debug (skips the CL/OL analysis figures via `continue`).
**Why:** User sees no stim-evoked deflection in the cut trials although the full-trace debug shows a clear effect — need to verify, before controller analysis, whether the trial windows are aligned (residual stimStarts issue) and whether the pixel/kernel is correct. Example trials + input overlay test the cut; the brain overlay tests the pixel.
**Next:** Run with `RUN_ANALYSIS=false` first; inspect the brain overlay (right pixel?), example trials (does dF deflect after the input pulse at t=0?). If examples are flat but the full trace deflects, the onset count likely didn't match trials (constant-shift path) or the input pulse sits off-window — report the console `Re-detected N onsets` vs trial count.

### 2026-06-23 — cl_ol_single_session.m: override findStims onsets via input re-detection
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — after `initialize_data`, when `REDETECT_STIMS` (new knob, default true) re-derive stim onsets from the input command (`detectStimsFromInput` helper: rising edge > `IN_THRESH` on combined `IN_FIELDS`={inpVals638,inpVals594}, `MIN_GAP`-second refractory) and override `d.stimStarts`/`d.stimEnds`, fixing the findStims mode-1 ~37 s buffer offset. If detected count == trial count, replace directly (preserves per-trial order → OL/CL flags stay aligned, `stimEnds = onset + dur`); else apply a constant shift `det(1)-stimStarts(1)` to the findStims onsets and warn. Corrected onsets are cached with the assembled session.
**Why:** User confirmed on the debug plot that the input-re-detected onsets land on the real command edges (79.44 s) while findStims onsets are ~37 s early; asked to bake the fix into the main analysis script. `utils/findStims.m` is do-not-modify, so the override lives in the analysis script.
**Next:** Run CL/OL analysis with `r_load=0` (or delete the clol cache) so onsets are recomputed; confirm the re-detected count matches the trial count (else it constant-shifts). If OL trials carry no input pulse, revisit whether detection captures all trials.

### 2026-06-23 — FOUND: stimStarts offset ~37 s — findStims mode-1 horizon subtraction (stale zero-buffer)
**Changed/Found:** AL_0048 2026-06-20 controller session: `d.stimStarts(1)=42.41 s` but the real first input (d.inpVals638/594 rising edge) is at 79.44 s. Root cause: `initialize_data` calls `findStims(d,1)`; mode 1 computes `t(input_params(:,2) - horizon)` with `horizon=40*35=1400` samples (~40 s), which assumes a leading zero-buffer the file no longer has → all onsets land ~37–40 s early. `utils/findStims.m` is do-not-modify. Added a re-detection overlay to `bilateral-analysis/debug_stimstarts.m` (knobs `SHOW_DETECTED`/`IN_THRESH`/`MIN_GAP`): builds a combined input from IN_FIELDS, takes rising edges > IN_THRESH with a MIN_GAP refractory, draws them magenta, and prints first-onset offset vs stimStarts(1).
**Why:** Confirm visually that input-derived onsets (mode-0 style) align with the command/dF while the mode-1 onsets are offset, before overriding stimStarts in the analysis.
**Next:** If the magenta onsets sit on the input edges, add the same rising-edge detection to `cl_ol_single_session.m` to override `d.stimStarts`/`d.stimEnds` after `initialize_data` (do not edit findStims). Then re-run CL/OL analysis.

### 2026-06-23 — debug_stimstarts.m: plot d.inpVals638 / d.inpVals594 fields directly
**Changed/Found:** `bilateral-analysis/debug_stimstarts.m` — `inpVals638`/`inpVals594` are literal struct FIELD names, not column indices into `d.inpVals`. Replaced the `IN_CH=[638 594]` matrix-indexing logic with `IN_FIELDS = {'inpVals638','inpVals594'}` and plot `d.(fn)` directly against `d.inpTime` (length-guarded, skip+warn if a field is absent). Removed the inpVals orientation/`iv` block.
**Why:** Earlier versions misread the request as `d.inpVals(:,638)`; the data actually has separate fields `d.inpVals638` and `d.inpVals594`.
**Next:** Run on the loaded session; check `stimStarts` alignment against the two input field traces.

### 2026-06-23 — debug_stimstarts.m: collapse to single axes, overlay input channels 638 & 594
**Changed/Found:** `bilateral-analysis/debug_stimstarts.m` — removed the per-site/input subplot layout (and the second zoom figure); now one axes overlaying all dF/F site trace(s) plus selected `d.inpVals` channels (knob `IN_CH = [638 594]`) against `d.inpTime`, with `stimStarts`/`stimEnds` verticals (HandleVisibility off so they stay out of the legend) and a legend. `d.inpVals` is oriented to [time × channels] by matching `inpTime` length; out-of-range channels are skipped with a console note. `ZOOM_S` now just sets xlim on the single axes. Dropped unused `padlim`/`ternary` helpers; fixed a half-edited `plot(axI, it, d.,...)` line.
**Why:** User asked to keep it simple — one plot, no subplots, overlaying input channels 638 and 594 directly with the dF and stim lines for a single-glance onset-alignment check.
**Next:** Run on the loaded session; compare whether `stimStarts` lines sit on the inpVals(:,638/594) command edges vs the dF deflections to localise the onset-timing bug.

### 2026-06-23 — debug_stimstarts.m: add laser-input panel (d.inpVals vs d.inpTime)
**Changed/Found:** `bilateral-analysis/debug_stimstarts.m` — added an input-trace subplot below the per-site dF panels (main + zoom figures): plots `d.inpVals` against `d.inpTime` with the same `stimStarts`/`stimEnds` vertical lines, length-guarded to `min(numel(inpVals),numel(inpTime))`. Added console diagnostic for inpTime (samples/range/dt/Fs). Factored the vertical-line drawing into `drawStimLines` and ylim padding into `padlim` (used by all panels).
**Why:** User asked to also see the command input on the debug plot — comparing `stimStarts` alignment against the laser input onsets (high-rate inpTime) is a more direct check of onset timing than the dF deflection alone.
**Next:** Run on the loaded session; if the red lines align with the input-command edges but not the dF deflections (or vice versa), that localises whether the offset is in stim detection vs the dF time base.

### 2026-06-23 — Add debug_stimstarts.m: overlay stimStarts on whole-experiment dF trace
**Changed/Found:** `bilateral-analysis/debug_stimstarts.m` (new) — debug plot for misaligned stim onsets. Uses the `d` already in the workspace; reconstructs whole-experiment dF/F at each `d.params.pixel` site (SVD) and overlays `d.stimStarts` as red verticals (+ `d.stimEnds` green dashed) on `d.timeBlue`, one subplot per site. Prints diagnostics: timeBlue range/dt/Fs, stimStarts count/range, a heuristic flag if stimStarts look like sample indices rather than times, a warning if any onset falls outside the timeBlue range, and the median inter-stim interval. Optional `ZOOM_S` second figure.
**Why:** User reports stimStarts are not properly set in the AL_0048 controller session; needed a quick visual to see whether onset markers line up with the stim-evoked deflections, which determines whether the bug is in `findStims`/units (times vs indices) vs trial selection.
**Next:** Run it on the loaded session; if red lines don't sit on the dF deflections, inspect `findStims` mode / stimStarts units. Likely related to the `[CP]`/controller stim-detection mode for AL_0048 (initialize_data uses findStims mode 1).

### 2026-06-23 — cl_ol_single_session.m: source controlled pixel from d.params (not input_params)
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — restructured pixel sourcing per user: the controlled pixel(s) are read from `d.params.pixel` (actual image pixels, Nsite×2 [x y]) with side/reference from `d.params.pixel_positions_mm` (mm from bregma; x<0 → left/excitatory/REF_L, x>0 → right/inhibitory/REF_R). Removed the `input_params` cols-15/16 path, the `HEMI_COL`/`SIGN_L` hemisphere filter, and the `resolvePixel` helper. Sides are now one per `d.params.pixel` row; OL/CL split still via `input_params(:,OLCL_COL)`; trial→site defaults to all-trials-per-site (single-site case) with optional `TRIAL_SITE_COL` for multi-site tagging (warns if >1 site and untagged). dF/F still reconstructed via `pixelDFoF` (SVD) at the d.params pixel.
**Why:** First two pixel-sourcing attempts failed — `input_params(:,15:16)` came back all-zero for the hemisphere-masked subset (open-loop trials carry no pixel and/or col 4 was not hemisphere). User confirmed `d.params.pixel` / `d.params.pixel_positions_mm` hold the correct pixel + bregma-mm info directly, matching how the classic `load_sessions.m` pipeline used `d.params.pixel`.
**Next:** Rerun on rig; confirm `d.params.pixel` row count/order matches the controlled site(s) and that mm-x sign gives the right side→ref. If a multi-site session interleaves per-site trials, set `TRIAL_SITE_COL`. Still verify `OLCL_COL` (default 3) from the printed input_params column diagnostic.

### 2026-06-22 — [CP-HEMI] RESULT: whole-hemi prediction plateaus at 0.925 (vs Zhiwen 0.99)
**Changed/Found:** Ran [CP-HEMI] on AL_0033 (selExp=3) via MATLAB MCP. Built ipsi SVD (`AL_0033cp01291_svdraw_ipsi.mat`, ~7 min redoSVD). Results: POOLED ipsi-hemi R²=**0.925@rank10, 0.931 peak@rank50**; RAW primary-pixel R²=0.856@rank10/0.861 (vs single-pixel 200-mode OLS 0.899); per-pixel median 0.924 (p10 0.852, p90 0.952). Rank curve reproduces Zhiwen Fig-3C shape (plateau by rank~10). Exported `paper/cp_hemi_rank.png` + `cp_hemi_r2map.png`. Also fixed legend bug in source (`ItemTokenSize` must be set on the legend object, not passed as name-value). Confirmed data loads "corrected temporal components" (hemo-corrected → parity OK).
**Why:** Gate `2026-06-22.8` — is our contra→ipsi predictor as good as Zhiwen's ~0.99? Answer: METHOD faithfully replicated (CanonCor2 RRR, 50 modes, whole-hemi target, sseExplainedCal), but pooled R²=0.93, ~0.065 short of 0.99. Tested vessel exclusion (dark-pixel >p40/>p60 cut): pooled barely moves (0.925→0.927) and per-pixel R² is uniform → the gap is NOT a vasculature/edge artifact, it's intrinsic bilateral-coherence/SNR of this prep. Slightly below the gate's 0.95 floor.
**Next:** Definitive validation = run our CanonCor2/sseExplainedCal port on Zhiwen's OWN session as positive control (needs his figshare data) to prove gap is data-not-code. Optional: tighter cortical-core ROI (away from window edge/sinus). Decide with Nick whether 0.93 suffices to proceed to the residual/state-dependence analysis.

### 2026-06-22 — cl_ol_single_session.m: robust pixel resolution (fix out-of-frame index error)
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — `pixelDFoF` threw `Index in position 1 is invalid` (`U(y-k:y+k,...)` with `y-k<1`) because the plain `mode()` over a side's `input_params(:,15:16)` picked up placeholder rows (OL/non-targeted trials carry pixel `[0 0]`), giving `y≈0`. Added helper `resolvePixel` that drops non-positive/non-finite coord rows, takes the modal valid (x,y), validates it sits inside the SVD frame with a kernel margin, auto-swaps x↔y if only the transpose is in-frame, and errors with the side's unique pairs + frame size if neither fits. Frame size detected from `d.svd.mimg`/`U`; added a bounds guard inside `pixelDFoF`; richer per-side console diagnostic.
**Why:** First rig run failed on the controlled-pixel reconstruction; the mode was contaminated by zero-placeholder trials. Filtering to in-frame coords recovers the true per-side controlled pixel, and the clear error path distinguishes the "cols 15/16 are mm/downsampled, not pixels" case.
**Next:** Rerun on rig; if it still errors, read the printed unique (x,y) pairs + `SVD frame HxW` to confirm cols 15/16 are raw image pixels in [x=col, y=row] (vs mm/downsampled) and adjust `PIX_X_COL`/`PIX_Y_COL` or add a mm→pixel conversion.

### 2026-06-22 — Add [CP-HEMI] whole-hemisphere RRR; faithful Zhiwen/Ye replication
**Changed/Found:** New `%% [CP-HEMI]` section in `impulse-analysis/contra_prediction.m` (inserted before [CP-BLEED]) + vendored `utils/CanonCor2.m` (cell variant) and `utils/sseExplainedCal.m` from Ye et al. 2023 `spirals_mirror`. Faithful port of his `getReducedRankRegressionHEMI.m`: predict the WHOLE ipsi-hemisphere pixel field from h_K=50 z-scored contra SVD modes via reduced-rank regression (CanonCor2), instantaneous, contiguous early-train/late-test split, scored with sseExplainedCal. Outputs (1) pooled ipsi-hemi R² vs rank (Zhiwen-comparable, target ~0.99), (2) per-pixel R² map, (3) RAW primary-pixel R² vs rank through the same shared latents. Exports `paper/cp_hemi_rank.png` + `cp_hemi_r2map.png`. check_matlab_code clean on all three (no new warnings in contra_prediction.m).
**Why:** Meeting action `2026-06-22.8` — validate predictor reaches Zhiwen's ~0.95–0.99 at rank ~10 before extending residual/state-dependence. Read his source (repo one folder up): his 0.99 is POOLED variance-explained over the whole right hemisphere (all sensory pixels × time), NOT a single pixel. Our existing single-pixel CV R²≈0.90 (CP-FIT) is a different, harder quantity and structurally cannot reach 0.99 — confirmed by his code, not just suspected. CP-HEMI reproduces his target/metric so the comparison is honest; primary pixel falls out as one column.
**Next:** Run `load_experiments.m` → `contra_prediction.m` setup → [CP-FIT] → [CP-HEMI] (selExp=3; ipsi redoSVD is new → first run builds `_svdraw_ipsi.mat` cache, needs server data). Report pooled R²@rank10 vs Zhiwen 0.99. Parity to confirm: AL_0033 SVD hemodynamically corrected? No Allen-atlas sensory restriction (using hemisphere polygon + complement) — check if it materially lowers pooled R².

### 2026-06-22 — cl_ol_single_session.m: source pixel from input_params cols 15/16 (drop pixel cache)
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` — replaced the `*_bil_pix_*` pixel-cache dependency with per-trial controlled-pixel coords read from `input_params` (x = col 15, y = col 16, knobs `PIX_X_COL`/`PIX_Y_COL`). Trials are grouped into sides by `HEMI_COL`; each side's controlled pixel = modal (x,y) over its trials; dF/F is reconstructed from the session SVD at that pixel via new helper `pixelDFoF` (mirrors `getpixel_dFoF` mode-1 SVD math: `F = mean(U_kernel)*V`, `dFk = F/mean(mimg_kernel)*100`), falling back to `getpixel_dFoF(d,1,px,0)` if `d.svd` absent. Side trial-mask now stored on `sides` and reused in the analysis loop (fixes the `'all'` no-filter case that previously fell through to the right-hemisphere branch).
**Why:** No exp-2 pixel cache exists (`AL_0048_bil_pix_06202.mat` missing); user confirmed the controlled pixel is in `input_params` cols 15/16, so the script can rebuild each side's dF/F directly and needs no pre-made pixel cache.
**Next:** Run on the rig; verify cols 15/16 are image x/y in the same convention `getpixel_dFoF` expects ([x=col, y=row]) and that `d.svd` is populated (else it re-reads server SVD per pixel). Confirm `OLCL_COL`/`HEMI_COL` from the printed input_params diagnostic.

### 2026-06-22 — Add standalone single-session CL-vs-OL script for AL_0048
**Changed/Found:** `bilateral-analysis/cl_ol_single_session.m` (new) — self-contained single-session controller analysis (open-loop vs closed-loop) for one AL_0048 bilateral session (default AL_0048 / 2026-06-20 / exp 2). Loops over hemispheres present in the `*_bil_pix_*` pixel cache, filters trials by hemisphere (`HEMI_COL`) then OL/CL flag (`OLCL_COL`, default col 3 = 0/1 like classic `controllerData`), windows each trial (−3 s..dur+3 s), computes per-trial MSE = `norm(seg − ref)` over 0..dur, and draws per-side: all-trials+mean (OL|CL), variance-over-time (OL vs CL), MSE half-violin (OL vs CL) + console OL/CL ratio. Reuses `paperFig/paperStyle/addStimPatch/paperLegend`.
**Why:** User converted the old single-session CL/OL script into the multi-session bilateral pipeline (`load_bilateral.m`+`cl_constant_ref.m`/`ol_characterization.m`) and wanted a standalone script to analyse just the new AL_0048 controller session. Per-side refs are signed (REF_L +5 excit, REF_R −5 inhib); classic `input_params(:,3)` 0/1 flag is used but flagged as VERIFY because the bilateral column map lists col 3 = trial_type — script prints unique values per input_params column on load so the correct flag column can be confirmed.
**Next:** Run on the rig (needs `initialize_data` server access); confirm from the printed input_params diagnostic that col 3 is the OL/CL flag and col 4 the hemisphere — adjust `OLCL_COL`/`HEMI_COL` knobs if not. Note pixel cache present is `06201` (exp 1); an exp-2 pixel cache must be generated first.

### 2026-06-22 — Write Methods for low-frequency disturbance attribution; lock spectral-analysis plan
**Changed/Found:** `Closedloop_edit/methods.tex` — added `\subsection{Disturbance attribution analysis}` (per-trial RMS error t=+1→+3 s, z-scored, top-quartile = high-error; motion regression + z-motion>1.5 exclusion; low-frequency = absolute 1–4 Hz pre-stim power on motion-clean trials only) + supplementary placeholder figure `fig:lowfreq_examples`. `Closedloop_edit/results.tex` — rewrote the Fig-4 spectral `\todo` to the locked plan and switched all band terminology from "2–4 Hz" to **low-frequency (1–4 Hz)**.
**Why:** User decisions: band = 1–4 Hz called "low frequency" everywhere; high-error = top quartile; spectral analysis on motion-clean trials only (motion handled by exclusion, not co-regression); representative examples → supplementary, user to pick. Methods can be written now since procedural; numbers pending the analysis run.
**Next:** Run the attribution on cached `ncFreqPow`/`wcFreqPow` (motion-clean, top-quartile CL) → report fraction of high-error CL trials with elevated low-freq power; AL to select the two representative example trials for Fig S.

### 2026-06-22 — Add [CP-MOTION-AMP] section to contra_residual.m (figure-3 per-amplitude, large fonts)
**Changed/Found:** New `%% [CP-MOTION-AMP]` section in `contra_residual.m` + helper `utils/cp_motion_amp.m` — reproduces motion_analysis fig 3 (per-amplitude motion vs |dip dev|, one subplot per amp, tertile-coloured) inside the residual workbench with LARGER fonts (title 12 / labels 11 / ticks 10 / sgtitle 14, vs paper 6pt). `amp_sig` switch: 'Actual' (=fig 3, default) | 'Global' (contra pred) | 'Local' (residual). check_matlab_code clean on both files. NOTE: uses `imp.mot` (=R.mot, the covariate every other section uses) for internal consistency, NOT motion_analysis's motTrace[-1,0.5] mean → per-panel Pearson r differs (here −0.08..+0.09, mixed) from fig 3's −0.07..−0.18. Visual pattern identical though: high-motion (red) dots hug the floor at every amplitude, tall high-deviation column sits at motion≈0. Continuous r is a weak summary of this THRESHOLD/tail effect regardless of method (median split is the decisive test, fig 6: 0.59 vs 1.23 p=9e-11). Exports cp_motion_amp_<sig>.png.
**Why:** User asked to make fig-3 text bigger and keep it in contra_residual.m as a runnable section.
**Next:** Run amp_sig='Local' to get the per-amplitude breakdown of the isolated stim effect (expected motion-null). If exact fig-3 reproduction needed, swap imp.mot → mean(motTrace[-1,0.5]).

### 2026-06-22 — Reproduced motion_analysis fig 6 (ipsi peakdev, pooled): motion→predictability is a strong THRESHOLD effect
**Changed/Found:** Reproduced motion_analysis.m "figure 6" with dev_metric=peakdev (ipsi-only |Peak_imp−mean|, pooled 3 sessions, n=1467) to show the actual-ipsi motion→predictability result. Motion (z>0.5) trials n=111 vs No-motion n=1356: **median |Peak dev| No-motion 1.234 vs Motion 0.588 (ranksum p=9e-11)** — motion HALVES the deviation; the high-deviation tail (|Peak dev| 4–14) is essentially all No-motion. Continuous Spearman only −0.064 (p=0.015) because motion is zero-inflated (1356/1467 near-zero) → it's a THRESHOLD/tail effect, not graded ("with motion, no excessively bad prediction"). Exported `paper/images/figure2/fig6_motion_peakdev_pooled.png`.
**Why:** User asked to see figure 6 to reconcile it with the residual-stream "motion null." Quantifies the ACTUAL-level effect (vs the residual-level null).
**Conclusion:** Consistent with [CP-LOCAL]: this strong motion→predictability is the ACTUAL/GLOBAL signal; splitting via contra puts it all in the Global component, LOCAL residual is null. Fig 6 and the local null are two ends of one story (motion = low-variance global state contra reads out).
**Next:** none (explanatory). Note script default dev_metric=cperr needs contra_prediction first; peakdev is the ipsi-only route.

### 2026-06-22 — explore5.m brain map: working amp picker (both levels) on the map figure
**Changed/Found:** `explore/explore5.m` — reworked the brain-map GUI so amplitude is selected by clickable buttons on the brain-map figure itself. Fixes from the prior version: (1) the amp picker now always builds ONE BUTTON PER AMPLITUDE LEVEL PRESENT (`sel_amps = amp_lvls_m`), so with 2 levels you actually get two buttons to choose between — the earlier `SEL_AMPS=1` gate left only one. (2) Buttons moved onto the brain-map figure (`fig_map`) instead of the side-picker window, and `renderBrainMap` now redraws the map axes IN PLACE (`cla(ax_map)`) instead of close+reopen, so the buttons no longer get buried by a fresh figure each click. Side is still chosen by clicking a marker in the small side-picker window; shared state lives in `fig_pick` UserData. Removed the now-unused `SEL_AMPS` param and `resolveAmps()` helper. Analyzer clean.
**Why:** User reported the amp picker was wrong — it couldn't select between the two amp levels (only one button) and the controls weren't on the brain-map GUI they were looking at.
**Next:** Run on the real session to confirm both amp buttons appear and switch the map in place (couldn't test the GUI live — MATLAB base workspace was empty). If the user wants side selection on the map figure too, add LEFT/RIGHT buttons next to the amp buttons.

### 2026-06-22 — explore5.m brain map: clickable amplitude selector buttons in the GUI
**Changed/Found:** `explore/explore5.m` — replaced the click-to-cycle amplitude behaviour in the brain-map picker with explicit on-GUI controls: stim SIDE is chosen by clicking a recording-pixel marker, amplitude LEVEL by clicking one of a row of `uicontrol` pushbuttons (one per `sel_amps`, active one highlighted yellow). Split the monolithic callback into `bmSideCb` (side), `bmAmpCb` (amp), and a shared `renderBrainMap` that resolves the condition for the current (side, amp) and redraws. Picker now uses an explicit `ax_pick` axes (stored in UserData) so button clicks don't disturb axis state. Also factored SEL_AMPS resolution into a local `resolveAmps()` function — this removes the recurring `%#ok<UNRCH>` lint churn (a function argument can't be constant-folded, so flipping the SEL_AMPS default no longer moves a dead branch). Analyzer clean (only pre-existing style notes).
**Why:** User asked for a clickable amp selector on the GUI for the brain-map section instead of cycling amplitudes by repeated marker clicks.
**Next:** Run on the real session to confirm the amp buttons render and switch maps correctly (couldn't test the GUI live — MATLAB base workspace was empty). Pixel-viewer section still uses the param-based `SEL_AMPS` filter; leave unless the user wants buttons there too.

### 2026-06-22 — explore5.m: explicit condition selectors (side × amp) for pixel viewer + brain map
**Changed/Found:** `explore/explore5.m` — replaced the implicit/pooled selection in the pixel-viewer and brain-map sections with explicit condition parameters, mirroring explore.m's `STIM_SIDES` pattern. Pixel viewer: `SEL_SIDES` ({'left'}|{'right'}|both) + `SEL_AMPS` ('all'|numeric vector) filter which trials build the viewer. Brain map: `SEL_SIDES` + `SEL_AMPS` ('all'|'max'|'min'|numeric) build a condition list = sides × amps; trial averages precomputed per condition; the click-picker now cycles a clicked side's amplitudes (auto-shows the first condition immediately). Renamed callback brainMapPickerCb3→Cb5; sites_bm→conds. Two UNRCH lint warnings are false positives from constant-folding the default param literals (numeric branches go live when SEL_AMPS is a vector) — suppressed with `%#ok<UNRCH>`. Analyzer otherwise clean.
**Why:** User asked to be able to specifically select the condition (left/right and which amp level) for both the pixel viewer and the brain map, and compute trial averages from the picked condition — the prior code pooled sides/amps (brain map only toggled side via click).
**Next:** Run on the real session to verify the picker cycles amps correctly and per-condition trial counts are non-empty; this also isolates the earlier "right region looks flat" issue — select right side at each amp separately to see whether flatness is amplitude-specific or a sparse-trial artifact (couldn't inspect live: MATLAB base workspace was empty).

### 2026-06-22 — Made explore5.m amplitude-aware for two-spot 2-amplitude experiment
**Changed/Found:** `explore/explore5.m` — added amplitude (input_params col 5) as a separation dimension throughout. (1) Top section prints a stim-side × amplitude trial-count breakdown to confirm the data was read correctly. (2) Trial plots expanded from 2×2 (side × pixel) to (side × amp) × pixel rows, general over nAmp, amplitude encoded by colour shade. (3) Pixel-viewer event labels now `stim <SIDE> a<amp>` so pixelTuningCurveViewerSVD splits conditions by side × amp. (4) Brain map gained an `AMP_SEL` param ('max'|'min'|'all'|numeric, default 'max') that amplitude-masks both sites; title carries the chosen amp. Fixed stale header (said explore3.m). Code Analyzer clean (only pre-existing style notes).
**Why:** User flagged that the target session stimulates two spots (L/R) at 1 s steps with 2 amplitude levels; the two-spot variants (explore3/4/5) had dropped the amplitude handling present in canonical explore.m and were silently pooling both amplitude levels in every plot/map.
**Next:** Run on the real session (set mn/td/en at top) to confirm col-5 amplitudes and per-condition trial counts look right; if the rig encodes amplitude in a different column for this session, update the col-5 reads.

### 2026-06-20 — Decided AGAINST widening inhibition-energy window to 0-300 ms (keep 0-200 ms)
**Changed/Found:** Tested 0-200 vs 0-300 ms dip window on AL_0033 residual stream (R in workspace). Dip TROUGH = 114 ms (actual AND residual) — fully inside 0-200 ms. The 200-300 ms window is NOT more inhibition: ACTUAL mean [0-200]=−1.280 vs [200-300]=**+0.349** (post-dip REBOUND, opposite sign); RESIDUAL [0-200]=−0.268 vs [200-300]=+0.014 (flat). So 0-300 ms would average the dip with the rebound → dilutes actual inhibition energy by ~27% and conflates inhibition with rebound. State partials robust to window (Var 0.085→0.103, Delta 0.073→0.076, Motion null both) — 0-300 only nudges Var significance because dev_stim is a variability metric gaining scatter from recovery samples, not more inhibition.
**Why:** User asked whether to switch inhibition energy to 0-0.3 s.
**Decision:** KEEP 0-200 ms (peak_mode=3, the locked project-wide inhibition-energy def used by dose_response/motion_analysis/paper). 0-300 ms mixes in the rebound and breaks cross-analysis consistency; dev_stim window is non-critical so no reason to change. NOTE: `neg_win=[0 0.30]` is the bleed-subtraction window, NOT the energy window — no conflict. If the rebound is of interest, give it its OWN metric (e.g. mean 0.2-0.5 s).
**Next:** none (window stays 0-200 ms).

### 2026-06-20 — Restructured contra_residual.m into runnable %% sections (per-state); helpers → utils/
**Changed/Found:** Rewrote `impulse-analysis/contra_residual.m` as a SECTIONED workbench: `[CP-SETUP]` (run first: core + build R/S/AGL decomposition) then independently-runnable `[CP-RESi]` (inspector), `[CP-LOCAL]` (3×3 overview rows=state×cols=Actual/Global/Local + printed rho table), `[CP-MOTION]` (No-motion vs Motion bars + mean dip traces), `[CP-VAR]`, `[CP-DELTA]` (each: A/G/L scatter + Local quintile dose-response + decisive partial). Factored the two shared helpers to utils/ so any section runs standalone once SETUP has added the path: `utils/cp_agl.m` (per-trial |dip−amp-mean| z-within-amp for Actual/Global/Local + pooled dip traces) and `utils/cp_cont_state.m` (continuous-state figure + partial test). use_motion default stays false. Each section guards `if ~exist('dL','var'), error('Run [CP-SETUP] first.')`. VERIFIED: full run clean + `[CP-VAR]` re-run STANDALONE (helpers resolve, R/AGL reused). check_matlab_code: 0 issues on all 3 files. New/updated figures: cp_local_state.png (now 3×3), cp_motion_residual.png, cp_var_residual.png (new), cp_delta_residual.png (new).
**Why:** User wanted the residual analysis kept in contra_residual.m but split so they can run motion-only / variance-only / delta-only sections as they see fit. Helpers in utils/ (not local functions) guarantee a lone section never hits "Undefined function" — and matches how cp_res_inspector already lives in utils/.
**Result (AL_0033):** CP-LOCAL rho table |dip dev| vs state — Motion: A −0.001/G −0.004/L +0.033; PreVar: A +0.428/G +0.440/**L +0.143**; PreDelta: A +0.370/G +0.378/**L +0.114**. Local variance dose-response (quintiles) rises from −0.30 (lowest var) and saturates ~+0.15; delta similar (−0.25 → +0.16). Confirms: state effect is mostly GLOBAL (contra), local retains a small variance (partial 0.085, p=0.022) and borderline delta (partial 0.073, p=0.051) effect; motion null.
**Next:** Replicate the sectioned run on AL_0041 (ROI draw). Optionally add a `[CP-MOTION]`-style trace-by-class view for var/delta tertiles if needed.

### 2026-06-20 — Motion figure for the residual stream + zero-inflation binning note
**Changed/Found:** Generated `paper/images/figure2/cp_motion_residual.png` (ad-hoc, from the use_motion=false R) — 2 rows × 3 cols (Actual / Global / Local): top = stim-response predictability (|dip−amp-mean| z-within-amp) by motion class; bottom = mean dip trace by motion class (±SEM). DATA NOTE: session-z motion is zero-inflated/right-skewed → the fig-6 "Low" tertile (z≤−0.5) is EMPTY (n=0); usable bins are only Mid/No-motion (n=690) and High/Motion (z>0.5, n=58). So the meaningful contrast is the binary No-motion vs Motion split (matches motion_analysis.m motThr_hi=0.5 binary classification), not tertiles. Result confirms CP-LOCAL: top row Motion-bar drops for Actual (ranksum p=0.07) and Global (p=0.15) but RISES for Local (p=0.83); bottom row dip traces overlap within SEM across motion class for all three → motion changes neither the mean dip nor the LOCAL reproducibility, only the GLOBAL reproducibility. n=58 motion trials = weak/threshold (was p≈0.007 at z>1.5 windowed-MSE).
**Why:** User asked for motion-analysis plots from the residual stream + explanation. The cp_local_state.png scatter is unreadable for motion (zero-inflated x), so a class-split bar+trace view is clearer.
**Next:** Optionally persist as a reusable `[CP-MOTION]` section in contra_residual.m (would run for AL_0041 too). Replicate on AL_0041.

### 2026-06-20 — New [CP-LOCAL] stream in contra_residual.m: fig-6 predictability is GLOBAL (contra), NOT local
**Changed/Found:** Added `%% [CP-LOCAL]` section to `impulse-analysis/contra_residual.m` + flipped its `use_motion` default true→false (motion must stay OUT of the contra map or it is partly regressed out of the local residual, biasing the motion state-test; variance/delta results identical either way, R²=0.899 vs 0.900). The section ports motion_analysis.m fig-6 metric (`|dip − amp-mean|`, the single-trial deviation from the trial-average = assumed low-dim LTI/SISO impulse response) and SPLITS it: dA=actual ipsi (=fig6, global+local mixed), dG=contra prediction (GLOBAL incoming network activity), dL=residual (LOCAL stim effect, contra removed). DV |dev| z-within-amp. RAN selExp=3 (AL_0033, n=748). RESULT — the predictability state-dependence is GLOBAL, not local:
  - **Motion**: Actual rho=−0.001 (p=0.98), Global −0.004 (p=0.91), Local +0.033 (p=0.37). fig-6 High-motion(z>0.5,n=58) vs rest median |dev|: Actual −0.435/−0.302 (p=0.07, the fig-6 effect, weak here), Global −0.286/−0.278 (p=0.15), **Local −0.212/−0.274 (p=0.83, sign REVERSED)**. → removing contra-predicted global activity ABOLISHES the motion-predictability effect. Fig 6 is a global/network-state effect, not a local stim-response property.
  - **Variance**: Actual +0.428, Global +0.440, **Local +0.143 (p=1.3e-4)**. Variance also mostly global, but the LOCAL effect keeps a small genuine variance dependence (matches CP-RES partial 0.085, p=0.02). This is the ONLY state-dependence the isolated local effect retains.
  Exported `paper/images/figure2/cp_local_state.png` (2 rows motion/var × 3 cols actual/global/local). check_matlab_code: 0 issues.
**Why:** User's framing — trial-average dynamics ≈ low-dim LTI SISO on ipsi; trial-level TF fit infeasible on the real nonlinear system; so use contra to predict GLOBAL activity incoming to the ipsi kernel, isolate the LOCAL stim effect, and test its state-dependence. Wanted the fig-6 "heavy motion → closer to average" analysis redone on the local-only (contra-residual) signal.
**Conclusion:** The fig-6 motion-predictability lives in the GLOBAL (contra-shared) component; the isolated LOCAL stim effect is NOT motion-state-dependent (and the high-motion sign even flips). The local effect's only retained state-dependence is a weak pre-stim-variance one (+0.14 raw, +0.085 partial). So "local stim effect is motion-steerable" is NOT supported; "local effect is weakly variance-dependent" is.
**Next:** Replicate [CP-LOCAL] on AL_0041 (e1/e2, needs ROI draw). Consider variance-quantile dose-response on the LOCAL deviation (dL) specifically; and a signed variance decomposition (var(dA)=var(dG)+var(dL)+2cov) to quantify the global/local split of total trial variability.

### 2026-06-20 — Steerability = a VARIANCE axis (RAW response); motion steerability is variance-mediated — bridges controller MSE
**Changed/Found:** Tested the controller's "motion trials → lower MSE → steerable" in the impulse study using the RAW actual stim response (NOT the contra residual — that regresses motion out + absorbs the state structure). Metric = per-trial deviation from amplitude-mean response (open-loop MSE analog), z-within-amp. AL_0033, n=748. **VARIANCE = the steerability axis:** dip-window (0-200 ms) MSE rho=+0.428, full-window (0-1 s) MSE rho=+0.682 (both p≈0); var|motion partial +0.444 (p=2e-37) — ~3× bigger than the contra-residual dev_stim (+0.145), which had been eating it. **MOTION steerable but variance-MEDIATED:** windowMSE rho=−0.082 (p=0.026, moving→lower MSE); dip MSE continuous null (+0.002) but THRESHOLD split high(z>1.5,n=31) vs low median devA −0.472 vs −0.384 (ranksum p=0.007) — matches locked motion-threshold finding + controller motThresh=1.5. At fixed variance motion adds nothing: partial(MSE, motion|var)=+0.099 (p=0.007, slightly WORSE on dip); indirect motion→var→MSE = −0.20×0.43 = −0.086 ≈ entire raw effect. dip-DEPTH (gain) vs motion −0.057 (ns).
**Why:** User clarified the real claim — controller shows motion→lower MSE→steerable; wants the impulse analog as state-dependence, with variance as a SEPARATE state expected to show higher prediction error. Required the correct substrate (raw response, not motion-regressed contra residual).
**Conclusion:** ONE arousal/desynchronization axis. Pre-stim variance is the strong NEURAL readout of steerability (high var → high open-loop response MSE → poor steerability); motion is a weak BEHAVIORAL proxy (steerable only because it indexes low variance; threshold effect). The impulse study supplies the MECHANISM for the controller's motion-MSE benefit (OL response reproducibility is variance-governed; motion marks the low-variance state). Use RAW-response deviation for the state/steerability question; reserve the contra residual for local-magnitude/dose-response.
**Next:** Replicate on AL_0041. Deepen variance axis: (1) variance-quantile MSE dose-response figure; (2) substitute delta/synchronization for raw variance; (3) separate gain (dip depth) vs reproducibility (noise) for variance; (4) within-amp shape vs noise; optionally TF-model residual as MSE once tf_fit run.

### 2026-06-20 — Mediation test: "motion→controllable via low variance" story REFUTED; carrier is neural variance
**Changed/Found:** Tested the proposed narrative (movement states are low-variance → stim response lands closer to expected → more "controllable") by calling `cp_residual_core(allExperiments,3,...)` directly and decomposing the paths on AL_0033 (n=748). Premise CONFIRMED: motion vs PreVar rho=−0.197 (p=6e-8), vs PreDelta −0.155 (p=2e-5) — moving = low-variance, desynchronized. Controllability concept CONFIRMED for variance: PreVar→dev_stim +0.145, and the stim-specific slice survives the dev_pre control (partial 0.087, p=0.02). BUT motion link REFUTED: motion→dev_stim direct +0.015 (p=0.68, null), motion|var partial +0.022 (p=0.56, null); var|motion partial +0.147 (≈unchanged from 0.145) → variance does NOT act through motion. Implied indirect motion→dev_stim via variance = −0.197×0.145 = −0.029 (too small to detect at n=748, and observed +0.015 is the wrong sign). Conclusion: controllability axis = intrinsic neural variance/synchronization state; motion is a lossy correlate (shares ~4% variance) and must NOT be the variable in the claim.
**Why:** User proposed writing the result as "motion is more amenable to stim prediction (low variance → close to prediction → controllable)." Needed to verify the mediation before adopting the framing.
**Next:** Adopt the controllability framing on pre-stim VARIANCE/delta, not motion. Replicate on AL_0041 before stating firmly; stim-specific partial is modest (0.087) so most of the raw var–dev_stim link is shared general prediction quality.

### 2026-06-20 — Ran residual state-dependence with bleed negation OFF (contra-prediction as-is) on AL_0033
**Changed/Found:** Ran `cp_state_batch` (sessions=3, `decontam=false`, `use_motion=true`) on AL_0033 2025-01-29 via the MATLAB MCP — the "step back from stim-bleed negation, take leftover as ipsi-only local effect" reframe. Held-out contra→ipsi R²=0.900. Raw `β·contra` absorbs **79% of the dip** (per-amp 59–97%) so the no-negation residual recovers only **21%** of the dip magnitude (lower bound); amp-0 control clean (act +0.018 / pred +0.014). State partials `partial(dev_stim, state | dev_pre)`: Motion +0.018 (p=0.62, NULL); PreVar +0.087 (p=0.020, survives); PreDelta +0.077 (p=0.040, marginal). For both var/delta, dev_pre (no-stim control) is MORE state-dependent than dev_stim (0.251>0.145, 0.228>0.130) → most apparent state-dependence is contra-prediction QUALITY, not stim-response biology. res_E magnitude vs motion/var/delta = −0.008/+0.126/+0.161. Numbers BYTE-IDENTICAL to the decontam=ON run, confirming the bleed template cancels in within-amp deviations. Exported `paper/images/figure2/cp_state_batch.png`.
**Why:** User asked to drop the negation and test state-dependence on the raw leftover for the working session only (selExp=3). Confirms feasibility AND that the negation never affected the state result.
**Next:** Cross-session replication (AL_0041 e1/e2, needs interactive ROI draw) is the only open step before deciding whether the weak PreVar partial (ρ≈0.087) is worth featuring; magnitude lower-bound (21%) means absolute dose-response still needs decontam=ON via `cp_doseresponse.m`.

### 2026-06-20 — lds_ipsi.m rewritten: ERA (Ho-Kalman) realization, n4sid dropped entirely
**Changed/Found:** `impulse-analysis/lds_ipsi.m` now identifies the SISO laser→ipsi impulse response via ERA (empirical impulse response → Hankel SVD → A,B,C,D), with AR(p=10) least-squares for spontaneous timescales. NO System Identification Toolbox: n4sid's sssim MEX crashed a 4th time (identical Access violation in sssim→mkl dcopy) even with NaN guards. KEY EVIDENCE: reproduced the real-data scale on SYNTHETIC data via MCP (157,080 samples, 748 windows, horizon 20, autonomous) → ran CLEAN (|eig|max=0.96). So n4sid fails ONLY on real dF — a finite-but-degenerate (flat/gap-filled) window survives isfinite and makes n4sid's internal solve rank-deficient → sssim segfault. ERA uses only svd/eig/hankel/\ (stress-tested safe). Verified ERA core via MCP: recovers known eigenvalues exactly, IR reconstruction err 3e-15. check_matlab_code: 0 issues.
**Why:** Four crashes (hang→OOM→segfault→segfault) all traced to the n4sid MEX. ERA is robust to degenerate data (truncates small singular values), needs no iddata/sssim, and is better-motivated ("realize the impulse-response dynamics directly"). Drops flat windows (std<1e-9) defensively.
**Next:** Run `load_experiments; lds_ipsi` (selExp=3,n_states=5). Read held-out test dip R² + A_spont (AR) vs A_evoked (ERA) taus; check ERA singular-value spectrum implies ~n_states order. Then n_states sweep, compare OL response to tfest. For v2 (MIMO contra+motion), generalize ERA to MIMO (block-Hankel) or use the same svd-based realization — still avoid n4sid.

### 2026-06-20 — Ruled OUT MKL/CPU illegal-instruction bug as lds_ipsi crash cause (red herring)
**Changed/Found:** Checked the hypothesis that the n4sid crash was MathWorks issue 1043644 (AMD Jaguar "Illegal Instruction" in MKL linalg). Does NOT apply: machine is Intel Comet Lake (crash dump Family 6 Model 165 GenuineIntel), MKL 2024.1 AVX2 (CNR) path, and the original fault was an Access Violation (null-ptr dcopy), not an Illegal Instruction. Verified via MCP: deliberate MKL stress (gemm+svd+dcopy ×20 on 800²) ran clean in 1.5 s; synthetic n4sid (driven+autonomous) also clean. MKL is healthy.
**Why:** Crash reproduces only on real dF, never on clean synthetic data of the same shape/ops → data-triggered (non-finite dF), not a CPU/MKL dispatch bug. No code change this entry — hypothesis elimination only.
**Next:** Run hardened lds_ipsi after load_experiments. If it still crashes, distinguish by the dump header: Access violation = NaN guard missed a path (e.g. internally-generated NaN from degenerate-but-finite segment); Illegal Instruction = revisit MKL (try maxNumCompThreads(1)) — but unlikely given stress test passes.

### 2026-06-19 — lds_ipsi.m crash diagnosed = non-finite dF (n4sid MEX segfault); added NaN guards
**Changed/Found:** MATLAB access-violation crash in `sssim.mexw64`→mkl `dcopy` (null ptr) inside n4sid's linreg(B,D,x0) step. Diagnosed via MATLAB MCP: synthetic n4sid on CLEAN data works in this env (driven |eig|max=0.96, autonomous |eig|max=0.94) → not a toolbox/MKL/memory bug. Crash is data-triggered: `dF` contains NaN/Inf gaps; `omitnan` in the demean left NaNs inside the segments fed to iddata, and the n4sid MEX segfaults on non-finite input. `impulse-analysis/lds_ipsi.m` hardened: drop any spont/driven/eval window containing ~isfinite; `assert(all(isfinite(...)))` before each n4sid (clean error vs crash); KF full-session loop coasts (innov=0) through NaN; per-amp plot guards `ion-Fs<1`; SETUP prints non-finite count. check_matlab_code: 0 issues.
**Why:** Three crashes (hang→OOM→segfault) all from feeding n4sid hostile data; this one was NaN, not size. Guarding finiteness is the durable fix and converts any residual non-finite into a readable assert instead of a process kill.
**Next:** Workspace was cleared by the crash — reload via load_experiments.m, then `lds_ipsi` (selExp=3,n_states=5). Read off test-set open-loop dip R² + A_spont/A_evoked taus. If assert trips ("non-finite/empty after filtering"), dF for selExp=3 is mostly NaN → investigate load_experiments.m dF construction.

### 2026-06-19 — New lds_ipsi.m: minimal SISO (laser→ipsi) LDS, no SVD load (avoids OOM)
**Changed/Found:** `impulse-analysis/lds_ipsi.m` (NEW, ~190 lines) — Version-1 SISO input-driven LDS: `x⁺=Ax+Bu+w; y=c'x+du+v`, y=ipsi (`allExperiments.dF`), u=laser amplitude at onset. Uses ONLY allExperiments.{dF,timeBlue,imp,uAmp} — NO SVD/contra/ROI/server load, so the ~3 GB SVD cache that crashed contra_lds.m never enters memory. Sections LDS-SETUP/SPONT/DRIVEN/OL/KF. n4sid with concatenated single-experiment iddata + bounded N4Horizon. Reports test-set open-loop dip R² (the meaningful number), A_spont vs A_evoked timescales, one-step KF R². check_matlab_code: 0 issues.
**Why:** contra_lds.m crashed AGAIN (OOM) even after free-arrays + horizon-bound fixes; user wants a working minimal baseline first — SISO ipsi-only (1 output, 5 latents), which needs no SVD at all. Also user noted SVD likely already in allExperiments, so reload is avoidable (confirmed irrelevant for v1).
**Next:** Restart MATLAB, run `selExp=3; n_states=5; lds_ipsi`. If clean, sweep n_states=[3 5 8 12] and compare open-loop response to tfest (SISO SS≡TF check). Then build v2 (MIMO: add contra+motion as outputs) — at that point check whether contra SVD modes are already cached in allExperiments to avoid the 3 GB reload.

### 2026-06-19 — Fix contra_lds.m OOM crash: free 3GB SVD arrays + bound n4sid horizon
**Changed/Found:** `impulse-analysis/contra_lds.m` — after building Z_obs/y_dm/mot_z, `clear V_c_full X_cp_raw X_cp_z U_cp V_cp` (V_c_full is [2000×193206]≈3GB because the cached SVD has 2000 modes; U_cp/V_cp ≈1.5GB and unused on the cache-hit path). Added bounded `n4sidOptions('N4Horizon',[r r r])`, r=max(20,2·n_states), to both n4sid calls (default 'Auto' can allocate huge Hankel/QR intermediates). MATLAB hard-crashed (no error) after the prior concat fix = OOM kill. check_matlab_code clean.
**Why:** Concatenation fixed the linreg/x0 hang but left ~5GB resident before n4sid then allocated its own large matrices → out-of-memory process kill. Clearing the SVD arrays (only Z_obs/y_dm/mot_z needed downstream) + bounding the horizon caps peak memory.
**Next:** Restart MATLAB; SMOKE TEST first with `n_states=5; nSV_lds=3;` and watch Task Manager memory before scaling to 15/10. If still tight, the continuous full-session refactor (no windowed concat) is the durable fix. Consider trimming the 2000-mode SVD cache load.

### 2026-06-19 — Fix contra_lds.m hang: n4sid multi-experiment x0 blowup (748 experiments → 1)
**Changed/Found:** `impulse-analysis/contra_lds.m` — `data_spont` and `data_train` now concatenate their windows into ONE `iddata` experiment (`cell2mat(...)`) instead of 748 / ~598 separate experiments. Root cause confirmed from user traceback: `n4sid` hung in `linreg` (`[B,D,x0]=linreg`) at LDS-SPONT — multi-experiment data makes n4sid estimate a separate x0 per experiment (748×n_states=15 ≈ 11,220 x0 unknowns in one least-squares). Session is large (nF_m=193,206 frames, 748 trials, 9 amp levels). check_matlab_code: clean (only pre-existing preallocation infos).
**Why:** Concatenating drops x0 count to n_states (=15) so linreg is trivial; per-window demean keeps join discontinuities small. Driven fit would have hit the same hang next. Minimal unblock so user can re-run; does not change model structure.
**Next:** Re-run LDS-SETUP→LDS-KF on selExp=3; verify A_spont/A_evoked stable + timescales. Caveat: concatenation injects ~748 artificial join transitions — the principled fix is continuous full-session identification (input u_full + outputs [contra,ipsi] over all frames, eval windows separate), which also enables the obs_mode siso/mimo + LDS-OBS refactor. Offer that refactor.

### 2026-06-19 — Design: two observability versions of the LDS (SISO ipsi-only vs MIMO ipsi+contra+motion), distinct from existing fork-A build
**Changed/Found:** No code yet — clarified that the user's two requested LDS versions are OBSERVER/estimation models (ipsi IS observed) and differ from the already-built `contra_lds.m`, which is a fork-A PREDICTION model (contra observed, ipsi held-out, partial gain `K_c=K[:,1:nSV_lds]`). V1 = SISO `u→y_ipsi` (control-faithful: PI controller fed back on ipsi; OL sim = tfest TF). V2 = MIMO output `[ipsi,contra,motion]` — motion as OUTPUT (readout of latent state) to raise observability. Each version identified with its OWN output set ⇒ correct `K` per version, no column-dropping.
**Why:** The scientific question is whether extra sensing (contra+motion) increases observability/estimation of the latent state beyond ipsi alone. Quantify via observability Gramian eigenspectrum (`gram(sys,'o')`), steady-state Kalman error cov `P∞` (`idare`) trace ratio + ipsi one-step prediction variance, and single-trial dip R² + CP-RES state partials on the innovation. Nick (MEETINGS.md:135) flagged SS≠TF for MIMO: holds — V1 SISO≡TF, V2 genuine MIMO.
**Next:** On user confirm, refactor `contra_lds.m` to an `obs_mode` switch ('siso'|'mimo', keep fork-A held-out as 3rd mode) sharing iddata/n4sid/KF; add `LDS-OBS` section (Gramian + P∞ comparison). Keep A_spont vs A_evoked timescale test (GCaMP-vs-neural control). Build only; user runs section-by-section. Pending from prior build: LDS-STATE, LDS-EIG, and an actual run on selExp=3 (no R² logged yet).

### 2026-06-19 — Scaffold contra_lds.m: universal input-driven LDS + Kalman filter (LDS-SETUP through LDS-KF)
**Changed/Found:** `impulse-analysis/contra_lds.m` (new, ~260 lines) — 5 sections:
  - LDS-SETUP: load SVD/ROI cache (shared with cp_residual_core), build Z_obs [nF_m × nSV_lds], u_full (amplitude at stim onset frames, 0 elsewhere)
  - LDS-SPONT: n4sid on pre-trial 6 s windows (no input) → A_spont + spontaneous time constants
  - LDS-DRIVEN: n4sid on multi-trial windows [−1, +2.5] s with u = amplitude at onset → A_evoked, B, C, D, K; 80/20 train/test; open-loop dip R² on test set
  - LDS-OL: open-loop simulation (no Kalman) vs per-amplitude trial averages → exported lds_ol_impulse.png
  - LDS-KF: universal Kalman predictor over FULL session (not per-trial reset); contra-only update (K_c = K[:,1:nSV_lds]); ipsi held out; extracts per-trial baseline-corrected predictions; reports KF dip R²; saves cache `lds_{mn}_{MMDD}_e{en}_n{n}.mat`
**Why:** Static OLS map (R²≈0.90) hit ceiling; weak PreVar state effect (partial ρ≈0.085) may be residual prediction-quality bias. SS model trained on driven data (B captures impulse response, not inferable from spont). Universal KF: state at each trial onset reflects full session contra history — no artificial reset. Fork A: contra=observation, ipsi=held-out; K_c drops ipsi column so filter never observes the output being evaluated.
**Next:** Run LDS-SETUP → LDS-KF on selExp=3 (AL_0033); sweep n_states=[5 10 15 20]; check stability warnings. Then add LDS-STATE (partialcorr of KF residual vs motion/var/delta, same test as CP-RES) and LDS-EIG (complex z-plane eig(A_spont) vs eig(A_evoked)).

### 2026-06-19 — Refactored residual pipeline into a shared core function + 3 thin entry points
**Changed/Found:** New `utils/cp_residual_core.m` — function `[R,S]=cp_residual_core(allExperiments,selExp,opts)` factoring the whole SVD/ROI/CP-FIT/CP-IMP/CP-RES pipeline out of contra_residual.m. opts: `decontam`/`use_motion`/`make_wide`/`plot`. Returns R (res_imp/act_imp/per-trial DVs+covariates/inspector payload/metadata) + S (state partials). Three thin wrappers call it:
  - `contra_residual.m` (rewritten) — INTERACTIVE: decontam=false default, make_wide+plot on, opens CP-RESi inspector.
  - `cp_state_batch.m` (NEW) — canonical/replicable: loops sessions, decontam off, aggregates partial(dev_stim,state|dev_pre) → table + summary figure (filled = p<0.05). Cross-session replication = just run it.
  - `cp_doseresponse.m` (NEW) — bleed-comp absolute magnitude: decontam=true, amp vs residual dip energy (physical ΔF/F).
  Path anchoring fixed: core resolves `impulse-analysis/data` relative to `utils/` (mfilename). VERIFIED byte-identical to pre-refactor: R²=0.900, PreVar partial 0.087 (p=0.0204), PreDelta 0.077 (p=0.0402). All 3 scripts run clean; check_matlab_code: no errors. Original contra_residual body preserved exactly via `cp` then surgical function-conversion (figures gated by `opts.plot`, inspector moved to wrapper).
**Why:** User wanted the clickable inspector preserved, cross-session replication ready as a separate script, and a separate bleed-comp dose-response capability — with shared logic in ONE place (chose factor-core over copy).
**Next:** Run `cp_state_batch` over all 3 sessions when ready (will draw ROIs interactively for un-cached AL_0041 e1/e2). Commit the refactor.

### 2026-06-19 — Strategy: INPUT-DRIVEN state-space model of the impulse response + Kalman in-trial disturbance rejection
**Changed/Found:** No code yet — design clarified after user correction. CORRECT design (supersedes my earlier disturbance-detection misread): fit an input-driven stochastic SS model `x⁺=Ax+Bu+w; z_c=C_c x+v; y=c_y'x+d·u+v` on the DRIVEN trials so the deterministic part IS the stim impulse response (= MIMO/stochastic generalization of the existing tfest model, and the generative unification of the contra-residual). Kalman filter's job is NOT to remove stim — it uses contra observations to reject per-trial disturbances (nonlinear/state-dependent deviations) from the nominal linear response; it's the optimal linear observer, rejecting only what's observable through contra. Validated user's key argument: spontaneous-only training has nothing in the MEAN to learn (E[x]→0, zero-mean stable) → must train on the response; refinement: spontaneous still constrains A (fluctuation–dissipation), only B/d need driven data.
**Why:** Static map near-ceiling on spontaneous (R²≈0.90), lags proven to hurt, current state effects weak (PreVar partial ρ≈0.087, PreDelta ρ≈0.077). Input-driven SS gives a controllable plant for the MPC motivation (FINDINGS), and a decisive test: compare eig-timescales of A_spont vs A_evoked vs A_per-state — equality ⇒ clean LTI/relaxation (also the GCaMP-vs-neural control: if A is just indicator decay they're trivially equal); inequality/state-variation ⇒ the state-dependent-dynamics finding.
**Next:** Resolve the in-trial observation fork with user (A: contra obs / ipsi held-out [recommended]; B: contra+early-ipsi online/MPC; C: contra as measured input ≈ tfest). Then scaffold `impulse-analysis/contra_lds.m` for fork A: n4sid/ssest with exogenous u=laser waveform (physical units, 'EnforceStability'), spontaneous pre-fit to init A, compare open-loop impulse response to tfest, report contra-Kalman single-trial pred error vs motion/var/delta. Replicate across 3 sessions. Refs: Semedo 2019 Neuron, Semedo 2022 Sci Adv, Linderman 2017 rSLDS.

### 2026-06-19 — Bleed comp is MATHEMATICALLY irrelevant to the residual STATE analysis (cancels in within-amp deviation)
**Changed/Found:** `contra_residual.m` — added `use_motion` + presettable `decontam` + `make_inspector` guards. Ran 3 conditions on selExp=3:
  - motion-free + comp (prior): PreVar partial ρ=0.085 (p=0.022), PreDelta 0.073 (p=0.051).
  - **motion + comp:** R²=0.900 (vs 0.899); PreVar partial 0.087 (p=0.020), PreDelta 0.077 (p=0.040).
  - **motion + NO comp:** PreVar partial 0.087 (p=0.0204), PreDelta 0.077 (p=0.0402), res_E vs var/delta +0.126/+0.161 — **BYTE-IDENTICAL to motion+comp**.
  PROOF: bleed comp subtracts a per-AMPLITUDE CONSTANT template (kernel_alpha·art_tap, same for every trial of an amp). dev_stim/dev_pre (deviation from amp-average) and z-within-amp res_E all subtract the amp-mean → the constant CANCELS exactly. So every within-amp state correlation is invariant to bleed comp. Comp changes ONLY absolute magnitude: dip recovery 97% (comp) vs 21% (no comp); no-comp residual traces SMALLER but SMOOTHER (boundary 0.055 vs 0.327, no decontam noise). Motion-in-baseline: negligible (R² +0.001, state corrs ~unchanged) — contra modes already carry motion.
**Why:** User overruled the earlier "motion stays out" diagnostic (controller + non-contra evidence) and asked whether bleed comp is needed at all since the real question is state-dependence of local effects.
**Decision:** STATE-dependence pipeline = `decontam=false`, `use_motion=true` (identical result, cleaner traces, no noisy decontam). Bleed comp (Stage-2 pooled) reserved ONLY for the absolute dose-response (amp vs res_E in physical units). Supersedes the prior "don't add motion" entry (motion now IN as a harmless nuisance regressor).
**Next:** Replicate across sessions (AL_0041). For dose-response only, implement Stage-2 pooled bleed. State result stands: var partial ~0.087 (p=0.02), delta ~0.077 (p=0.04) survive dev_pre control; motion null; bulk of raw effect = prediction quality.

### 2026-06-19 — Decided AGAINST re-adding motion to baseline: motion is not the prediction-error driver
**Changed/Found:** Diagnostic on contra_residual wide traces (selExp=3): residual energy in NO-bleed windows vs motion — pre-stim rho=−0.085 (p=0.02, weak NEGATIVE), **post-stim (0.4–1s) rho=−0.010 (p=0.78, NULL)**; high-motion trials (z>1.5, n=31, 4.1%) median post-stim resE=0.486 vs rest 0.484 (identical). So contra prediction is NOT degraded by motion, even post-stim (no bleed). The worst-dev inspector trial (#667) being high-motion was an outlier coincidence. Motion anti-correlates with arousal (vs var rho=−0.197, vs delta rho=−0.155) → movement=desynchronized/aroused=low delta. Earlier motion-in-baseline gave R²=0.898 vs 0.899 (no gain). REAL cause of poor stim-window prediction = the noisy per-amp decontam (Stage 1: sign-flips, −103% at A=0.5), injected only in 0–0.4s → fix is Stage 2 pooled bleed, NOT motion.
**Why:** User observed (via inspector) poor contra prediction in the stim window on a high-motion trial and proposed re-adding motion to the baseline predictors. Evidence contraindicates: no prediction benefit + reintroduces the motion-as-regressor-AND-test circularity.
**Next:** Implement Stage 2 (pooled amplitude-independent bleed) to clean the stim-window residual. Optionally test contra history (pX>0) to lower the R²=0.90 baseline floor. Motion stays OUT of the baseline.

### 2026-06-19 — CP-RESi clickable residual inspector (3-row trial detail, -2..+2 s)
**Changed/Found:** `utils/cp_res_inspector.m` (new) + `contra_residual.m` [CP-RES] — clickable inspector: 3-panel scatter (dev_stim vs motion / pre-stim var / delta, colored by amplitude); click a point → 3-row detail over -2..+2 s: (1) trial actual + amp-average + contra prediction, (2) trial residual + amp-average residual, (3) motion (z). CP-RES loop now also builds wide-window per-trial traces (actW/prdW/resW/motW), decontam (scaledkernel) re-applied over the dip rows in the wide window; `make_inspector` flag (default true). Verified traces [748×141] (Fs≈35, ±2 s); center matches res_imp; preview of worst trial (#667, A=4.9, dev_stim=6.58) is a HIGH-MOTION trial with a large residual deviation.
**Why:** User requested the clickable inspector (like CP-6i) for the residual analysis — eyeball per-trial stim responses, contra prediction, residual vs predicted-avg residual, and motion against the dev_stim-vs-state scatter.
**Next:** Click high-dev_stim outliers to check failure modes (several may be high-motion despite the pooled motion null). Replicate CP-RES across sessions.

### 2026-06-19 — CP-RES: stim-response "state-dependence" is mostly prediction-quality; weak variance effect survives
**Changed/Found:** `contra_residual.m` — added `%% [CP-RES]` section: per-trial residual DVs (res_E = 0-200ms residual energy; dev_stim = mean-sq deviation of residual from amp-avg over 0-200ms; **dev_pre** = same over 0.2s PRE-onset = contra-prediction-quality control) + motion/var/delta covariates; **partialcorr(dev_stim, state | dev_pre)** as the decisive test. DVs z-within-amp, covariates session-z, motexcl(z>1.5) for var/delta. RAN selExp=3 (AL_0033):
  - **Motion: NULL** — dev_stim rho=−0.004 (p=0.91), dev_pre ns, partial ns. Directly CONTRADICTS the "high motion → more predictable" hypothesis.
  - **PreVar:** dev_stim rho=+0.147 (p=8e-5); **dev_pre rho=+0.264 (p=9e-13) — LARGER**; PARTIAL(stim|pre) rho=+0.085 (**p=0.022, survives**). Small genuine stim-specific effect remains.
  - **PreDelta:** dev_stim rho=+0.130 (p=5e-4); dev_pre rho=+0.239 (p=1e-10); PARTIAL rho=+0.073 (**p=0.051, does NOT survive**). Effect explained by prediction quality.
  - res_E magnitude: motion −0.006, var +0.125, delta +0.158.
  - **HEADLINE:** dev_pre (no-stim prediction error) is MORE state-dependent than dev_stim for both var and delta → the apparent "stim response is state-dependent" is largely **contra-prediction QUALITY**, not stim-response biology. Only pre-stim variance keeps a small significant partial effect; delta and motion do not. The stim response itself is comparatively state-robust. Exported cp_res_state.png.
**Why:** Decisive test for the residual-reframing claim (JOURNAL 2026-06-17). The dev_pre control separates true stim-response state-dependence from baseline-prediction-quality state-dependence — without it the claim would be inflated (raw var rho=0.15, delta=0.13).
**Next:** Replicate across sessions (selExp 1,2 = AL_0041) — single-session so far. Decide if the weak variance partial (rho=0.085) is worth featuring. Stage-2 pooled bleed still needed for the amp-vs-res_E dose-response.

### 2026-06-18 — Add face motion energy to AL_0046 grid bundle (computed from raw face.mp4)
**Changed/Found:** `bilateral-analysis/compute_face_motion_AL0046.m` (new) — no face_proc.mat exists for this session, so computed motion energy directly from `face.mp4` (752×590, 374,947 frames; mp4 header says 30 fps but real timestamps are ~69 Hz). Motion energy = mean over the lab's saved face ROI `[488 136 752 590]` of |frame(t)−frame(t−1)| gray levels, over the grid window (179,685 face frames), interpolated onto the 89,843 imaging frames. Added to bundle `aligned/`: `motion_energy.npy` (imaging grid), `face_motion_raw.npy`+`face_frame_times.npy` (native rate), `motion_aligned.csv`, `motion.mat`. Verified: n=89,843 matches frameTimes, 0 NaNs, min 0.25/med 0.34/max 7.94. README updated (folder tree + method, replaced the "no motion" note). Bundle now 0.76 GB.
**Why:** User asked to compute and ship face motion energy for the grid dataset. **Process note:** running the ~57-min decode as a blocking MCP MATLAB call got killed by the tool timeout at 2.2% (twice-bitten); had to relaunch as a detached `matlab -batch` background process (exit 0) — over the share, a cold instance decodes at only ~53 fps, not the ~320 fps a warmed session benchmarked.
**Next:** none — bundle is share-ready. (If Facemap-comparable motion is needed later, run Facemap on face.mp4 to get the ROI-SVD instead of plain frame-difference energy.)

### 2026-06-18 — Package AL_0046 grid-activation dataset for sharing (SVD + stim table + README)
**Changed/Found:** `bilateral-analysis/package_grid_for_collab.m` (new) — collaborator bundle for AL_0046 2026-02-22 grid photostim. Ships canonical SVD (U[Y,X,500], V[500,nFrames], meanImage, frameTimes; single .npy) **cropped to the grid window** (±1 s pad → 89,843 of 187,472 frames) + `stim/trials.csv` (5,200 trials: onset/offset, svd_frame, galvo_x/y_mm, laser_power_mW, site_id) + `sites.csv` (52 sites) + `stim.mat`, and a tailored `README.md`. Output `collab_export_AL0046_grid/` = **0.75 GB** (vs ~254 GB raw). Excluded the unrelated 638 nm block (0 trials in grid window) and shipped only 594 nm. No face_proc.mat exists (only raw face.mp4) → no motion/face SVD shipped, noted in README. Verified from the exported bundle: svd_frame ∈ [39, 89639] within nFrames, site (1.5,2.0) STA reconstructs 1.47% focal activation.
**Why:** User asked to make the grid dataset shareable like the impulse one. Cropping to the grid window keeps the share focused and avoids sharing the unrelated first-block experiment.
**Next:** If the collaborator wants motion, run Facemap on face.mp4 first; topographic galvo→cortex summary still pending from prior entry.

### 2026-06-18 — Grid-activation (AL_0046) read from server + first plots: topographic cortical responses
**Changed/Found:** Explored AL_0046 2026-02-22 en1 grid-activation (594 nm) for a collaborator; translated their Python loader to MATLAB (inline; PNGs in `scratch_grid/`). Design: **52 galvo sites** (8×8 minus 12 anterior corners/midline), 2 powers (1 & 3 mW), 50 reps → **5,200 trials**, 23 ms pulses @ ~2 Hz, randomized order. Computed stim-triggered ΔF/F maps (nSV=200, baseline −200..0 ms, response +30..300 ms) per site → montage shows **focal activation that tracks galvo position** (topographic), peak ΔF/F median 0.45% / max 1.93%. Two bugs caught: (a) `expStartStopTimes` is stored `[2×2]` with rows = `[start stop]` pairs — flatten+reshape scrambles it; grid window = `ess(2,:)`. (b) overview plot had `YDir` reversed (upside down) — fixed to Y-up; also corrected site count 64→52 (blank the 12 empty cells).
**Why:** User asked to read a lab collaborator's grid-activation dataset from the server and show plots in MATLAB.
**Next:** Fix montage colorbar label (shows 0–1, true clim ±0.77%); build galvo→cortex topographic summary (per-site peak centroid); 1 vs 3 mW comparison; verify cortical-image orientation vs galvo space; save reusable script if continuing.

### 2026-06-18 — Collab README rewritten as standalone collaborator doc; fixed ΔF/F units error
**Changed/Found:** `collab_export/README.md` — rewrote as a self-contained collaborator-facing document (what the dataset is, file/units tables, trial table incl. catch trials, Python+MATLAB load recipes). **Caught a units bug** in the prior draft: it claimed the SVD was already in ΔF/F. Verified against the data that `U·V` is the mean-subtracted hemodynamic-corrected fluorescence **deviation (ΔF)**; **ΔF/F (%) = (U·V)/meanImage·100** (brain-pixel std ≈2%, p99 ≈8.6%). Ran the README's exact MATLAB snippet end-to-end: 560×560 ΔF/F image (−9..+12%), and strongest-dose STA (n=89) reconstructs the expected inhibitory dip (down to −1.1%) → trial indexing + reconstruction validated.
**Why:** README is the collaborator's only instruction sheet; the wrong-units claim would have made every reconstructed map ~500× off. Also removed internal workflow leakage (pending/"on request" rows).
**Next:** Same units note applies when AL_0041 sessions are added; no other follow-up.

### 2026-06-18 — package_for_collab.m: trial table from ground-truth input_params (incl. zero-amp catch trials) + markdown
**Changed/Found:** `impulse-analysis/package_for_collab.m` — replaced the analog-detection + gap-fill stim list with the authoritative schedule from `input_params.csv`: col9 = commanded amplitude (10 levels incl. 0), col2 = onset frame index. For mode-1 (AL_0033), recovered per-session frame `horizon` by matching commanded nonzero onsets to measured analog pulses → **horizon=1396 frames, residual 0.0 ms**. New `trials.csv` = all **900 trials, 90 zero-amp catch trials**, cols trial/stim_time_s/svd_frame/amplitude/measured_peak_V (measured_peak_V≈−0.01 V on catch trials confirms no fire, ~1 V median on real). Wrote delivery-root `collab_export/README.md` explaining format, trial table, time base, and U·V reconstruction (Python+MATLAB). Added `stimMode` col to sessions array (mode 2 = AL_0041, decode TBD).
**Why:** User needs the zero-amplitude (no-stim) trials, which the gap-fill only approximated (196 inferred, 944≠900 commanded). input_params is the complete balanced design (90/level); detection had missed ~62 nonzero trials. Markdown explanation requested alongside the data.
**Next:** Decode mode-2 onset timing for the two AL_0041 sessions, then run selRows=1:3; fill the AL_0041 rows in README.md sessions table; pick transfer mechanism; confirm Yazdan-lab data format.

### 2026-06-18 — Add package_for_collab.m: minimal collaborator export bundle
**Changed/Found:** `impulse-analysis/package_for_collab.m` (new) — exports a self-contained per-session bundle for sharing impulse widefield data. Ships canonical SVD (U[Y,X,500], V[500,nFrames], meanImage, frameTimes re-saved as single .npy), laser input binned to SVD frames, motion energy interpolated onto SVD frame times, a stim-onset table (time/svd_frame/amplitude/is_real_stim), and the face motion SVD (motSVD_0 + masks + avg frames). Ran on session 3 (AL_0033 2025-01-29): output 1.71 GB vs 254 GB raw folder (~150x). Verified: shapes index-matched (all 193206 frames), input median 1.05V at real-stim frames vs −0.01V baseline, 9 amplitudes 0.5–4.9V, 748 real stims + 196 gap-fillers, no NaNs.
**Why:** Collaborator (+ Yazdan lab) needs the data but the 254 GB/session folder is 97% raw widefield.tar that the pipeline never loads — only the SVD components + aligned signals are needed. Re-saving U as single/500-comp also shrinks it 2.4 GB→0.6 GB and removes np.load/MATLAB transpose ambiguity.
**Next:** Run for the other 2 sessions (AL_0041 en1/en2, set selRows=1:3); decide transfer mechanism (cloud vs Globus); clarify Yazdan-lab data format to see if same export applies.

### 2026-06-17 — Stage 1 bleed-spatial-decay GATE: separable+decays but NOT amp-scaled → use pooled bleed
**Changed/Found:** `contra_prediction.m` — added `%% [CP-BLEED-SD]` Stage-1 diagnostic (per-amplitude contra bleed maps → distance-from-stim, rank-1 separability, amplitude scaling). RAN on selExp=3 (AL_0033). Results:
  - **Separable: YES** — PC1 = 91.6% of the [pixel × amp] bleed (one spatial pattern scaled across amps).
  - **Decays from stim: YES (moderate)** — pooled |profile| vs distance-from-stim Spearman rho=−0.392 p≈0; beats distance-from-midline (rho=−0.204) → organized around the STIM site, not callosal/midline entry. λ≈464 px (gradual); per-amp curves PEAK at ~200–250 px (a localized contra locus), not monotonic from nearest pixel.
  - **Amplitude-scaled: NO** — blob bleed per amp = −17.9,−11.7,−4.3,−2.3,−8.0,**+11.5**,−2.2,−19.0,−6.8 (rho=+0.20 p=0.61); sign-flips at 3.2V; weakest stim (0.5V) shows LARGEST bleed. Per-amp bleed is noise-dominated.
  - **GATE VERDICT: PARTIAL** — per-amp `gain(amp)·profile(d)` model NOT supported (gain unreliable). A single POOLED, amplitude-INDEPENDENT bleed pattern IS supported. Exported cp_bleed_spatial_decay.png.
**Why:** Stage 1 is the GATE for the spatial-decay residual route (residual=actual−clean-contra, replacing err4 per JOURNAL 2026-06-17). Validate the bleed model before retiring err4. Fixed a dim bug (uAmp_cp is [10×1] col; corr transpose mismatch).
**Next:** Stage 2 → POOLED amplitude-independent bleed removal (α=1, no per-amp gain), likely in `contra_residual.m`. OPEN: why is contra bleed amplitude-independent (stim artifact vs onset-locked arousal/hemodynamic)? — affects residual interpretation. Then residual DV (0–200ms inhibition energy) + amp/state characterization.

### 2026-06-17 — Create contra_residual.m: simplified pipeline stopping after CP-IMP
**Changed/Found:** `impulse-analysis/contra_residual.m` (new) — copy of contra_prediction.m trimmed to stop after `%% [CP-IMP]`; removes CP-2, CP-RRR, CP-BLEED, CP-4/5/6/7; retains all infrastructure (SVD, ROI, CP-FIT) and the full CP-IMP residual computation; workspace after run contains `res_imp`, `act_imp`, `prd_imp`, `t_imp`, `ia_imp`
**Why:** User wants to focus on residual analysis without the concurrent-prediction / TF / error-correlation machinery
**Next:** Add residual-analysis sections below the CP-IMP block as the new work develops

### 2026-06-17 — Option C shifted-window in build_onset_artifact (corrected)
**Changed/Found:** `utils/build_onset_artifact.m` — implemented the actual Option C: shifted_window mode computes a matched spontaneous reference window shifted `spont_offset_f` frames (~20 s default) before each stim onset, averages across trials, and subtracts from the stim-locked average. Result = stim_mean − spont_mean = pure stim-specific artifact. 'detrend' and 'none' modes retained. `contra_prediction.m` — default `bleed_spont_ref = 'shifted_window'`, `bleed_spont_offset_s = 20`; both call sites guard for missing workspace vars and pass `round(bleed_spont_offset_s * Fs)` as the frame offset. Previous 'detrend' commit was a wrong substitution — this corrects it.
**Why:** User pointed out the shifted-window subtraction (artifact = stim_mean − spont_mean) was agreed but not implemented — linear detrend was substituted without disclosure.
**Next:** Re-run CP-IMP and CP-4; check console for spont-window validity warnings (if many trials report fewer valid spont frames than stim frames, the 20 s offset is too large for the session's recording length — reduce bleed_spont_offset_s).

### 2026-06-17 — Option C detrend in build_onset_artifact (bleed_spont_ref)
**Changed/Found:** `utils/build_onset_artifact.m` — added optional `spont_ref` arg ('none' | 'detrend'). 'detrend' fits a line to each mode's pre-stim window per trial and extrapolates it forward; subtracts this expected spontaneous trajectory instead of the flat pre-stim mean. Isolates the stim-specific deflection, removing slow hemodynamic/arousal drifts correlated with trial timing. `contra_prediction.m` — added `bleed_spont_ref = 'detrend'` knob in Setup section; threaded through both build_onset_artifact call sites (CP-IMP and CP-4).
**Why:** Current mean-subtraction baseline leaves any pre-stim slope extrapolated into the stim window, inflating the artifact estimate and degrading decontamination. Linear detrend removes this without needing a fixed-offset spontaneous reference window (which risks landing on a prior stim period given unknown ITI).
**Next:** Re-run CP-IMP and CP-4 sections; compare art_ratio4 and held-out R² before/after. If decontam quality improves (lower art_ratio, higher R²), keep 'detrend' as default. If no change, the drifts weren't the limiting factor — investigate pixel-space bleed detection instead.

### 2026-06-17 — CP-BLEED: add accurate algorithm description to section header
**Changed/Found:** `contra_prediction.m` — rewrote the `%% [CP-BLEED]` section comment to accurately describe the algorithm (signed mean deviation in dip window relative to pre-stim mean, soft-weighted not SNR-thresholded) and explicitly note that it does NOT compare to baseline variance. User queried whether the bleed detection gates on dip-vs-baseline-variance; it does not — it uses the signed trial-averaged mean change as a continuous weight.
**Why:** User wanted to understand the detection criterion and verify whether it matches their mental model (SNR-style gating). It doesn't — current method is soft weighting. Note added explaining how to add SNR threshold if desired.
**Next:** Decide whether to add hard SNR gating (require dip > k × baseline SD before pixel projection) or leave as soft weighting.

### 2026-06-17 — Fix: ROI cache not found when impulseDir resolves to wrong dir
**Changed/Found:** `contra_prediction.m` — expanded legacy migration to search `impulseDir`, `pwd`, AND `fileparts(impulseDir)` (project root) for the roi_name, not just `impulseDir`. Added `fprintf` that prints the expected cache path every run. `load_experiments.m` — added `addpath(impulseDir)` so `which('contra_prediction')` works as a fallback when `mfilename` returns empty (command-window runs). Root cause: if `mfilename` returns '' and `which` can't find `contra_prediction` (not on path), the `pwd` fallback fires; if pwd is `brain_paper/` rather than `impulse-analysis/`, `dataDir` becomes `brain_paper/data/` and the cache is not found.
**Why:** User reported ROI selector firing every run despite cache file existing. The file was likely saved at the project root level by an earlier version.
**Next:** Run the section; confirm `[CP] ROI cache expected at: …impulse-analysis/data/…` is printed and the migration fires once if the file is found in a neighbouring directory. After that runs should skip the selector.

### 2026-06-17 — CP-7 per-session error-sorted heatmap (clickable)
**Changed/Found:** Added `%% [CP-7]` section to `contra_prediction.m` and created `utils/cp_error_heatmap.m`. One panel per amplitude: imagesc heatmap with x=time(ms), y=trial rank sorted ascending by prediction error (rank 1=best predicted at bottom). Blue-white-red diverging colormap symmetric about zero. Click any row → detail figure: actual (black) + amplitude average ±SEM (gray) + contra SVD (blue dashed) + ARX+TF (red dashed), with error window marker. Exports `cp7_error_heatmap.png`.
**Why:** Session-level overview of prediction quality across all trials and amplitudes, with drill-down on any individual trial.
**Next:** Run CP-7; click worst-predicted trials to look for systematic failure modes (large-motion, non-responsive, etc).

### 2026-06-17 — CP-6i inspector + local helpers moved to utils/
**Changed/Found:** `contra_prediction.m` — (1) added `onset4` cell preallocation and `onset_ia4(j4) = ion4` in the CP-4c per-trial loop; (2) stacked `onset_all4` and `amp_all4` after the CP-4d cell2mat block; (3) added `%% [CP-6i]` section that packs S_insp struct and calls `cp_trial_inspector`; (4) removed 6 local function bodies from end of script (moved to utils/). Created 7 new utils files: `compute_r2.m`, `per_trial_r2.m`, `plot_window.m`, `rrr_fit.m`, `build_onset_artifact.m`, `brain_overlay_fig.m`, `cp_trial_inspector.m`.
**Why:** Requested interactive trial inspector: clickable motion-vs-error scatter, per-trial detail pop-up (actual + amp-average + prediction top panel; z-scored motion trace bottom panel). Moving helpers to utils/ makes them available to future scripts without duplicating code.
**Next:** Run CP-6i section in MATLAB; click outlier trials with high motion or high error to check whether prediction failures are identifiable by eye.

### 2026-06-17 — CP-6c/6d state splits + dip/recovery dissociation finalized (decision: report split)
**Changed/Found:** `contra_prediction.m` — added CP-6c (pre-stim variance split) + CP-6d (pre-stim delta split), median hi/lo on motion-excluded trials, mirroring CP-6b (trace means + error-bar + t-test); rewrote CP-6w to report DISJOINT dip (0-200ms) vs recovery (200-500ms) vs full (0-500ms) windows. RAN. Results (motexcl n=561, canonical err = full 0-500ms):
  - **Dissociation (CP-6w):** dip 0-200ms → variance p=0.25, delta p=0.16, motion p=0.86 (ALL NULL). recovery 200-500ms → **variance r=+0.130 p=0.002 (Spearman p<1e-4), delta r=+0.114 p=0.007 (Spearman p<1e-4)**, motion p=0.61 (null). Recovery effect is STRONGER than full window. State effect is exclusively in the recovery, absent in the inhibition dip.
  - **CP-6c variance split:** High=0.703±0.033 vs Low=0.567±0.026, **t-test p=0.0014** (high pre-stim variance → less predictable).
  - **CP-6d delta split:** High=0.685±0.029 vs Low=0.585±0.031, **t-test p=0.019** (high pre-stim delta → less predictable).
  - **CP-6b motion split:** High=0.611 vs Low=0.625, no difference (motion null, as expected).
  New PNGs: cp6c_pvar_split.png, cp6d_delta_split.png.
**Why:** User chose "keep both windows, report the dip/recovery split" + "add CP-6c/6d". Confirms the refined claim: inhibition DEPTH is state-invariant; post-inhibition RECOVERY predictability is state-dependent (pre-stim synchronization: variance+delta), NOT motion.
**Next:** Fill FINDINGS.md (variance r=+0.119 p=0.005 / delta r=+0.108 p=0.011 full; recovery-window + split t-tests; motion clean null); draft results.tex pre-stim-brain-state subsection around the dissociation. Commit + push.

### 2026-06-17 — Motion-free baseline + window matters: state effect lives in recovery, not dip
**Changed/Found:** `contra_prediction.m` — dropped motion from the contra OLS baseline (`X_cp_m = X_cp_z`, RRR `cols_r` de-motioned) to remove the motion-as-regressor-AND-split circularity; added [CP-6w] diagnostic that recomputes cp_err over 0-200ms vs 0-500ms and re-runs state correlations. RAN (warm workspace, selExp=3 AL_0033). Result: held-out R²=0.899 (was 0.898 w/ motion → motion adds ~nothing to prediction). State correlations (motexcl n=561):
  - **0-200ms (inhibition dip):** Motion r=-0.006 p=0.86; PreVar r=+0.049 p=0.25; PreDelta r=+0.059 p=0.16 — **ALL NULL**.
  - **0-500ms (dip+recovery):** Motion r=+0.019 p=0.60 (null); **PreVar r=+0.119 p=0.005 (Spearman rho=+0.171 p<1e-4)**; **PreDelta r=+0.108 p=0.011 (Spearman rho=+0.158 p=2e-4)** — variance & delta SIG, motion null.
  - CP-6b motion split (z>0): High=0.611±0.045 vs Low=0.625±0.020 — no difference.
**Why:** User asked to test whether stim quality is state-dependent via the contra+stim route; needed motion non-circular and both candidate error windows compared before picking canonical. Removing motion from baseline also STRENGTHENED the variance correlation (was "ns" in CP-4e when motion was in baseline → now p=0.005), confirming motion was confounding it.
**Next:** USER DECISION pending on canonical error window. Scientific fork: 0-200ms = inhibition depth is state-INDEPENDENT (clean null); 0-500ms = post-inhibition recovery predictability IS state-dependent (variance+delta, NOT motion). Once chosen, set err_end_s, re-export, update FINDINGS.md/manuscript. Premise correction: motion does NOT make stim more predictable (clean null both windows); the real state variables are pre-stim variance + delta, and only in the recovery window.

### 2026-06-17 — State-effect audit of contra+stim pipeline: 2 bugs found (parallel-agent debug)
**Found:** `contra_prediction.m` — (1) **Motion circularity**: motion is a regressor in the contra OLS baseline (`X_cp_m = [X_cp_z, mot_z]`, ~line 236) AND the CP-6b split / CP-6 correlation variable (~lines 1351, 1413). So `cp_err` is a motion-regressed residual; splitting/correlating it by motion is partially circular — likely explains why motion looks NULL on `cperr` (r=-0.015) while it's significant on the original `peakdev` metric (z>1.5, p=0.017). Motion also gets a different trial set (no motExcl) than pvar/delta (motExcl4), so it's not directly comparable. (2) **Stale window/alpha labels**: `err_end_s=0.5`/`cp4_alpha=0.9` (lines 874/877), but commit 37108fc had set 0.2/1.0; revert in 83f8f55 was undocumented. Line 1017 comment still says "alpha=1", line 1356 fprintf still says "MSE 0-200ms" (actual = 0-500ms, per lines 1036/1480). cp_err exported to downstream scripts is therefore 0-500ms MSE, not the 0-200ms inhibition-energy window the original finding was built on.
**Why:** User wants to condition contra+stim prediction error on motion/variance/delta state to test whether stim quality itself is state-dependent. Verified by reading current file: err_end_s=0.5, cp4_alpha=0.9 confirmed at lines 874/877; stale labels confirmed at 1017/1356.
**Next:** Decide canonical error window (0.2 vs 0.5 s) and fix stale labels; add a motion-free baseline variant (`beta_cp_nomot`) so the motion-state split is non-circular; add CP-6c (variance split) and CP-6d (delta split) to mirror CP-6b. Reconcile premise: pipeline records motion-NULL/delta-SIG on cperr, opposite to user's stated "high motion more predictable" framing.

### 2026-06-17 — Audit: state finding present in RESEARCH.md, partial in FINDINGS.md, ABSENT in manuscript
**Found:** The "stim predictability vs brain state" finding is recorded in `RESEARCH.md` (lines 19-23 motion threshold; 88-91 CP-4e delta result) but: `FINDINGS.md:61` motion entry is outdated (claims motion predicts deviation, doesn't reference the CP-4e motion-NULL/delta-SIG result); `FINDINGS.md:71` delta entry has placeholder "r=XX p=XX"; manuscript `Closedloop_edit/results.tex` has it only as TODOs (lines 64-69 pre-stim brain state `\todo{SECTION TO BE WRITTEN}`, 108-112 spectral `\todo`). Motion section of results.tex is about CONTROLLER motion rejection (OL vs CL), not impulse-prediction state effects.
**Why:** User asked whether the finding is present in pipeline + paper before building the state-conditioned contra-prediction analysis on top of it.
**Next:** Fill FINDINGS.md delta-power r/p from CP-4e (r=+0.10 p=0.017; Spearman rho=+0.14 p=0.001); flag FINDINGS.md:61 motion entry as peakdev-stream-only; draft the results.tex pre-stim-brain-state subsection once window bug (above) is resolved so the cp_err numbers are canonical.

### 2026-06-16 — Motion effect is a THRESHOLD (z>1.5) effect; split motion/prestim into two parallel streams
**Found:** Built high-motion example figure `paper/images/figure2/cp_highmotion_examples.png` (12 highest-motion trials, actual vs contra-pred vs ARX+TF) after the user flagged the "motion doesn't matter" result as contrary to the original setup. Several high-motion trials ARE badly predicted (e.g. 2.7V motZ=4.2 err=2.00). Threshold sweep on CP-4 error (selExp=3): z>0 p=0.78 ns, z>0.5 p=0.50, z>1.0 p=0.27, **z>1.5 p=0.017 SIG (Hi med 0.66 vs Lo 0.50), z>2.0 p=0.020**; Spearman all-trials rho=-0.01 ns, Pearson r=+0.075. => Motion corrupts prediction ONLY above gross-motion z>1.5 (= project motThresh=1.5); the linear/rank correlation and the z>0 CP-6b split are null because mild motion (the bulk) genuinely doesn't matter. RECONCILES old-vs-new: original setup used the 1.5 threshold (where motion matters); CP-6b used z>0 (where it doesn't). Nothing broken — effect is real but nonlinear/threshold.
**Changed:** `motion_analysis.m` + `prestim_variance.m` — added `dev_metric` switch ('peakdev' = original |Peak_imp-mean| deviation-from-trial-avg | 'cperr' = CP-4 contra-pred error). Selection, y-labels, and EXPORT FILENAMES all tagged with the stream so the two never overwrite. `run_all.m` loops both streams. Added `expColors` guard so motion_analysis runs standalone. Verified both streams run clean and produce `..._peakdev` / `..._cperr` outputs; pooled motion r: peakdev -0.131, cperr -0.075.
**Why:** User: "keep two separate streams — original (motion/variance vs per-trial deviation from trial average) AND the contra-prediction route." Plus wanted to see high-motion example predictions.
**Next:** Decide canonical motion threshold (1.5) + error window for the paper. prestim `prevar_heatmap_with_blockfit.pdf` (fig_pvs 2H) is not yet stream-tagged (uses its own pred-error metric). Stale untagged orphans `imp_motion_devscatter_all_sessions.pdf` / `prevar_vs_dev_allamps_motexcl_AL_0033...pdf` can be deleted.

### 2026-06-16 — contra_prediction: fix ROI/SVD cache path (re-detection bug) + CP-6b motion z-score bug
**Changed:** `impulse-analysis/contra_prediction.m` — (1) anchor `roi_file` and `path_cp` to `fullfile(impulseDir,'data')` (absolute) instead of pwd-relative; resolve `impulseDir` via `mfilename('fullpath')` (robust to cwd/path) with which()/pwd fallback; auto-migrate a legacy `cp_roi_*.mat` from the script folder into `data/`. (2) CP-4c motion z-score: was normalizing per-trial `imp_data.mot` (already ~centered, mean 0.08) by the raw continuous `mot_full` trace (mean 8213, std 5338) → every trial z≈−1.5 → CP-6b high/low split gave 0 high-motion trials. Fixed to pool per-trial `imp_data.mot` across amplitudes for the z-score.
**Why:** User reported the midline/mask ROI re-detecting on each run "which should not happen if it's in the data." Root cause: bare-filename cache saved to pwd; the orphaned ROI sat in `impulse-analysis/` while runs from root looked in `./`. The motion bug surfaced when verifying the run (CP-6b "0 high-motion trials").
**Found (verified run load_experiments→tf_fit→contra_prediction):** ROI + SVD now load from absolute `...\impulse-analysis\data\` regardless of cwd, no re-detection. CP-6b now splits High=105 / Low=643 (was 0/748); motion scatter x-range 0–15 (was −1.55..−1.40). Pred error High 0.65±0.05 vs Low 0.62±0.02 (≈equal, consistent with motion-null). Pipeline runs clean end-to-end; CP-4d ARX=0.952, +TF=0.970.
**Next:** Note CP-6 now uses 0–500 ms error window + onset-clamped pink → all 4 predictors weakly+significant (motion r=0.075 p=0.04, prestim var r=0.115 p=0.006). Earlier 0–200 ms run had motion null — window choice changes the motion result; decide final error window. run_all.m reordered (contra_prediction before motion/prestim, which now consume `imp.cp_err`).

### 2026-06-16 — motion_analysis.m: add pooled prediction error vs motion z-score, threshold 1.5
**Changed:** `impulse-analysis/motion_analysis.m` — new figure at end: scatter of prediction error (Y) vs motion z-score (X) pooling all sessions and all amps; binary split at z=1.5 (blue below, red above); Pearson r + n printed; exports `imp_motion_pred_err_thr15.png`
**Why:** User requested combined all-session/all-amp view with 1.5 as the high-motion threshold (stricter than the existing 0.5 threshold used in the tertile figures)
**Next:** Check whether high-motion trials (z>1.5) have systematically higher prediction error; if r is near zero, motion is not a driver of prediction error

### 2026-06-16 — CP-5 trial grid: rows = amplitude levels, columns = trials sorted by error
**Changed:** `impulse-analysis/contra_prediction.m` CP-5 Fig A — replaced single-amplitude 25-trial grid with amplitude-stratified layout: `nAmps5g` rows × `nPerAmp5` (=5) columns; each row is one valid amplitude, trials within row sorted ascending by prediction error; y-axis label of first column shows amplitude in volts; empty cells (fewer trials than nPerAmp5) are blanked with `axis off`
**Why:** User requested trial-level responses characterised by amplitude to see whether prediction quality differs across stim levels
**Next:** Check figure — expect worse prediction (higher error) at stronger amplitudes if stim bleed is not fully removed; if best-predicted trials across all amps look similar, decontam is working

### 2026-06-16 — CP-6b: high/low motion split + error window 0–500 ms + session z-score motion
**Changed:** `impulse-analysis/contra_prediction.m` — (1) `err_end_s` 0.2→0.5 s so prediction error MSE spans dip + early recovery; (2) per-trial motion (`mot_ia4`) now z-scored against full-session `mot_full` mean/std (same scale as predictor `mot_z`); (3) new `[CP-6b]` section after CP-6 scatter: splits trials by session-z-scored motion at threshold 0 (= session mean), computes mean ± SEM actual + contra-pred traces per group and mean prediction error ± SEM, exports `cp6_motion_split.png` (Fig 14)
**Why:** User observed spaghetti (Fig 12) shows residual contamination; motion classification needed to determine whether high-motion trials drive prediction error; z-score against session mean makes high/low split interpretable (0 = session average motion)
**Next:** Run CP-4 through CP-6b; check Fig 14 — does high-motion group have larger prediction error and/or different mean actual trace? If yes, motion is a confound not a predictor

### 2026-06-16 — contra_prediction.m: move CP-4e + scatter to new [CP-6] section after CP-5
**Changed:** `impulse-analysis/contra_prediction.m` — removed CP-4e correlations block and Fig 4 scatter from between CP-4d and CP-4f; re-inserted as `%% [CP-6]` after CP-5 Done fprintf; CP-4 fprintf trimmed to 3 exports; scatter sgtitle updated to "CP-6 error correlations"; Fig 4 is now Fig 13
**Why:** User requested motion/variance/delta analysis run last so all CP-4 prediction errors are fully populated before cross-correlating with behavioural predictors; simplifies script narrative: model build → visualise → trial inspect → diagnose
**Next:** Verify CP-6 runs cleanly — err_all4, mot_all4, pv_all4, dpre_all4, dstim_all4 are all accumulated in CP-4c so they are in scope at CP-6

### 2026-06-16 — CP-4c: clamp pink to zero at t=0 (consistent with act reference)
**Changed:** `impulse-analysis/contra_prediction.m` CP-4c per-trial loop — added `pink_j4 = pink_j4 - pink_j4(1)` after computing pink, before computing red. `act_j4` was always zero at t=0 (actual minus its own onset value). `pink_j4` was NOT zero at t=0 (model prediction at t=0 minus the actual onset value — a different reference). Clamping pink to its own onset value makes both act and pink express change-from-stim-onset, giving them a consistent reference. `red = pink + h4` inherits the fix; h4(1)≈0 for a strictly proper TF so red also starts near zero.
**Why:** User identified the mismatch: averaging collapses actual to exactly 0 at t=0, while pink doesn't share that property, creating a visual initial-condition gap in all trace figures (fig9/CP-5).
**Next:** Re-run CP-4 and check fig9 (mean traces) and CP-5 trial grid: all three signals (act/pink/red) should start at 0 at t=0. Note that err_ia4 values will shift slightly (initial-condition contribution to MSE removed).

### 2026-06-16 — CP-4a fig8: onset-baseline + SEM band to show actual initial condition
**Changed:** `impulse-analysis/contra_prediction.m` CP-4a — accumulation loop now also tracks per-trial mode-averaged contra sum + sum-of-squares → `sem4_all` (across-trial SEM). Figure baseline changed from pre-stim mean to stim-onset value (`bl4_fig = mu4_all(onset_idx4)`) so traces are relative to t=0, consistent with all other trace figures. Gray per-mode lines also shifted to onset baseline. Added SEM shading band to mean trace so the spread of initial conditions across trials is visible in the pre-stim period. Pre-stim mean (`bl4_a`) kept for artifact ratio computation only.
**Why:** Pre-stim mean subtraction forced the pooled mean trace to exactly zero pre-stim, erasing all initial condition information. Other trace figures (CP-IMP, CP-4 traces) are all relative to stim onset, not pre-stim mean — this fix restores consistency.
**Next:** Re-run CP-4a and inspect fig8: pre-stim SEM band should be non-zero; post-stim artifact should be visible as deviation above/below the stim-onset level.

### 2026-06-16 — CP-4: cp4_alpha=0.8 (match CP-IMP kernel_alpha); fig8 shows raw+decontam mean
**Changed:** `impulse-analysis/contra_prediction.m` — `cp4_alpha` changed from 0.0 to 0.8 (was temporarily set to 0.0 to fix flatness; now matched to CP-IMP `kernel_alpha=0.8`). Fig 8 (artifact check) updated: now shows raw pooled mean (black) AND decontaminated pooled mean (blue dash) so the before/after is explicit and the continuity from figure 7 → 8 is visible.
**Why:** cp4_alpha=0 fed raw contra (full bleed) into beta_cp, making pink contaminated by the stim effect. cp4_alpha=0.8 removes 80% of the bleed consistently with CP-IMP while leaving 20% of the mean artifact + spontaneous trial-to-trial variation → pink is smooth, non-flat, and mostly bleed-free. Figure 8 previously showed only raw contra (user expected to see the decontam effect after figure 7 addressed the bleed).
**Next:** Re-run CP-4 and inspect fig8 (raw vs decontam line) and CP-5 trial grid to confirm pink is smooth and non-flat but not driven by the full bleed.

### 2026-06-16 — CP-4: set cp4_alpha=0 so pink uses raw contra dynamics (not flat)
**Changed:** `impulse-analysis/contra_prediction.m` — `cp4_alpha` changed from 1.0 to 0.0. With alpha=1 the full mean per-amplitude contra artifact was subtracted from X_win4 before beta_cp, leaving near-zero signal during the stim window → flat pink. alpha=0 skips that subtraction so the ARX model sees real contra dynamics (including contra's own stim response) → smooth non-flat pink, matching the PCA-era behaviour.
**Why:** User observed pink was flat during the impulse effect window; PCA had given a smooth natural response because it only removed the PC1 stim direction rather than the full mean signal.
**Next:** Re-run CP-4 and inspect CP-5 trial grid to confirm pink is now non-flat; check whether red = pink + TF over-predicts (since contra's stim response is counted twice — once in pink via beta_cp, once in h_TF4). If double-counting is visible, may need to revisit the decomposition.

### 2026-06-16 — Add [CP-5] individual trial on-stim trace inspection section
**Changed:** `impulse-analysis/contra_prediction.m` — new `[CP-5]` section appended before local functions. Two figures: (A) `cp5_trial_grid.png` — configurable grid of `nShow=25` trials sorted by prediction error, each subplot shows actual/pink/red with error-window shading; (B) `cp5_trial_spaghetti.png` — all-trials faint overlay + mean thick for every valid amplitude. Parameters `selAmp_tr`, `nShow`, `sort_by_err` at top of section.
**Why:** User suspects a conceptual issue in the on-stim predictions; per-trial view needed to diagnose whether contra SVD (pink) is contaminated by ipsilateral response or the TF decomposition has structural problems.
**Next:** Run [CP-5] and inspect: does pink track actual with an amplitude-dependent pattern during stim? Does red-pink (TF contribution) have the expected impulse shape on every trial?

### 2026-06-16 — Wire cp_err into motion_analysis and prestim_variance as deviation signal
**Changed:** `impulse-analysis/contra_prediction.m` — after CP-4c loop, writes `err4` and `pvar4` into `allExperiments(selExp).imp.cp_err` and `.cp_pvar` so downstream scripts can read them.
**Changed:** `impulse-analysis/motion_analysis.m` — Section 1 (per-experiment scatter) and Section 2 (selExp motion-sorted + pooled all-sessions scatter) now use `imp_e.cp_err{iAmp}` as the Y deviation signal when present; fall back to `Peak_imp_dev` otherwise.
**Changed:** `impulse-analysis/prestim_variance.m` — both Section 1 (all experiments) and Section 2 (motion-excluded) now use `cp_err` as Y when present; ylabel updated from '|Peak dev|' → 'Prediction error'. Section 2 also clips `n_total` to `numel(cp_err{iAmp})` so trial indexing stays aligned.
**Why:** Methodology change: prediction error from the contra-SVD+TF model is now the canonical measure of trial-to-trial variability, replacing raw deviation from mean peak.
**Next:** Run contra_prediction → motion_analysis → prestim_variance (via run_all) and confirm figures update; check that cp_err is non-NaN for enough trials before deciding whether to tighten nzMask or add a NaN-count guard.

### 2026-06-16 — run_all.m: move contra_prediction before motion/prestim scripts
**Changed:** `impulse-analysis/run_all.m` — moved `contra_prediction.m` to run immediately after `tf_fit.m` (was last); updated comment to document the full dependency chain.
**Why:** motion_analysis and prestim_variance now use contra_prediction residuals as the deviation signal, so contra_prediction must complete before those scripts run. tf_fit → contra_prediction → motion_analysis / prestim_variance.
**Next:** confirm motion_analysis.m and prestim_variance.m actually read the residual/prediction-error variables contra_prediction writes, rather than the raw Peak_imp_dev from load_experiments.

### 2026-06-16 — TF fit + CP-4 layered model run: impulse-prediction error tracks pre-stim DELTA power, NOT motion
**Changed:** `impulse-analysis/contra_prediction.m` CP-4 — error window changed from 0-1s to 0-200ms (`iErr`, the dip, matches peak_mode=3); wired the no-stim baseline (pink) to a cosine-tapered per-amplitude artifact at `cp4_alpha=1` (`art_tap4_amp`, full decontam) so red=pink+full-TF doesn't double-count the bleed and post-dip dynamics are kept.
**Found (ran load_experiments->tf_fit->contra_prediction, selExp=3):** (1) TF fit best = 2p0z0d, G(s)=-505.1/(s^2+7.37s+200.6), underdamped 2nd-order (wn~2.25Hz, zeta~0.26 = dip+rebound), pooled R2=0.797, per-amp up to 0.88, LOAO tracks full fit (0.55->0.87) -> generalizes across amplitude. (2) CP-4 layered: contra baseline R2=0.870, +TF=0.893 (TF adds +0.023; TF alone 0.025). (3) CP-4e per-trial error (MSE 0-200ms) vs brain state: Motion (all 748) r=-0.015 ns; pre-stim var (motexcl 561) ns; pre-stim DELTA (1-4Hz) power r=+0.10 p=0.017 SIG; stim delta ns. Amplitude-normalized (z within amp) + Spearman CONFIRMS: motion robust NULL (rho=-0.01), prestim var weak trend (rho=+0.08 p=0.07), prestim delta rho=+0.14 p=0.001 (real), stim delta rho=+0.10 p=0.023 (weak). HEADLINE: impulse-prediction error driven by pre-stim DELTA-band power (synchronized/down-state), NOT motion or broadband variance — cleaner & more specific than the original motion hypothesis; connects to Curto&Issa synced-vs-desynced. Figs cp4_traces.png, cp4_r2_bars.png, cp4_error_correlations.png.
**Why:** User (JOURNAL 2026-06-16) wanted prediction=contra+TF vs actual, error over 0-200ms -> brain state, motion first then variance on motion-excluded trials. CP-4 already had the scaffold; only the error window + baseline decontam needed fixing.
**Next:** Bake amplitude-normalized error (z within amp) + Spearman into CP-4e (more sensitive; tested inline). Decide final kernel_alpha. CP-4 traces fig still 45cm sliver (cosmetic).

### 2026-06-16 — contra_prediction: ADOPT scaledkernel decontam (alpha*tapered per-amp artifact) — stable, no flips
**Changed:** `impulse-analysis/contra_prediction.m` CP-IMP — added `decontam_mode='scaledkernel'` (now default), `kernel_alpha=0.8`, `taper_win=[0.20 0.40]`. Builds a cosine-tapered per-amplitude artifact (`art_tap_imp`) and subtracts `kernel_alpha * art_tap_imp` from contra. Pass-2 branch added.
**Why:** User decision tree "improve PCA, else fix flattening, else PCA". PCA proved NOT improvable: low-rank PCA (per-amp basis AND deterministic mean-artifact template, any rank 1–4) STILL flips positive at 2.1/3.2/4.3/4.9V because PCA orders modes by artifact VARIANCE not by beta-weight, so beta*(truncated artifact) mis-scales and overshoots the bleed — intrinsic, not tunable. The fix-the-flattening branch wins: beta*(full artifact)=the bleed, so subtracting alpha<1 * artifact is a CONVEX scaling of the bleed -> mathematically cannot overshoot (NO flips), the taper fades the subtraction out past the dip (no boundary step), and alpha<1 leaves natural dynamics (no switch-off).
**Found (full pipeline run, alpha=0.8):** per-amplitude bleed left = CONSISTENT 13–20% at ALL 9 amplitudes (no scatter, no flips), pooled 79%->16% (recovers 84% of dip), boundary smoothness 0.322 (vs kernel ~0.6, vs per-amp PCA 0.435), post-dip 75% explained, amp-0 unbiased. Figs: `paper/cp_decontam_scaled_kernel.png` (clean compact), `paper/cp_decontam_pca_vs_kernel.png`, `paper/cp_impulse_pred_peramp_pca.png`. alpha is the bleed-removal knob (1.0==kernel/switch-off, 0.8 leaves ~15%).
**Next:** Optionally sweep alpha vs the residual (alpha=0 lower bound) for the final figure. CP-4 still blocked on tf_fit.m. Decide TF target (residual vs scaledkernel-decontam dip).

### 2026-06-16 — contra_prediction: per-amplitude PCA decontam implemented + run — still flips; kernel is the per-amp-exact option
**Changed:** `impulse-analysis/contra_prediction.m` CP-IMP — added `pca_per_amp` flag; build a separate rank-1 PCA artifact basis per amplitude (`Ab_pca_amp{ia}` from each amplitude's own dip epoch, all in-sample trials, fall back to pooled if <3 trials); Pass 2 selects the amplitude-specific basis. Set `decontam=true`, `pca_per_amp=true`. Also computed `kernel` mode inline for comparison.
**Found (full pipeline run, in-sample):** Per-amplitude PCA does NOT fix over-subtraction. Per-amp PC1 explains only 34–72% (artifact not rank-1 dominant), so rank-1 projection over-subtracts: per-amp bleed left = 1.6V +14%, 2.1V -27%, 2.7V +24%, 3.2V -50%, 3.7V +12%, 4.3V -12%, 4.9V -7% — STILL flips positive at 2.1/3.2/4.3/4.9V even in-sample; pooled looks clean (4%, recovers 96%) but that is the same averaging mirage; boundary smoothness WORSE (0.435 vs pooled 0.055). Figs `paper/cp_impulse_pred_peramp_pca.png`, `paper/cp_decontam_pca_vs_kernel.png`. KERNEL mode (per-amplitude onset-mean subtraction) gives EXACT per-amplitude zero bleed (0% at all 9 amplitudes) but switches contra OFF in the dip (flat prediction) + hard 0.3s boundary step — the switch-off the user rejected earlier. TRILEMMA: per-amp-exact / smooth-through-dip / stable-no-flip — pick 2. Kernel=exact+stable not smooth; per-amp PCA=smooth not exact/stable; residual=smooth+stable+exact but LOWER BOUND (21%).
**Why:** User asked to implement per-amplitude correction, re-run the whole pipeline, regenerate plots, and asked whether PCA is the only decontam method.
**Next:** User decision pending on which method to adopt as default (residual vs kernel vs per-amp PCA). decontam=true/pca_per_amp=true currently committed-pending; revert to residual if not chosen. CP-4 still blocked on tf_fit.m.

### 2026-06-16 — contra_prediction: full run with residual default — guard verified, Ye benchmark, OLS wins
**Found:** Ran `impulse-analysis/contra_prediction.m` end-to-end with `decontam=false` (committed 6afcff0). (1) Guard works: CP-IMP prints raw_pred==decontam_pred at every amplitude (bleed % identical), confirming the kernel branch no longer fires when decontam off → residual = actual−raw β. (2) CP-FIT held-out R²=0.900. (3) Ye benchmark (CP-RRR): OLS-200 CV R²=0.900 vs RRR/PCR r*=100 CV R²=0.890 — OLS-200 wins marginally (RRR at full rank IS OLS). Ye's ~16-comp plateau does NOT replicate: rank sweep climbs to ~100 comps (single focal-pixel target is higher-dim than Ye's hemisphere field). Both well-conditioned (Gram cond 50–62). DECISION: keep instantaneous OLS-200; PCR/PCA gives no accuracy gain. (4) Residual: pooled bleed 79% (recovers 21% of dip), post-dip 78% explained, boundary smoothness 0.055 (no subtraction → no artifact), amp-0 unbiased (+0.018 vs +0.014).
**Why:** User asked to commit then run to see live OLS-vs-Ye-RRR contrast and confirm the residual default runs clean.
**Next:** CP-4 errors at TF dependency (needs `best_sys`/`uA_s` from tf_fit.m) — known, in TASKS. Cosmetic: cp_impulse_pred_noTF.png still exports 45×8cm sliver (pre-existing). Pending decision: TF fit on directly-measured impulse vs residual.

### 2026-06-16 — Manuscript: resolved 4 open \todo blocks across results + methods (Closedloop_edit)
**Changed/Found:** `Closedloop_edit/results.tex`, `methods_edit.tex` — (1) variance-convergence \todo → batch-mean stationarity result; (2) OL onset-variance claim kept "not significant", forward-ref to Fig 3I OL/CL ratio; (3) new motion-disturbance paragraph + Fig (motion_mse_quartile.pdf); (4) TF \todo → cross-session comparison + Table 1; (5) spatial-spread \todo → Fig 6/7; expanded Statistical analysis (Wilcoxon/Spearman/rank-sum/sign); stated both MSE windows (0→3 general, +1→3 disturbance); mouse breakdown (AL_0033 ×9, AL_0039 ×4 = 13); sum-vs-MSE wording. Copied 4 figures into Closedloop_edit/images/.
**Why:** User directive: write up impulse #1/#2 + controller #5/#10 results, keep #4 claim, improve controller methods sections.
**Next:** draftmode pdflatex exit 0, all new \ref/\label resolve. Pending author judgment: feedforward eq degeneracy (Kr vs Kp identical error-driven form, eq:ff/eq:pi); Nuoli/Svoboda .bib keys; pre-existing undefined `eq:controller` ref in discussion.tex. Not pushed — awaiting user command.

### 2026-06-16 — motion_mse_significance.m: closed-loop feedback rejects motion disturbance (controller #5)
**Changed/Found:** `controller-analysis/motion_mse_significance.m` (new) — per-session regression of within-session-z MSE on concurrent motion, OL vs CL. OL slope median +0.72 (signrank vs 0 p=0.0039); CL slope median −0.016 (p=0.91); interaction signrank p=0.0078 (OL>CL 8/9 sessions); top motion quartile OL z-MSE +0.71 vs CL −0.44 (ranksum p=4.3e−16). Exported motion_mse_quartile_OLCL.pdf.
**Why:** User wanted significance for "high-motion → high OL MSE, low CL MSE"; chose slope-interaction over quartile-only (consistent with pre-stim-variance regression framework).
**Next:** Spectral disturbance-attribution portion still held. Panel needs Illustrator compositing into Fig 3/4.

### 2026-06-16 — batch_mean_stationarity.m: spontaneous batch mean stationary across all 13 sessions (#10)
**Changed/Found:** `controller-analysis/batch_mean_stationarity.m` (new) — regressed per-trial spontaneous mean ΔF/F on acquisition order. 0/13 significant (OLS all p>0.24, Spearman all p>0.12); slopes balanced 8+/5− (sign-test p=0.58); median drift 0.17 trial-SD. Exported batch_mean_stationarity.png.
**Why:** Resolve reviewer \todo — variance convergence (CLT) doesn't exclude a drifting mean; needed direct mean-stationarity evidence (option i).
**Next:** Wrote into results.tex + methods Fig S2. None further.

### 2026-06-16 — tf_fit_allsessions.m: TF dynamics agree across 2 of 3 impulse sessions (impulse #1)
**Changed/Found:** `impulse-analysis/tf_fit_allsessions.m` (new) — looped tf_fit logic over 3 sessions, AIC-selected, poles→time constants. AL_0033: 2p/0z, τ=271 ms, 2.2 Hz, R²=0.80; AL_0041 en1: 3p/1z, τ=196 ms, 2.8 Hz, R²=0.74; AL_0041 en2: 2p/1z, R²=0.48 (low-SNR, fit→slow drift, excluded).
**Why:** Nick 2026-05-08 request; validate stationary-LTI assumption across sessions.
**Next:** Two well-fit sessions agree (~200–270 ms, 2–3 Hz). Wrote into methods Table 1. en2 SNR revisit optional, not blocking.

### 2026-06-16 — spatial_spread.m: exported supplementary inhibition-area figure (impulse #2)
**Changed/Found:** `impulse-analysis/spatial_spread.m` (run) — exported spatial_spread_AL_0033_2025-01-29_en1.pdf (6×4) + spatial_maps PNG. Inhibition area grows monotonically with power (focal → ~85–91 mm² at high amp); low-amp points noisy (qualitative thresholding).
**Why:** Resolve methods \todo for spatial-spread supplementary panel.
**Next:** Inserted as methods Fig 6/7. Citation (Nuoli/Svoboda) still needs .bib keys.

### 2026-06-15 — contra_prediction: per-amplitude PCA basis does NOT rescue decontam — PCA abandoned
**Found:** Built per-amplitude rank-1 PCA artifact bases from train trials (fair rng=42 70/30 split, 524/224) and applied to held-out val, vs pooled basis. Fig `paper/cp_val_peramp_pca_rescue.png`. Per-amp PC1 explains only 34–68% (vs pooled 79%) → noisier basis, less reliable per-trial coeffs. Per-amp decontam still non-monotonic and STILL flips positive at 2.7V (+0.07) and 4.3V (+1.08, worse than pooled +0.74); bleed-removal swings 96%→−75% across amplitudes with no consistency. Marginally better than pooled only at 2.7V. Root cause unchanged: projecting per-trial contra onto a spatial artifact basis with a per-trial scalar coeff is unstable because the coeff is fit on dip-window data containing real neural structure spatially correlated with the artifact.
**Why:** User asked to attempt the per-amplitude PCA rescue and show plots before abandoning PCA.
**Next:** PCA decontam (pooled or per-amplitude) abandoned for per-trial/per-amplitude impulse recovery. Adopt residual (actual − raw β) as the impulse estimate; proceed to TF fit on the residual dip.

### 2026-06-15 — contra_prediction: residual impulse (actual − raw β) beats decontam — stable, monotonic, flat baseline
**Found:** Prototyped `Imp_res = actual − raw_β` vs `Imp_dec = actual − decontam` on the 224 held-out validation trials. Fig `paper/cp_val_residual_vs_decontam.png`. Residual recovered-impulse dip is MONOTONIC in amplitude (−0.01 → −0.11 → −0.19 → −0.34 → −0.38 → −0.47 → −0.49 → −0.48 across 0.5–4.9V) with small SEM, always correct sign; baseline flat (pre ≈ 0.00, post a small physiological rebound +0.01..+0.17) — i.e. correctly reads "no impulse" outside the stim window. Decontam-recovered dip is non-monotonic and overshoots: at 2.7V it is −1.44, DEEPER than the actual signal (−1.04), which is unphysical (implies contra predicted an upward deflection). Outside the stim window Imp_res and Imp_dec coincide (decontam only edits neg_win); they diverge only in-window where decontam over-subtracts.
**Why:** User asked to prototype the actual−raw_β residual as a more stable alternative to the unstable per-amplitude decontam (see prior entry).
**Caveat (important):** residual is a LOWER BOUND on the true stim effect — because contra is itself stim-driven, raw β legitimately explains away the portion of the ipsi dip that co-occurs with the contra dip, so actual−raw_β removes real shared stim variance along with spontaneous coupling. True impulse lies between residual (lower) and actual dip (upper). Residual wins on stability/monotonicity, not on absolute magnitude.
**Next:** Decide residual-vs-decontam for Phase 2 TF fit. If absolute magnitude matters, consider scaling residual by 1/(1−bleed_frac) using the measured ~79% bleed, or report residual as a conservative lower-bound impulse.

### 2026-06-15 — contra_prediction: held-out validation confirms PCA decontam over-subtracts per-amplitude (2.7V positive bump)
**Found:** Built PCA artifact basis from TRAIN trials only (524), applied decontam to 224 held-out validation trials, per-amplitude. Fig `paper/cp_val_decontam_smoothness.png`. RAW β prediction (no decontam) is well-behaved at every amplitude: dip always negative, reproduces 54–95% of actual dip (the bleed) and is SMOOTH. PCA-decontam is NOT smooth per-amplitude: dip fraction scatters wildly — 0.5V −165%/255%, 1.1V FLIPS to +0.11 (−50%), 2.1V clean (0.5%), **2.7V FLIPS to +0.40 (−39%)**, 3.2V 13%, 3.7V −42% (under-corrects), 4.9V FLIPS to +0.31 (−16%). Confirms user's 2.7V observation: the spurious positive "impulse-type" bump in the stim window is **PCA over-subtraction by the rank-1 pooled basis, NOT a β fit problem** (raw β at 2.7V is correctly −0.70). Pooled −4% was averaging positive overshoots against under-corrections (1.6/3.7 still −66%/−42%).
**Why:** User flagged 2.7V positive overshoot on validation; wanted to verify whether PCA-based ipsi prediction stays smooth during the trial on random held-out trials.
**Next:** Rank-1 pooled basis cannot match per-amplitude artifact magnitude. Options: (a) scale projection coeff per-amplitude / per-amplitude PCA basis; (b) preferred — treat raw β prediction as the trustworthy "bleed" reference and define impulse response as actual − raw_β residual, rather than flattening the prediction itself. Decide before Phase 2 TF fit.

### 2026-06-15 — contra_prediction: PCA stim-artifact removal (decontam_mode='pca') — smooth, no switch-off
**Changed:** `impulse-analysis/contra_prediction.m` CP-IMP — added `decontam_mode` ('pca' default | 'kernel'), `n_art_pca`. PCA path (from `pca_stim_artifact_removal.md`): mean stim-artifact epoch over dip window, pooled → PCA → rank-1 artifact basis (PC1=84%); per-trial project contra onto basis within neg_win and subtract. Added boundary-smoothness diagnostic. Compact compare fig cp_impulse_pred_pca_compact.png.
**Why:** kernel method (mean subtraction) flattens the dip = "switch-off" + hard boundary step; user wanted prediction smooth through the impulse. PCA removes only the rank-1 artifact SUBSPACE per trial, keeping the neural subspace. Result: pooled bleed 79%→-4% (recovers 104% of dip), post-dip explains 78% (matches kernel), boundary step 0.244 vs kernel ~0.6 (~2.5x smoother), spontaneous R²=0.900 UNTOUCHED (input-only cleaning, β unchanged — unlike soft-weighting which collapsed R²). Amp-0 unbiased. Key design: restrict projection to dip window (neg_win) so post-dip contra stays intact (projecting over whole window dropped post-dip to 34%).
**Caveats:** per-amplitude bleed noisy (pooled clean; rank-1 pooled basis + per-trial coeffs scatters per amp, e.g. A=2.10 -114%) — fine for pooled/TF fit. Small residual bump at 0.3s remains (~2.5x reduced) — hard window edge; taper would remove it. kernel mode still available.
**Next:** Optional — boundary taper and/or per-amplitude PCA basis. Then Phase 2: TF fit to recovered dip residual (needs tf_fit.m).

### 2026-06-15 — contra_prediction: SVD mode selection as decontam strategy — failed
**Changed/Found:** `impulse-analysis/contra_prediction.m` — Tested using predictive weight P_j = |beta_cp(j+1)| vs contamination C_j = mean contra dip deviation per mode to select "clean" modes. Result: r = −0.014 (P vs C uncorrelated); bleed ranged −112% to +103% across modes with no exploitable structure. Even the 30 least-contaminated modes still showed full bleed because stim propagates through the same interhemispheric channels as spontaneous activity.
**Why:** User wanted to see if mode-level selection could avoid the switch-off during stim — this was a plausible approach given we have per-mode predictive weights and per-mode bleed estimates.
**Next:** Mode selection is not viable. Temporal decontam (zeroing neg_win) remains the only sound approach.

### 2026-06-15 — contra_prediction: CP-BLEED map added; confirmed no truly uninfected contra pixels
**Changed/Found:** `impulse-analysis/contra_prediction.m` — Added `[CP-BLEED]` section. Computed onset-locked contra deviation per SVD mode during dip window (0–300 ms), back-projected to pixel space: `bleed_pixel = U_svd_cp * C_signed_orig`. Distribution: min=0.00006, median=1.22, max=4.16 — unimodal, no gap. "Green" pixels (bleed_n < 0.20) had mean absolute bleed = 0.396; no truly uninfected region exists. Exports `cp_bleed_map.png`.
**Why:** User wanted spatial map of which contra pixels carry stim bleed vs prediction signal, to potentially use only uninfected pixels for stim-window prediction.
**Next:** Map is useful as a diagnostic figure. Pixel-level selection is not viable (no clean pixels). Temporal decontam remains the only approach.

### 2026-06-15 — contra_prediction: CP-ARX module removed; replaced with lean CP-FIT
**Changed/Found:** `impulse-analysis/contra_prediction.m` — Removed `[CP-ARX]` + `[CP-ARX-var]` + `[CP-ARX-fig]` blocks (was ~189 lines, 4 figure exports: cp_r2_per_trial.png, cp_r2_distribution.png, cp_r2_vs_variance.png, cp_arx_test_windows.png). Replaced with lean `[CP-FIT]` block (~35 lines): single held-out fold quick R², then full-data fit → `beta_cp`. Confirmed working: `[CP-FIT] 748 windows  nSV=200  held-out R²=0.900`. Full pipeline end-to-end clean.
**Why:** CP-ARX was producing diagnostic figures that cluttered the pipeline; the actual model (instantaneous map, pX=0, pY=0) is unchanged — CP-FIT is the same fit with diagnostics stripped.
**Next:** Pipeline verified. Phase 2 (fit best_sys TF to CP-IMP residual) is still pending. Motion/variance analysis on CP-IMP residuals also pending.

### 2026-06-15 — contra_prediction: soft-weighting (map-based) rejected; temporal decontam confirmed correct
**Found:** Tested user's idea — down-weight stim/motion-affected contra pixels so prediction comes from "clean" pixels (soft weighting via contamination-penalized ridge: per-mode λ_j=Σ_p (impulse+motion)(p)·U(p,j)², same midline+left mask). Built 3 per-pixel maps (impulse-affected, prediction-kernel, motion). Pixel-level feasibility looked great: pred-vs-impulse spatial corr=+0.07 (near-orthogonal), pred-vs-motion=+0.31. BUT the penalized refit fails: bleed and R² fall together (κ sweep: 79%/0.899 → 76%/0.893 → 72%/0.864 → 59%/0.744). ~1:1 tradeoff, no clean separation.
**Why it fails / decided NOT to use it:** Spatial separability ≠ temporal separability. The stimulus propagates into contra through the SAME interhemispheric channels (modes) that carry the spontaneous prediction, so predictive modes == bleed modes. Can mask a pixel, not a temporal channel. ALSO confirmed the temporal-subtraction "switch-off" concern is a mean-only artifact: A=3.70 decontam prediction in dip window has mean +0.005 (flat) but across-trial SD=4.2% (132% of pre-stim) — per-trial natural dynamics ARE fully preserved; the mean is flat only because spontaneous activity isn't onset-locked. Temporal dip-window subtraction (CP-IMP) is the principled approach: bleed→0%, zero R² cost, per-trial dynamics intact.
**Next:** Keep CP-IMP temporal decontam. Optional: keep the 3-map overlay as a paper diagnostic (drop weighting); add single-trial overlay panel to CP-IMP to visualize per-trial dynamics through the dip. Then Phase 2 TF fit to residual.

### 2026-06-15 — contra_prediction: restrict bleed negation to dip window (fix over-suppression)
**Changed/Found:** `impulse-analysis/contra_prediction.m` CP-IMP — the decontam was subtracting the onset-locked contra artifact over the ENTIRE post-onset second, which switched the contra prediction off for the whole second (prediction flat throughout, residual = dip + everything after). Fixed: artifact now subtracted only within a tunable `neg_win=[0 0.30] s` (the stim dip); outside neg_win the contra prediction runs on actual contra and resumes tracking. Added post-dip tracking metric (iPostDip).
**Why:** Desired behaviour = isolate the sharp stim dip in the residual but let contra EXPLAIN the post-dip recovery (coupled network activity). After fix: dip bleed still 0% (dip fully in residual), POST-DIP (0.30-1 s) |actual|=0.241 → |residual|=0.053, i.e. contra explains 78% of the post-dip recovery; residual is now a clean dip returning to baseline. Amp-0 control still unbiased.
**Next:** neg_win is tunable — widen if high-amplitude dips run >300 ms. Possible hard-boundary artifact at neg_win(2) (flat→tracking handoff); offer taper (ramp art down ~3 frames) if a post-dip residual bump appears. Then Phase 2: fit learned TF to the recovered dip residual.

### 2026-06-15 — contra_prediction: CP-IMP stim-bleed negation (decontam) working
**Changed/Found:** `impulse-analysis/contra_prediction.m` — added stim-bleed negation to CP-IMP (`decontam` toggle). Method: per-amplitude trial-averaged onset-locked contra deviation (the stim bleed, per SVD mode) subtracted from contra predictors over the POST-onset window only (pre-stim untouched), then beta applied. Each trace baselined by its OWN pre-stim mean (removes model DC bias → decontam prediction flat at exactly zero, clean per-amplitude validation). Built-in validation: raw-vs-decontam bleed per amplitude + pooled, amp-0 no-stim control. Figure cp_impulse_pred_noTF.png now shows actual + raw pred (bleed) + decontam pred (flat) + residual.
**Why:** Raw contra prediction reproduced 79% of the stim dip (bleedover). After negation: per-amplitude decontam bleed 0% (±1%), pooled raw 79% → -0%, residual recovers 100% of the dip. Amp-0 control (n=196 no-stim trials): actual +0.018, pred +0.014 → model unbiased, no spurious drift. Desired effect achieved: mean prediction flat during stim (no +/- drift), residual = full stim response with tight pre-stim baseline (spontaneous variance removed per trial). Linear-model identity: subtracting onset-locked contra = driving mean predicted deflection to zero.
**Next:** Phase 2 — fit the learned TF (best_sys from tf_fit.m) to the recovered residual; consistency check: TF(residual) should match directly-measured impulse/dose-response. Caveat: mean subtraction leaves trial-level bleed variability — fine for mean/TF, flag before single-trial residual correlations. tf_fit.m still required for CP-4/Phase 2.

### 2026-06-15 — contra_prediction: X-lag (causal FIR) model rejected — instantaneous optimal
**Found:** Tested the one untested lag structure — causal FIR (concurrent + L past lags of X, no Y-lags): ŷ(t)=Σ_{l=0..L} β_l·X(t−l). 5-fold pooled R²: L=0 0.8986, L=1 0.8977, L=2 0.8971, L=3 0.8962, L=5 0.8958 — monotonically worse, doubling params each lag. All three lag structures now tested independently and all lose to instantaneous: past-only (buildLagMatrix), concurrent+future (acausal), concurrent+past (causal FIR).
**Why (decided NOT to use X-lags):** (1) No R² gain — coupling is purely same-frame. (2) Usability for stim window: instantaneous bleed correction is pointwise β·X_stim(t); an FIR makes it a convolution β(L)⊛X_stim that smears post-onset contamination across L frames and low-pass-filters/blurs the residual's sharp inhibitory onset. Instantaneous wins on BOTH R² and decontam tractability — no trade-off.
**Next:** Model locked as instantaneous (pX=0). Proceed to stim-effect negation: per-amplitude evoked subtraction ŷ_clean=β·X−β·art_shape_a + scaled-canonical refinement, amp-0 control + 4 validation checks, as a decontam toggle in CP-IMP.

### 2026-06-14 — contra_prediction: CP-IMP impulse-window prediction (no TF) + bleed quantified
**Changed/Found:** `impulse-analysis/contra_prediction.m` — added [CP-IMP] section: applies the spontaneous instantaneous contra map (beta_cp) across the impulse window [-1,+1] s using only concurrent contra+motion (NO stim info, NO TF). Exports cp_impulse_pred_noTF.png (per-amplitude actual vs pred + residual). Runs before CP-4, no best_sys dependency.
**Why:** Phase 1 of impulse prediction. Confirmed the short 2 s prediction window is a non-issue: the instantaneous model (pX=0) is memoryless, predicts pointwise, needs no history buffer — training-window length (6 s) only set sample count for estimating beta. KEY RESULT: prediction is NOT flat — large contralateral bleedover. At the dip (0-200 ms), the no-stim contra prediction reproduces ~79% of the actual inhibition (pooled actual=-1.28%, pred=-1.02%; per-amplitude 66-100%). So residual=actual-pred currently retains only ~21% of the true stim response. Contra (left) hemisphere is strongly contaminated by the stim-driven inhibition.
**Next:** Decontamination is mandatory before Phase 2 — must negate the contra bleed (per-amplitude artifact shape, cf. CP-4a) so the residual recovers the full stim response, THEN add the learned-TF stim-response model. Verify bleed drops toward 0 after decontam. tf_fit.m still needed for the TF layer.

### 2026-06-13 — contra_prediction: 5-fold CV + per-window variance diagnostic
**Changed/Found:** `impulse-analysis/contra_prediction.m` — replaced single random 60/20/20 split with 5-fold CV (every valid window gets one out-of-fold R²); final beta_cp refit on all 748 valid windows for downstream CP-1..4. Added [CP-ARX-var] per-window variance diagnostic + 4 reworked/new figures (cp_r2_per_trial = OOF scatter, cp_r2_distribution = OOF histogram + per-fold bars, cp_r2_vs_variance = NEW diagnostic, cp_arx_test_windows = worst→best OOF panels).
**Why:** Confirmed two things. (1) The earlier single-split train/test gap (0.916 vs 0.868) was sampling variance, not overfitting — 5-fold CV R²=0.898±0.005 (SD across folds only 0.005, rock-stable). Ridge sweep beforehand moved test R² by <0.001 → nothing to regularize (470:1 samples:params, cond 57). (2) The R²=0 windows are near-flat low-variance pre-stim windows: low-var (bottom 10%, var≤1.22) median OOF R²=0.441 vs 0.883 for the rest. They floor R² at 0 by construction (no signal to explain), not model failure. Target is already kernel-patch-averaged+SVD (load_experiments.m:197-207) so target-denoising lever is spent. Model is at its linear ceiling.
**Next:** Headline number for paper = 5-fold CV R²=0.898±0.005. Optionally report median OOF R² among signal-bearing windows (0.883). CP-4 still pending tf_fit.m. pY=0 must stay (AR self-lags would corrupt the counterfactual residual).

### 2026-06-13 — contra_prediction: random train/test/val split
**Changed/Found:** `impulse-analysis/contra_prediction.m` — replaced chronological 60/20/20 split with random sampling across all pre-stim windows (`randperm`, fixed `split_seed=42`). Per-trial R² figure A now plots each window at its true chronological trial index coloured by split (interleaved), dropped the split-boundary xlines.
**Why:** Chronological blocks let the test set fall on one contiguous (easy) stretch of the session, biasing the held-out estimate. Random sampling makes train/test/val each span the full range of behavioural/state conditions. Result: train/test/val R²=0.916/0.868/0.913 (vs chronological 0.902/0.930/0.900). Test dropped from the optimistic 0.930 to a more honest 0.868; train≈val≈0.91 confirms no overfitting.
**Next:** Per-trial stem plot is dense/overplotted with 748 interleaved points — consider switching stems→scatter for readability. CP-4 still pending tf_fit.m.

### 2026-06-13 — contra_prediction: instantaneous map beats lagged ARX
**Changed/Found:** `impulse-analysis/contra_prediction.m` — switched contra→primary predictor from lagged ARX (pX=10) to an **instantaneous map** (pX=0, y(t)=B·contra(t)), z-scored all SVD predictors, raised nSV_use 20→200. CP-2 rewritten to branch on pX (instantaneous = single standardized weight per mode, stem plot; lagged = old kernel lines).
**Why:** Interhemispheric coupling is near-instantaneous (callosal delay << 28.6 ms frame) and slow GCaMP makes lag copies collinear → the rank-200/211 deficiency seen earlier. Empirical lag sweep on AL_0033 confirmed: pX=0 test R²=0.905 > pX=10 R²=0.880, with 1/10th the params. pX=1 worst (0.843) — drops the concurrent sample. Z-scoring cut cond 1.1e8→6; nSV sweep at pX=0: 100→0.918, 200→0.930, 500→0.938 but cond 2.4e6 (overfit). Final model train/test/val R²=0.902/0.930/0.900. Mode weights concentrate in first ~25 modes; kernel-energy map sharpens onto the homotopic mirror of the primary pixel.
**Next:** CP-4 (concurrent + TF) still needs tf_fit.m run first — verify the instantaneous map + decontam behaves during the stim window (concurrent contra now more exposed to laser bleedover than the past-only lag model was). Consider whether nSV=200 should be locked as the design default.

### 2026-06-13 — contra_prediction: SVD-only, 60/20/20 split, per-trial R² figures
**Changed:** `impulse-analysis/contra_prediction.m` — (1) Removed pixel-grid predictor branch entirely; script is now SVD-direct only (`nSV_use=20` contra modes). Grid-node ROI code and all `~use_svd_direct` branches deleted. (2) Changed train/test split to chronological 60/20/20 (train/test/validate). (3) Added per-trial R² output: stem plot over all trials coloured by split, boxplot distribution per split, trace panels for evenly-spaced test and val windows. (4) Moved local functions (`compute_r2`, `per_trial_r2`, `plot_window`) to end of script file (MATLAB requires this for scripts).
**Why:** Pixel-grid mode was slower and less interpretable than SVD-direct; removing it simplifies the code path. Validate split needed to confirm generalisation beyond the test set. Per-trial R² requested for debugging model quality.
**Next:** Run script in MATLAB with allExperiments in workspace; check R²_test and R²_val are comparable (gap > 0.1 would suggest overfitting). If low overall, try increasing nSV_use or pX.

### 2026-06-12 — contra_prediction: test-set per-window prediction figure
**Changed:** `impulse-analysis/contra_prediction.m` — Added [CP-ARX-fig] section after the ARX fit. Computes per-window R² for all held-out test windows, then plots 6 evenly-spaced examples as actual vs predicted pre-trial traces with R² in each panel title. Also prints median/min/max R² across all test windows to console.
**Why:** User asked how the model is fit and wanted to see one-step-ahead performance on individual test windows, not just the pooled scalar R².
**Next:** If per-window R² is highly variable, may indicate some pre-trial windows are dominated by motion; cross-check with motion z-score.

### 2026-06-12 — contra_prediction: SVD-direct knob + per-amplitude bleedover compensation
**Changed:** `impulse-analysis/contra_prediction.m` — (1) Added `use_svd_direct` / `nSV_direct` knobs (default off). When enabled, runs `redoSVD` on the full contra-hemisphere mask and uses top `nSV_direct` temporal components as predictors instead of per-pixel grid dFk. Results cached to `*_svddirect.mat`. (2) CP-3 kernel map shows pixel-projected SVD energy (`U_svd_cp^2 * kern_energy`) when `use_svd_direct=true`. (3) CP-4a now computes per-amplitude artifact shapes (`art_shape4_amp{ia}`) in addition to global shape; CP-4c subtracts the amplitude-matched artifact instead of a single global mean.
**Why:** User request — improve bleedover subtraction (per-amplitude rather than global mean) and add SVD-direct prediction mode from widebrain_hemi_kernel.m approach; confusing Pink/Red labels also fixed this session.
**Next:** Test `use_svd_direct=true` with `nSV_direct=20`; compare R² against per-pixel mode. Check whether per-amplitude artifact subtraction reduces residual artifact correlation with amplitude.

### 2026-06-11 — CP-4: concurrent contra + TF impulse prediction implemented and run
**Changed:** `impulse-analysis/contra_prediction.m` — Added [CP-4] section (~240 lines): artifact check, TF simulation per amplitude, per-trial concurrent prediction loop, R² comparison, error correlation analysis, 4 figures.
**Found:** (1) Contra artifact ratio=36.5 (bilateral laser response) — decontamination applied. (2) CP-4 pink R²=0.862 but CP-1 (pre-trial only) R²=0.000 — the high CP-4 R² is entirely from in-trial bilateral laser response, NOT spontaneous state. (3) Prediction error (after bilateral correction) correlates with pre-stim variance (r=+0.225, p<0.001), pre-stim delta power (r=+0.231, p<0.001), stim-window delta (r=+0.211, p<0.001). Motion NOT significant (r=-0.056, p=0.12). (4) ARX fit R²_train=0.898, R²_test=0.861 (68 contra pixels + motion predictor).
**Why:** User requested full concurrent prediction pipeline with error vs brain-state correlations (motion, pre-stim variance, delta power). Also ran programmatic ROI (no ginput) since no cached ROI existed.
**Next:** Reconcile CP-4 finding with pre-stim variance result from prestim_variance.m. The prediction error r~+0.23 with delta/variance extends the K-figure finding to regression residuals. Consider whether CP-1 pre-trial prediction error (not CP-4) is the cleaner analysis for correlating with pre-stim state (avoids bilateral artifact confound).

### 2026-06-11 — CP-ARX model validation figures generated
**Changed:** New diagnostic figures: `cp_pretrial_validation.png` (8 held-out test windows, per-window R² 0.23–0.95) and `cp_continuous_validation.png` (60 s continuous held-out stretch, R²=0.924). Stim onsets marked — brief divergences visible at each onset confirming model tracks spontaneous dynamics only.
**Why:** Visual confirmation that beta_cp genuinely learns contra→ipsi mapping on spontaneous data, not overfitting. Supports interpretation that R²=0 on CP-1 trial prediction is because laser drives a novel ipsi dynamic, not a model failure.
**Next:** Use continuous figure to show in paper or supplement as model diagnostic.

### 2026-06-11 — CP-ARX trained: R²_test=0.861 (68 contra pixels, pX=10, pY=0, AL_0033 e1)
**Changed:** Workspace — ran ARX fitting on spontaneous pre-trial windows (6s each, 80/20 train/test split, 598 train trials, 150 test trials). Programmatic contra ROI (midline at col=280, left hemisphere, 68 grid nodes), motion from `d.motion.motion_1(1:2:end)`.
**Why:** Setting up workspace for CP-4 — no cached ROI or SVD existed; built programmatic ROI avoiding ginput.
**Next:** ROI saved to impulse-analysis/cp_roi_AL_0033_0129_e1.mat. SVD cached to impulse-analysis/data/AL_0033cp01291.mat. Subsequent runs will use caches.


<!-- older entries → RESEARCH_archive_2026H1.md -->
